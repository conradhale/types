import type NM from './nm-1.0.d.ts';
import Gio from '@girs/gio-2.0';
import GObject from '@girs/gobject-2.0';
import { type GObjectClasses } from '@girs/gobject-2.0/gobject-2.0';
import GLib from '@girs/glib-2.0';
import GModule from '@girs/gmodule-2.0';
declare namespace classes {
    class AccessPoint extends Object {
        static '$gtype': GObject.GType<AccessPoint>;
        // Properties
        /**
         * The channel bandwidth announced by the AP in MHz.
         */
        get bandwidth(): number;
        /**
         * The BSSID of the access point.
         */
        get bssid(): string;
        /**
         * The flags of the access point.
         */
        get flags(): NM.__80211ApFlags;
        /**
         * The frequency of the access point.
         */
        get frequency(): number;
        /**
         * Alias for #NMAccessPoint:bssid.
         */
        get hw_address(): string;
        /**
         * Alias for #NMAccessPoint:bssid.
         */
        get hwAddress(): string;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * access point was found in scan results.  A value of -1 means the
         * access point has not been found in a scan.
         */
        get last_seen(): number;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * access point was found in scan results.  A value of -1 means the
         * access point has not been found in a scan.
         */
        get lastSeen(): number;
        /**
         * The maximum bit rate of the access point in kbit/s.
         */
        get max_bitrate(): number;
        /**
         * The maximum bit rate of the access point in kbit/s.
         */
        get maxBitrate(): number;
        /**
         * The mode of the access point; either "infrastructure" (a central
         * coordinator of the wireless network allowing clients to connect) or
         * "ad-hoc" (a network with no central controller).
         */
        get mode(): NM.__80211Mode;
        /**
         * The RSN flags of the access point.
         */
        get rsn_flags(): NM.__80211ApSecurityFlags;
        /**
         * The RSN flags of the access point.
         */
        get rsnFlags(): NM.__80211ApSecurityFlags;
        /**
         * The SSID of the access point, or %NULL if it is not known.
         */
        get ssid(): GLib.Bytes;
        /**
         * The current signal strength of the access point.
         */
        get strength(): number;
        /**
         * The WPA flags of the access point.
         */
        get wpa_flags(): NM.__80211ApSecurityFlags;
        /**
         * The WPA flags of the access point.
         */
        get wpaFlags(): NM.__80211ApSecurityFlags;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.AccessPoint.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.AccessPoint.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.AccessPoint.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.AccessPoint.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.AccessPoint.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.AccessPoint.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.AccessPoint.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.AccessPoint.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Validates a given connection against a given Wi-Fi access point to ensure that
         * the connection may be activated with that AP.  The connection must match the
         * `ap'`s SSID, (if given) BSSID, and other attributes like security settings,
         * channel, band, etc.
         *
         * @returns %TRUE if the connection may be activated with this Wi-Fi AP,
         * %FALSE if it cannot be.
         * @param connection an #NMConnection to validate against `ap`
         */
        connection_valid(connection: NM.Connection): boolean;
        /**
         * Filters a given array of connections for a given #NMAccessPoint object and
         * returns connections which may be activated with the access point.  Any
         * returned connections will match the `ap'`s SSID and (if given) BSSID and
         * other attributes like security settings, channel, etc.
         *
         * To obtain the list of connections that are compatible with this access point,
         * use nm_client_get_connections() and then filter the returned list for a given
         * #NMDevice using nm_device_filter_connections() and finally filter that list
         * with this function.
         *
         * @returns an array of
         * #NMConnections that could be activated with the given `ap`.  The array should
         * be freed with g_ptr_array_unref() when it is no longer required.
         *
         * WARNING: the transfer annotation for this function may not work correctly
         *   with bindings. See https://gitlab.gnome.org/GNOME/gobject-introspection/-/issues/305.
         *   You can filter the list yourself with nm_access_point_connection_valid().
         * @param connections an array of #NMConnections to
         * filter
         */
        filter_connections(connections: NM.Connection[]): NM.Connection[];
        /**
         * Gets the bandwidth advertised by the access point in MHz.
         *
         * @returns the advertised bandwidth (MHz)
         */
        get_bandwidth(): number;
        /**
         * Gets the Basic Service Set ID (BSSID) of the Wi-Fi access point.
         *
         * @returns the BSSID of the access point. This is an internal string and must
         * not be modified or freed.
         */
        get_bssid(): string;
        /**
         * Gets the flags of the access point.
         *
         * @returns the flags
         */
        get_flags(): NM.__80211ApFlags;
        /**
         * Gets the frequency of the access point in MHz.
         *
         * @returns the frequency in MHz
         */
        get_frequency(): number;
        /**
         * Returns the timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * access point was found in scan results.  A value of -1 means the access
         * point has not been found in a scan.
         *
         * @returns the last seen time in seconds
         */
        get_last_seen(): number;
        /**
         * Gets the maximum bit rate of the access point in kbit/s.
         *
         * @returns the maximum bit rate (kbit/s)
         */
        get_max_bitrate(): number;
        /**
         * Gets the mode of the access point.
         *
         * @returns the mode
         */
        get_mode(): NM.__80211Mode;
        /**
         * Gets the RSN (Robust Secure Network, ie WPA version 2) flags of the access
         * point.
         *
         * @returns the RSN flags
         */
        get_rsn_flags(): NM.__80211ApSecurityFlags;
        /**
         * Gets the SSID of the access point.
         *
         * @returns the #GBytes containing the SSID, or %NULL if the
         *   SSID is unknown.
         */
        get_ssid(): GLib.Bytes;
        /**
         * Gets the current signal strength of the access point as a percentage.
         *
         * @returns the signal strength (0 to 100)
         */
        get_strength(): number;
        /**
         * Gets the WPA (version 1) flags of the access point.
         *
         * @returns the WPA flags
         */
        get_wpa_flags(): NM.__80211ApSecurityFlags;
    }
    class ActiveConnection extends Object {
        static '$gtype': GObject.GType<ActiveConnection>;
        // Properties
        /**
         * The connection that this is an active instance of.
         */
        get connection(): NM.RemoteConnection;
        /**
         * The controller device if one exists. This replaces the deprecated
         * "master" property.
         */
        get controller(): NM.Device;
        /**
         * Whether the active connection is the default IPv4 one.
         */
        get default(): boolean;
        /**
         * Whether the active connection is the default IPv6 one.
         */
        get default6(): boolean;
        /**
         * The devices of the active connection.
         */
        get devices(): NM.Device[];
        /**
         * The IPv4 #NMDhcpConfig of the connection.
         */
        get dhcp4_config(): NM.DhcpConfig;
        /**
         * The IPv4 #NMDhcpConfig of the connection.
         */
        get dhcp4Config(): NM.DhcpConfig;
        /**
         * The IPv6 #NMDhcpConfig of the connection.
         */
        get dhcp6_config(): NM.DhcpConfig;
        /**
         * The IPv6 #NMDhcpConfig of the connection.
         */
        get dhcp6Config(): NM.DhcpConfig;
        /**
         * The active connection's ID
         */
        get id(): string;
        /**
         * The IPv4 #NMIPConfig of the connection.
         */
        get ip4_config(): NM.IPConfig;
        /**
         * The IPv4 #NMIPConfig of the connection.
         */
        get ip4Config(): NM.IPConfig;
        /**
         * The IPv6 #NMIPConfig of the connection.
         */
        get ip6_config(): NM.IPConfig;
        /**
         * The IPv6 #NMIPConfig of the connection.
         */
        get ip6Config(): NM.IPConfig;
        /**
         * The controller device if one exists. Replaced by the "controller" property.
         */
        get master(): NM.Device;
        /**
         * The path to the "specific object" of the active connection; see
         * nm_active_connection_get_specific_object_path() for more details.
         */
        get specific_object_path(): string;
        /**
         * The path to the "specific object" of the active connection; see
         * nm_active_connection_get_specific_object_path() for more details.
         */
        get specificObjectPath(): string;
        /**
         * The state of the active connection.
         */
        get state(): NM.ActiveConnectionState;
        /**
         * The state flags of the active connection.
         */
        get state_flags(): number;
        /**
         * The state flags of the active connection.
         */
        get stateFlags(): number;
        /**
         * The active connection's type
         */
        get type(): string;
        /**
         * The active connection's UUID
         */
        get uuid(): string;
        /**
         * Whether the active connection is a VPN connection.
         */
        get vpn(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.ActiveConnection.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.ActiveConnection.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.ActiveConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.ActiveConnection.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.ActiveConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.ActiveConnection.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.ActiveConnection.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.ActiveConnection.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the #NMRemoteConnection associated with `connection`.
         *
         * @returns the #NMRemoteConnection which this
         * #NMActiveConnection is an active instance of.
         */
        get_connection(): NM.RemoteConnection;
        /**
         * Gets the #NMConnection's type.
         *
         * @returns the type of the #NMConnection that backs the #NMActiveConnection.
         * This is the internal string used by the connection, and must not be modified.
         */
        get_connection_type(): string;
        /**
         * Gets the controller #NMDevice of the connection. This replaces the
         * deprecated nm_active_connection_get_master() method.
         *
         * @returns the controller #NMDevice of the #NMActiveConnection.
         */
        get_controller(): any | null;
        /**
         * Whether the active connection is the default IPv4 one (that is, is used for
         * the default IPv4 route and DNS information).
         *
         * @returns %TRUE if the active connection is the default IPv4 connection
         */
        get_default(): boolean;
        /**
         * Whether the active connection is the default IPv6 one (that is, is used for
         * the default IPv6 route and DNS information).
         *
         * @returns %TRUE if the active connection is the default IPv6 connection
         */
        get_default6(): boolean;
        /**
         * Gets the #NMDevices used for the active connections.
         *
         * @returns the #GPtrArray containing #NMDevices.
         * This is the internal copy used by the connection, and must not be modified.
         */
        get_devices(): NM.Device[];
        /**
         * Gets the current IPv4 #NMDhcpConfig (if any) associated with the
         * #NMActiveConnection.
         *
         * @returns the IPv4 #NMDhcpConfig, or %NULL if the connection
         *   does not use DHCP, or is not in the %NM_ACTIVE_CONNECTION_STATE_ACTIVATED
         *   state.
         */
        get_dhcp4_config(): NM.DhcpConfig;
        /**
         * Gets the current IPv6 #NMDhcpConfig (if any) associated with the
         * #NMActiveConnection.
         *
         * @returns the IPv6 #NMDhcpConfig, or %NULL if the connection
         *   does not use DHCPv6, or is not in the %NM_ACTIVE_CONNECTION_STATE_ACTIVATED
         *   state.
         */
        get_dhcp6_config(): NM.DhcpConfig;
        /**
         * Gets the #NMConnection's ID.
         *
         * @returns the ID of the #NMConnection that backs the #NMActiveConnection.
         * This is the internal string used by the connection, and must not be modified.
         */
        get_id(): string;
        /**
         * Gets the current IPv4 #NMIPConfig associated with the #NMActiveConnection.
         *
         * @returns the IPv4 #NMIPConfig, or %NULL if the connection is
         *   not in the %NM_ACTIVE_CONNECTION_STATE_ACTIVATED state.
         */
        get_ip4_config(): NM.IPConfig;
        /**
         * Gets the current IPv6 #NMIPConfig associated with the #NMActiveConnection.
         *
         * @returns the IPv6 #NMIPConfig, or %NULL if the connection is
         *   not in the %NM_ACTIVE_CONNECTION_STATE_ACTIVATED state.
         */
        get_ip6_config(): NM.IPConfig;
        /**
         * Gets the controller #NMDevice of the connection.
         *
         * @returns the controller #NMDevice of the #NMActiveConnection.
         */
        get_master(): any | null;
        /**
         * Gets the path of the "specific object" used at activation.
         *
         * Currently, there is no single method that will allow you to automatically turn
         * this into an appropriate #NMObject; you need to know what kind of object it
         * is based on other information. (Eg, if `connection` corresponds to a Wi-Fi
         * connection, then the specific object will be an #NMAccessPoint, and you can
         * resolve it with nm_device_wifi_get_access_point_by_path().)
         *
         * @returns the specific object's D-Bus path. This is the internal string used
         * by the connection, and must not be modified.
         */
        get_specific_object_path(): string;
        /**
         * Gets the active connection's state.
         *
         * @returns the state
         */
        get_state(): NM.ActiveConnectionState;
        /**
         * Gets the active connection's state flags.
         *
         * @returns the state flags
         */
        get_state_flags(): NM.ActivationStateFlags;
        /**
         * Gets the reason for active connection's state.
         *
         * @returns the reason
         */
        get_state_reason(): NM.ActiveConnectionStateReason;
        /**
         * Gets the #NMConnection's UUID.
         *
         * @returns the UUID of the #NMConnection that backs the #NMActiveConnection.
         * This is the internal string used by the connection, and must not be modified.
         */
        get_uuid(): string;
        /**
         * Whether the active connection is a VPN connection.
         *
         * @returns %TRUE if the active connection is a VPN connection
         */
        get_vpn(): boolean;
    }
    class Checkpoint extends Object {
        static '$gtype': GObject.GType<Checkpoint>;
        // Properties
        /**
         * The timestamp (in CLOCK_BOOTTIME milliseconds) of checkpoint creation.
         */
        get created(): number;
        /**
         * The devices that are part of this checkpoint.
         */
        get devices(): NM.Device[];
        /**
         * Timeout in seconds for automatic rollback, or zero.
         */
        get rollback_timeout(): number;
        /**
         * Timeout in seconds for automatic rollback, or zero.
         */
        get rollbackTimeout(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Checkpoint.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Checkpoint.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.Checkpoint.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Checkpoint.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Checkpoint.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Checkpoint.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Checkpoint.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Checkpoint.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the timestamp (in CLOCK_BOOTTIME milliseconds) of checkpoint creation.
         *
         * Use nm_utils_get_timestamp_msec() to obtain current time value suitable for
         * comparing to this value.
         *
         * @returns the timestamp of checkpoint creation.
         */
        get_created(): number;
        /**
         * The devices that are part of this checkpoint.
         *
         * @returns the devices list.
         */
        get_devices(): NM.Device[];
        /**
         * Gets the timeout in seconds for automatic rollback.
         *
         * @returns the rollback timeout.
         */
        get_rollback_timeout(): number;
    }
    class Client extends GObjectClasses.Object implements Gio.AsyncInitable<NM.Client>, Gio.Initable {
        static '$gtype': GObject.GType<Client>;
        // Properties
        /**
         * The #NMActiveConnection of the activating connection that is
         * likely to become the new #NMClient:primary-connection.
         */
        get activating_connection(): NM.ActiveConnection;
        /**
         * The #NMActiveConnection of the activating connection that is
         * likely to become the new #NMClient:primary-connection.
         */
        get activatingConnection(): NM.ActiveConnection;
        /**
         * The active connections.
         */
        get active_connections(): NM.ActiveConnection[];
        /**
         * The active connections.
         */
        get activeConnections(): NM.ActiveConnection[];
        /**
         * List of both real devices and device placeholders.
         */
        get all_devices(): NM.Device[];
        /**
         * List of both real devices and device placeholders.
         */
        get allDevices(): NM.Device[];
        /**
         * If %TRUE, adding and modifying connections is supported.
         */
        get can_modify(): boolean;
        /**
         * If %TRUE, adding and modifying connections is supported.
         */
        get canModify(): boolean;
        /**
         * The list of capabilities numbers as guint32 or %NULL if
         * there are no capabilities. The numeric value correspond
         * to %NMCapability enum.
         */
        get capabilities(): number[];
        /**
         * The list of active checkpoints.
         */
        get checkpoints(): NM.Checkpoint[];
        /**
         * The list of configured connections that are available to the user. (Note
         * that this differs from the underlying D-Bus property, which may also
         * contain the object paths of connections that the user does not have
         * permission to read the details of.)
         */
        get connections(): NM.RemoteConnection[];
        /**
         * The network connectivity state.
         */
        get connectivity(): NM.ConnectivityState;
        get connectivity_check_available(): boolean;
        get connectivityCheckAvailable(): boolean;
        get connectivity_check_enabled(): boolean;
        set connectivity_check_enabled(val: boolean);
        get connectivityCheckEnabled(): boolean;
        set connectivityCheckEnabled(val: boolean);
        /**
         * The used URI for connectivity checking.
         */
        get connectivity_check_uri(): string;
        /**
         * The used URI for connectivity checking.
         */
        get connectivityCheckUri(): string;
        /**
         * The #GDBusConnection to use.
         *
         * If this is not set during object construction, the D-Bus connection will
         * automatically be chosen during async/sync initalization via g_bus_get().
         */
        get dbus_connection(): Gio.DBusConnection;
        /**
         * The #GDBusConnection to use.
         *
         * If this is not set during object construction, the D-Bus connection will
         * automatically be chosen during async/sync initalization via g_bus_get().
         */
        get dbusConnection(): Gio.DBusConnection;
        /**
         * The name owner of the NetworkManager D-Bus service.
         */
        get dbus_name_owner(): string;
        /**
         * The name owner of the NetworkManager D-Bus service.
         */
        get dbusNameOwner(): string;
        /**
         * List of real network devices.  Does not include placeholder devices.
         */
        get devices(): NM.Device[];
        /**
         * The current DNS configuration, represented as an array
         * of #NMDnsEntry objects.
         */
        get dns_configuration(): NM.DnsEntry[];
        /**
         * The current DNS configuration, represented as an array
         * of #NMDnsEntry objects.
         */
        get dnsConfiguration(): NM.DnsEntry[];
        /**
         * The current DNS processing mode.
         */
        get dns_mode(): string;
        /**
         * The current DNS processing mode.
         */
        get dnsMode(): string;
        /**
         * The current resolv.conf management mode.
         */
        get dns_rc_manager(): string;
        /**
         * The current resolv.conf management mode.
         */
        get dnsRcManager(): string;
        /**
         * The machine hostname stored in persistent configuration. This can be
         * modified by calling nm_client_save_hostname().
         */
        get hostname(): string;
        /**
         * #NMClientInstanceFlags for the instance. These affect behavior of #NMClient.
         * This is a construct property and you may only set most flags only during
         * construction.
         *
         * The flag %NM_CLIENT_INSTANCE_FLAGS_NO_AUTO_FETCH_PERMISSIONS can be toggled any time,
         * even after constructing the instance. Note that you may want to watch NMClient:permissions-state
         * property to know whether permissions are ready. Note that permissions are only fetched
         * when NMClient has a D-Bus name owner.
         *
         * The flags %NM_CLIENT_INSTANCE_FLAGS_INITIALIZED_GOOD and %NM_CLIENT_INSTANCE_FLAGS_INITIALIZED_BAD
         * cannot be set, however they will be returned by the getter after initialization completes.
         */
        get instance_flags(): number;
        set instance_flags(val: number);
        /**
         * #NMClientInstanceFlags for the instance. These affect behavior of #NMClient.
         * This is a construct property and you may only set most flags only during
         * construction.
         *
         * The flag %NM_CLIENT_INSTANCE_FLAGS_NO_AUTO_FETCH_PERMISSIONS can be toggled any time,
         * even after constructing the instance. Note that you may want to watch NMClient:permissions-state
         * property to know whether permissions are ready. Note that permissions are only fetched
         * when NMClient has a D-Bus name owner.
         *
         * The flags %NM_CLIENT_INSTANCE_FLAGS_INITIALIZED_GOOD and %NM_CLIENT_INSTANCE_FLAGS_INITIALIZED_BAD
         * cannot be set, however they will be returned by the getter after initialization completes.
         */
        get instanceFlags(): number;
        set instanceFlags(val: number);
        /**
         * Whether the connectivity is metered.
         */
        get metered(): number;
        /**
         * Whether networking is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get networking_enabled(): boolean;
        set networking_enabled(val: boolean);
        /**
         * Whether networking is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get networkingEnabled(): boolean;
        set networkingEnabled(val: boolean);
        /**
         * Whether the daemon is running.
         */
        get nm_running(): boolean;
        /**
         * Whether the daemon is running.
         */
        get nmRunning(): boolean;
        /**
         * The state of the cached permissions. The value %NM_TERNARY_DEFAULT
         * means that no permissions are yet received (or not yet requested).
         * %NM_TERNARY_TRUE means that permissions are received, cached and up
         * to date. %NM_TERNARY_FALSE means that permissions were received and are
         * cached, but in the meantime a "CheckPermissions" signal was received
         * that invalidated the cached permissions.
         * Note that NMClient will always emit a notify::permissions-state signal
         * when a "CheckPermissions" signal got received or after new permissions
         * got received (that is regardless whether the value of the permission state
         * actually changed). With this you can watch the permissions-state property
         * to know whether the permissions are ready. Note that while NMClient has
         * no D-Bus name owner, no permissions are fetched (and this property won't
         * change).
         */
        get permissions_state(): NM.Ternary;
        /**
         * The state of the cached permissions. The value %NM_TERNARY_DEFAULT
         * means that no permissions are yet received (or not yet requested).
         * %NM_TERNARY_TRUE means that permissions are received, cached and up
         * to date. %NM_TERNARY_FALSE means that permissions were received and are
         * cached, but in the meantime a "CheckPermissions" signal was received
         * that invalidated the cached permissions.
         * Note that NMClient will always emit a notify::permissions-state signal
         * when a "CheckPermissions" signal got received or after new permissions
         * got received (that is regardless whether the value of the permission state
         * actually changed). With this you can watch the permissions-state property
         * to know whether the permissions are ready. Note that while NMClient has
         * no D-Bus name owner, no permissions are fetched (and this property won't
         * change).
         */
        get permissionsState(): NM.Ternary;
        /**
         * The #NMActiveConnection of the device with the default route;
         * see nm_client_get_primary_connection() for more details.
         */
        get primary_connection(): NM.ActiveConnection;
        /**
         * The #NMActiveConnection of the device with the default route;
         * see nm_client_get_primary_connection() for more details.
         */
        get primaryConnection(): NM.ActiveConnection;
        /**
         * Flags for radio interfaces. See #NMRadioFlags.
         */
        get radio_flags(): number;
        /**
         * Flags for radio interfaces. See #NMRadioFlags.
         */
        get radioFlags(): number;
        /**
         * Whether the daemon is still starting up.
         */
        get startup(): boolean;
        /**
         * The current daemon state.
         */
        get state(): NM.State;
        /**
         * The NetworkManager version.
         */
        get version(): string;
        /**
         * Expose version info and capabilities of NetworkManager. If non-empty,
         * the first element is NM_VERSION, which encodes the version of the
         * daemon as "(major << 16 | minor << 8 | micro)". The following elements
         * is a bitfields of %NMVersionInfoCapability. If a bit is set, then
         * the running NetworkManager has the respective capability.
         */
        get version_info(): number[];
        /**
         * Expose version info and capabilities of NetworkManager. If non-empty,
         * the first element is NM_VERSION, which encodes the version of the
         * daemon as "(major << 16 | minor << 8 | micro)". The following elements
         * is a bitfields of %NMVersionInfoCapability. If a bit is set, then
         * the running NetworkManager has the respective capability.
         */
        get versionInfo(): number[];
        /**
         * Whether WiMAX functionality is enabled.
         */
        get wimax_enabled(): boolean;
        set wimax_enabled(val: boolean);
        /**
         * Whether WiMAX functionality is enabled.
         */
        get wimaxEnabled(): boolean;
        set wimaxEnabled(val: boolean);
        /**
         * Whether the WiMAX hardware is enabled.
         */
        get wimax_hardware_enabled(): boolean;
        /**
         * Whether the WiMAX hardware is enabled.
         */
        get wimaxHardwareEnabled(): boolean;
        /**
         * Whether wireless is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get wireless_enabled(): boolean;
        set wireless_enabled(val: boolean);
        /**
         * Whether wireless is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get wirelessEnabled(): boolean;
        set wirelessEnabled(val: boolean);
        /**
         * Whether the wireless hardware is enabled.
         */
        get wireless_hardware_enabled(): boolean;
        /**
         * Whether the wireless hardware is enabled.
         */
        get wirelessHardwareEnabled(): boolean;
        /**
         * Whether WWAN functionality is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get wwan_enabled(): boolean;
        set wwan_enabled(val: boolean);
        /**
         * Whether WWAN functionality is enabled.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get wwanEnabled(): boolean;
        set wwanEnabled(val: boolean);
        /**
         * Whether the WWAN hardware is enabled.
         */
        get wwan_hardware_enabled(): boolean;
        /**
         * Whether the WWAN hardware is enabled.
         */
        get wwanHardwareEnabled(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Client.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Client.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(cancellable?: Gio.Cancellable | null): NM.Client;
        static new_finish(result: Gio.AsyncResult): NM.Client;
        // Conflicted with Gio.AsyncInitable.new_finish
        static new_finish(...args: never[]): any;
        // Signals
        connect<K extends keyof NM.Client.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Client.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Client.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Client.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Client.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Client.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Creates a new #NMClient asynchronously.
         * `callback` will be called when it is done. Use
         * nm_client_new_finish() to get the result.
         *
         * This does nothing beside calling g_async_initable_new_async(). You are free to
         * call g_async_initable_new_async() or g_object_new()/g_async_initable_init_async()
         * directly for more control, to set GObject properties or get access to the NMClient
         * instance while it is still initializing.
         *
         * Creating an #NMClient instance can only fail for two reasons. First, if you didn't
         * provide a %NM_CLIENT_DBUS_CONNECTION and the call to g_bus_get()
         * fails. You can avoid that by using g_async_initable_new_async() directly and
         * set a D-Bus connection.
         * Second, if you cancelled the creation. If you do that, then note
         * that after the failure there might still be idle actions pending
         * which keep nm_client_get_main_context() alive. That means,
         * in that case you must continue iterating the context to avoid
         * leaks. See nm_client_get_context_busy_watcher().
         *
         * Creating an #NMClient instance when NetworkManager is not running
         * does not cause a failure.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to call when the client is created
         */
        static new_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<NM.Client> | null,
        ): void;
        static wait_shutdown_finish(result: Gio.AsyncResult): boolean;
        // Methods
        /**
         * Asynchronously starts a connection to a particular network using the
         * configuration settings from `connection` and the network device `device`.
         * Certain connection types also take a "specific object" which is the object
         * path of a connection- specific object, like an #NMAccessPoint for Wi-Fi
         * connections, or an #NMWimaxNsp for WiMAX connections, to which you wish to
         * connect.  If the specific object is not given, NetworkManager can, in some
         * cases, automatically determine which network to connect to given the settings
         * in `connection`.
         *
         * If `connection` is not given for a device-based activation, NetworkManager
         * picks the best available connection for the device and activates it.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can use the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param connection an #NMConnection
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param cancellable a #GCancellable, or %NULL
         */
        activate_connection_async(
            connection?: NM.Connection | null,
            device?: NM.Device | null,
            specific_object?: string | null,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<NM.ActiveConnection>;
        /**
         * Asynchronously starts a connection to a particular network using the
         * configuration settings from `connection` and the network device `device`.
         * Certain connection types also take a "specific object" which is the object
         * path of a connection- specific object, like an #NMAccessPoint for Wi-Fi
         * connections, or an #NMWimaxNsp for WiMAX connections, to which you wish to
         * connect.  If the specific object is not given, NetworkManager can, in some
         * cases, automatically determine which network to connect to given the settings
         * in `connection`.
         *
         * If `connection` is not given for a device-based activation, NetworkManager
         * picks the best available connection for the device and activates it.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can use the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param connection an #NMConnection
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        activate_connection_async(
            connection: NM.Connection | null,
            device: NM.Device | null,
            specific_object: string | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously starts a connection to a particular network using the
         * configuration settings from `connection` and the network device `device`.
         * Certain connection types also take a "specific object" which is the object
         * path of a connection- specific object, like an #NMAccessPoint for Wi-Fi
         * connections, or an #NMWimaxNsp for WiMAX connections, to which you wish to
         * connect.  If the specific object is not given, NetworkManager can, in some
         * cases, automatically determine which network to connect to given the settings
         * in `connection`.
         *
         * If `connection` is not given for a device-based activation, NetworkManager
         * picks the best available connection for the device and activates it.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can use the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param connection an #NMConnection
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        activate_connection_async(
            connection?: NM.Connection | null,
            device?: NM.Device | null,
            specific_object?: string | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<NM.ActiveConnection> | void;
        /**
         * Gets the result of a call to nm_client_activate_connection_async().
         *
         * @returns the new #NMActiveConnection on success, %NULL on
         *   failure, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        activate_connection_finish(result: Gio.AsyncResult): NM.ActiveConnection;
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * This is identical to nm_client_add_and_activate_connection_async() but takes
         * a further `options` parameter. Currently, the following options are supported
         * by the daemon:
         *  * "persist": A string describing how the connection should be stored.
         *               The default is "disk", but it can be modified to "memory" (until
         *               the daemon quits) or "volatile" (will be deleted on disconnect).
         *  * "bind-activation": Bind the connection lifetime to something. The default is "none",
         *            meaning an explicit disconnect is needed. The value "dbus-client"
         *            means the connection will automatically be deactivated when the calling
         *            D-Bus client disappears from the system bus.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (i.e., no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param options a #GVariant containing a dictionary with options, or %NULL
         * @param cancellable a #GCancellable, or %NULL
         */
        add_and_activate_connection2(
            partial: NM.Connection | null,
            device: NM.Device | null,
            specific_object: string | null,
            options: GLib.Variant,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<[NM.ActiveConnection, GLib.Variant | null]>;
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * This is identical to nm_client_add_and_activate_connection_async() but takes
         * a further `options` parameter. Currently, the following options are supported
         * by the daemon:
         *  * "persist": A string describing how the connection should be stored.
         *               The default is "disk", but it can be modified to "memory" (until
         *               the daemon quits) or "volatile" (will be deleted on disconnect).
         *  * "bind-activation": Bind the connection lifetime to something. The default is "none",
         *            meaning an explicit disconnect is needed. The value "dbus-client"
         *            means the connection will automatically be deactivated when the calling
         *            D-Bus client disappears from the system bus.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (i.e., no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param options a #GVariant containing a dictionary with options, or %NULL
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        add_and_activate_connection2(
            partial: NM.Connection | null,
            device: NM.Device | null,
            specific_object: string | null,
            options: GLib.Variant,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * This is identical to nm_client_add_and_activate_connection_async() but takes
         * a further `options` parameter. Currently, the following options are supported
         * by the daemon:
         *  * "persist": A string describing how the connection should be stored.
         *               The default is "disk", but it can be modified to "memory" (until
         *               the daemon quits) or "volatile" (will be deleted on disconnect).
         *  * "bind-activation": Bind the connection lifetime to something. The default is "none",
         *            meaning an explicit disconnect is needed. The value "dbus-client"
         *            means the connection will automatically be deactivated when the calling
         *            D-Bus client disappears from the system bus.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (i.e., no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         * @param options a #GVariant containing a dictionary with options, or %NULL
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        add_and_activate_connection2(
            partial: NM.Connection | null,
            device: NM.Device | null,
            specific_object: string | null,
            options: GLib.Variant,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<[NM.ActiveConnection, GLib.Variant | null]> | void;
        /**
         * Gets the result of a call to nm_client_add_and_activate_connection2().
         *
         * You can call nm_active_connection_get_connection() on the returned
         * #NMActiveConnection to find the path of the created #NMConnection.
         *
         * @returns the new #NMActiveConnection on success, %NULL on
         *   failure, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        add_and_activate_connection2_finish(result: Gio.AsyncResult): [NM.ActiveConnection, GLib.Variant | null];
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         *   If the variant is floating, it will be consumed.
         * @param cancellable a #GCancellable, or %NULL
         */
        add_and_activate_connection_async(
            partial?: NM.Connection | null,
            device?: NM.Device | null,
            specific_object?: string | null,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<NM.ActiveConnection>;
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         *   If the variant is floating, it will be consumed.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        add_and_activate_connection_async(
            partial: NM.Connection | null,
            device: NM.Device | null,
            specific_object: string | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Adds a new connection using the given details (if any) as a template,
         * automatically filling in missing settings with the capabilities of the given
         * device and specific object.  The new connection is then asynchronously
         * activated as with nm_client_activate_connection_async(). Cannot be used for
         * VPN connections at this time.
         *
         * Note that the callback is invoked when NetworkManager has started activating
         * the new connection, not when it finishes. You can used the returned
         * #NMActiveConnection object (in particular, #NMActiveConnection:state) to
         * track the activation to its completion.
         *
         * @param partial an #NMConnection to add; the connection may be
         *   partially filled (or even %NULL) and will be completed by NetworkManager
         *   using the given `device` and `specific_object` before being added
         * @param device the #NMDevice
         * @param specific_object the object path of a connection-type-specific
         *   object this activation should use. This parameter is currently ignored for
         *   wired and mobile broadband connections, and the value of %NULL should be used
         *   (ie, no specific object).  For Wi-Fi or WiMAX connections, pass the object
         *   path of a #NMAccessPoint or #NMWimaxNsp owned by `device,` which you can
         *   get using nm_object_get_path(), and which will be used to complete the
         *   details of the newly added connection.
         *   If the variant is floating, it will be consumed.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the activation has started
         */
        add_and_activate_connection_async(
            partial?: NM.Connection | null,
            device?: NM.Device | null,
            specific_object?: string | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<NM.ActiveConnection> | void;
        /**
         * Gets the result of a call to nm_client_add_and_activate_connection_async().
         *
         * You can call nm_active_connection_get_connection() on the returned
         * #NMActiveConnection to find the path of the created #NMConnection.
         *
         * @returns the new #NMActiveConnection on success, %NULL on
         *   failure, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        add_and_activate_connection_finish(result: Gio.AsyncResult): NM.ActiveConnection;
        /**
         * Call AddConnection2() D-Bus API asynchronously.
         *
         * @param settings the "a{sa{sv}}" #GVariant with the content of the setting.
         * @param flags the %NMSettingsAddConnection2Flags argument.
         * @param args the "a{sv}" #GVariant with extra argument or %NULL
         *   for no extra arguments.
         * @param ignore_out_result this function wraps AddConnection2(), which has an
         *   additional result "a{sv}" output parameter. By setting this to %TRUE,
         *   you signal that you are not interested in that output parameter.
         *   This allows the function to fall back to AddConnection() and AddConnectionUnsaved(),
         *   which is interesting if you run against an older server version that does
         *   not yet provide AddConnection2(). By setting this to %FALSE, the function
         *   under the hood always calls AddConnection2().
         * @param cancellable a #GCancellable, or %NULL
         */
        add_connection2(
            settings: GLib.Variant,
            flags: NM.SettingsAddConnection2Flags | null,
            args: GLib.Variant | null,
            ignore_out_result: boolean,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<[NM.RemoteConnection, GLib.Variant | null]>;
        /**
         * Call AddConnection2() D-Bus API asynchronously.
         *
         * @param settings the "a{sa{sv}}" #GVariant with the content of the setting.
         * @param flags the %NMSettingsAddConnection2Flags argument.
         * @param args the "a{sv}" #GVariant with extra argument or %NULL
         *   for no extra arguments.
         * @param ignore_out_result this function wraps AddConnection2(), which has an
         *   additional result "a{sv}" output parameter. By setting this to %TRUE,
         *   you signal that you are not interested in that output parameter.
         *   This allows the function to fall back to AddConnection() and AddConnectionUnsaved(),
         *   which is interesting if you run against an older server version that does
         *   not yet provide AddConnection2(). By setting this to %FALSE, the function
         *   under the hood always calls AddConnection2().
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        add_connection2(
            settings: GLib.Variant,
            flags: NM.SettingsAddConnection2Flags | null,
            args: GLib.Variant | null,
            ignore_out_result: boolean,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Call AddConnection2() D-Bus API asynchronously.
         *
         * @param settings the "a{sa{sv}}" #GVariant with the content of the setting.
         * @param flags the %NMSettingsAddConnection2Flags argument.
         * @param args the "a{sv}" #GVariant with extra argument or %NULL
         *   for no extra arguments.
         * @param ignore_out_result this function wraps AddConnection2(), which has an
         *   additional result "a{sv}" output parameter. By setting this to %TRUE,
         *   you signal that you are not interested in that output parameter.
         *   This allows the function to fall back to AddConnection() and AddConnectionUnsaved(),
         *   which is interesting if you run against an older server version that does
         *   not yet provide AddConnection2(). By setting this to %FALSE, the function
         *   under the hood always calls AddConnection2().
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        add_connection2(
            settings: GLib.Variant,
            flags: NM.SettingsAddConnection2Flags | null,
            args: GLib.Variant | null,
            ignore_out_result: boolean,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<[NM.RemoteConnection, GLib.Variant | null]> | void;
        add_connection2_finish(result: Gio.AsyncResult): [NM.RemoteConnection, GLib.Variant | null];
        /**
         * Requests that the remote settings service add the given settings to a new
         * connection.  If `save_to_disk` is %TRUE, the connection is immediately written
         * to disk; otherwise it is initially only stored in memory, but may be saved
         * later by calling the connection's nm_remote_connection_commit_changes()
         * method.
         *
         * `connection` is untouched by this function and only serves as a template of
         * the settings to add.  The #NMRemoteConnection object that represents what
         * NetworkManager actually added is returned to `callback` when the addition
         * operation is complete.
         *
         * Note that the #NMRemoteConnection returned in `callback` may not contain
         * identical settings to `connection` as NetworkManager may perform automatic
         * completion and/or normalization of connection properties.
         *
         * @param connection the connection to add. Note that this object's settings will be
         *   added, not the object itself
         * @param save_to_disk whether to immediately save the connection to disk
         * @param cancellable a #GCancellable, or %NULL
         */
        add_connection_async(
            connection: NM.Connection,
            save_to_disk: boolean,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<NM.RemoteConnection>;
        /**
         * Requests that the remote settings service add the given settings to a new
         * connection.  If `save_to_disk` is %TRUE, the connection is immediately written
         * to disk; otherwise it is initially only stored in memory, but may be saved
         * later by calling the connection's nm_remote_connection_commit_changes()
         * method.
         *
         * `connection` is untouched by this function and only serves as a template of
         * the settings to add.  The #NMRemoteConnection object that represents what
         * NetworkManager actually added is returned to `callback` when the addition
         * operation is complete.
         *
         * Note that the #NMRemoteConnection returned in `callback` may not contain
         * identical settings to `connection` as NetworkManager may perform automatic
         * completion and/or normalization of connection properties.
         *
         * @param connection the connection to add. Note that this object's settings will be
         *   added, not the object itself
         * @param save_to_disk whether to immediately save the connection to disk
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        add_connection_async(
            connection: NM.Connection,
            save_to_disk: boolean,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Requests that the remote settings service add the given settings to a new
         * connection.  If `save_to_disk` is %TRUE, the connection is immediately written
         * to disk; otherwise it is initially only stored in memory, but may be saved
         * later by calling the connection's nm_remote_connection_commit_changes()
         * method.
         *
         * `connection` is untouched by this function and only serves as a template of
         * the settings to add.  The #NMRemoteConnection object that represents what
         * NetworkManager actually added is returned to `callback` when the addition
         * operation is complete.
         *
         * Note that the #NMRemoteConnection returned in `callback` may not contain
         * identical settings to `connection` as NetworkManager may perform automatic
         * completion and/or normalization of connection properties.
         *
         * @param connection the connection to add. Note that this object's settings will be
         *   added, not the object itself
         * @param save_to_disk whether to immediately save the connection to disk
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        add_connection_async(
            connection: NM.Connection,
            save_to_disk: boolean,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<NM.RemoteConnection> | void;
        /**
         * Gets the result of a call to nm_client_add_connection_async().
         *
         * @returns the new #NMRemoteConnection on success, %NULL on
         *   failure, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        add_connection_finish(result: Gio.AsyncResult): NM.RemoteConnection;
        /**
         * Updates the network connectivity state and returns the (new)
         * current state. Contrast nm_client_get_connectivity(), which returns
         * the most recent known state without re-checking.
         *
         * This is a blocking call; use nm_client_check_connectivity_async()
         * if you do not want to block.
         *
         * @returns the (new) current connectivity state
         * @param cancellable a #GCancellable
         */
        check_connectivity(cancellable?: Gio.Cancellable | null): NM.ConnectivityState;
        /**
         * Asynchronously updates the network connectivity state and invokes
         * `callback` when complete. Contrast nm_client_get_connectivity(),
         * which (immediately) returns the most recent known state without
         * re-checking, and nm_client_check_connectivity(), which blocks.
         *
         * @param cancellable a #GCancellable
         */
        check_connectivity_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<NM.ConnectivityState>;
        /**
         * Asynchronously updates the network connectivity state and invokes
         * `callback` when complete. Contrast nm_client_get_connectivity(),
         * which (immediately) returns the most recent known state without
         * re-checking, and nm_client_check_connectivity(), which blocks.
         *
         * @param cancellable a #GCancellable
         * @param callback callback to call with the result
         */
        check_connectivity_async(
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously updates the network connectivity state and invokes
         * `callback` when complete. Contrast nm_client_get_connectivity(),
         * which (immediately) returns the most recent known state without
         * re-checking, and nm_client_check_connectivity(), which blocks.
         *
         * @param cancellable a #GCancellable
         * @param callback callback to call with the result
         */
        check_connectivity_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<NM.ConnectivityState> | void;
        /**
         * Retrieves the result of an nm_client_check_connectivity_async()
         * call.
         *
         * @returns the (new) current connectivity state
         * @param result the #GAsyncResult
         */
        check_connectivity_finish(result: Gio.AsyncResult): NM.ConnectivityState;
        /**
         * Resets the timeout for the checkpoint with path `checkpoint_path`
         * to `timeout_add`.
         *
         * @param checkpoint_path a D-Bus path to a checkpoint
         * @param add_timeout the timeout in seconds counting from now.
         *   Set to zero, to disable the timeout.
         * @param cancellable a #GCancellable, or %NULL
         */
        checkpoint_adjust_rollback_timeout(
            checkpoint_path: string,
            add_timeout: number,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<boolean>;
        /**
         * Resets the timeout for the checkpoint with path `checkpoint_path`
         * to `timeout_add`.
         *
         * @param checkpoint_path a D-Bus path to a checkpoint
         * @param add_timeout the timeout in seconds counting from now.
         *   Set to zero, to disable the timeout.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_adjust_rollback_timeout(
            checkpoint_path: string,
            add_timeout: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Resets the timeout for the checkpoint with path `checkpoint_path`
         * to `timeout_add`.
         *
         * @param checkpoint_path a D-Bus path to a checkpoint
         * @param add_timeout the timeout in seconds counting from now.
         *   Set to zero, to disable the timeout.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_adjust_rollback_timeout(
            checkpoint_path: string,
            add_timeout: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_client_checkpoint_adjust_rollback_timeout().
         *
         * @returns %TRUE on success or %FALSE on failure.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        checkpoint_adjust_rollback_timeout_finish(result: Gio.AsyncResult): boolean;
        /**
         * Creates a checkpoint of the current networking configuration
         * for given interfaces. An empty `devices` argument means all
         * devices. If `rollback_timeout` is not zero, a rollback is
         * automatically performed after the given timeout.
         *
         * @param devices a list of devices for which a
         *   checkpoint should be created.
         * @param rollback_timeout the rollback timeout in seconds
         * @param flags creation flags
         * @param cancellable a #GCancellable, or %NULL
         */
        checkpoint_create(
            devices: NM.Device[],
            rollback_timeout: number,
            flags: NM.CheckpointCreateFlags | null,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<NM.Checkpoint>;
        /**
         * Creates a checkpoint of the current networking configuration
         * for given interfaces. An empty `devices` argument means all
         * devices. If `rollback_timeout` is not zero, a rollback is
         * automatically performed after the given timeout.
         *
         * @param devices a list of devices for which a
         *   checkpoint should be created.
         * @param rollback_timeout the rollback timeout in seconds
         * @param flags creation flags
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_create(
            devices: NM.Device[],
            rollback_timeout: number,
            flags: NM.CheckpointCreateFlags | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Creates a checkpoint of the current networking configuration
         * for given interfaces. An empty `devices` argument means all
         * devices. If `rollback_timeout` is not zero, a rollback is
         * automatically performed after the given timeout.
         *
         * @param devices a list of devices for which a
         *   checkpoint should be created.
         * @param rollback_timeout the rollback timeout in seconds
         * @param flags creation flags
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_create(
            devices: NM.Device[],
            rollback_timeout: number,
            flags: NM.CheckpointCreateFlags | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<NM.Checkpoint> | void;
        /**
         * Gets the result of a call to nm_client_checkpoint_create().
         *
         * @returns the new #NMCheckpoint on success, %NULL on
         *   failure, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        checkpoint_create_finish(result: Gio.AsyncResult): NM.Checkpoint;
        /**
         * Destroys an existing checkpoint without performing a rollback.
         *
         * @param checkpoint_path the D-Bus path for the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         */
        checkpoint_destroy(checkpoint_path: string, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Destroys an existing checkpoint without performing a rollback.
         *
         * @param checkpoint_path the D-Bus path for the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_destroy(
            checkpoint_path: string,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Destroys an existing checkpoint without performing a rollback.
         *
         * @param checkpoint_path the D-Bus path for the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_destroy(
            checkpoint_path: string,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_client_checkpoint_destroy().
         *
         * @returns %TRUE on success or %FALSE on failure, in which case
         *   `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        checkpoint_destroy_finish(result: Gio.AsyncResult): boolean;
        /**
         * Performs the rollback of a checkpoint before the timeout is reached.
         *
         * @param checkpoint_path the D-Bus path to the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         */
        checkpoint_rollback(
            checkpoint_path: string,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<GLib.HashTable<string, number>>;
        /**
         * Performs the rollback of a checkpoint before the timeout is reached.
         *
         * @param checkpoint_path the D-Bus path to the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_rollback(
            checkpoint_path: string,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Performs the rollback of a checkpoint before the timeout is reached.
         *
         * @param checkpoint_path the D-Bus path to the checkpoint
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        checkpoint_rollback(
            checkpoint_path: string,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<GLib.HashTable<string, number>> | void;
        /**
         * Gets the result of a call to nm_client_checkpoint_rollback().
         *
         * @returns an hash table of
         *   devices and results. Devices are represented by their original
         *   D-Bus path; each result is a #NMRollbackResult.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        checkpoint_rollback_finish(result: Gio.AsyncResult): GLib.HashTable<string, number>;
        /**
         * Determine whether connectivity checking is available.  This
         * requires that the URI of a connectivity service has been set in the
         * configuration file.
         *
         * @returns %TRUE if connectivity checking is available.
         */
        connectivity_check_get_available(): boolean;
        /**
         * Determine whether connectivity checking is enabled.
         *
         * @returns %TRUE if connectivity checking is enabled.
         */
        connectivity_check_get_enabled(): boolean;
        /**
         * Get the URI that will be queried to determine if there is internet
         * connectivity.
         *
         * @returns the connectivity URI in use
         */
        connectivity_check_get_uri(): string;
        /**
         * Enable or disable connectivity checking.  Note that if a
         * connectivity checking URI has not been configured, this will not
         * have any effect.
         *
         * @param enabled %TRUE to enable connectivity checking
         */
        connectivity_check_set_enabled(enabled: boolean): void;
        /**
         * Call g_dbus_connection_call() on the current name owner with the specified
         * arguments. Most importantly, this invokes g_dbus_connection_call() with the
         * client's #GMainContext, so that the response is always in order with other
         * events D-Bus events. Of course, the call uses #GTask and will invoke the
         * callback on the current g_main_context_get_thread_default().
         *
         * This API is merely a convenient wrapper for g_dbus_connection_call(). You can
         * also use g_dbus_connection_call() directly, with the same effect.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface to invoke method on
         * @param method_name the name of the method to invoke
         * @param parameters a #GVariant tuple with parameters for the method
         *     or %NULL if not passing parameters
         * @param reply_type the expected type of the reply (which will be a
         *     tuple), or %NULL
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         */
        dbus_call(
            object_path: string,
            interface_name: string,
            method_name: string,
            parameters: GLib.Variant | null,
            reply_type: GLib.VariantType | null,
            timeout_msec: number,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<GLib.Variant>;
        /**
         * Call g_dbus_connection_call() on the current name owner with the specified
         * arguments. Most importantly, this invokes g_dbus_connection_call() with the
         * client's #GMainContext, so that the response is always in order with other
         * events D-Bus events. Of course, the call uses #GTask and will invoke the
         * callback on the current g_main_context_get_thread_default().
         *
         * This API is merely a convenient wrapper for g_dbus_connection_call(). You can
         * also use g_dbus_connection_call() directly, with the same effect.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface to invoke method on
         * @param method_name the name of the method to invoke
         * @param parameters a #GVariant tuple with parameters for the method
         *     or %NULL if not passing parameters
         * @param reply_type the expected type of the reply (which will be a
         *     tuple), or %NULL
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         * @param callback a #GAsyncReadyCallback to call when the request
         *     is satisfied or %NULL if you don't care about the result of the
         *     method invocation
         */
        dbus_call(
            object_path: string,
            interface_name: string,
            method_name: string,
            parameters: GLib.Variant | null,
            reply_type: GLib.VariantType | null,
            timeout_msec: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Call g_dbus_connection_call() on the current name owner with the specified
         * arguments. Most importantly, this invokes g_dbus_connection_call() with the
         * client's #GMainContext, so that the response is always in order with other
         * events D-Bus events. Of course, the call uses #GTask and will invoke the
         * callback on the current g_main_context_get_thread_default().
         *
         * This API is merely a convenient wrapper for g_dbus_connection_call(). You can
         * also use g_dbus_connection_call() directly, with the same effect.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface to invoke method on
         * @param method_name the name of the method to invoke
         * @param parameters a #GVariant tuple with parameters for the method
         *     or %NULL if not passing parameters
         * @param reply_type the expected type of the reply (which will be a
         *     tuple), or %NULL
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         * @param callback a #GAsyncReadyCallback to call when the request
         *     is satisfied or %NULL if you don't care about the result of the
         *     method invocation
         */
        dbus_call(
            object_path: string,
            interface_name: string,
            method_name: string,
            parameters: GLib.Variant | null,
            reply_type: GLib.VariantType | null,
            timeout_msec: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<GLib.Variant> | void;
        /**
         * Gets the result of a call to nm_client_dbus_call().
         *
         * @returns the result #GVariant or %NULL on error.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        dbus_call_finish(result: Gio.AsyncResult): GLib.Variant;
        /**
         * Like nm_client_dbus_call() but calls "Set" on the standard "org.freedesktop.DBus.Properties"
         * D-Bus interface.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface for the property to set.
         * @param property_name the name of the property to set
         * @param value a #GVariant with the value to set.
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         */
        dbus_set_property(
            object_path: string,
            interface_name: string,
            property_name: string,
            value: GLib.Variant,
            timeout_msec: number,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<boolean>;
        /**
         * Like nm_client_dbus_call() but calls "Set" on the standard "org.freedesktop.DBus.Properties"
         * D-Bus interface.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface for the property to set.
         * @param property_name the name of the property to set
         * @param value a #GVariant with the value to set.
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         * @param callback a #GAsyncReadyCallback to call when the request
         *     is satisfied or %NULL if you don't care about the result of the
         *     method invocation
         */
        dbus_set_property(
            object_path: string,
            interface_name: string,
            property_name: string,
            value: GLib.Variant,
            timeout_msec: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Like nm_client_dbus_call() but calls "Set" on the standard "org.freedesktop.DBus.Properties"
         * D-Bus interface.
         *
         * @param object_path path of remote object
         * @param interface_name D-Bus interface for the property to set.
         * @param property_name the name of the property to set
         * @param value a #GVariant with the value to set.
         * @param timeout_msec the timeout in milliseconds, -1 to use the default
         *     timeout or %G_MAXINT for no timeout
         * @param cancellable a #GCancellable or %NULL
         * @param callback a #GAsyncReadyCallback to call when the request
         *     is satisfied or %NULL if you don't care about the result of the
         *     method invocation
         */
        dbus_set_property(
            object_path: string,
            interface_name: string,
            property_name: string,
            value: GLib.Variant,
            timeout_msec: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_client_dbus_set_property().
         *
         * @returns %TRUE on success or %FALSE on failure.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        dbus_set_property_finish(result: Gio.AsyncResult): boolean;
        /**
         * Deactivates an active #NMActiveConnection.
         *
         * @returns success or failure
         * @param active the #NMActiveConnection to deactivate
         * @param cancellable a #GCancellable, or %NULL
         */
        deactivate_connection(active: NM.ActiveConnection, cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously deactivates an active #NMActiveConnection.
         *
         * @param active the #NMActiveConnection to deactivate
         * @param cancellable a #GCancellable, or %NULL
         */
        deactivate_connection_async(
            active: NM.ActiveConnection,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<boolean>;
        /**
         * Asynchronously deactivates an active #NMActiveConnection.
         *
         * @param active the #NMActiveConnection to deactivate
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the deactivation has completed
         */
        deactivate_connection_async(
            active: NM.ActiveConnection,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously deactivates an active #NMActiveConnection.
         *
         * @param active the #NMActiveConnection to deactivate
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the deactivation has completed
         */
        deactivate_connection_async(
            active: NM.ActiveConnection,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_client_deactivate_connection_async().
         *
         * @returns success or failure
         * @param result the result passed to the #GAsyncReadyCallback
         */
        deactivate_connection_finish(result: Gio.AsyncResult): boolean;
        /**
         * Gets the #NMActiveConnection corresponding to a
         * currently-activating connection that is expected to become the new
         * #NMClient:primary-connection upon successful activation.
         *
         * @returns the appropriate #NMActiveConnection, if
         * any.
         */
        get_activating_connection(): NM.ActiveConnection;
        /**
         * Gets the active connections.
         *
         * @returns a #GPtrArray
         *  containing all the active #NMActiveConnections.
         * The returned array is owned by the client and should not be modified.
         */
        get_active_connections(): NM.ActiveConnection[];
        /**
         * Gets both real devices and device placeholders (eg, software devices which
         * do not currently exist, but could be created automatically by NetworkManager
         * if one of their NMDevice::ActivatableConnections was activated).  Use
         * nm_device_is_real() to determine whether each device is a real device or
         * a placeholder.
         *
         * Use nm_device_get_type() or the NM_IS_DEVICE_XXXX() functions to determine
         * what kind of device each member of the returned array is, and then you may
         * use device-specific methods such as nm_device_ethernet_get_hw_address().
         *
         * @returns a #GPtrArray
         * containing all the #NMDevices.  The returned array is owned by the
         * #NMClient object and should not be modified.
         */
        get_all_devices(): NM.Device[];
        get_capabilities(): number[];
        /**
         * Gets all the active checkpoints.
         *
         * @returns a #GPtrArray
         * containing all the #NMCheckpoint.  The returned array is owned by the
         * #NMClient object and should not be modified.
         */
        get_checkpoints(): NM.Checkpoint[];
        /**
         * Returns the first matching %NMRemoteConnection matching a given `id`.
         *
         * @returns the remote connection object on success, or %NULL if no
         *  matching object was found.
         *
         * The connection is as received from D-Bus and might not validate according
         * to nm_connection_verify().
         * @param id the id of the remote connection
         */
        get_connection_by_id(id: string): NM.RemoteConnection;
        /**
         * Returns the %NMRemoteConnection representing the connection at `path`.
         *
         * @returns the remote connection object on success, or %NULL if the object was
         *  not known
         *
         * The connection is as received from D-Bus and might not validate according
         * to nm_connection_verify().
         * @param path the D-Bus object path of the remote connection
         */
        get_connection_by_path(path: string): NM.RemoteConnection;
        /**
         * Returns the %NMRemoteConnection identified by `uuid`.
         *
         * @returns the remote connection object on success, or %NULL if the object was
         *  not known
         *
         * The connection is as received from D-Bus and might not validate according
         * to nm_connection_verify().
         * @param uuid the UUID of the remote connection
         */
        get_connection_by_uuid(uuid: string): NM.RemoteConnection;
        get_connections(): NM.RemoteConnection[];
        /**
         * Gets the current network connectivity state. Contrast
         * nm_client_check_connectivity() and
         * nm_client_check_connectivity_async(), which re-check the
         * connectivity state first before returning any information.
         *
         * @returns the current connectivity state
         */
        get_connectivity(): NM.ConnectivityState;
        get_context_busy_watcher<T = GObject.Object>(): T;
        /**
         * Gets the %GDBusConnection of the instance. This can be either passed when
         * constructing the instance (as "dbus-connection" property), or it will be
         * automatically initialized during async/sync init.
         *
         * @returns the D-Bus connection of the client, or %NULL if none is set.
         */
        get_dbus_connection(): Gio.DBusConnection;
        get_dbus_name_owner(): string;
        /**
         * Gets a #NMDevice from a #NMClient.
         *
         * @returns the #NMDevice for the given `iface` or %NULL if none is found.
         * @param iface the interface name to search for
         */
        get_device_by_iface(iface: string): NM.Device;
        /**
         * Gets a #NMDevice from a #NMClient.
         *
         * @returns the #NMDevice for the given `object_path` or %NULL if none is found.
         * @param object_path the object path to search for
         */
        get_device_by_path(object_path: string): NM.Device;
        /**
         * Gets all the known network devices.  Use nm_device_get_type() or the
         * <literal>NM_IS_DEVICE_XXXX</literal> functions to determine what kind of
         * device member of the returned array is, and then you may use device-specific
         * methods such as nm_device_ethernet_get_hw_address().
         *
         * @returns a #GPtrArray
         * containing all the #NMDevices.  The returned array is owned by the
         * #NMClient object and should not be modified.
         */
        get_devices(): NM.Device[];
        /**
         * Gets the current DNS configuration
         *
         * @returns a #GPtrArray
         * containing #NMDnsEntry elements or %NULL in case the value is not
         * available.  The returned array is owned by the #NMClient object
         * and should not be modified.
         */
        get_dns_configuration(): NM.DnsEntry[];
        /**
         * Gets the current DNS processing mode.
         *
         * @returns the DNS processing mode, or %NULL in case the
         *   value is not available.
         */
        get_dns_mode(): string;
        /**
         * Gets the current DNS resolv.conf manager.
         *
         * @returns the resolv.conf manager or %NULL in case the
         *   value is not available.
         */
        get_dns_rc_manager(): string;
        get_instance_flags(): NM.ClientInstanceFlags;
        /**
         * Gets NetworkManager current logging level and domains.
         *
         * @returns %TRUE on success, %FALSE otherwise
         */
        get_logging(): [boolean, string, string];
        /**
         * The #NMClient instance is permanently associated with the current
         * thread default #GMainContext, referenced the time when the instance
         * was created. To receive events, the user must iterate this context
         * and can use it to synchronize access to the client.
         *
         * Note that even after #NMClient instance got destroyed, there might
         * still be pending sources registered in the context. That means, to fully
         * clean up, the user must continue iterating the context as long as
         * the nm_client_get_context_busy_watcher() object is alive.
         *
         * @returns the #GMainContext of the client.
         */
        get_main_context(): GLib.MainContext;
        get_metered(): NM.Metered;
        /**
         * Determines whether the daemon is running.
         *
         * @returns %TRUE if the daemon is running
         */
        get_nm_running(): boolean;
        get_object_by_path(dbus_path: string): NM.Object;
        /**
         * Requests the result of a specific permission, which indicates whether the
         * client can or cannot perform the action the permission represents
         *
         * @returns the permission's result, one of #NMClientPermissionResult
         * @param permission the permission for which to return the result, one of #NMClientPermission
         */
        get_permission_result(permission: NM.ClientPermission | null): NM.ClientPermissionResult;
        get_permissions_state(): NM.Ternary;
        /**
         * Gets the #NMActiveConnection corresponding to the primary active
         * network device.
         *
         * In particular, when there is no VPN active, or the VPN does not
         * have the default route, this returns the active connection that has
         * the default route. If there is a VPN active with the default route,
         * then this function returns the active connection that contains the
         * route to the VPN endpoint.
         *
         * If there is no default route, or the default route is over a
         * non-NetworkManager-recognized device, this will return %NULL.
         *
         * @returns the appropriate #NMActiveConnection, if
         * any
         */
        get_primary_connection(): NM.ActiveConnection;
        /**
         * Get radio flags.
         *
         * @returns the #NMRadioFlags.
         */
        get_radio_flags(): NM.RadioFlags;
        /**
         * Tests whether the daemon is still in the process of activating
         * connections at startup.
         *
         * @returns whether the daemon is still starting up
         */
        get_startup(): boolean;
        /**
         * Gets the current daemon state.
         *
         * @returns the current %NMState
         */
        get_state(): NM.State;
        /**
         * Gets NetworkManager version.
         *
         * @returns string with the version (or %NULL if NetworkManager is not running)
         */
        get_version(): string;
        /**
         * If available, the first element in the array is NM_VERSION which
         * encodes the daemon version as "(major << 16 | minor << 8 | micro)".
         * The following elements are a bitfield of %NMVersionInfoCapability
         * that indicate that the daemon supports a certain capability.
         *
         * @returns the
         *   list of capabilities reported by the server or %NULL
         *   if the capabilities are unknown.
         */
        get_version_info(): number[];
        /**
         * Requests that the remote settings service load or reload the given files,
         * adding or updating the connections described within.
         *
         * The changes to the indicated files will not yet be reflected in
         * `client'`s connections array when the function returns.
         *
         * If all of the indicated files were successfully loaded, the
         * function will return %TRUE, and `failures` will be set to %NULL. If
         * NetworkManager tried to load the files, but some (or all) failed,
         * then `failures` will be set to a %NULL-terminated array of the
         * filenames that failed to load.
         *
         * @returns %TRUE on success.
         *
         * Warning: before libnm 1.22, the boolean return value was inconsistent.
         *   That is made worse, because when running against certain server versions
         *   before 1.20, the server would return wrong values for success/failure.
         *   This means, if you use this function in libnm before 1.22, you are advised
         *   to ignore the boolean return value and only look at `failures` and `error`.
         *   With libnm >= 1.22, the boolean return value corresponds to whether `error` was
         *   set. Note that even in the success case, you might have individual `failures`.
         *   With 1.22, the return value is consistent with nm_client_load_connections_finish().
         * @param filenames %NULL-terminated array of filenames to load
         * @param cancellable a #GCancellable, or %NULL
         */
        load_connections(filenames: string[], cancellable?: Gio.Cancellable | null): [boolean, string];
        /**
         * Requests that the remote settings service asynchronously load or reload the
         * given files, adding or updating the connections described within.
         *
         * See nm_client_load_connections() for more details.
         *
         * @param filenames %NULL-terminated array of filenames to load
         * @param cancellable a #GCancellable, or %NULL
         */
        load_connections_async(filenames: string[], cancellable?: Gio.Cancellable | null): globalThis.Promise<string[]>;
        /**
         * Requests that the remote settings service asynchronously load or reload the
         * given files, adding or updating the connections described within.
         *
         * See nm_client_load_connections() for more details.
         *
         * @param filenames %NULL-terminated array of filenames to load
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the operation completes
         */
        load_connections_async(
            filenames: string[],
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Requests that the remote settings service asynchronously load or reload the
         * given files, adding or updating the connections described within.
         *
         * See nm_client_load_connections() for more details.
         *
         * @param filenames %NULL-terminated array of filenames to load
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the operation completes
         */
        load_connections_async(
            filenames: string[],
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<string[]> | void;
        /**
         * Gets the result of an nm_client_load_connections_async() call.
         *
         * See nm_client_load_connections() for more details.
         *
         * @returns %TRUE on success.
         *   Note that even in the success case, you might have individual `failures`.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        load_connections_finish(result: Gio.AsyncResult): [boolean, string[]];
        /**
         * Whether networking is enabled or disabled.
         *
         * @returns %TRUE if networking is enabled, %FALSE if networking is disabled
         */
        networking_get_enabled(): boolean;
        /**
         * Enables or disables networking.  When networking is disabled, all controlled
         * interfaces are disconnected and deactivated.  When networking is enabled,
         * all controlled interfaces are available for activation.
         *
         * @returns %TRUE on success, %FALSE otherwise
         * @param enabled %TRUE to set networking enabled, %FALSE to set networking disabled
         */
        networking_set_enabled(enabled: boolean): boolean;
        /**
         * Reload NetworkManager's configuration and perform certain updates, like
         * flushing caches or rewriting external state to disk. This is similar to
         * sending SIGHUP to NetworkManager but it allows for more fine-grained control
         * over what to reload (see `flags)`. It also allows non-root access via
         * PolicyKit and contrary to signals it is synchronous.
         *
         * @param flags flags indicating what to reload.
         * @param cancellable a #GCancellable, or %NULL
         */
        reload(flags: NM.ManagerReloadFlags | null, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Reload NetworkManager's configuration and perform certain updates, like
         * flushing caches or rewriting external state to disk. This is similar to
         * sending SIGHUP to NetworkManager but it allows for more fine-grained control
         * over what to reload (see `flags)`. It also allows non-root access via
         * PolicyKit and contrary to signals it is synchronous.
         *
         * @param flags flags indicating what to reload.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        reload(
            flags: NM.ManagerReloadFlags | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Reload NetworkManager's configuration and perform certain updates, like
         * flushing caches or rewriting external state to disk. This is similar to
         * sending SIGHUP to NetworkManager but it allows for more fine-grained control
         * over what to reload (see `flags)`. It also allows non-root access via
         * PolicyKit and contrary to signals it is synchronous.
         *
         * @param flags flags indicating what to reload.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the add operation completes
         */
        reload(
            flags: NM.ManagerReloadFlags | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Requests that the remote settings service reload all connection
         * files from disk, adding, updating, and removing connections until
         * the in-memory state matches the on-disk state.
         *
         * @returns %TRUE on success, %FALSE on failure
         * @param cancellable a #GCancellable, or %NULL
         */
        reload_connections(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Requests that the remote settings service begin reloading all connection
         * files from disk, adding, updating, and removing connections until the
         * in-memory state matches the on-disk state.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        reload_connections_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Requests that the remote settings service begin reloading all connection
         * files from disk, adding, updating, and removing connections until the
         * in-memory state matches the on-disk state.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reload operation completes
         */
        reload_connections_async(
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Requests that the remote settings service begin reloading all connection
         * files from disk, adding, updating, and removing connections until the
         * in-memory state matches the on-disk state.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reload operation completes
         */
        reload_connections_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of an nm_client_reload_connections_async() call.
         *
         * @returns %TRUE on success, %FALSE on failure
         * @param result the result passed to the #GAsyncReadyCallback
         */
        reload_connections_finish(result: Gio.AsyncResult): boolean;
        /**
         * Gets the result of a call to nm_client_reload().
         *
         * @returns %TRUE on success or %FALSE on failure.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        reload_finish(result: Gio.AsyncResult): boolean;
        /**
         * Requests that the machine's persistent hostname be set to the specified value
         * or cleared.
         *
         * @returns %TRUE if the request was successful, %FALSE if it failed
         * @param hostname the new persistent hostname to set, or %NULL to
         *   clear any existing persistent hostname
         * @param cancellable a #GCancellable, or %NULL
         */
        save_hostname(hostname?: string | null, cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Requests that the machine's persistent hostname be set to the specified value
         * or cleared.
         *
         * @param hostname the new persistent hostname to set, or %NULL to
         *   clear any existing persistent hostname
         * @param cancellable a #GCancellable, or %NULL
         */
        save_hostname_async(
            hostname?: string | null,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<boolean>;
        /**
         * Requests that the machine's persistent hostname be set to the specified value
         * or cleared.
         *
         * @param hostname the new persistent hostname to set, or %NULL to
         *   clear any existing persistent hostname
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the operation completes
         */
        save_hostname_async(
            hostname: string | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Requests that the machine's persistent hostname be set to the specified value
         * or cleared.
         *
         * @param hostname the new persistent hostname to set, or %NULL to
         *   clear any existing persistent hostname
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the operation completes
         */
        save_hostname_async(
            hostname?: string | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of an nm_client_save_hostname_async() call.
         *
         * @returns %TRUE if the request was successful, %FALSE if it failed
         * @param result the result passed to the #GAsyncReadyCallback
         */
        save_hostname_finish(result: Gio.AsyncResult): boolean;
        /**
         * Sets NetworkManager logging level and/or domains.
         *
         * @returns %TRUE on success, %FALSE otherwise
         * @param level logging level to set (%NULL or an empty string for no change)
         * @param domains logging domains to set. The string should be a list of log
         *   domains separated by ",". (%NULL or an empty string for no change)
         */
        set_logging(level?: string | null, domains?: string | null): boolean;
        /**
         * The way to stop #NMClient is by unrefing it. That will cancel all
         * internally pending async operations. However, as async operations in
         * NMClient use GTask, hence they cannot complete right away. Instead,
         * their (internal) result callback still needs to be dispatched by iterating
         * the client's main context.
         *
         * You thus cannot stop iterating the client's main context until
         * everything is wrapped up. nm_client_get_context_busy_watcher()
         * helps to watch how long that will be.
         *
         * This function automates that waiting. Like all glib async operations
         * this honors the current g_main_context_get_thread_default().
         *
         * In any case, to complete the shutdown, nm_client_get_main_context()
         * must be iterated. If the current g_main_context_get_thread_default() is
         * the same as nm_client_get_main_context(), then `integrate_maincontext`
         * is ignored. In that case, the caller is required to iterate the context
         * for shutdown to complete. Otherwise, if g_main_context_get_thread_default()
         * differs from nm_client_get_main_context() and `integrate_maincontext`
         * is %FALSE, the caller must make sure that both contexts are iterated
         * until completion. Otherwise, if `integrate_maincontext` is %TRUE, then
         * nm_client_get_main_context() will be integrated in g_main_context_get_thread_default().
         * This means, the caller gives nm_client_get_main_context() up until the waiting
         * completes, the function will acquire the context and hook it into
         * g_main_context_get_thread_default().
         * It is a bug to request `integrate_maincontext` while having nm_client_get_main_context()
         * acquired or iterated otherwise because a context can only be acquired once
         * at a time.
         *
         * Shutdown can only complete after all references to `client` were released.
         *
         * It is possible to call this function multiple times for the same client.
         * But note that with `integrate_maincontext` the client's context is acquired,
         * which can only be done once at a time.
         *
         * It is permissible to start waiting before the objects is fully initialized.
         *
         * The function really allows two separate things. To get a notification (callback) when
         * shutdown is complete, and to integrate the client's context in another context.
         * The latter case is useful if the client has a separate context and you hand it
         * over to another GMainContext to wrap up.
         *
         * The main use is to have a NMClient and a separate GMainContext on a worker
         * thread. When being done, you can hand over the cleanup of the context
         * to g_main_context_default(), assuming that the main thread iterates
         * the default context. In that case, you don't need to care about passing
         * a callback to know when shutdown completed.
         *
         * @param integrate_maincontext whether to hook the client's maincontext
         *   in the current thread default. Otherwise, you must ensure
         *   that the client's maincontext gets iterated so that it can complete.
         *   By integrating the maincontext in the current thread default, you
         *   may instead only iterate the latter.
         * @param cancellable the #GCancellable to abort the shutdown.
         * @param callback a #GAsyncReadyCallback to call when the request
         *   is satisfied or %NULL if you don't care about the result of the
         *   method invocation.
         */
        wait_shutdown(
            integrate_maincontext: boolean,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Determines whether WiMAX is enabled.
         *
         * @returns %TRUE if WiMAX is enabled
         */
        wimax_get_enabled(): boolean;
        /**
         * Determines whether the WiMAX hardware is enabled.
         *
         * @returns %TRUE if the WiMAX hardware is enabled
         */
        wimax_hardware_get_enabled(): boolean;
        /**
         * Enables or disables WiMAX devices.
         *
         * @param enabled %TRUE to enable WiMAX
         */
        wimax_set_enabled(enabled: boolean): void;
        /**
         * Determines whether the wireless is enabled.
         *
         * @returns %TRUE if wireless is enabled
         */
        wireless_get_enabled(): boolean;
        /**
         * Determines whether the wireless hardware is enabled.
         *
         * @returns %TRUE if the wireless hardware is enabled
         */
        wireless_hardware_get_enabled(): boolean;
        /**
         * Enables or disables wireless devices.
         *
         * @param enabled %TRUE to enable wireless
         */
        wireless_set_enabled(enabled: boolean): void;
        /**
         * Determines whether WWAN is enabled.
         *
         * @returns %TRUE if WWAN is enabled
         */
        wwan_get_enabled(): boolean;
        /**
         * Determines whether the WWAN hardware is enabled.
         *
         * @returns %TRUE if the WWAN hardware is enabled
         */
        wwan_hardware_get_enabled(): boolean;
        /**
         * Enables or disables WWAN devices.
         *
         * @param enabled %TRUE to enable WWAN
         */
        wwan_set_enabled(enabled: boolean): void;
        // Inherited methods
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init_async(io_priority: number, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        init_async(
            io_priority: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        init_async(
            io_priority: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Finishes asynchronous initialization and returns the result.
         * See g_async_initable_init_async().
         *
         * @returns %TRUE if successful. If an error has occurred, this function
         * will return %FALSE and set `error` appropriately if present.
         * @param res a #GAsyncResult.
         */
        init_finish(res: Gio.AsyncResult): boolean;
        /**
         * Finishes the async construction for the various g_async_initable_new
         * calls, returning the created object or %NULL on error.
         *
         * @returns a newly created #GObject,
         *      or %NULL on error. Free with g_object_unref().
         * @param res the #GAsyncResult from the callback
         */
        new_finish(res: Gio.AsyncResult): NM.Client;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        vfunc_init_async(
            io_priority: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Finishes asynchronous initialization and returns the result.
         * See g_async_initable_init_async().
         *
         * @param res a #GAsyncResult.
         */
        vfunc_init_finish(res: Gio.AsyncResult): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @returns %TRUE if successful. If an error has occurred, this function will
         *     return %FALSE and set `error` appropriately if present.
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        vfunc_init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Disconnects a handler from an instance so it will not be called during any future or currently ongoing emissions of the signal it has been connected to.
         *
         * @param id Handler ID of the handler to be disconnected
         */
        disconnect(id: number): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    abstract class Device extends Object {
        static '$gtype': GObject.GType<Device>;
        // Properties
        /**
         * The #NMActiveConnection object that "owns" this device during activation.
         */
        get active_connection(): NM.ActiveConnection;
        /**
         * The #NMActiveConnection object that "owns" this device during activation.
         */
        get activeConnection(): NM.ActiveConnection;
        /**
         * Whether the device can auto-activate a connection.
         *
         * The property setter is a synchronous D-Bus call. This is deprecated since 1.22.
         */
        get autoconnect(): boolean;
        set autoconnect(val: boolean);
        /**
         * The available connections of the device
         */
        get available_connections(): NM.RemoteConnection[];
        /**
         * The available connections of the device
         */
        get availableConnections(): NM.RemoteConnection[];
        /**
         * The capabilities of the device.
         */
        get capabilities(): NM.DeviceCapabilities;
        /**
         * The numeric type of the device.
         */
        get device_type(): NM.DeviceType;
        /**
         * The numeric type of the device.
         */
        get deviceType(): NM.DeviceType;
        /**
         * The IPv4 #NMDhcpConfig of the device.
         */
        get dhcp4_config(): NM.DhcpConfig;
        /**
         * The IPv4 #NMDhcpConfig of the device.
         */
        get dhcp4Config(): NM.DhcpConfig;
        /**
         * The IPv6 #NMDhcpConfig of the device.
         */
        get dhcp6_config(): NM.DhcpConfig;
        /**
         * The IPv6 #NMDhcpConfig of the device.
         */
        get dhcp6Config(): NM.DhcpConfig;
        /**
         * The driver of the device.
         */
        get driver(): string;
        /**
         * The version of the device driver.
         */
        get driver_version(): string;
        /**
         * The version of the device driver.
         */
        get driverVersion(): string;
        /**
         * When %TRUE indicates the device is likely missing firmware required
         * for its operation.
         */
        get firmware_missing(): boolean;
        /**
         * When %TRUE indicates the device is likely missing firmware required
         * for its operation.
         */
        get firmwareMissing(): boolean;
        /**
         * The firmware version of the device.
         */
        get firmware_version(): string;
        /**
         * The firmware version of the device.
         */
        get firmwareVersion(): string;
        /**
         * The hardware address of the device.
         */
        get hw_address(): string;
        /**
         * The hardware address of the device.
         */
        get hwAddress(): string;
        /**
         * The interface of the device.
         */
        get interface(): string;
        /**
         * The interface flags.
         */
        get interface_flags(): number;
        /**
         * The interface flags.
         */
        get interfaceFlags(): number;
        /**
         * The IP interface of the device which should be used for all IP-related
         * operations like addressing and routing.
         */
        get ip_interface(): string;
        /**
         * The IP interface of the device which should be used for all IP-related
         * operations like addressing and routing.
         */
        get ipInterface(): string;
        /**
         * The #NMIP4Config of the device.
         */
        get ip4_config(): NM.IPConfig;
        /**
         * The #NMIP4Config of the device.
         */
        get ip4Config(): NM.IPConfig;
        /**
         * The IPv4 connectivity state of the device.
         */
        get ip4_connectivity(): NM.ConnectivityState;
        /**
         * The IPv4 connectivity state of the device.
         */
        get ip4Connectivity(): NM.ConnectivityState;
        /**
         * The IPv6 #NMIPConfig of the device.
         */
        get ip6_config(): NM.IPConfig;
        /**
         * The IPv6 #NMIPConfig of the device.
         */
        get ip6Config(): NM.IPConfig;
        /**
         * The IPv6 connectivity state of the device.
         */
        get ip6_connectivity(): NM.ConnectivityState;
        /**
         * The IPv6 connectivity state of the device.
         */
        get ip6Connectivity(): NM.ConnectivityState;
        /**
         * The LLDP neighbors.
         */
        get lldp_neighbors(): any[];
        /**
         * The LLDP neighbors.
         */
        get lldpNeighbors(): any[];
        /**
         * Whether the device is managed by NetworkManager.
         */
        get managed(): boolean;
        /**
         * Whether the device is metered.
         */
        get metered(): number;
        /**
         * The MTU of the device.
         */
        get mtu(): number;
        /**
         * When %TRUE indicates that the NetworkManager plugin for the device
         * is not installed.
         */
        get nm_plugin_missing(): boolean;
        /**
         * When %TRUE indicates that the NetworkManager plugin for the device
         * is not installed.
         */
        get nmPluginMissing(): boolean;
        /**
         * The device path as exposed by the udev property ID_PATH.
         *
         * The string is backslash escaped (C escaping) for invalid
         * characters. The escaping can be reverted with g_strcompress(),
         * however the result may not be valid UTF-8.
         */
        get path(): string;
        /**
         * The physical port ID of the device. (See
         * nm_device_get_physical_port_id().)
         */
        get physical_port_id(): string;
        /**
         * The physical port ID of the device. (See
         * nm_device_get_physical_port_id().)
         */
        get physicalPortId(): string;
        /**
         * The port devices of the controller device. For devices that cannot be
         * controllers this is likely to be always empty.
         */
        get ports(): any[];
        /**
         * The product string of the device.
         */
        get product(): string;
        /**
         * Whether the device is real or is a placeholder device that could
         * be created automatically by NetworkManager if one of its
         * #NMDevice:available-connections was activated.
         */
        get real(): boolean;
        /**
         * The state of the device.
         */
        get state(): NM.DeviceState;
        /**
         * The reason for the device state.
         */
        get state_reason(): number;
        /**
         * The reason for the device state.
         */
        get stateReason(): number;
        /**
         * An operating-system specific device hardware identifier; this is not
         * unique to a specific hardware device across reboots or hotplugs.  It
         * is an opaque string which for some device types (Bluetooth, Modem)
         * contains an identifier provided by the underlying hardware service daemon
         * such as Bluez or ModemManager, and clients can use this property to
         * request more information about the device from those services.
         */
        get udi(): string;
        /**
         * The vendor string of the device.
         */
        get vendor(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Device.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Device.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.Device.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Device.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Device.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Device.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Device.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Device.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Generates a list of short-ish unique presentation names for the
         * devices in `devices`.
         *
         * @param devices an array of #NMDevice
         */
        static disambiguate_names(devices: NM.Device[]): string[];
        // Methods
        /**
         * Validates a given connection for a given #NMDevice object and returns
         * whether the connection may be activated with the device. For example if
         * `device` is a Wi-Fi device that supports only WEP encryption, the connection
         * will only be valid if it is a Wi-Fi connection which describes a WEP or open
         * network, and will not be valid if it describes a WPA network, or if it is
         * an Ethernet, Bluetooth, WWAN, etc connection that is incompatible with the
         * device.
         *
         * This function does the same as nm_device_connection_valid(), i.e. checking
         * compatibility of the given device and connection. But, in addition, it sets
         * GError when FALSE is returned.
         *
         * @returns %TRUE if the connection may be activated with this device, %FALSE
         * if is incompatible with the device's capabilities and characteristics.
         * @param connection an #NMConnection to validate against `device`
         */
        connection_compatible(connection: NM.Connection): boolean;
        /**
         * Validates a given connection for a given #NMDevice object and returns
         * whether the connection may be activated with the device. For example if
         * `device` is a Wi-Fi device that supports only WEP encryption, the connection
         * will only be valid if it is a Wi-Fi connection which describes a WEP or open
         * network, and will not be valid if it describes a WPA network, or if it is
         * an Ethernet, Bluetooth, WWAN, etc connection that is incompatible with the
         * device.
         *
         * @returns %TRUE if the connection may be activated with this device, %FALSE
         * if is incompatible with the device's capabilities and characteristics.
         * @param connection an #NMConnection to validate against `device`
         */
        connection_valid(connection: NM.Connection): boolean;
        /**
         * Deletes the software device. Hardware devices can't be deleted.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error`
         * will be set.
         * @param cancellable a #GCancellable, or %NULL
         */
        delete(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously begins deleting the software device. Hardware devices can't
         * be deleted.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        delete_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously begins deleting the software device. Hardware devices can't
         * be deleted.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when delete operation completes
         */
        delete_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Asynchronously begins deleting the software device. Hardware devices can't
         * be deleted.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when delete operation completes
         */
        delete_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_device_delete_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error`
         * will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        delete_finish(result: Gio.AsyncResult): boolean;
        /**
         * Disconnects the device if currently connected, and prevents the device from
         * automatically connecting to networks until the next manual network connection
         * request.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param cancellable a #GCancellable, or %NULL
         */
        disconnect(cancellable?: Gio.Cancellable | null): boolean;
        // Conflicted with GObject.Object.disconnect
        disconnect(...args: never[]): any;
        /**
         * Asynchronously begins disconnecting the device if currently connected, and
         * prevents the device from automatically connecting to networks until the next
         * manual network connection request.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        disconnect_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously begins disconnecting the device if currently connected, and
         * prevents the device from automatically connecting to networks until the next
         * manual network connection request.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the disconnect operation completes
         */
        disconnect_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Asynchronously begins disconnecting the device if currently connected, and
         * prevents the device from automatically connecting to networks until the next
         * manual network connection request.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the disconnect operation completes
         */
        disconnect_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_device_disconnect_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error`
         * will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        disconnect_finish(result: Gio.AsyncResult): boolean;
        /**
         * Filters a given array of connections for a given #NMDevice object and returns
         * connections which may be activated with the device. For example if `device`
         * is a Wi-Fi device that supports only WEP encryption, the returned array will
         * contain any Wi-Fi connections in `connections` that allow connection to
         * unencrypted or WEP-enabled SSIDs.  The returned array will not contain
         * Ethernet, Bluetooth, Wi-Fi WPA connections, or any other connection that is
         * incompatible with the device. To get the full list of connections see
         * nm_client_get_connections().
         *
         * @returns an array of
         * #NMConnections that could be activated with the given `device`.  The array
         * should be freed with g_ptr_array_unref() when it is no longer required.
         *
         * WARNING: the transfer annotation for this function may not work correctly
         *   with bindings. See https://gitlab.gnome.org/GNOME/gobject-introspection/-/issues/305.
         *   You can filter the list yourself with nm_device_connection_valid().
         * @param connections an array of #NMConnections to filter
         */
        filter_connections(connections: NM.Connection[]): NM.Connection[];
        /**
         * Gets the #NMActiveConnection object which owns this device during activation.
         *
         * @returns the #NMActiveConnection or %NULL if the device is
         * not part of an active connection
         */
        get_active_connection(): NM.ActiveConnection;
        /**
         * Fetch the currently applied connection on the device.
         *
         * @returns a %NMConnection with the currently applied settings
         *   or %NULL on error.
         *
         * The connection is as received from D-Bus and might not validate according
         * to nm_connection_verify().
         * @param flags the flags argument. See #NMDeviceReapplyFlags.
         * @param cancellable a #GCancellable, or %NULL
         */
        get_applied_connection(flags: number, cancellable?: Gio.Cancellable | null): [NM.Connection, number];
        /**
         * Asynchronously begins and gets the currently applied connection.
         *
         * @param flags the flags argument. See #NMDeviceReapplyFlags.
         * @param cancellable a #GCancellable, or %NULL
         */
        get_applied_connection_async(
            flags: number,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<[NM.Connection, number]>;
        /**
         * Asynchronously begins and gets the currently applied connection.
         *
         * @param flags the flags argument. See #NMDeviceReapplyFlags.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reapply operation completes
         */
        get_applied_connection_async(
            flags: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously begins and gets the currently applied connection.
         *
         * @param flags the flags argument. See #NMDeviceReapplyFlags.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reapply operation completes
         */
        get_applied_connection_async(
            flags: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<[NM.Connection, number]> | void;
        /**
         * Gets the result of a call to nm_device_get_applied_connection_async().
         *
         * @returns a currently applied %NMConnection or %NULL in case
         *   of error.
         *
         * The connection is as received from D-Bus and might not validate according
         * to nm_connection_verify().
         * @param result the result passed to the #GAsyncReadyCallback
         */
        get_applied_connection_finish(result: Gio.AsyncResult): [NM.Connection, number];
        /**
         * Whether the #NMDevice can be autoconnected.
         *
         * @returns %TRUE if the device is allowed to be autoconnected
         */
        get_autoconnect(): boolean;
        /**
         * Gets the #NMRemoteConnections currently known to the daemon that could
         * be activated on `device`.
         *
         * @returns the #GPtrArray
         * containing #NMRemoteConnections. This is the internal copy used by
         * the connection, and must not be modified.
         */
        get_available_connections(): NM.RemoteConnection[];
        /**
         * Gets the device' capabilities.
         *
         * @returns the capabilities
         */
        get_capabilities(): NM.DeviceCapabilities;
        /**
         * The connectivity state of the device for given address family.
         * Supported address families are %AF_INET for IPv4, %AF_INET6
         * for IPv6 or %AF_UNSPEC for any.
         *
         * @returns the current connectivity state
         * @param addr_family network address family
         */
        get_connectivity(addr_family: number): NM.ConnectivityState;
        /**
         * Gets a description of `device,` based on its vendor and product names.
         *
         * @returns a description of `device`. If either the vendor or the
         *   product name is unknown, this returns the interface name.
         */
        get_description(): string;
        /**
         * Returns the numeric type of the #NMDevice, ie Ethernet, Wi-Fi, etc.
         *
         * @returns the device type
         */
        get_device_type(): NM.DeviceType;
        /**
         * Gets the current IPv4 #NMDhcpConfig associated with the #NMDevice.
         *
         * You can alternatively use nm_active_connection_get_dhcp4_config(), which also
         * works with VPN connections.
         *
         * @returns the IPv4 #NMDhcpConfig, or %NULL if the device is
         * not activated or not using DHCP.
         */
        get_dhcp4_config(): NM.DhcpConfig;
        /**
         * Gets the current IPv6 #NMDhcpConfig associated with the #NMDevice.
         *
         * You can alternatively use nm_active_connection_get_dhcp6_config(), which also
         * works with VPN connections.
         *
         * @returns the IPv6 #NMDhcpConfig, or %NULL if the device is
         * not activated or not using DHCPv6.
         */
        get_dhcp6_config(): NM.DhcpConfig;
        /**
         * Gets the driver of the #NMDevice.
         *
         * @returns the driver of the device. This is the internal string used by the
         * device, and must not be modified.
         */
        get_driver(): string;
        /**
         * Gets the driver version of the #NMDevice.
         *
         * @returns the version of the device driver. This is the internal string used by the
         * device, and must not be modified.
         */
        get_driver_version(): string;
        /**
         * Indicates that firmware required for the device's operation is likely
         * to be missing.
         *
         * @returns %TRUE if firmware required for the device's operation is likely
         * to be missing.
         */
        get_firmware_missing(): boolean;
        /**
         * Gets the firmware version of the #NMDevice.
         *
         * @returns the firmware version of the device. This is the internal string used by the
         * device, and must not be modified.
         */
        get_firmware_version(): string;
        /**
         * Gets the current a hardware address (MAC) for the `device`.
         *
         * @returns the current MAC of the device, or %NULL.
         * This is the internal string used by the device, and must not be modified.
         */
        get_hw_address(): string;
        /**
         * Gets the interface name of the #NMDevice.
         *
         * @returns the interface of the device. This is the internal string used by the
         * device, and must not be modified.
         */
        get_iface(): string;
        /**
         * Gets the interface flags of the device.
         *
         * @returns the flags
         */
        get_interface_flags(): NM.DeviceInterfaceFlags;
        /**
         * Gets the current IPv4 #NMIPConfig associated with the #NMDevice.
         *
         * You can alternatively use nm_active_connection_get_ip4_config(), which also
         * works with VPN connections.
         *
         * @returns the IPv4 #NMIPConfig, or %NULL if the device is not
         * activated.
         */
        get_ip4_config(): NM.IPConfig;
        /**
         * Gets the current IPv6 #NMIPConfig associated with the #NMDevice.
         *
         * You can alternatively use nm_active_connection_get_ip6_config(), which also
         * works with VPN connections.
         *
         * @returns the IPv6 #NMIPConfig or %NULL if the device is not activated.
         */
        get_ip6_config(): NM.IPConfig;
        /**
         * Gets the IP interface name of the #NMDevice over which IP traffic flows
         * when the device is in the ACTIVATED state.
         *
         * @returns the IP traffic interface of the device. This is the internal string
         * used by the device, and must not be modified.
         */
        get_ip_iface(): string;
        /**
         * Gets the list of neighbors discovered through LLDP.
         *
         * @returns the #GPtrArray
         * containing #NMLldpNeighbor<!-- -->s. This is the internal copy used by the
         * device and must not be modified. The library never modifies the returned
         * array and thus it is safe for callers to reference and keep using it.
         */
        get_lldp_neighbors(): NM.LldpNeighbor[];
        /**
         * Whether the #NMDevice is managed by NetworkManager.
         *
         * @returns %TRUE if the device is managed by NetworkManager
         */
        get_managed(): boolean;
        /**
         * Gets the metered setting of a #NMDevice.
         *
         * @returns the metered setting.
         */
        get_metered(): NM.Metered;
        /**
         * Gets the  MTU of the #NMDevice.
         *
         * @returns the MTU of the device in bytes.
         */
        get_mtu(): number;
        /**
         * Indicates that the NetworkManager plugin for the device is not installed.
         *
         * @returns %TRUE if the device plugin not installed.
         */
        get_nm_plugin_missing(): boolean;
        /**
         * Gets the path of the #NMDevice as exposed by the udev property ID_PATH.
         *
         * @returns the path of the device.
         *
         * The string is backslash escaped (C escaping) for invalid characters. The escaping
         * can be reverted with g_strcompress(), however the result may not be valid UTF-8.
         */
        get_path(): string;
        /**
         * Gets the physical port ID of the #NMDevice. If non-%NULL, this is
         * an opaque string that can be used to recognize when
         * seemingly-unrelated #NMDevices are actually just different virtual
         * ports on a single physical port. (Eg, NPAR / SR-IOV.)
         *
         * @returns the physical port ID of the device, or %NULL if the port
         *   ID is unknown. This is the internal string used by the device and
         *   must not be modified.
         */
        get_physical_port_id(): string;
        /**
         * Gets the devices currently set as port of `device`.
         *
         * @returns the #GPtrArray containing #NMDevices that
         * are ports of `device`. This is the internal copy used by the device and
         * must not be modified.
         */
        get_ports(): NM.Device[];
        /**
         * Gets the product string of the #NMDevice.
         *
         * @returns the product name of the device. This is the internal string used by the
         * device, and must not be modified.
         *
         * The string is backslash escaped (C escaping) for invalid characters. The escaping
         * can be reverted with g_strcompress(), however the result may not be valid UTF-8.
         */
        get_product(): string;
        /**
         * Gets the (primary) #NMSetting subtype associated with connections
         * that can be used on `device`.
         *
         * @returns `device'`s associated #NMSetting type
         */
        get_setting_type(): GObject.GType;
        /**
         * Gets the current #NMDevice state.
         *
         * @returns the current device state
         */
        get_state(): NM.DeviceState;
        /**
         * Gets the reason for entering the current #NMDevice state.
         *
         * @returns the reason for entering the current device state
         */
        get_state_reason(): NM.DeviceStateReason;
        /**
         * Gets a (non-localized) description of the type of device that
         * `device` is.
         *
         * @returns the type description of the device. This is the internal
         * string used by the device, and must not be modified.
         */
        get_type_description(): string;
        /**
         * Gets the Unique Device Identifier of the #NMDevice.
         *
         * @returns the Unique Device Identifier of the device.  This identifier may be
         * used to gather more information about the device from various operating
         * system services like udev or sysfs.
         */
        get_udi(): string;
        /**
         * Gets the vendor string of the #NMDevice.
         *
         * @returns the vendor name of the device. This is the internal string used by the
         * device, and must not be modified.
         *
         * The string is backslash escaped (C escaping) for invalid characters. The escaping
         * can be reverted with g_strcompress(), however the result may not be valid UTF-8.
         */
        get_vendor(): string;
        is_real(): boolean;
        /**
         * Whether the device is a software device.
         *
         * @returns %TRUE if `device` is a software device, %FALSE if it is a hardware device.
         */
        is_software(): boolean;
        /**
         * Attempts to update device with changes to the currently active connection
         * made since it was last applied.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param connection the #NMConnection to replace the applied
         *   settings with or %NULL to reuse existing
         * @param version_id zero or the expected version id of the applied connection.
         *   If specified and the version id mismatches, the call fails without
         *   modification. This allows one to catch concurrent accesses.
         * @param flags always set this to zero
         * @param cancellable a #GCancellable, or %NULL
         */
        reapply(
            connection: NM.Connection | null,
            version_id: number,
            flags: number,
            cancellable?: Gio.Cancellable | null,
        ): boolean;
        /**
         * Asynchronously begins an attempt to update device with changes to the
         * currently active connection made since it was last applied.
         *
         * @param connection the #NMConnection to replace the applied
         *   settings with or %NULL to reuse existing
         * @param version_id zero or the expected version id of the applied
         *   connection. If specified and the version id mismatches, the call
         *   fails without modification. This allows one to catch concurrent
         *   accesses.
         * @param flags always set this to zero
         * @param cancellable a #GCancellable, or %NULL
         */
        reapply_async(
            connection: NM.Connection | null,
            version_id: number,
            flags: number,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<boolean>;
        /**
         * Asynchronously begins an attempt to update device with changes to the
         * currently active connection made since it was last applied.
         *
         * @param connection the #NMConnection to replace the applied
         *   settings with or %NULL to reuse existing
         * @param version_id zero or the expected version id of the applied
         *   connection. If specified and the version id mismatches, the call
         *   fails without modification. This allows one to catch concurrent
         *   accesses.
         * @param flags always set this to zero
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reapply operation completes
         */
        reapply_async(
            connection: NM.Connection | null,
            version_id: number,
            flags: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously begins an attempt to update device with changes to the
         * currently active connection made since it was last applied.
         *
         * @param connection the #NMConnection to replace the applied
         *   settings with or %NULL to reuse existing
         * @param version_id zero or the expected version id of the applied
         *   connection. If specified and the version id mismatches, the call
         *   fails without modification. This allows one to catch concurrent
         *   accesses.
         * @param flags always set this to zero
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the reapply operation completes
         */
        reapply_async(
            connection: NM.Connection | null,
            version_id: number,
            flags: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_device_reapply_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error`
         * will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        reapply_finish(result: Gio.AsyncResult): boolean;
        /**
         * Enables or disables automatic activation of the #NMDevice.
         *
         * @param autoconnect %TRUE to enable autoconnecting
         */
        set_autoconnect(autoconnect: boolean): void;
        /**
         * Enables or disables management of  #NMDevice by NetworkManager.
         *
         * @param managed %TRUE to make the device managed by NetworkManager.
         */
        set_managed(managed: boolean): void;
    }
    class Device6Lowpan extends Device {
        static '$gtype': GObject.GType<Device6Lowpan>;
        // Properties
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Device6Lowpan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Device6Lowpan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.Device6Lowpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Device6Lowpan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Device6Lowpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Device6Lowpan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Device6Lowpan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Device6Lowpan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_parent(): NM.Device;
    }
    class DeviceAdsl extends Device {
        static '$gtype': GObject.GType<DeviceAdsl>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceAdsl.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceAdsl.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceAdsl.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceAdsl.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceAdsl.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceAdsl.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceAdsl.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceAdsl.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
    }
    class DeviceBond extends Device {
        static '$gtype': GObject.GType<DeviceBond>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * The devices attached as port to the bond device.
         */
        get slaves(): NM.Device[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceBond.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceBond.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceBond.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBond.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceBond.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBond.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceBond.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceBond.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
        /**
         * Gets the devices currently attached as port to `device`.
         *
         * @returns the #GPtrArray containing
         * #NMDevices that are slaves of `device`. This is the internal
         * copy used by the device, and must not be modified.
         */
        get_slaves(): NM.Device[];
    }
    class DeviceBridge extends Device {
        static '$gtype': GObject.GType<DeviceBridge>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * The devices attached as port to the bridge device.
         */
        get slaves(): NM.Device[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceBridge.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceBridge.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBridge.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBridge.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceBridge.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceBridge.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
        /**
         * Gets the devices currently attached as port to `device`.
         *
         * @returns the #GPtrArray containing
         * #NMDevices that are ports of `device`. This is the internal
         * copy used by the device, and must not be modified.
         */
        get_slaves(): NM.Device[];
    }
    class DeviceBt extends Device {
        static '$gtype': GObject.GType<DeviceBt>;
        // Properties
        /**
         * The device's bluetooth capabilities, a combination of #NMBluetoothCapabilities.
         */
        get bt_capabilities(): NM.BluetoothCapabilities;
        /**
         * The device's bluetooth capabilities, a combination of #NMBluetoothCapabilities.
         */
        get btCapabilities(): NM.BluetoothCapabilities;
        /**
         * The name of the bluetooth device.
         */
        get name(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceBt.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceBt.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceBt.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBt.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceBt.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceBt.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceBt.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceBt.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the Bluetooth device's usable capabilities.
         *
         * @returns a combination of #NMBluetoothCapabilities
         */
        get_capabilities(): NM.BluetoothCapabilities;
        // Conflicted with NM.Device.get_capabilities
        get_capabilities(...args: never[]): any;
        /**
         * Gets the name of the #NMDeviceBt.
         *
         * @returns the name of the device
         */
        get_name(): string;
    }
    class DeviceDummy extends Device {
        static '$gtype': GObject.GType<DeviceDummy>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceDummy.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceDummy.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceDummy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceDummy.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceDummy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceDummy.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceDummy.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceDummy.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class DeviceEthernet extends Device {
        static '$gtype': GObject.GType<DeviceEthernet>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * The permanent hardware (MAC) address of the device.
         */
        get perm_hw_address(): string;
        /**
         * The permanent hardware (MAC) address of the device.
         */
        get permHwAddress(): string;
        /**
         * Identifies subchannels of this network device used for
         * communication with z/VM or s390 host.
         */
        get s390_subchannels(): string[];
        /**
         * Identifies subchannels of this network device used for
         * communication with z/VM or s390 host.
         */
        get s390Subchannels(): string[];
        /**
         * The speed of the device.
         */
        get speed(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceEthernet.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceEthernet.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceEthernet.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceEthernet.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceEthernet.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceEthernet.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceEthernet.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceEthernet.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
        /**
         * Gets the permanent hardware (MAC) address of the #NMDeviceEthernet
         *
         * @returns the permanent hardware address. This is the internal string used by the
         * device, and must not be modified.
         */
        get_permanent_hw_address(): string;
        /**
         * Return the list of s390 subchannels if the device supports them.
         *
         * @returns array of strings, each specifying
         *   one subchannel the s390 device uses to communicate to the host.
         */
        get_s390_subchannels(): string[];
        /**
         * Gets the speed of the #NMDeviceEthernet in Mbit/s.
         *
         * @returns the speed of the device in Mbit/s
         */
        get_speed(): number;
    }
    class DeviceGeneric extends Device {
        static '$gtype': GObject.GType<DeviceGeneric>;
        // Properties
        /**
         * A description of the specific type of device this is, or %NULL
         * if not known.
         */
        get type_description(): string;
        /**
         * A description of the specific type of device this is, or %NULL
         * if not known.
         */
        get typeDescription(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceGeneric.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceGeneric.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceGeneric.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceGeneric.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceGeneric.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceGeneric.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceGeneric.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceGeneric.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class DeviceHsr extends Device {
        static '$gtype': GObject.GType<DeviceHsr>;
        // Properties
        /**
         * The device last byte of the supervision address.
         */
        get multicast_spec(): number;
        /**
         * The device last byte of the supervision address.
         */
        get multicastSpec(): number;
        /**
         * The device's port1 device.
         */
        get port1(): NM.Device;
        /**
         * The device's port2 device.
         */
        get port2(): NM.Device;
        /**
         * Whether the PRP protocol is used or not.
         */
        get prp(): boolean;
        /**
         * The device supervision MAC adddress.
         */
        get supervision_address(): string;
        /**
         * The device supervision MAC adddress.
         */
        get supervisionAddress(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceHsr.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceHsr.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceHsr.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceHsr.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceHsr.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceHsr.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceHsr.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceHsr.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_multicast_spec(): number;
        get_port1(): NM.Device;
        get_port2(): NM.Device;
        get_prp(): boolean;
        get_supervision_address(): string;
    }
    class DeviceIPTunnel extends Device {
        static '$gtype': GObject.GType<DeviceIPTunnel>;
        // Properties
        /**
         * How many additional levels of encapsulation are permitted to
         * be prepended to packets. This property applies only to IPv6
         * tunnels.
         */
        get encapsulation_limit(): number;
        /**
         * How many additional levels of encapsulation are permitted to
         * be prepended to packets. This property applies only to IPv6
         * tunnels.
         */
        get encapsulationLimit(): number;
        /**
         * Tunnel flags.
         */
        get flags(): number;
        /**
         * The flow label to assign to tunnel packets. This property
         * applies only to IPv6 tunnels.
         */
        get flow_label(): number;
        /**
         * The flow label to assign to tunnel packets. This property
         * applies only to IPv6 tunnels.
         */
        get flowLabel(): number;
        /**
         * The fwmark value to assign to tunnel packets. This property applies only
         * to VTI tunnels.
         */
        get fwmark(): number;
        /**
         * The key used for tunneled input packets, if applicable.
         */
        get input_key(): string;
        /**
         * The key used for tunneled input packets, if applicable.
         */
        get inputKey(): string;
        /**
         * The local endpoint of the tunnel.
         */
        get local(): string;
        /**
         * The tunneling mode of the device.
         */
        get mode(): number;
        /**
         * The key used for tunneled output packets, if applicable.
         */
        get output_key(): string;
        /**
         * The key used for tunneled output packets, if applicable.
         */
        get outputKey(): string;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Whether path MTU discovery is enabled on this tunnel.
         */
        get path_mtu_discovery(): boolean;
        /**
         * Whether path MTU discovery is enabled on this tunnel.
         */
        get pathMtuDiscovery(): boolean;
        /**
         * The remote endpoint of the tunnel.
         */
        get remote(): string;
        /**
         * The type of service (IPv4) or traffic class (IPv6) assigned to
         * tunneled packets.
         */
        get tos(): number;
        /**
         * The TTL assigned to tunneled packets. 0 is a special value
         *  meaning that packets inherit the TTL value
         */
        get ttl(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceIPTunnel.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceIPTunnel.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceIPTunnel.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceIPTunnel.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceIPTunnel.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceIPTunnel.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceIPTunnel.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceIPTunnel.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_encapsulation_limit(): number;
        get_flags(): NM.IPTunnelFlags;
        get_flow_label(): number;
        get_fwmark(): number;
        get_input_key(): string;
        get_local(): string;
        get_mode(): NM.IPTunnelMode;
        get_output_key(): string;
        get_parent(): NM.Device;
        get_path_mtu_discovery(): boolean;
        get_remote(): string;
        get_tos(): number;
        get_ttl(): number;
    }
    class DeviceInfiniband extends Device {
        static '$gtype': GObject.GType<DeviceInfiniband>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceInfiniband.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceInfiniband.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceInfiniband.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceInfiniband.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceInfiniband.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceInfiniband.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceInfiniband.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceInfiniband.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
    }
    class DeviceIpvlan extends Device {
        static '$gtype': GObject.GType<DeviceIpvlan>;
        // Properties
        /**
         * The IPVLAN mode.
         */
        get mode(): string;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Whether the device has the private flag.
         */
        get private(): boolean;
        /**
         * Whether the device has the VEPA flag.
         */
        get vepa(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceIpvlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceIpvlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceIpvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceIpvlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceIpvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceIpvlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceIpvlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceIpvlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the IPVLAN mode of the device.
         *
         * @returns the IPVLAN mode. This is the internal string used by the
         * device, and must not be modified.
         */
        get_mode(): string;
        get_parent(): NM.Device;
        /**
         * Gets the private flag of the device.
         *
         * @returns the private flag of the device.
         */
        get_private(): boolean;
        /**
         * Gets the VEPA flag of the device.
         *
         * @returns the VEPA flag of the device.
         */
        get_vepa(): boolean;
    }
    class DeviceLoopback extends Device {
        static '$gtype': GObject.GType<DeviceLoopback>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceLoopback.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceLoopback.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceLoopback.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceLoopback.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceLoopback.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceLoopback.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceLoopback.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceLoopback.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class DeviceMacsec extends Device {
        static '$gtype': GObject.GType<DeviceMacsec>;
        // Properties
        /**
         * The set of cryptographic algorithms in use.
         */
        get cipher_suite(): number;
        /**
         * The set of cryptographic algorithms in use.
         */
        get cipherSuite(): number;
        /**
         * The value of the Association Number (0..3) for the Security
         * Association in use.
         */
        get encoding_sa(): number;
        /**
         * The value of the Association Number (0..3) for the Security
         * Association in use.
         */
        get encodingSa(): number;
        /**
         * Whether encryption of transmitted frames is enabled.
         */
        get encrypt(): boolean;
        /**
         * Whether the ES (End station) bit is enabled in SecTAG for
         * transmitted frames.
         */
        get es(): boolean;
        /**
         * The length of ICV (Integrity Check Value).
         */
        get icv_length(): number;
        /**
         * The length of ICV (Integrity Check Value).
         */
        get icvLength(): number;
        /**
         * Whether the SCI is always included in SecTAG for transmitted
         * frames.
         */
        get include_sci(): boolean;
        /**
         * Whether the SCI is always included in SecTAG for transmitted
         * frames.
         */
        get includeSci(): boolean;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Whether protection of transmitted frames is enabled.
         */
        get protect(): boolean;
        /**
         * Whether replay protection is enabled.
         */
        get replay_protect(): boolean;
        /**
         * Whether replay protection is enabled.
         */
        get replayProtect(): boolean;
        /**
         * Whether the SCB (Single Copy Broadcast) bit is enabled in
         * SecTAG for transmitted frames.
         */
        get scb(): boolean;
        /**
         * The Secure Channel Identifier in use.
         */
        get sci(): number;
        /**
         * The validation mode for incoming packets (strict, check,
         * disabled).
         */
        get validation(): string;
        /**
         * The size of the replay window.
         */
        get window(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceMacsec.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceMacsec.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceMacsec.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceMacsec.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceMacsec.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceMacsec.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceMacsec.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceMacsec.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the set of cryptographic algorithms in use
         *
         * @returns the set of cryptographic algorithms in use
         */
        get_cipher_suite(): number;
        /**
         * Gets the value of the Association Number (0..3) for the Security
         * Association in use.
         *
         * @returns the current Security Association
         */
        get_encoding_sa(): number;
        /**
         * Gets whether encryption of transmitted frames is enabled
         *
         * @returns whether encryption is enabled
         */
        get_encrypt(): boolean;
        /**
         * Gets whether the ES (End station) bit is enabled in SecTAG for
         * transmitted frames
         *
         * @returns whether the ES (End station) bit is enabled
         */
        get_es(): boolean;
        /**
         * Gets the length of ICV (Integrity Check Value)
         *
         * @returns the length of ICV
         */
        get_icv_length(): number;
        /**
         * Gets whether the SCI is always included in SecTAG for transmitted
         * frames
         *
         * @returns whether the SCI is always included
         */
        get_include_sci(): boolean;
        get_parent(): NM.Device;
        /**
         * Gets whether protection of transmitted frames is enabled
         *
         * @returns whether protection is enabled
         */
        get_protect(): boolean;
        /**
         * Gets whether replay protection is enabled
         *
         * @returns whether replay protection is enabled
         */
        get_replay_protect(): boolean;
        /**
         * Gets whether the SCB (Single Copy Broadcast) bit is enabled in
         * SecTAG for transmitted frames
         *
         * @returns whether the SCB (Single Copy Broadcast) bit is enabled
         */
        get_scb(): boolean;
        /**
         * Gets the Secure Channel Identifier in use
         *
         * @returns the SCI
         */
        get_sci(): number;
        /**
         * Gets the validation mode for incoming packets (strict, check,
         * disabled)
         *
         * @returns the validation mode
         */
        get_validation(): string;
        /**
         * Gets the size of the replay window
         *
         * @returns size of the replay window
         */
        get_window(): number;
    }
    class DeviceMacvlan extends Device {
        static '$gtype': GObject.GType<DeviceMacvlan>;
        // Properties
        /**
         * The MACVLAN mode.
         */
        get mode(): string;
        /**
         * Whether the device has the no-promiscuos flag.
         */
        get no_promisc(): boolean;
        /**
         * Whether the device has the no-promiscuos flag.
         */
        get noPromisc(): boolean;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Whether the device is a MACVTAP.
         */
        get tap(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceMacvlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceMacvlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceMacvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceMacvlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceMacvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceMacvlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceMacvlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceMacvlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the MACVLAN mode of the device.
         *
         * @returns the MACVLAN mode. This is the internal string used by the
         * device, and must not be modified.
         */
        get_mode(): string;
        /**
         * Gets the no-promiscuous flag of the device.
         *
         * @returns the no-promiscuous flag of the device.
         */
        get_no_promisc(): boolean;
        get_parent(): NM.Device;
        /**
         * Gets the device type (MACVLAN or MACVTAP).
         *
         * @returns %TRUE if the device is a MACVTAP, %FALSE if it is a MACVLAN.
         */
        get_tap(): boolean;
    }
    class DeviceModem extends Device {
        static '$gtype': GObject.GType<DeviceModem>;
        // Properties
        get apn(): string;
        /**
         * The generic family of access technologies the modem currently supports
         * without a firmware reload or reinitialization.
         */
        get current_capabilities(): NM.DeviceModemCapabilities;
        /**
         * The generic family of access technologies the modem currently supports
         * without a firmware reload or reinitialization.
         */
        get currentCapabilities(): NM.DeviceModemCapabilities;
        get device_id(): string;
        get deviceId(): string;
        /**
         * The generic family of access technologies the modem supports.  Not all
         * capabilities are available at the same time however; some modems require
         * a firmware reload or other reinitialization to switch between eg
         * CDMA/EVDO and GSM/UMTS.
         */
        get modem_capabilities(): NM.DeviceModemCapabilities;
        /**
         * The generic family of access technologies the modem supports.  Not all
         * capabilities are available at the same time however; some modems require
         * a firmware reload or other reinitialization to switch between eg
         * CDMA/EVDO and GSM/UMTS.
         */
        get modemCapabilities(): NM.DeviceModemCapabilities;
        get operator_code(): string;
        get operatorCode(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceModem.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceModem.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceModem.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceModem.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceModem.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceModem.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceModem.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceModem.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * The access point name the modem is connected to.
         *
         * @returns the APN name or %NULL if disconnected
         */
        get_apn(): string;
        /**
         * Returns a bitfield of the generic access technology families the modem
         * supports without a firmware reload or reinitialization.  This value
         * represents the network types the modem can immediately connect to.
         *
         * @returns the generic access technology families the modem supports without
         * a firmware reload or other reinitialization
         */
        get_current_capabilities(): NM.DeviceModemCapabilities;
        /**
         * An identifier used by the modem backend (ModemManager) that aims to
         * uniquely identify the a device. Can be used to match a connection to a
         * particular device.
         *
         * @returns a device-id string
         */
        get_device_id(): string;
        /**
         * Returns a bitfield of the generic access technology families the modem
         * supports.  Not all capabilities are available concurrently however; some
         * may require a firmware reload or reinitialization.
         *
         * @returns the generic access technology families the modem supports
         */
        get_modem_capabilities(): NM.DeviceModemCapabilities;
        /**
         * The MCC and MNC (concatenated) of the network the modem is connected to.
         *
         * @returns the operator code or %NULL if disconnected or not a 3GPP modem.
         */
        get_operator_code(): string;
    }
    class DeviceOlpcMesh extends Device {
        static '$gtype': GObject.GType<DeviceOlpcMesh>;
        // Properties
        /**
         * The device's active channel.
         */
        get active_channel(): number;
        /**
         * The device's active channel.
         */
        get activeChannel(): number;
        /**
         * The companion device.
         */
        get companion(): NM.DeviceWifi;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceOlpcMesh.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceOlpcMesh.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceOlpcMesh.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOlpcMesh.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceOlpcMesh.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOlpcMesh.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceOlpcMesh.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceOlpcMesh.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the active channel of the #NMDeviceOlpcMesh device.
         *
         * @returns active channel of the device
         */
        get_active_channel(): number;
        /**
         * Gets the companion device of the #NMDeviceOlpcMesh.
         *
         * @returns the companion of the device of %NULL
         */
        get_companion(): NM.DeviceWifi;
    }
    class DeviceOvsBridge extends Device {
        static '$gtype': GObject.GType<DeviceOvsBridge>;
        // Properties
        /**
         * Gets the ports currently attached as port to the device.
         */
        get slaves(): NM.Device[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceOvsBridge.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceOvsBridge.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceOvsBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsBridge.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceOvsBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsBridge.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceOvsBridge.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceOvsBridge.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the ports currently attached as port to `device`.
         *
         * @returns the #GPtrArray containing
         * #NMDevices that are ports of `device`. This is the internal
         * copy used by the device, and must not be modified.
         */
        get_slaves(): NM.Device[];
    }
    class DeviceOvsInterface extends Device {
        static '$gtype': GObject.GType<DeviceOvsInterface>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceOvsInterface.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceOvsInterface.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceOvsInterface.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsInterface.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceOvsInterface.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsInterface.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceOvsInterface.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceOvsInterface.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class DeviceOvsPort extends Device {
        static '$gtype': GObject.GType<DeviceOvsPort>;
        // Properties
        /**
         * Gets the interfaces currently attached as port to the device.
         */
        get slaves(): NM.Device[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceOvsPort.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceOvsPort.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceOvsPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsPort.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceOvsPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceOvsPort.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceOvsPort.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceOvsPort.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the interfaces currently attached as port to `device`.
         *
         * @returns the #GPtrArray containing
         * #NMDevices that are ports of `device`. This is the internal
         * copy used by the device, and must not be modified.
         */
        get_slaves(): NM.Device[];
    }
    class DevicePpp extends Device {
        static '$gtype': GObject.GType<DevicePpp>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DevicePpp.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DevicePpp.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DevicePpp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DevicePpp.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DevicePpp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DevicePpp.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DevicePpp.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DevicePpp.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class DeviceTeam extends Device {
        static '$gtype': GObject.GType<DeviceTeam>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * The current JSON configuration of the device.
         */
        get config(): string;
        /**
         * The devices attached as port to the team device.
         */
        get slaves(): NM.Device[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceTeam.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceTeam.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceTeam.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceTeam.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceTeam.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceTeam.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceTeam.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceTeam.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
        /**
         * Gets the current JSON configuration of the #NMDeviceTeam
         *
         * @returns the current configuration. This is the internal string used by the
         * device, and must not be modified.
         */
        get_config(): string;
        /**
         * Gets the devices currently attach as port to `device`.
         *
         * @returns the #GPtrArray containing
         * #NMDevices that are ports of `device`. This is the internal
         * copy used by the device, and must not be modified.
         */
        get_slaves(): NM.Device[];
    }
    class DeviceTun extends Device {
        static '$gtype': GObject.GType<DeviceTun>;
        // Properties
        /**
         * The gid of the tunnel group, or -1 if it has no owner.
         */
        get group(): number;
        /**
         * The tunnel mode, either "tun" or "tap".
         */
        get mode(): string;
        /**
         * The tunnel's "TUN_TAP_MQ" flag; true if callers can connect to
         * the tap device multiple times, for multiple send/receive
         * queues.
         */
        get multi_queue(): boolean;
        /**
         * The tunnel's "TUN_TAP_MQ" flag; true if callers can connect to
         * the tap device multiple times, for multiple send/receive
         * queues.
         */
        get multiQueue(): boolean;
        /**
         * The tunnel's "TUN_NO_PI" flag; true if no protocol info is
         * prepended to the tunnel packets.
         */
        get no_pi(): boolean;
        /**
         * The tunnel's "TUN_NO_PI" flag; true if no protocol info is
         * prepended to the tunnel packets.
         */
        get noPi(): boolean;
        /**
         * The uid of the tunnel owner, or -1 if it has no owner.
         */
        get owner(): number;
        /**
         * The tunnel's "TUN_VNET_HDR" flag; true if the tunnel packets
         * include a virtio network header.
         */
        get vnet_hdr(): boolean;
        /**
         * The tunnel's "TUN_VNET_HDR" flag; true if the tunnel packets
         * include a virtio network header.
         */
        get vnetHdr(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceTun.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceTun.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceTun.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceTun.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceTun.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceTun.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceTun.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceTun.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the tunnel group.
         *
         * @returns the gid of the tunnel group, or -1 if it has no owner.
         */
        get_group(): number;
        /**
         * Returns the TUN/TAP mode for the device.
         *
         * @returns 'tun' or 'tap'
         */
        get_mode(): string;
        /**
         * Returns whether the #NMDeviceTun has the IFF_MULTI_QUEUE flag.
         *
         * @returns %TRUE if the device doesn't have the flag, %FALSE otherwise
         */
        get_multi_queue(): boolean;
        /**
         * Returns whether the #NMDeviceTun has the IFF_NO_PI flag.
         *
         * @returns %TRUE if the device has the flag, %FALSE otherwise
         */
        get_no_pi(): boolean;
        /**
         * Gets the tunnel owner.
         *
         * @returns the uid of the tunnel owner, or -1 if it has no owner.
         */
        get_owner(): number;
        /**
         * Returns whether the #NMDeviceTun has the IFF_VNET_HDR flag.
         *
         * @returns %TRUE if the device has the flag, %FALSE otherwise
         */
        get_vnet_hdr(): boolean;
    }
    class DeviceVeth extends DeviceEthernet {
        static '$gtype': GObject.GType<DeviceVeth>;
        // Properties
        /**
         * The device's peer device.
         */
        get peer(): NM.Device;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceVeth.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceVeth.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceVeth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVeth.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceVeth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVeth.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceVeth.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceVeth.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_peer(): NM.Device;
    }
    class DeviceVlan extends Device {
        static '$gtype': GObject.GType<DeviceVlan>;
        // Properties
        /**
         * Whether the device has carrier.
         */
        get carrier(): boolean;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * The device's VLAN ID.
         */
        get vlan_id(): number;
        /**
         * The device's VLAN ID.
         */
        get vlanId(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceVlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceVlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceVlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceVlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceVlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceVlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier
         */
        get_carrier(): boolean;
        get_parent(): NM.Device;
        get_vlan_id(): number;
    }
    class DeviceVrf extends Device {
        static '$gtype': GObject.GType<DeviceVrf>;
        // Properties
        /**
         * The device's VRF table.
         */
        get table(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceVrf.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceVrf.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceVrf.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVrf.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceVrf.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVrf.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceVrf.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceVrf.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_table(): number;
    }
    class DeviceVxlan extends Device {
        static '$gtype': GObject.GType<DeviceVxlan>;
        // Properties
        /**
         * The lifetime in seconds of FDB entries learnt by the kernel.
         */
        get ageing(): number;
        /**
         * Whether the device has carrier.
         *
         * This property is not implemented yet, and the property is always FALSE.
         */
        get carrier(): boolean;
        /**
         * The UDP destination port used to communicate with the remote VXLAN tunnel
         * endpoint.
         */
        get dst_port(): number;
        /**
         * The UDP destination port used to communicate with the remote VXLAN tunnel
         * endpoint.
         */
        get dstPort(): number;
        /**
         * The unicast destination IP address used in outgoing packets when the
         * destination link layer address is not known in the VXLAN device
         * forwarding database or the multicast IP address joined.
         */
        get group(): string;
        /**
         * The device's VXLAN ID.
         */
        get id(): number;
        /**
         * Whether netlink LL ADDR miss notifications are generated.
         */
        get l2miss(): boolean;
        /**
         * Whether netlink IP ADDR miss notifications are generated.
         */
        get l3miss(): boolean;
        /**
         * Whether unknown source link layer addresses and IP addresses are entered
         * into the VXLAN device forwarding database.
         */
        get learning(): boolean;
        /**
         * The maximum number of entries that can be added to the forwarding table.
         */
        get limit(): number;
        /**
         * The source IP address to use in outgoing packets.
         */
        get local(): string;
        /**
         * The devices's parent device.
         */
        get parent(): NM.Device;
        /**
         * Whether ARP proxy is turned on.
         */
        get proxy(): boolean;
        /**
         * Whether route short circuit is turned on.
         */
        get rsc(): boolean;
        /**
         * The maximum UDP source port used to communicate with the remote VXLAN
         * tunnel endpoint.
         */
        get src_port_max(): number;
        /**
         * The maximum UDP source port used to communicate with the remote VXLAN
         * tunnel endpoint.
         */
        get srcPortMax(): number;
        /**
         * The minimum UDP source port used to communicate with the remote VXLAN
         * tunnel endpoint.
         */
        get src_port_min(): number;
        /**
         * The minimum UDP source port used to communicate with the remote VXLAN
         * tunnel endpoint.
         */
        get srcPortMin(): number;
        /**
         * The TOS value to use in outgoing packets.
         */
        get tos(): number;
        /**
         * The time-to-live value to use in outgoing packets.
         */
        get ttl(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceVxlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceVxlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceVxlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVxlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceVxlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceVxlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceVxlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceVxlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_ageing(): number;
        /**
         * Whether the device has carrier.
         *
         * @returns %TRUE if the device has carrier.
         *
         * This property is not implemented yet, and the function always returns
         * FALSE.
         */
        get_carrier(): boolean;
        get_dst_port(): number;
        get_group(): string;
        get_id(): number;
        get_l2miss(): boolean;
        get_l3miss(): boolean;
        get_learning(): boolean;
        get_limit(): number;
        get_local(): string;
        get_parent(): NM.Device;
        get_proxy(): boolean;
        get_rsc(): boolean;
        get_src_port_max(): number;
        get_src_port_min(): number;
        get_tos(): number;
        get_ttl(): number;
    }
    class DeviceWifi extends Device {
        static '$gtype': GObject.GType<DeviceWifi>;
        // Properties
        /**
         * List of all Wi-Fi access points the device can see.
         */
        get access_points(): NM.AccessPoint[];
        /**
         * List of all Wi-Fi access points the device can see.
         */
        get accessPoints(): NM.AccessPoint[];
        /**
         * The active #NMAccessPoint of the device.
         */
        get active_access_point(): NM.AccessPoint;
        /**
         * The active #NMAccessPoint of the device.
         */
        get activeAccessPoint(): NM.AccessPoint;
        /**
         * The bit rate of the device in kbit/s.
         */
        get bitrate(): number;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last finished
         * network scan. A value of -1 means the device never scanned for
         * access points.
         */
        get last_scan(): number;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last finished
         * network scan. A value of -1 means the device never scanned for
         * access points.
         */
        get lastScan(): number;
        /**
         * The mode of the device.
         */
        get mode(): NM.__80211Mode;
        /**
         * The hardware (MAC) address of the device.
         */
        get perm_hw_address(): string;
        /**
         * The hardware (MAC) address of the device.
         */
        get permHwAddress(): string;
        /**
         * The wireless capabilities of the device.
         */
        get wireless_capabilities(): NM.DeviceWifiCapabilities;
        /**
         * The wireless capabilities of the device.
         */
        get wirelessCapabilities(): NM.DeviceWifiCapabilities;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceWifi.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceWifi.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceWifi.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWifi.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceWifi.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWifi.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceWifi.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceWifi.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets a #NMAccessPoint by path.
         *
         * @returns the access point or %NULL if none is found.
         * @param path the object path of the access point
         */
        get_access_point_by_path(path: string): NM.AccessPoint;
        /**
         * Gets all the scanned access points of the #NMDeviceWifi.
         *
         * @returns a #GPtrArray containing all the
         * scanned #NMAccessPoints.
         * The returned array is owned by the client and should not be modified.
         */
        get_access_points(): NM.AccessPoint[];
        /**
         * Gets the active #NMAccessPoint.
         *
         * @returns the access point or %NULL if none is active
         */
        get_active_access_point(): NM.AccessPoint;
        /**
         * Gets the bit rate of the #NMDeviceWifi in kbit/s.
         *
         * @returns the bit rate (kbit/s)
         */
        get_bitrate(): number;
        /**
         * Gets the Wi-Fi capabilities of the #NMDeviceWifi.
         *
         * @returns the capabilities
         */
        get_capabilities(): NM.DeviceWifiCapabilities;
        // Conflicted with NM.Device.get_capabilities
        get_capabilities(...args: never[]): any;
        /**
         * Returns the timestamp (in CLOCK_BOOTTIME milliseconds) for the last finished
         * network scan. A value of -1 means the device never scanned for access points.
         *
         * Use nm_utils_get_timestamp_msec() to obtain current time value suitable for
         * comparing to this value.
         *
         * @returns the last scan time in milliseconds (in clock_gettime(CLOCK_BOOTTIME) scale).
         */
        get_last_scan(): number;
        /**
         * Gets the #NMDeviceWifi mode.
         *
         * @returns the mode
         */
        get_mode(): NM.__80211Mode;
        /**
         * Gets the permanent hardware (MAC) address of the #NMDeviceWifi
         *
         * @returns the permanent hardware address. This is the internal string used by the
         * device, and must not be modified.
         */
        get_permanent_hw_address(): string;
        /**
         * Request NM to scan for access points on `device`. Note that the function
         * returns immediately after requesting the scan, and it may take some time
         * after that for the scan to complete.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be
         * set.
         * @param cancellable a #GCancellable, or %NULL
         */
        request_scan(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Request NM to scan for access points on `device`. Note that `callback` will be
         * called immediately after requesting the scan, and it may take some time after
         * that for the scan to complete.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        request_scan_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Request NM to scan for access points on `device`. Note that `callback` will be
         * called immediately after requesting the scan, and it may take some time after
         * that for the scan to complete.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the scan has been requested
         */
        request_scan_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Request NM to scan for access points on `device`. Note that `callback` will be
         * called immediately after requesting the scan, and it may take some time after
         * that for the scan to complete.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the scan has been requested
         */
        request_scan_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_device_wifi_request_scan_async() and
         * nm_device_wifi_request_scan_options_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be
         * set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        request_scan_finish(result: Gio.AsyncResult): boolean;
        /**
         * Request NM to scan for access points on `device`. Note that the function
         * returns immediately after requesting the scan, and it may take some time
         * after that for the scan to complete.
         * This is the same as `nm_device_wifi_request_scan` except it accepts `options`
         * for the scanning. The argument is the dictionary passed to RequestScan()
         * D-Bus call. Valid options inside the dictionary are:
         * 'ssids' => array of SSIDs (saay)
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be
         * set.
         * @param options dictionary with options for RequestScan(), or %NULL
         * @param cancellable a #GCancellable, or %NULL
         */
        request_scan_options(options: GLib.Variant, cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Request NM to scan for access points on `device`. Note that `callback` will be
         * called immediately after requesting the scan, and it may take some time after
         * that for the scan to complete.
         * This is the same as `nm_device_wifi_request_scan_async` except it accepts `options`
         * for the scanning. The argument is the dictionary passed to RequestScan()
         * D-Bus call. Valid options inside the dictionary are:
         * 'ssids' => array of SSIDs (saay)
         *
         * To complete the request call nm_device_wifi_request_scan_finish().
         *
         * @param options dictionary with options for RequestScan(), or %NULL
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the scan has been requested
         */
        request_scan_options_async(
            options: GLib.Variant,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): void;
    }
    class DeviceWifiP2P extends Device {
        static '$gtype': GObject.GType<DeviceWifiP2P>;
        // Properties
        /**
         * List of all Wi-Fi P2P peers the device can see.
         */
        get peers(): NM.WifiP2PPeer[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceWifiP2P.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceWifiP2P.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceWifiP2P.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWifiP2P.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceWifiP2P.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWifiP2P.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceWifiP2P.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceWifiP2P.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets a #NMWifiP2PPeer by path.
         *
         * @returns the peer or %NULL if none is found.
         * @param path the object path of the peer
         */
        get_peer_by_path(path: string): NM.WifiP2PPeer;
        /**
         * Gets all the found peers of the #NMDeviceWifiP2P.
         *
         * @returns a #GPtrArray containing all the
         *          found #NMWifiP2PPeers.
         * The returned array is owned by the client and should not be modified.
         */
        get_peers(): NM.WifiP2PPeer[];
        /**
         * Request NM to search for Wi-Fi P2P peers on `device`. Note that the call
         * returns immediately after requesting the find, and it may take some time
         * after that for peers to be found.
         *
         * The find operation will run for 30s by default. You can stop it earlier
         * using nm_device_p2p_wifi_stop_find().
         *
         * @param options optional options passed to StartFind.
         * @param cancellable a #GCancellable, or %NULL
         */
        start_find(options?: GLib.Variant | null, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Request NM to search for Wi-Fi P2P peers on `device`. Note that the call
         * returns immediately after requesting the find, and it may take some time
         * after that for peers to be found.
         *
         * The find operation will run for 30s by default. You can stop it earlier
         * using nm_device_p2p_wifi_stop_find().
         *
         * @param options optional options passed to StartFind.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback a #GAsyncReadyCallback, or %NULL
         */
        start_find(
            options: GLib.Variant | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Request NM to search for Wi-Fi P2P peers on `device`. Note that the call
         * returns immediately after requesting the find, and it may take some time
         * after that for peers to be found.
         *
         * The find operation will run for 30s by default. You can stop it earlier
         * using nm_device_p2p_wifi_stop_find().
         *
         * @param options optional options passed to StartFind.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback a #GAsyncReadyCallback, or %NULL
         */
        start_find(
            options?: GLib.Variant | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Finish an operation started by nm_device_wifi_p2p_start_find().
         *
         * @returns %TRUE if the call was successful
         * @param result the #GAsyncResult
         */
        start_find_finish(result: Gio.AsyncResult): boolean;
        /**
         * Request NM to stop any ongoing find operation for Wi-Fi P2P peers on `device`.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        stop_find(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Request NM to stop any ongoing find operation for Wi-Fi P2P peers on `device`.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback a #GAsyncReadyCallback, or %NULL
         */
        stop_find(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Request NM to stop any ongoing find operation for Wi-Fi P2P peers on `device`.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback a #GAsyncReadyCallback, or %NULL
         */
        stop_find(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Finish an operation started by nm_device_wifi_p2p_stop_find().
         *
         * @returns %TRUE if the call was successful
         * @param result the #GAsyncResult
         */
        stop_find_finish(result: Gio.AsyncResult): boolean;
    }
    class DeviceWimax extends Device {
        static '$gtype': GObject.GType<DeviceWimax>;
        // Properties
        /**
         * The active #NMWimaxNsp of the device.
         */
        get active_nsp(): NM.WimaxNsp;
        /**
         * The active #NMWimaxNsp of the device.
         */
        get activeNsp(): NM.WimaxNsp;
        /**
         * The ID of the serving base station as received from the network.  Has
         * no meaning when the device is not connected.
         */
        get bsid(): string;
        /**
         * The center frequency (in KHz) of the radio channel the device is using to
         * communicate with the network when connected.  Has no meaning when the
         * device is not connected.
         */
        get center_frequency(): number;
        /**
         * The center frequency (in KHz) of the radio channel the device is using to
         * communicate with the network when connected.  Has no meaning when the
         * device is not connected.
         */
        get centerFrequency(): number;
        /**
         * CINR (Carrier to Interference + Noise Ratio) of the current radio link
         * in dB.  CINR is a more accurate measure of radio link quality.  Has no
         * meaning when the device is not connected.
         */
        get cinr(): number;
        /**
         * The hardware (MAC) address of the device.
         */
        get hw_address(): string;
        /**
         * The hardware (MAC) address of the device.
         */
        get hwAddress(): string;
        /**
         * List of all WiMAX Network Service Providers the device can see.
         */
        get nsps(): NM.WimaxNsp[];
        /**
         * RSSI of the current radio link in dBm.  This value indicates how strong
         * the raw received RF signal from the base station is, but does not
         * indicate the overall quality of the radio link.  Has no meaning when the
         * device is not connected.
         */
        get rssi(): number;
        /**
         * Average power of the last burst transmitted by the device, in units of
         * 0.5 dBm.  i.e. a TxPower of -11 represents an actual device TX power of
         * -5.5 dBm.  Has no meaning when the device is not connected.
         */
        get tx_power(): number;
        /**
         * Average power of the last burst transmitted by the device, in units of
         * 0.5 dBm.  i.e. a TxPower of -11 represents an actual device TX power of
         * -5.5 dBm.  Has no meaning when the device is not connected.
         */
        get txPower(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceWimax.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceWimax.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceWimax.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWimax.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceWimax.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWimax.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceWimax.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceWimax.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the active #NMWimaxNsp.
         *
         * @returns the access point or %NULL if none is active
         */
        get_active_nsp(): NM.WimaxNsp;
        /**
         * Gets the ID of the serving Base Station when the device is connected.
         *
         * @returns the ID of the serving Base Station, or %NULL
         */
        get_bsid(): string;
        /**
         * Gets the center frequency (in KHz) of the radio channel the device is using
         * to communicate with the network when connected.  Has no meaning when the
         * device is not connected.
         *
         * @returns the center frequency in KHz, or 0
         */
        get_center_frequency(): number;
        /**
         * Gets the CINR (Carrier to Interference + Noise Ratio) of the current radio
         * link in dB.  CINR is a more accurate measure of radio link quality.  Has no
         * meaning when the device is not connected.
         *
         * @returns the CINR in dB, or 0
         */
        get_cinr(): number;
        /**
         * Gets the hardware (MAC) address of the #NMDeviceWimax
         *
         * @returns the hardware address. This is the internal string used by the
         *          device, and must not be modified.
         */
        get_hw_address(): string;
        /**
         * Gets a #NMWimaxNsp by path.
         *
         * @returns the access point or %NULL if none is found.
         * @param path the object path of the NSP
         */
        get_nsp_by_path(path: string): NM.WimaxNsp;
        /**
         * Gets all the scanned NSPs of the #NMDeviceWimax.
         *
         * @returns a #GPtrArray containing
         *          all the scanned #NMWimaxNsps.
         * The returned array is owned by the client and should not be modified.
         */
        get_nsps(): NM.WimaxNsp[];
        /**
         * Gets the RSSI of the current radio link in dBm.  This value indicates how
         * strong the raw received RF signal from the base station is, but does not
         * indicate the overall quality of the radio link.  Has no meaning when the
         * device is not connected.
         *
         * @returns the RSSI in dBm, or 0
         */
        get_rssi(): number;
        /**
         * Average power of the last burst transmitted by the device, in units of
         * 0.5 dBm.  i.e. a TxPower of -11 represents an actual device TX power of
         * -5.5 dBm.  Has no meaning when the device is not connected.
         *
         * @returns the TX power in dBm, or 0
         */
        get_tx_power(): number;
    }
    class DeviceWireGuard extends Device {
        static '$gtype': GObject.GType<DeviceWireGuard>;
        // Properties
        /**
         * Optional firewall mark - see ip-rule(8).
         * Used when setting routing policy for outgoing encrypted packets.
         * Set to 0 to disable the mark (default).
         */
        get fwmark(): number;
        /**
         * Local UDP listen port.
         * Set to 0 to allow a random port to be chosen (default).
         */
        get listen_port(): number;
        /**
         * Local UDP listen port.
         * Set to 0 to allow a random port to be chosen (default).
         */
        get listenPort(): number;
        /**
         * 32-byte public key, derived from the current private key.
         */
        get public_key(): GLib.Bytes;
        /**
         * 32-byte public key, derived from the current private key.
         */
        get publicKey(): GLib.Bytes;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceWireGuard.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceWireGuard.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceWireGuard.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWireGuard.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceWireGuard.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWireGuard.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceWireGuard.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceWireGuard.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the fwmark (firewall mark) for this interface.
         * It can be used to set routing policy for outgoing encrypted packets.
         * See: ip-rule(8)
         *
         * @returns 0 if fwmark not in use, 32-bit fwmark value otherwise
         */
        get_fwmark(): number;
        /**
         * Gets the local UDP port this interface listens on
         *
         * @returns UDP listen port
         */
        get_listen_port(): number;
        /**
         * Gets the public key for this interface
         *
         * @returns the #GBytes containing the 32-byte public key
         */
        get_public_key(): GLib.Bytes;
    }
    class DeviceWpan extends Device {
        static '$gtype': GObject.GType<DeviceWpan>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DeviceWpan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DeviceWpan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DeviceWpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWpan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DeviceWpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DeviceWpan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DeviceWpan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DeviceWpan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    abstract class DhcpConfig extends Object {
        static '$gtype': GObject.GType<DhcpConfig>;
        // Properties
        /**
         * The IP address family of the configuration; either
         * <literal>AF_INET</literal> or <literal>AF_INET6</literal>.
         */
        get family(): number;
        /**
         * The #GHashTable containing options of the configuration.
         */
        get options(): GLib.HashTable<string, string>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.DhcpConfig.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.DhcpConfig.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.DhcpConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DhcpConfig.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.DhcpConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.DhcpConfig.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.DhcpConfig.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.DhcpConfig.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the IP address family of the configuration
         *
         * @returns the IP address family; either <literal>AF_INET</literal> or
         *   <literal>AF_INET6</literal>
         */
        get_family(): number;
        /**
         * Gets one option by option name.
         *
         * @returns the configuration option's value. This is the internal string used by the
         * configuration, and must not be modified.
         * @param option the option to retrieve
         */
        get_one_option(option: string): string;
        /**
         * Gets all the options contained in the configuration.
         *
         * @returns the #GHashTable containing
         * strings for keys and values.  This is the internal copy used by the
         * configuration, and must not be modified.
         */
        get_options(): GLib.HashTable<string, string>;
    }
    abstract class IPConfig extends Object {
        static '$gtype': GObject.GType<IPConfig>;
        // Properties
        /**
         * A #GPtrArray containing the addresses (#NMIPAddress) of the configuration.
         */
        get addresses(): any[];
        /**
         * The array containing domain strings of the configuration.
         */
        get domains(): string[];
        /**
         * The IP address family of the configuration; either
         * <literal>AF_INET</literal> or <literal>AF_INET6</literal>.
         */
        get family(): number;
        /**
         * The IP gateway address of the configuration as string.
         */
        get gateway(): string;
        /**
         * The array containing name server IP addresses of the configuration.
         */
        get nameservers(): string[];
        /**
         * A #GPtrArray containing the routes (#NMIPRoute) of the configuration.
         */
        get routes(): NM.IPRoute[];
        /**
         * The array containing DNS search strings of the configuration.
         */
        get searches(): string[];
        /**
         * The array containing WINS server IP addresses of the configuration.
         * (This will always be empty for IPv6 configurations.)
         */
        get wins_servers(): string[];
        /**
         * The array containing WINS server IP addresses of the configuration.
         * (This will always be empty for IPv6 configurations.)
         */
        get winsServers(): string[];
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.IPConfig.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.IPConfig.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.IPConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.IPConfig.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.IPConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.IPConfig.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.IPConfig.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.IPConfig.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the IP addresses (containing the address, prefix, and gateway).
         *
         * @returns the #GPtrArray
         * containing #NMIPAddress<!-- -->es.  This is the internal copy used by the
         * configuration and must not be modified. The library never modifies the
         * returned array and thus it is safe for callers to reference and keep using it.
         */
        get_addresses(): NM.IPAddress[];
        /**
         * Gets the domain names.
         *
         * @returns the array of domains.
         * (This is never %NULL, though it may be 0-length).
         */
        get_domains(): string[];
        /**
         * Gets the IP address family
         *
         * @returns the IP address family; either <literal>AF_INET</literal> or
         * <literal>AF_INET6</literal>
         */
        get_family(): number;
        /**
         * Gets the IP gateway address.
         *
         * @returns the IP address of the gateway.
         */
        get_gateway(): string;
        /**
         * Gets the domain name servers (DNS).
         *
         * @returns the array of nameserver IP addresses
         */
        get_nameservers(): string[];
        /**
         * Gets the routes.
         *
         * @returns the #GPtrArray containing
         * #NMIPRoute<!-- -->s. This is the internal copy used by the configuration, and must
         * not be modified. The library never modifies the returned array and thus it is
         * safe for callers to reference and keep using it.
         */
        get_routes(): NM.IPRoute[];
        /**
         * Gets the DNS searches.
         *
         * @returns the array of DNS search strings.
         * (This is never %NULL, though it may be 0-length).
         */
        get_searches(): string[];
        /**
         * Gets the Windows Internet Name Service servers (WINS).
         *
         * @returns the arry of WINS server IP address strings.
         * (This is never %NULL, though it may be 0-length.)
         */
        get_wins_servers(): string[];
    }
    abstract class Object extends GObjectClasses.Object {
        static '$gtype': GObject.GType<Object>;
        // Properties
        /**
         * The NMClient instance as returned by nm_object_get_client().
         *
         * When an NMObject gets removed from the NMClient cache,
         * the NMObject:path property stays unchanged, but this client
         * instance gets reset to %NULL. You can use this property to
         * track removal of the object from the cache.
         */
        get client(): NM.Client;
        /**
         * The D-Bus object path.
         *
         * The D-Bus path of an object instance never changes, even if the object
         * gets removed from the cache. To see whether the object is still in the
         * cache, check NMObject:client.
         */
        get path(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Object.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Object.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.Object.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Object.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Object.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Object.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Object.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Object.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the #NMClient instance in which object is cached.
         * Also, if the object got removed from the client cached,
         * this returns %NULL. So it can be used to check whether the
         * object is still alive.
         *
         * @returns the #NMClient cache in which the
         * object can be found, or %NULL if the object is no longer
         * cached.
         */
        get_client(): any | null;
        /**
         * Gets the DBus path of the #NMObject.
         *
         * @returns the object's path. This is the internal string used by the
         * object, and must not be modified.
         *
         * Note that the D-Bus path of an NMObject never changes, even
         * if the instance gets removed from the cache. To find out
         * whether the object is still alive/cached, check nm_object_get_client().
         */
        get_path(): string;
    }
    class RemoteConnection extends Object implements NM.Connection {
        static '$gtype': GObject.GType<RemoteConnection>;
        // Properties
        /**
         * File that stores the connection in case the connection is
         * file-backed.
         */
        get filename(): string;
        /**
         * The flags of the connection as unsigned integer. The values
         * correspond to the #NMSettingsConnectionFlags enum.
         */
        get flags(): number;
        /**
         * %TRUE if the remote connection contains changes that have not been saved
         * to disk, %FALSE if the connection is the same as its on-disk representation.
         */
        get unsaved(): boolean;
        /**
         * The version ID of the profile that is incremented when the profile gets modified.
         * This can be used to track concurrent modifications of the profile.
         */
        get version_id(): number;
        /**
         * The version ID of the profile that is incremented when the profile gets modified.
         * This can be used to track concurrent modifications of the profile.
         */
        get versionId(): number;
        /**
         * %TRUE if the remote connection is visible to the current user, %FALSE if
         * not.  If the connection is not visible then it is essentially useless; it
         * will not contain any settings, and operations such as
         * nm_remote_connection_save() and nm_remote_connection_delete() will always
         * fail. (#NMRemoteSettings will not normally return non-visible connections
         * to callers, but it is possible for a connection's visibility to change
         * after you already have a reference to it.)
         */
        get visible(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.RemoteConnection.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.RemoteConnection.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.RemoteConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.RemoteConnection.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.RemoteConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.RemoteConnection.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.RemoteConnection.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.RemoteConnection.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Send any local changes to the settings and properties of `connection` to
         * NetworkManager. If `save_to_disk` is %TRUE, the updated connection will be saved to
         * disk; if %FALSE, then only the in-memory representation will be changed.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param save_to_disk whether to persist the changes to disk
         * @param cancellable a #GCancellable, or %NULL
         */
        commit_changes(save_to_disk: boolean, cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously sends any local changes to the settings and properties of
         * `connection` to NetworkManager. If `save` is %TRUE, the updated connection will
         * be saved to disk; if %FALSE, then only the in-memory representation will be
         * changed.
         *
         * @param save_to_disk whether to save the changes to persistent storage
         * @param cancellable a #GCancellable, or %NULL
         */
        commit_changes_async(save_to_disk: boolean, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously sends any local changes to the settings and properties of
         * `connection` to NetworkManager. If `save` is %TRUE, the updated connection will
         * be saved to disk; if %FALSE, then only the in-memory representation will be
         * changed.
         *
         * @param save_to_disk whether to save the changes to persistent storage
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the commit operation completes
         */
        commit_changes_async(
            save_to_disk: boolean,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously sends any local changes to the settings and properties of
         * `connection` to NetworkManager. If `save` is %TRUE, the updated connection will
         * be saved to disk; if %FALSE, then only the in-memory representation will be
         * changed.
         *
         * @param save_to_disk whether to save the changes to persistent storage
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the commit operation completes
         */
        commit_changes_async(
            save_to_disk: boolean,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_remote_connection_commit_changes_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        commit_changes_finish(result: Gio.AsyncResult): boolean;
        /**
         * Deletes the connection.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param cancellable a #GCancellable, or %NULL
         */
        delete(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously deletes the connection.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        delete_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously deletes the connection.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the delete operation completes
         */
        delete_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Asynchronously deletes the connection.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the delete operation completes
         */
        delete_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_remote_connection_delete_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        delete_finish(result: Gio.AsyncResult): boolean;
        get_filename(): string;
        get_flags(): NM.SettingsConnectionFlags;
        /**
         * Request the connection's secrets. Note that this is a blocking D-Bus call,
         * not a simple property accessor.
         *
         * @returns a #GVariant of type %NM_VARIANT_TYPE_CONNECTION containing
         * `connection'`s secrets, or %NULL on error.
         *
         * Warning: NMClient contains a cache of objects on D-Bus. This cache gets updated
         *   with D-Bus signals when iterating the GMainContext. This function performs a
         *   (pseudo) blocking D-Bus call. Aside blocking, the result will not be in sync
         *   and not be ordered with the content of the NMClient cache.
         *   This function used to be deprecated between 1.22 and 1.38 releases.
         * @param setting_name the #NMSetting object name to get secrets for
         * @param cancellable a #GCancellable, or %NULL
         */
        get_secrets(setting_name: string, cancellable?: Gio.Cancellable | null): GLib.Variant;
        /**
         * Asynchronously requests the connection's secrets.
         *
         * @param setting_name the #NMSetting object name to get secrets for
         * @param cancellable a #GCancellable, or %NULL
         */
        get_secrets_async(setting_name: string, cancellable?: Gio.Cancellable | null): globalThis.Promise<GLib.Variant>;
        /**
         * Asynchronously requests the connection's secrets.
         *
         * @param setting_name the #NMSetting object name to get secrets for
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the secret request completes
         */
        get_secrets_async(
            setting_name: string,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously requests the connection's secrets.
         *
         * @param setting_name the #NMSetting object name to get secrets for
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the secret request completes
         */
        get_secrets_async(
            setting_name: string,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<GLib.Variant> | void;
        /**
         * Gets the result of a call to nm_remote_connection_get_secrets_async().
         *
         * @returns a #GVariant of type %NM_VARIANT_TYPE_CONNECTION
         *   containing `connection'`s secrets, or %NULL on error.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        get_secrets_finish(result: Gio.AsyncResult): GLib.Variant;
        get_unsaved(): boolean;
        get_version_id(): number;
        /**
         * Checks if the connection is visible to the current user.  If the
         * connection is not visible then it is essentially useless; it will
         * not contain any settings, and operations such as
         * nm_remote_connection_save() and nm_remote_connection_delete() will
         * always fail. (#NMRemoteSettings will not normally return
         * non-visible connections to callers, but it is possible for a
         * connection's visibility to change after you already have a
         * reference to it.)
         *
         * @returns %TRUE if the remote connection is visible to the current
         * user, %FALSE if not.
         */
        get_visible(): boolean;
        /**
         * Saves the connection to disk if the connection has changes that have not yet
         * been written to disk, or if the connection has never been saved.
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param cancellable a #GCancellable, or %NULL
         */
        save(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Saves the connection to disk if the connection has changes that have not yet
         * been written to disk, or if the connection has never been saved.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        save_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Saves the connection to disk if the connection has changes that have not yet
         * been written to disk, or if the connection has never been saved.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the save operation completes
         */
        save_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Saves the connection to disk if the connection has changes that have not yet
         * been written to disk, or if the connection has never been saved.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the save operation completes
         */
        save_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_remote_connection_save_async().
         *
         * @returns %TRUE on success, %FALSE on error, in which case `error` will be set.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        save_finish(result: Gio.AsyncResult): boolean;
        /**
         * Asynchronously calls the Update2() D-Bus method.
         *
         * @param settings optional connection to update the settings.
         * @param flags update-flags
         * @param args optional arguments.
         * @param cancellable a #GCancellable, or %NULL
         */
        update2(
            settings: GLib.Variant | null,
            flags: NM.SettingsUpdate2Flags | null,
            args?: GLib.Variant | null,
            cancellable?: Gio.Cancellable | null,
        ): globalThis.Promise<GLib.Variant>;
        /**
         * Asynchronously calls the Update2() D-Bus method.
         *
         * @param settings optional connection to update the settings.
         * @param flags update-flags
         * @param args optional arguments.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the commit operation completes
         */
        update2(
            settings: GLib.Variant | null,
            flags: NM.SettingsUpdate2Flags | null,
            args: GLib.Variant | null,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Asynchronously calls the Update2() D-Bus method.
         *
         * @param settings optional connection to update the settings.
         * @param flags update-flags
         * @param args optional arguments.
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to be called when the commit operation completes
         */
        update2(
            settings: GLib.Variant | null,
            flags: NM.SettingsUpdate2Flags | null,
            args?: GLib.Variant | null,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<GLib.Variant> | void;
        /**
         * Gets the result of a call to nm_remote_connection_commit_changes_async().
         *
         * @returns on success, a #GVariant of type "a{sv}" with the result. On failure,
         *   %NULL.
         * @param result the result passed to the #GAsyncReadyCallback
         */
        update2_finish(result: Gio.AsyncResult): GLib.Variant;
        // Inherited methods
        /**
         * Adds a #NMSetting to the connection, replacing any previous #NMSetting of the
         * same name which has previously been added to the #NMConnection.  The
         * connection takes ownership of the #NMSetting object and does not increase
         * the setting object's reference count.
         *
         * @param setting the #NMSetting to add to the connection object
         */
        add_setting(setting: NM.Setting): void;
        /**
         * Clears and frees any secrets that may be stored in the connection, to avoid
         * keeping secret data in memory when not needed.
         */
        clear_secrets(): void;
        /**
         * Clears and frees secrets determined by `func`.
         *
         * @param func function to be called to determine whether a
         *     specific secret should be cleared or not. If %NULL, all secrets are cleared.
         */
        clear_secrets_with_flags(func?: NM.SettingClearSecretsWithFlagsFn | null): void;
        /**
         * Deletes all of `connection'`s settings.
         */
        clear_settings(): void;
        /**
         * Compares two #NMConnection objects for similarity, with comparison behavior
         * modified by a set of flags.  See nm_setting_compare() for a description of
         * each flag's behavior.
         *
         * @returns %TRUE if the comparison succeeds, %FALSE if it does not
         * @param b a second #NMConnection to compare with the first
         * @param flags compare flags, e.g. %NM_SETTING_COMPARE_FLAG_EXACT
         */
        compare(b: NM.Connection, flags: NM.SettingCompareFlags | null): boolean;
        /**
         * Print the connection (including secrets!) to stdout. For debugging
         * purposes ONLY, should NOT be used for serialization of the setting,
         * or machine-parsed in any way. The output format is not guaranteed to
         * be stable and may change at any time.
         */
        dump(): void;
        /**
         * Iterates over the properties of each #NMSetting object in the #NMConnection,
         * calling the supplied user function for each property.
         *
         * @param func user-supplied function called for each setting's property
         */
        for_each_setting_value(func: NM.SettingValueIterFn): void;
        /**
         * A shortcut to return the type from the connection's #NMSettingConnection.
         *
         * @returns the type from the connection's 'connection' setting
         */
        get_connection_type(): string;
        /**
         * A shortcut to return the ID from the connection's #NMSettingConnection.
         *
         * @returns the ID from the connection's 'connection' setting
         */
        get_id(): string;
        /**
         * Returns the interface name as stored in NMSettingConnection:interface_name.
         * If the connection contains no NMSettingConnection, it will return %NULL.
         *
         * For hardware devices and software devices created outside of NetworkManager,
         * this name is used to match the device. for software devices created by
         * NetworkManager, this is the name of the created interface.
         *
         * @returns Name of the kernel interface or %NULL
         */
        get_interface_name(): string;
        /**
         * Returns the connection's D-Bus path.
         *
         * @returns the D-Bus path of the connection, previously set by a call to
         * nm_connection_set_path().
         */
        get_path(): string;
        /**
         * Gets the #NMSetting with the given #GType, if one has been previously added
         * to the #NMConnection.
         *
         * @returns the #NMSetting, or %NULL if no setting of that type was previously
         * added to the #NMConnection
         * @param setting_type the #GType of the setting object to return
         */
        get_setting(setting_type: GObject.GType): NM.Setting;
        /**
         * A shortcut to return any #NMSetting8021x the connection might contain.
         *
         * @returns an #NMSetting8021x if the connection contains one, otherwise %NULL
         */
        get_setting_802_1x(): NM.Setting8021x;
        /**
         * A shortcut to return any #NMSettingAdsl the connection might contain.
         *
         * @returns an #NMSettingAdsl if the connection contains one, otherwise %NULL
         */
        get_setting_adsl(): NM.SettingAdsl;
        /**
         * A shortcut to return any #NMSettingBluetooth the connection might contain.
         *
         * @returns an #NMSettingBluetooth if the connection contains one, otherwise %NULL
         */
        get_setting_bluetooth(): NM.SettingBluetooth;
        /**
         * A shortcut to return any #NMSettingBond the connection might contain.
         *
         * @returns an #NMSettingBond if the connection contains one, otherwise %NULL
         */
        get_setting_bond(): NM.SettingBond;
        /**
         * A shortcut to return any #NMSettingBridge the connection might contain.
         *
         * @returns an #NMSettingBridge if the connection contains one, otherwise %NULL
         */
        get_setting_bridge(): NM.SettingBridge;
        /**
         * A shortcut to return any #NMSettingBridgePort the connection might contain.
         *
         * @returns an #NMSettingBridgePort if the connection contains one, otherwise %NULL
         */
        get_setting_bridge_port(): NM.SettingBridgePort;
        /**
         * Gets the #NMSetting with the given name, if one has been previously added
         * the #NMConnection.
         *
         * @returns the #NMSetting, or %NULL if no setting with that name was previously
         * added to the #NMConnection
         * @param name a setting name
         */
        get_setting_by_name(name: string): NM.Setting;
        /**
         * A shortcut to return any #NMSettingCdma the connection might contain.
         *
         * @returns an #NMSettingCdma if the connection contains one, otherwise %NULL
         */
        get_setting_cdma(): NM.SettingCdma;
        /**
         * A shortcut to return any #NMSettingConnection the connection might contain.
         *
         * @returns an #NMSettingConnection if the connection contains one, otherwise %NULL
         */
        get_setting_connection(): NM.SettingConnection;
        /**
         * A shortcut to return any #NMSettingDcb the connection might contain.
         *
         * @returns an #NMSettingDcb if the connection contains one, otherwise NULL
         */
        get_setting_dcb(): NM.SettingDcb;
        /**
         * A shortcut to return any #NMSettingDummy the connection might contain.
         *
         * @returns an #NMSettingDummy if the connection contains one, otherwise %NULL
         */
        get_setting_dummy(): NM.SettingDummy;
        /**
         * A shortcut to return any #NMSettingGeneric the connection might contain.
         *
         * @returns an #NMSettingGeneric if the connection contains one, otherwise NULL
         */
        get_setting_generic(): NM.SettingGeneric;
        /**
         * A shortcut to return any #NMSettingGsm the connection might contain.
         *
         * @returns an #NMSettingGsm if the connection contains one, otherwise %NULL
         */
        get_setting_gsm(): NM.SettingGsm;
        /**
         * A shortcut to return any #NMSettingInfiniband the connection might contain.
         *
         * @returns an #NMSettingInfiniband if the connection contains one, otherwise %NULL
         */
        get_setting_infiniband(): NM.SettingInfiniband;
        /**
         * A shortcut to return any #NMSettingIP4Config the connection might contain.
         *
         * Note that it returns the value as type #NMSettingIPConfig, since the vast
         * majority of IPv4-setting-related methods are on that type, not
         * #NMSettingIP4Config.
         *
         * @returns an #NMSettingIP4Config if the
         * connection contains one, otherwise %NULL
         */
        get_setting_ip4_config(): NM.SettingIP4Config;
        /**
         * A shortcut to return any #NMSettingIP6Config the connection might contain.
         *
         * Note that it returns the value as type #NMSettingIPConfig, since the vast
         * majority of IPv6-setting-related methods are on that type, not
         * #NMSettingIP6Config.
         *
         * @returns an #NMSettingIP6Config if the
         * connection contains one, otherwise %NULL
         */
        get_setting_ip6_config(): NM.SettingIP6Config;
        /**
         * A shortcut to return any #NMSettingIPTunnel the connection might contain.
         *
         * @returns an #NMSettingIPTunnel if the connection contains one, otherwise %NULL
         */
        get_setting_ip_tunnel(): NM.SettingIPTunnel;
        /**
         * A shortcut to return any #NMSettingMacsec the connection might contain.
         *
         * @returns an #NMSettingMacsec if the connection contains one, otherwise %NULL
         */
        get_setting_macsec(): NM.SettingMacsec;
        /**
         * A shortcut to return any #NMSettingMacvlan the connection might contain.
         *
         * @returns an #NMSettingMacvlan if the connection contains one, otherwise %NULL
         */
        get_setting_macvlan(): NM.SettingMacvlan;
        /**
         * A shortcut to return any #NMSettingOlpcMesh the connection might contain.
         *
         * @returns an #NMSettingOlpcMesh if the connection contains one, otherwise %NULL
         */
        get_setting_olpc_mesh(): NM.SettingOlpcMesh;
        /**
         * A shortcut to return any #NMSettingOvsBridge the connection might contain.
         *
         * @returns an #NMSettingOvsBridge if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_bridge(): NM.SettingOvsBridge;
        /**
         * A shortcut to return any #NMSettingOvsInterface the connection might contain.
         *
         * @returns an #NMSettingOvsInterface if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_interface(): NM.SettingOvsInterface;
        /**
         * A shortcut to return any #NMSettingOvsPatch the connection might contain.
         *
         * @returns an #NMSettingOvsPatch if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_patch(): NM.SettingOvsPatch;
        /**
         * A shortcut to return any #NMSettingOvsPort the connection might contain.
         *
         * @returns an #NMSettingOvsPort if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_port(): NM.SettingOvsPort;
        /**
         * A shortcut to return any #NMSettingPpp the connection might contain.
         *
         * @returns an #NMSettingPpp if the connection contains one, otherwise %NULL
         */
        get_setting_ppp(): NM.SettingPpp;
        /**
         * A shortcut to return any #NMSettingPppoe the connection might contain.
         *
         * @returns an #NMSettingPppoe if the connection contains one, otherwise %NULL
         */
        get_setting_pppoe(): NM.SettingPppoe;
        /**
         * A shortcut to return any #NMSettingProxy the connection might contain.
         *
         * @returns an #NMSettingProxy if the connection contains one, otherwise %NULL
         */
        get_setting_proxy(): NM.SettingProxy;
        /**
         * A shortcut to return any #NMSettingSerial the connection might contain.
         *
         * @returns an #NMSettingSerial if the connection contains one, otherwise %NULL
         */
        get_setting_serial(): NM.SettingSerial;
        /**
         * A shortcut to return any #NMSettingTCConfig the connection might contain.
         *
         * @returns an #NMSettingTCConfig if the connection contains one, otherwise %NULL
         */
        get_setting_tc_config(): NM.SettingTCConfig;
        /**
         * A shortcut to return any #NMSettingTeam the connection might contain.
         *
         * @returns an #NMSettingTeam if the connection contains one, otherwise %NULL
         */
        get_setting_team(): NM.SettingTeam;
        /**
         * A shortcut to return any #NMSettingTeamPort the connection might contain.
         *
         * @returns an #NMSettingTeamPort if the connection contains one, otherwise %NULL
         */
        get_setting_team_port(): NM.SettingTeamPort;
        /**
         * A shortcut to return any #NMSettingTun the connection might contain.
         *
         * @returns an #NMSettingTun if the connection contains one, otherwise %NULL
         */
        get_setting_tun(): NM.SettingTun;
        /**
         * A shortcut to return any #NMSettingVlan the connection might contain.
         *
         * @returns an #NMSettingVlan if the connection contains one, otherwise %NULL
         */
        get_setting_vlan(): NM.SettingVlan;
        /**
         * A shortcut to return any #NMSettingVpn the connection might contain.
         *
         * @returns an #NMSettingVpn if the connection contains one, otherwise %NULL
         */
        get_setting_vpn(): NM.SettingVpn;
        /**
         * A shortcut to return any #NMSettingVxlan the connection might contain.
         *
         * @returns an #NMSettingVxlan if the connection contains one, otherwise %NULL
         */
        get_setting_vxlan(): NM.SettingVxlan;
        /**
         * A shortcut to return any #NMSettingWimax the connection might contain.
         *
         * @returns an #NMSettingWimax if the connection contains one, otherwise %NULL
         */
        get_setting_wimax(): NM.SettingWimax;
        /**
         * A shortcut to return any #NMSettingWired the connection might contain.
         *
         * @returns an #NMSettingWired if the connection contains one, otherwise %NULL
         */
        get_setting_wired(): NM.SettingWired;
        /**
         * A shortcut to return any #NMSettingWireless the connection might contain.
         *
         * @returns an #NMSettingWireless if the connection contains one, otherwise %NULL
         */
        get_setting_wireless(): NM.SettingWireless;
        /**
         * A shortcut to return any #NMSettingWirelessSecurity the connection might contain.
         *
         * @returns an #NMSettingWirelessSecurity if the connection contains one, otherwise %NULL
         */
        get_setting_wireless_security(): NM.SettingWirelessSecurity;
        /**
         * Retrieves the settings in `connection`.
         *
         * The returned array is %NULL-terminated.
         *
         * @returns a
         *   %NULL-terminated array containing every setting of `connection`.
         *   If the connection has no settings, %NULL is returned.
         */
        get_settings(): NM.Setting[] | null;
        /**
         * A shortcut to return the UUID from the connection's #NMSettingConnection.
         *
         * @returns the UUID from the connection's 'connection' setting
         */
        get_uuid(): string;
        /**
         * Returns the name that nm_device_disambiguate_names() would
         * return for the virtual device that would be created for `connection`.
         * Eg, "VLAN (eth1.1)".
         *
         * @returns the name of `connection'`s device,
         *   or %NULL if `connection` is not a virtual connection type
         */
        get_virtual_device_description(): string;
        /**
         * A convenience function to check if the given `connection` is a particular
         * type (ie wired, Wi-Fi, ppp, etc). Checks the #NMSettingConnection:type
         * property of the connection and matches that against `type`.
         *
         * @returns %TRUE if the connection is of the given `type,` %FALSE if not
         * @param type a setting name to check the connection's type against (like
         * %NM_SETTING_WIRELESS_SETTING_NAME or %NM_SETTING_WIRED_SETTING_NAME)
         */
        is_type(type: string): boolean;
        /**
         * Checks if `connection` refers to a virtual device (and thus can potentially be
         * activated even if the device it refers to doesn't exist).
         *
         * @returns whether `connection` refers to a virtual device
         */
        is_virtual(): boolean;
        /**
         * Returns the name of the first setting object in the connection which would
         * need secrets to make a successful connection.  The returned hints are only
         * intended as a guide to what secrets may be required, because in some
         * circumstances, there is no way to conclusively determine exactly which
         * secrets are needed.
         *
         * @returns the setting name of the #NMSetting object which has
         *   invalid or missing secrets
         */
        need_secrets(): [string | null, string[] | null];
        /**
         * Does some basic normalization and fixup of well known inconsistencies
         * and deprecated fields. If the connection was modified in any way,
         * the output parameter `modified` is set %TRUE.
         *
         * Finally the connection will be verified and %TRUE returns if the connection
         * is valid. As this function only performs some specific normalization steps
         * it cannot repair all connections. If the connection has errors that
         * cannot be normalized, the connection will not be modified.
         *
         * @returns %TRUE if the connection is valid, %FALSE if it is not
         * @param parameters a #GHashTable with
         * normalization parameters to allow customization of the normalization by providing
         * specific arguments. Unknown arguments will be ignored and the default will be
         * used. The keys must be strings compared with g_str_equal() function.
         * The values are opaque and depend on the parameter name.
         */
        normalize(parameters: GLib.HashTable<string, any> | null): [boolean, boolean];
        /**
         * Removes the #NMSetting with the given #GType from the #NMConnection.  This
         * operation dereferences the #NMSetting object.
         *
         * @param setting_type the #GType of the setting object to remove
         */
        remove_setting(setting_type: GObject.GType): void;
        /**
         * Replaces `connection'`s settings with `new_settings` (which must be
         * syntactically valid, and describe a known type of connection, but does not
         * need to result in a connection that passes nm_connection_verify()).
         *
         * @returns %TRUE if connection was updated, %FALSE if `new_settings` could not
         *   be deserialized (in which case `connection` will be unchanged).
         * @param new_settings a #GVariant of type %NM_VARIANT_TYPE_CONNECTION, with the new settings
         */
        replace_settings(new_settings: GLib.Variant): boolean;
        /**
         * Deep-copies the settings of `new_connection` and replaces the settings of `connection`
         * with the copied settings.
         *
         * @param new_connection a #NMConnection to replace the settings of `connection` with
         */
        replace_settings_from_connection(new_connection: NM.Connection): void;
        /**
         * Sets the D-Bus path of the connection.  This property is not serialized, and
         * is only for the reference of the caller.  Sets the #NMConnection:path
         * property.
         *
         * @param path the D-Bus path of the connection as given by the settings service
         * which provides the connection
         */
        set_path(path: string): void;
        /**
         * Converts the #NMConnection into a #GVariant of type
         * %NM_VARIANT_TYPE_CONNECTION describing the connection, suitable for
         * marshalling over D-Bus or otherwise serializing.
         *
         * @returns a new floating #GVariant describing the connection,
         * its settings, and each setting's properties.
         * @param flags serialization flags, e.g. %NM_CONNECTION_SERIALIZE_ALL
         */
        to_dbus(flags: NM.ConnectionSerializationFlags | null): GLib.Variant;
        /**
         * Update the specified setting's secrets, given a dictionary of secrets
         * intended for that setting (deserialized from D-Bus for example).  Will also
         * extract the given setting's secrets hash if given a connection dictionary.
         * If `setting_name` is %NULL, expects a fully serialized #NMConnection as
         * returned by nm_connection_to_dbus() and will update all secrets from all
         * settings contained in `secrets`.
         *
         * @returns %TRUE if the secrets were successfully updated, %FALSE if the update
         * failed (tried to update secrets for a setting that doesn't exist, etc)
         * @param setting_name the setting object name to which the secrets apply
         * @param secrets a #GVariant of secrets, of type %NM_VARIANT_TYPE_CONNECTION
         *   or %NM_VARIANT_TYPE_SETTING
         */
        update_secrets(setting_name: string, secrets: GLib.Variant): boolean;
        /**
         * Validates the connection and all its settings.  Each setting's properties
         * have allowed values, and some values are dependent on other values.  For
         * example, if a Wi-Fi connection is security enabled, the #NMSettingWireless
         * setting object's 'security' property must contain the setting name of the
         * #NMSettingWirelessSecurity object, which must also be present in the
         * connection for the connection to be valid.  As another example, the
         * #NMSettingWired object's 'mac-address' property must be a validly formatted
         * MAC address.  The returned #GError contains information about which
         * setting and which property failed validation, and how it failed validation.
         *
         * @returns %TRUE if the connection is valid, %FALSE if it is not
         */
        verify(): boolean;
        /**
         * Verifies the secrets in the connection.
         *
         * @returns %TRUE if the secrets are valid, %FALSE if they are not
         */
        verify_secrets(): boolean;
        /**
         * emitted when any change to the connection's settings occurs
         */
        vfunc_changed(): void;
        /**
         * emitted when the connection's secrets are cleared
         */
        vfunc_secrets_cleared(): void;
        /**
         * emitted when the connection's secrets are updated
         *
         * @param setting
         */
        vfunc_secrets_updated(setting: string): void;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Disconnects a handler from an instance so it will not be called during any future or currently ongoing emissions of the signal it has been connected to.
         *
         * @param id Handler ID of the handler to be disconnected
         */
        disconnect(id: number): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    abstract class SecretAgentOld
        extends GObjectClasses.Object
        implements Gio.AsyncInitable<NM.SecretAgentOld>, Gio.Initable
    {
        static '$gtype': GObject.GType<SecretAgentOld>;
        // Properties
        /**
         * If %TRUE (the default), the agent will always be registered when
         * NetworkManager is running; if NetworkManager exits and restarts, the
         * agent will re-register itself automatically.
         *
         * In particular, if this property is %TRUE at construct time, then the
         * agent will register itself with NetworkManager during
         * construction/initialization and initialization will only complete
         * after registration is completed (either successfully or unsuccessfully).
         * Since 1.24, a failure to register will no longer cause initialization
         * of #NMSecretAgentOld to fail.
         *
         * If the property is %FALSE, the agent will not automatically register with
         * NetworkManager, and nm_secret_agent_old_enable() or
         * nm_secret_agent_old_register_async() must be called to register it.
         *
         * Calling nm_secret_agent_old_enable() has the same effect as setting this
         * property.
         */
        get auto_register(): boolean;
        set auto_register(val: boolean);
        /**
         * If %TRUE (the default), the agent will always be registered when
         * NetworkManager is running; if NetworkManager exits and restarts, the
         * agent will re-register itself automatically.
         *
         * In particular, if this property is %TRUE at construct time, then the
         * agent will register itself with NetworkManager during
         * construction/initialization and initialization will only complete
         * after registration is completed (either successfully or unsuccessfully).
         * Since 1.24, a failure to register will no longer cause initialization
         * of #NMSecretAgentOld to fail.
         *
         * If the property is %FALSE, the agent will not automatically register with
         * NetworkManager, and nm_secret_agent_old_enable() or
         * nm_secret_agent_old_register_async() must be called to register it.
         *
         * Calling nm_secret_agent_old_enable() has the same effect as setting this
         * property.
         */
        get autoRegister(): boolean;
        set autoRegister(val: boolean);
        /**
         * A bitfield of %NMSecretAgentCapabilities.
         *
         * Changing this property is possible at any time. In case the secret
         * agent is currently registered, this will cause a re-registration.
         */
        get capabilities(): NM.SecretAgentCapabilities;
        set capabilities(val: NM.SecretAgentCapabilities);
        /**
         * The #GDBusConnection used by the instance. You may either set this
         * as construct-only property, or otherwise #NMSecretAgentOld will choose
         * a connection via g_bus_get() during initialization.
         */
        get dbus_connection(): Gio.DBusConnection;
        /**
         * The #GDBusConnection used by the instance. You may either set this
         * as construct-only property, or otherwise #NMSecretAgentOld will choose
         * a connection via g_bus_get() during initialization.
         */
        get dbusConnection(): Gio.DBusConnection;
        /**
         * Identifies this agent; only one agent in each user session may use the
         * same identifier.  Identifier formatting follows the same rules as
         * D-Bus bus names with the exception that the ':' character is not
         * allowed.  The valid set of characters is "[A-Z][a-z][0-9]_-." and the
         * identifier is limited in length to 255 characters with a minimum
         * of 3 characters.  An example valid identifier is 'org.gnome.nm-applet'
         * (without quotes).
         */
        get identifier(): string;
        /**
         * %TRUE if the agent is registered with NetworkManager, %FALSE if not.
         */
        get registered(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SecretAgentOld.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SecretAgentOld.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.SecretAgentOld.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SecretAgentOld.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SecretAgentOld.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SecretAgentOld.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SecretAgentOld.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SecretAgentOld.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Virtual methods
        vfunc_cancel_get_secrets(connection_path: string, setting_name: string): void;
        /**
         * Asynchronously asks the agent to delete all saved secrets belonging to
         * `connection`.
         *
         * @param connection a #NMConnection
         * @param connection_path
         * @param callback a callback, to be invoked when the operation is done
         */
        vfunc_delete_secrets(
            connection: NM.Connection,
            connection_path: string,
            callback: NM.SecretAgentOldDeleteSecretsFunc,
        ): void;
        /**
         * Asynchronously retrieves secrets belonging to `connection` for the
         * setting `setting_name`.  `flags` indicate specific behavior that the secret
         * agent should use when performing the request, for example returning only
         * existing secrets without user interaction, or requesting entirely new
         * secrets from the user.
         *
         * @param connection the #NMConnection for which we're asked secrets
         * @param connection_path
         * @param setting_name the name of the secret setting
         * @param hints hints to the agent
         * @param flags flags that modify the behavior of the request
         * @param callback a callback, to be invoked when the operation is done
         */
        vfunc_get_secrets(
            connection: NM.Connection,
            connection_path: string,
            setting_name: string,
            hints: string[],
            flags: NM.SecretAgentGetSecretsFlags,
            callback: NM.SecretAgentOldGetSecretsFunc,
        ): void;
        /**
         * Asynchronously ensures that all secrets inside `connection` are stored to
         * disk.
         *
         * @param connection a #NMConnection
         * @param connection_path
         * @param callback a callback, to be invoked when the operation is done
         */
        vfunc_save_secrets(
            connection: NM.Connection,
            connection_path: string,
            callback: NM.SecretAgentOldSaveSecretsFunc,
        ): void;
        // Methods
        /**
         * Asynchronously asks the agent to delete all saved secrets belonging to
         * `connection`.
         *
         * @param connection a #NMConnection
         * @param callback a callback, to be invoked when the operation is done
         */
        delete_secrets(connection: NM.Connection, callback: NM.SecretAgentOldDeleteSecretsFunc): void;
        /**
         * Since 1.24, the instance will already register a D-Bus object on the
         * D-Bus connection during initialization. That object will stay registered
         * until `self` gets unrefed (destroyed) or this function is called. This
         * function performs the necessary cleanup to tear down the instance. Afterwards,
         * the function can not longer be used. This is optional, but necessary to
         * ensure unregistering the D-Bus object at a define point, when other users
         * might still have a reference on `self`.
         *
         * You may call this function any time and repeatedly. However, after destroying
         * the instance, it is a bug to still use the instance for other purposes. The
         * instance becomes defunct and cannot re-register.
         */
        destroy(): void;
        /**
         * This has the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER
         * property.
         *
         * Unlike most other functions, you may already call this function before
         * initialization completes.
         *
         * @param enable whether to enable or disable the listener.
         */
        enable(enable: boolean): void;
        /**
         * Returns a #GObject that stays alive as long as there are pending
         * requests in the #GDBusConnection. Such requests keep the #GMainContext
         * alive, and thus you may want to keep iterating the context as long
         * until a weak reference indicates that this object is gone. This is
         * useful because even when you destroy the instance right away (and all
         * the internally pending requests get cancelled), any pending g_dbus_connection_call()
         * requests will still invoke the result on the #GMainContext. Hence, this
         * allows you to know how long you must iterate the context to know
         * that all remains are cleaned up.
         *
         * @returns a #GObject that you may register a weak pointer
         *   to know that the #GMainContext is still kept busy by `self`.
         */
        get_context_busy_watcher<T = GObject.Object>(): T;
        get_dbus_connection(): Gio.DBusConnection;
        get_dbus_name_owner(): string;
        get_main_context(): GLib.MainContext;
        /**
         * Note that the secret agent transparently registers and re-registers
         * as the D-Bus name owner appears. Hence, this property is not really
         * useful. Also, to be graceful against races during registration, the
         * instance will already accept requests while being in the process of
         * registering.
         * If you need to avoid races and want to wait until `self` is registered,
         * call nm_secret_agent_old_register_async(). If that function completes
         * with success, you know the instance is registered.
         *
         * @returns a %TRUE if the agent is registered, %FALSE if it is not.
         */
        get_registered(): boolean;
        /**
         * Asynchronously retrieves secrets belonging to `connection` for the
         * setting `setting_name`.  `flags` indicate specific behavior that the secret
         * agent should use when performing the request, for example returning only
         * existing secrets without user interaction, or requesting entirely new
         * secrets from the user.
         *
         * @param connection the #NMConnection for which we're asked secrets
         * @param setting_name the name of the secret setting
         * @param hints hints to the agent
         * @param flags flags that modify the behavior of the request
         * @param callback a callback, to be invoked when the operation is done
         */
        get_secrets(
            connection: NM.Connection,
            setting_name: string,
            hints: string[],
            flags: NM.SecretAgentGetSecretsFlags | null,
            callback: NM.SecretAgentOldGetSecretsFunc,
        ): void;
        /**
         * Registers the #NMSecretAgentOld with the NetworkManager secret manager,
         * indicating to NetworkManager that the agent is able to provide and save
         * secrets for connections on behalf of its user.
         *
         * @returns %TRUE if registration was successful, %FALSE on error.
         *
         * Since 1.24, this can no longer fail unless the `cancellable` gets
         * cancelled. Contrary to nm_secret_agent_old_register_async(), this also
         * does not wait for the registration to succeed. You cannot synchronously
         * (without iterating the caller's GMainContext) wait for registration.
         *
         * Since 1.24, registration is idempotent. It has the same effect as setting
         * %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %TRUE or nm_secret_agent_old_enable().
         * @param cancellable a #GCancellable, or %NULL
         */
        register(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously registers the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent is able to provide and
         * save secrets for connections on behalf of its user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %TRUE
         * or nm_secret_agent_old_enable().
         *
         * Since 1.24, the asynchronous result indicates whether the instance is successfully
         * registered. In any case, this call enables the agent and it will automatically
         * try to register and handle secret requests. A failure of this function only indicates
         * that currently the instance might not be ready (but since it will automatically
         * try to recover, it might be ready in a moment afterwards). Use this function if
         * you want to check and ensure that the agent is registered.
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        register_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously registers the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent is able to provide and
         * save secrets for connections on behalf of its user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %TRUE
         * or nm_secret_agent_old_enable().
         *
         * Since 1.24, the asynchronous result indicates whether the instance is successfully
         * registered. In any case, this call enables the agent and it will automatically
         * try to register and handle secret requests. A failure of this function only indicates
         * that currently the instance might not be ready (but since it will automatically
         * try to recover, it might be ready in a moment afterwards). Use this function if
         * you want to check and ensure that the agent is registered.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to call when the agent is registered
         */
        register_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Asynchronously registers the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent is able to provide and
         * save secrets for connections on behalf of its user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %TRUE
         * or nm_secret_agent_old_enable().
         *
         * Since 1.24, the asynchronous result indicates whether the instance is successfully
         * registered. In any case, this call enables the agent and it will automatically
         * try to register and handle secret requests. A failure of this function only indicates
         * that currently the instance might not be ready (but since it will automatically
         * try to recover, it might be ready in a moment afterwards). Use this function if
         * you want to check and ensure that the agent is registered.
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to call when the agent is registered
         */
        register_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_secret_agent_old_register_async().
         *
         * @returns %TRUE if registration was successful, %FALSE on error.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %TRUE
         * or nm_secret_agent_old_enable().
         * @param result the result passed to the #GAsyncReadyCallback
         */
        register_finish(result: Gio.AsyncResult): boolean;
        /**
         * Asynchronously ensures that all secrets inside `connection` are stored to
         * disk.
         *
         * @param connection a #NMConnection
         * @param callback a callback, to be invoked when the operation is done
         */
        save_secrets(connection: NM.Connection, callback: NM.SecretAgentOldSaveSecretsFunc): void;
        /**
         * Unregisters the #NMSecretAgentOld with the NetworkManager secret manager,
         * indicating to NetworkManager that the agent will no longer provide or
         * store secrets on behalf of this user.
         *
         * @returns %TRUE if unregistration was successful, %FALSE on error
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %FALSE
         * or nm_secret_agent_old_enable().
         * @param cancellable a #GCancellable, or %NULL
         */
        unregister(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Asynchronously unregisters the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent will no longer provide
         * or store secrets on behalf of this user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %FALSE
         * or nm_secret_agent_old_enable().
         *
         * @param cancellable a #GCancellable, or %NULL
         */
        unregister_async(cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Asynchronously unregisters the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent will no longer provide
         * or store secrets on behalf of this user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %FALSE
         * or nm_secret_agent_old_enable().
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to call when the agent is unregistered
         */
        unregister_async(cancellable: Gio.Cancellable | null, callback: Gio.AsyncReadyCallback<this> | null): void;
        /**
         * Asynchronously unregisters the #NMSecretAgentOld with the NetworkManager secret
         * manager, indicating to NetworkManager that the agent will no longer provide
         * or store secrets on behalf of this user.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %FALSE
         * or nm_secret_agent_old_enable().
         *
         * @param cancellable a #GCancellable, or %NULL
         * @param callback callback to call when the agent is unregistered
         */
        unregister_async(
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Gets the result of a call to nm_secret_agent_old_unregister_async().
         *
         * @returns %TRUE if unregistration was successful, %FALSE on error.
         *
         * Since 1.24, registration cannot fail and is idempotent. It has
         * the same effect as setting %NM_SECRET_AGENT_OLD_AUTO_REGISTER to %FALSE
         * or nm_secret_agent_old_enable().
         * @param result the result passed to the #GAsyncReadyCallback
         */
        unregister_finish(result: Gio.AsyncResult): boolean;
        // Inherited methods
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init_async(io_priority: number, cancellable?: Gio.Cancellable | null): globalThis.Promise<boolean>;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        init_async(
            io_priority: number,
            cancellable: Gio.Cancellable | null,
            callback: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        init_async(
            io_priority: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): globalThis.Promise<boolean> | void;
        /**
         * Finishes asynchronous initialization and returns the result.
         * See g_async_initable_init_async().
         *
         * @returns %TRUE if successful. If an error has occurred, this function
         * will return %FALSE and set `error` appropriately if present.
         * @param res a #GAsyncResult.
         */
        init_finish(res: Gio.AsyncResult): boolean;
        /**
         * Finishes the async construction for the various g_async_initable_new
         * calls, returning the created object or %NULL on error.
         *
         * @returns a newly created #GObject,
         *      or %NULL on error. Free with g_object_unref().
         * @param res the #GAsyncResult from the callback
         */
        new_finish(res: Gio.AsyncResult): NM.SecretAgentOld;
        /**
         * Starts asynchronous initialization of the object implementing the
         * interface. This must be done before any real use of the object after
         * initial construction. If the object also implements #GInitable you can
         * optionally call g_initable_init() instead.
         *
         * This method is intended for language bindings. If writing in C,
         * g_async_initable_new_async() should typically be used instead.
         *
         * When the initialization is finished, `callback` will be called. You can
         * then call g_async_initable_init_finish() to get the result of the
         * initialization.
         *
         * Implementations may also support cancellation. If `cancellable` is not
         * %NULL, then initialization can be cancelled by triggering the cancellable
         * object from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL, and
         * the object doesn't support cancellable initialization, the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * As with #GInitable, if the object is not initialized, or initialization
         * returns with an error, then all operations on the object except
         * g_object_ref() and g_object_unref() are considered to be invalid, and
         * have undefined behaviour. They will often fail with g_critical() or
         * g_warning(), but this must not be relied on.
         *
         * Callers should not assume that a class which implements #GAsyncInitable can
         * be initialized multiple times; for more information, see g_initable_init().
         * If a class explicitly supports being initialized multiple times,
         * implementation requires yielding all subsequent calls to init_async() on the
         * results of the first call.
         *
         * For classes that also support the #GInitable interface, the default
         * implementation of this method will run the g_initable_init() function
         * in a thread, so if you want to support asynchronous initialization via
         * threads, just implement the #GAsyncInitable interface without overriding
         * any interface methods.
         *
         * @param io_priority the [I/O priority](iface.AsyncResult.html#io-priority) of the operation
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         * @param callback a #GAsyncReadyCallback to call when the request is satisfied
         */
        vfunc_init_async(
            io_priority: number,
            cancellable?: Gio.Cancellable | null,
            callback?: Gio.AsyncReadyCallback<this> | null,
        ): void;
        /**
         * Finishes asynchronous initialization and returns the result.
         * See g_async_initable_init_async().
         *
         * @param res a #GAsyncResult.
         */
        vfunc_init_finish(res: Gio.AsyncResult): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @returns %TRUE if successful. If an error has occurred, this function will
         *     return %FALSE and set `error` appropriately if present.
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        vfunc_init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Disconnects a handler from an instance so it will not be called during any future or currently ongoing emissions of the signal it has been connected to.
         *
         * @param id Handler ID of the handler to be disconnected
         */
        disconnect(id: number): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    abstract class Setting extends GObjectClasses.Object {
        static '$gtype': GObject.GType<Setting>;
        // Properties
        /**
         * The setting's name, which uniquely identifies the setting within the
         * connection.  Each setting type has a name unique to that type, for
         * example "ppp" or "802-11-wireless" or "802-3-ethernet".
         */
        get name(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Setting.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Setting.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.Setting.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Setting.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Setting.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Setting.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Get the type of the enum that defines the values that the property accepts. It is only
         * useful for properties configured to accept values from certain enum type, otherwise
         * it will return %G_TYPE_INVALID. Note that flags (children of G_TYPE_FLAGS) are also
         * considered enums.
         *
         * Note that the GObject property might be implemented as an integer, actually, and not
         * as enum. Find out what underlying type is used, checking the #GParamSpec, before
         * setting the GObject property.
         *
         * @param setting_type the GType of the NMSetting instance
         * @param property_name the name of the property
         */
        static get_enum_property_type(setting_type: GObject.GType, property_name: string): GObject.GType;
        /**
         * Returns the #GType of the setting's class for a given setting name.
         *
         * @param name a setting name
         */
        static lookup_type(name: string): GObject.GType;
        // Methods
        /**
         * Compares two #NMSetting objects for similarity, with comparison behavior
         * modified by a set of flags.  See the documentation for #NMSettingCompareFlags
         * for a description of each flag's behavior.
         *
         * @returns %TRUE if the comparison succeeds, %FALSE if it does not
         * @param b a second #NMSetting to compare with the first
         * @param flags compare flags, e.g. %NM_SETTING_COMPARE_FLAG_EXACT
         */
        compare(b: NM.Setting, flags: NM.SettingCompareFlags | null): boolean;
        /**
         * Compares two #NMSetting objects for similarity, with comparison behavior
         * modified by a set of flags.  See the documentation for #NMSettingCompareFlags
         * for a description of each flag's behavior.  If the settings differ, the keys
         * of each setting that differ from the other are added to `results,` mapped to
         * one or more #NMSettingDiffResult values.
         *
         * @returns %TRUE if the settings contain the same values, %FALSE if they do not
         * @param b a second #NMSetting to compare with the first
         * @param flags compare flags, e.g. %NM_SETTING_COMPARE_FLAG_EXACT
         * @param invert_results this parameter is used internally by libnm and should
         * be set to %FALSE.  If %TRUE inverts the meaning of the #NMSettingDiffResult.
         * @param results if the
         * settings differ, on return a hash table mapping the differing keys to one or
         * more %NMSettingDiffResult values OR-ed together.  If the settings do not
         * differ, any hash table passed in is unmodified.  If no hash table is passed
         * in and the settings differ, a new one is created and returned.
         */
        diff(
            b: NM.Setting,
            flags: NM.SettingCompareFlags | null,
            invert_results: boolean,
            results: GLib.HashTable<string, number>,
        ): [boolean, GLib.HashTable<string, number>];
        /**
         * Duplicates a #NMSetting.
         *
         * @returns a new #NMSetting containing the same properties and values as the
         * source #NMSetting
         */
        duplicate(): NM.Setting;
        /**
         * Iterates over each property of the #NMSetting object, calling the supplied
         * user function for each property.
         *
         * @param func user-supplied function called for each property of the setting
         */
        enumerate_values(func: NM.SettingValueIterFn): void;
        /**
         * Gets the D-Bus marshalling type of a property. `property_name` is a D-Bus
         * property name, which may not necessarily be a #GObject property.
         *
         * @returns the D-Bus marshalling type of `property` on `setting`.
         * @param property_name the property of `setting` to get the type of
         */
        get_dbus_property_type(property_name: string): GLib.VariantType;
        /**
         * Returns the type name of the #NMSetting object
         *
         * @returns a string containing the type name of the #NMSetting object,
         * like 'ppp' or 'wireless' or 'wired'.
         */
        get_name(): string;
        /**
         * For a given secret, retrieves the #NMSettingSecretFlags describing how to
         * handle that secret.
         *
         * @returns %TRUE on success (if the given secret name was a valid property of
         * this setting, and if that property is secret), %FALSE if not
         * @param secret_name the secret key name to get flags for
         * @param out_flags on success, the #NMSettingSecretFlags for the secret
         */
        get_secret_flags(secret_name: string, out_flags: NM.SettingSecretFlags | null): boolean;
        option_clear_by_name(predicate?: NM.UtilsPredicateStr | null): void;
        option_get(opt_name: string): GLib.Variant;
        /**
         * Gives the name of all set options.
         *
         * @returns A %NULL terminated array of key names. If no names are present, this returns
         *   %NULL. The returned array and the names are owned by %NMSetting and might be invalidated
         *   by the next operation.
         */
        option_get_all_names(): string[] | null;
        option_get_boolean(opt_name: string): [boolean, boolean];
        option_get_uint32(opt_name: string): [boolean, number];
        /**
         * If `variant` is %NULL, this clears the option if it is set.
         * Otherwise, `variant` is set as the option. If `variant` is
         * a floating reference, it will be consumed.
         *
         * Note that not all setting types support options. It is a bug
         * setting a variant to a setting that doesn't support it.
         * Currently, only #NMSettingEthtool supports it.
         *
         * @param opt_name the option name to set
         * @param variant the variant to set.
         */
        option_set(opt_name: string, variant?: GLib.Variant | null): void;
        /**
         * Like nm_setting_option_set() to set a boolean GVariant.
         *
         * @param opt_name
         * @param value the value to set.
         */
        option_set_boolean(opt_name: string, value: boolean): void;
        /**
         * Like nm_setting_option_set() to set a uint32 GVariant.
         *
         * @param opt_name
         * @param value the value to set.
         */
        option_set_uint32(opt_name: string, value: number): void;
        /**
         * For a given secret, stores the #NMSettingSecretFlags describing how to
         * handle that secret.
         *
         * @returns %TRUE on success (if the given secret name was a valid property of
         * this setting, and if that property is secret), %FALSE if not
         * @param secret_name the secret key name to set flags for
         * @param flags the #NMSettingSecretFlags for the secret
         */
        set_secret_flags(secret_name: string, flags: NM.SettingSecretFlags | null): boolean;
        /**
         * Convert the setting (including secrets!) into a string. For debugging
         * purposes ONLY, should NOT be used for serialization of the setting,
         * or machine-parsed in any way. The output format is not guaranteed to
         * be stable and may change at any time.
         *
         * @returns an allocated string containing a textual representation of the
         * setting's properties and values, which the caller should
         * free with g_free()
         */
        to_string(): string;
        /**
         * Validates the setting.  Each setting's properties have allowed values, and
         * some are dependent on other values (hence the need for `connection)`.  The
         * returned #GError contains information about which property of the setting
         * failed validation, and in what way that property failed validation.
         *
         * @returns %TRUE if the setting is valid, %FALSE if it is not
         * @param connection the #NMConnection that `setting` came from, or
         *   %NULL if `setting` is being verified in isolation.
         */
        verify(connection?: NM.Connection | null): boolean;
        /**
         * Verifies the secrets in the setting.
         * The returned #GError contains information about which secret of the setting
         * failed validation, and in what way that secret failed validation.
         * The secret validation is done separately from main setting validation, because
         * in some cases connection failure is not desired just for the secrets.
         *
         * @returns %TRUE if the setting secrets are valid, %FALSE if they are not
         * @param connection the #NMConnection that `setting` came from, or
         *   %NULL if `setting` is being verified in isolation.
         */
        verify_secrets(connection?: NM.Connection | null): boolean;
    }
    class Setting6Lowpan extends Setting {
        static '$gtype': GObject.GType<Setting6Lowpan>;
        // Properties
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * from which this 6LowPAN interface should be created.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Setting6Lowpan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Setting6Lowpan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.Setting6Lowpan;
        // Signals
        connect<K extends keyof NM.Setting6Lowpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting6Lowpan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Setting6Lowpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting6Lowpan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Setting6Lowpan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Setting6Lowpan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_parent(): string;
    }
    class Setting8021x extends Setting {
        static '$gtype': GObject.GType<Setting8021x>;
        // Properties
        /**
         * List of strings to be matched against the altSubjectName of the
         * certificate presented by the authentication server. If the list is empty,
         * no verification of the server certificate's altSubjectName is performed.
         */
        get altsubject_matches(): string[];
        set altsubject_matches(val: string[]);
        /**
         * List of strings to be matched against the altSubjectName of the
         * certificate presented by the authentication server. If the list is empty,
         * no verification of the server certificate's altSubjectName is performed.
         */
        get altsubjectMatches(): string[];
        set altsubjectMatches(val: string[]);
        /**
         * Anonymous identity string for EAP authentication methods.  Used as the
         * unencrypted identity with EAP types that support different tunneled
         * identity like EAP-TTLS.
         */
        get anonymous_identity(): string;
        set anonymous_identity(val: string);
        /**
         * Anonymous identity string for EAP authentication methods.  Used as the
         * unencrypted identity with EAP types that support different tunneled
         * identity like EAP-TTLS.
         */
        get anonymousIdentity(): string;
        set anonymousIdentity(val: string);
        /**
         * A timeout for the authentication. Zero means the global default; if the
         * global default is not set, the authentication timeout is 25 seconds.
         */
        get auth_timeout(): number;
        set auth_timeout(val: number);
        /**
         * A timeout for the authentication. Zero means the global default; if the
         * global default is not set, the authentication timeout is 25 seconds.
         */
        get authTimeout(): number;
        set authTimeout(val: number);
        /**
         * Contains the CA certificate if used by the EAP method specified in the
         * #NMSetting8021x:eap property.
         *
         * Certificate data is specified using a "scheme"; three are currently
         * supported: blob, path and pkcs#11 URL. When using the blob scheme this property
         * should be set to the certificate's DER encoded data. When using the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * certificate, prefixed with the string "file://" and ending with a terminating
         * NUL byte.
         * This property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Note that enabling NMSetting8021x:system-ca-certs will override this
         * setting to use the built-in path, if the built-in path is not a directory.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_ca_cert() function instead.
         */
        get ca_cert(): GLib.Bytes;
        set ca_cert(val: GLib.Bytes);
        /**
         * Contains the CA certificate if used by the EAP method specified in the
         * #NMSetting8021x:eap property.
         *
         * Certificate data is specified using a "scheme"; three are currently
         * supported: blob, path and pkcs#11 URL. When using the blob scheme this property
         * should be set to the certificate's DER encoded data. When using the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * certificate, prefixed with the string "file://" and ending with a terminating
         * NUL byte.
         * This property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Note that enabling NMSetting8021x:system-ca-certs will override this
         * setting to use the built-in path, if the built-in path is not a directory.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_ca_cert() function instead.
         */
        get caCert(): GLib.Bytes;
        set caCert(val: GLib.Bytes);
        /**
         * The password used to access the CA certificate stored in
         * #NMSetting8021x:ca-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get ca_cert_password(): string;
        set ca_cert_password(val: string);
        /**
         * The password used to access the CA certificate stored in
         * #NMSetting8021x:ca-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get caCertPassword(): string;
        set caCertPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:ca-cert-password property.
         */
        get ca_cert_password_flags(): NM.SettingSecretFlags;
        set ca_cert_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:ca-cert-password property.
         */
        get caCertPasswordFlags(): NM.SettingSecretFlags;
        set caCertPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * UTF-8 encoded path to a directory containing PEM or DER formatted
         * certificates to be added to the verification chain in addition to the
         * certificate specified in the #NMSetting8021x:ca-cert property.
         *
         * If NMSetting8021x:system-ca-certs is enabled and the built-in CA
         * path is an existing directory, then this setting is ignored.
         */
        get ca_path(): string;
        set ca_path(val: string);
        /**
         * UTF-8 encoded path to a directory containing PEM or DER formatted
         * certificates to be added to the verification chain in addition to the
         * certificate specified in the #NMSetting8021x:ca-cert property.
         *
         * If NMSetting8021x:system-ca-certs is enabled and the built-in CA
         * path is an existing directory, then this setting is ignored.
         */
        get caPath(): string;
        set caPath(val: string);
        /**
         * Contains the client certificate if used by the EAP method specified in
         * the #NMSetting8021x:eap property.
         *
         * Certificate data is specified using a "scheme"; two are currently
         * supported: blob and path. When using the blob scheme (which is backwards
         * compatible with NM 0.7.x) this property should be set to the
         * certificate's DER encoded data. When using the path scheme, this property
         * should be set to the full UTF-8 encoded path of the certificate, prefixed
         * with the string "file://" and ending with a terminating NUL byte.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_client_cert() function instead.
         */
        get client_cert(): GLib.Bytes;
        set client_cert(val: GLib.Bytes);
        /**
         * Contains the client certificate if used by the EAP method specified in
         * the #NMSetting8021x:eap property.
         *
         * Certificate data is specified using a "scheme"; two are currently
         * supported: blob and path. When using the blob scheme (which is backwards
         * compatible with NM 0.7.x) this property should be set to the
         * certificate's DER encoded data. When using the path scheme, this property
         * should be set to the full UTF-8 encoded path of the certificate, prefixed
         * with the string "file://" and ending with a terminating NUL byte.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_client_cert() function instead.
         */
        get clientCert(): GLib.Bytes;
        set clientCert(val: GLib.Bytes);
        /**
         * The password used to access the client certificate stored in
         * #NMSetting8021x:client-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get client_cert_password(): string;
        set client_cert_password(val: string);
        /**
         * The password used to access the client certificate stored in
         * #NMSetting8021x:client-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get clientCertPassword(): string;
        set clientCertPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:client-cert-password property.
         */
        get client_cert_password_flags(): NM.SettingSecretFlags;
        set client_cert_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:client-cert-password property.
         */
        get clientCertPasswordFlags(): NM.SettingSecretFlags;
        set clientCertPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * Constraint for server domain name. If set, this list of FQDNs is used as
         * a match requirement for dNSName element(s) of the certificate presented
         * by the authentication server.  If a matching dNSName is found, this
         * constraint is met.  If no dNSName values are present, this constraint is
         * matched against SubjectName CN using the same comparison.
         * Multiple valid FQDNs can be passed as a ";" delimited list.
         */
        get domain_match(): string;
        set domain_match(val: string);
        /**
         * Constraint for server domain name. If set, this list of FQDNs is used as
         * a match requirement for dNSName element(s) of the certificate presented
         * by the authentication server.  If a matching dNSName is found, this
         * constraint is met.  If no dNSName values are present, this constraint is
         * matched against SubjectName CN using the same comparison.
         * Multiple valid FQDNs can be passed as a ";" delimited list.
         */
        get domainMatch(): string;
        set domainMatch(val: string);
        /**
         * Constraint for server domain name. If set, this FQDN is used as a suffix
         * match requirement for dNSName element(s) of the certificate presented by
         * the authentication server.  If a matching dNSName is found, this
         * constraint is met.  If no dNSName values are present, this constraint is
         * matched against SubjectName CN using same suffix match comparison.
         * Since version 1.24, multiple valid FQDNs can be passed as a ";" delimited
         * list.
         */
        get domain_suffix_match(): string;
        set domain_suffix_match(val: string);
        /**
         * Constraint for server domain name. If set, this FQDN is used as a suffix
         * match requirement for dNSName element(s) of the certificate presented by
         * the authentication server.  If a matching dNSName is found, this
         * constraint is met.  If no dNSName values are present, this constraint is
         * matched against SubjectName CN using same suffix match comparison.
         * Since version 1.24, multiple valid FQDNs can be passed as a ";" delimited
         * list.
         */
        get domainSuffixMatch(): string;
        set domainSuffixMatch(val: string);
        /**
         * The allowed EAP method to be used when authenticating to the network with
         * 802.1x.  Valid methods are: "leap", "md5", "tls", "peap", "ttls", "pwd",
         * and "fast".  Each method requires different configuration using the
         * properties of this setting; refer to wpa_supplicant documentation for the
         * allowed combinations.
         */
        get eap(): string[];
        set eap(val: string[]);
        /**
         * Identity string for EAP authentication methods.  Often the user's user or
         * login name.
         */
        get identity(): string;
        set identity(val: string);
        /**
         * Define openssl_ciphers for wpa_supplicant. Openssl sometimes moves ciphers
         * among SECLEVELs, thus compiled-in default value in wpa_supplicant
         * (as modified by some linux distributions) sometimes prevents
         * to connect to old servers that do not support new protocols.
         */
        get openssl_ciphers(): string;
        set openssl_ciphers(val: string);
        /**
         * Define openssl_ciphers for wpa_supplicant. Openssl sometimes moves ciphers
         * among SECLEVELs, thus compiled-in default value in wpa_supplicant
         * (as modified by some linux distributions) sometimes prevents
         * to connect to old servers that do not support new protocols.
         */
        get opensslCiphers(): string;
        set opensslCiphers(val: string);
        /**
         * Whether the 802.1X authentication is optional. If %TRUE, the activation
         * will continue even after a timeout or an authentication failure. Setting
         * the property to %TRUE is currently allowed only for Ethernet connections.
         * If set to %FALSE, the activation can continue only after a successful
         * authentication.
         */
        get optional(): boolean;
        set optional(val: boolean);
        /**
         * UTF-8 encoded file path containing PAC for EAP-FAST.
         */
        get pac_file(): string;
        set pac_file(val: string);
        /**
         * UTF-8 encoded file path containing PAC for EAP-FAST.
         */
        get pacFile(): string;
        set pacFile(val: string);
        /**
         * UTF-8 encoded password used for EAP authentication methods. If both the
         * #NMSetting8021x:password property and the #NMSetting8021x:password-raw
         * property are specified, #NMSetting8021x:password is preferred.
         */
        get password(): string;
        set password(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:password property.
         */
        get password_flags(): NM.SettingSecretFlags;
        set password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:password property.
         */
        get passwordFlags(): NM.SettingSecretFlags;
        set passwordFlags(val: NM.SettingSecretFlags);
        /**
         * Password used for EAP authentication methods, given as a byte array to
         * allow passwords in other encodings than UTF-8 to be used. If both the
         * #NMSetting8021x:password property and the #NMSetting8021x:password-raw
         * property are specified, #NMSetting8021x:password is preferred.
         */
        get password_raw(): GLib.Bytes;
        set password_raw(val: GLib.Bytes);
        /**
         * Password used for EAP authentication methods, given as a byte array to
         * allow passwords in other encodings than UTF-8 to be used. If both the
         * #NMSetting8021x:password property and the #NMSetting8021x:password-raw
         * property are specified, #NMSetting8021x:password is preferred.
         */
        get passwordRaw(): GLib.Bytes;
        set passwordRaw(val: GLib.Bytes);
        /**
         * Flags indicating how to handle the #NMSetting8021x:password-raw property.
         */
        get password_raw_flags(): NM.SettingSecretFlags;
        set password_raw_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:password-raw property.
         */
        get passwordRawFlags(): NM.SettingSecretFlags;
        set passwordRawFlags(val: NM.SettingSecretFlags);
        /**
         * Specifies authentication flags to use in "phase 1" outer
         * authentication using #NMSetting8021xAuthFlags options.
         * The individual TLS versions can be explicitly disabled. TLS time checks
         * can be also disabled. If a certain TLS disable flag is not
         * set, it is up to the supplicant to allow or forbid it. The TLS options
         * map to tls_disable_tlsv1_x and tls_disable_time_checks settings.
         * See the wpa_supplicant documentation for more details.
         */
        get phase1_auth_flags(): number;
        set phase1_auth_flags(val: number);
        /**
         * Specifies authentication flags to use in "phase 1" outer
         * authentication using #NMSetting8021xAuthFlags options.
         * The individual TLS versions can be explicitly disabled. TLS time checks
         * can be also disabled. If a certain TLS disable flag is not
         * set, it is up to the supplicant to allow or forbid it. The TLS options
         * map to tls_disable_tlsv1_x and tls_disable_time_checks settings.
         * See the wpa_supplicant documentation for more details.
         */
        get phase1AuthFlags(): number;
        set phase1AuthFlags(val: number);
        /**
         * Enables or disables in-line provisioning of EAP-FAST credentials when
         * FAST is specified as the EAP method in the #NMSetting8021x:eap property.
         * Recognized values are "0" (disabled), "1" (allow unauthenticated
         * provisioning), "2" (allow authenticated provisioning), and "3" (allow
         * both authenticated and unauthenticated provisioning).  See the
         * wpa_supplicant documentation for more details.
         */
        get phase1_fast_provisioning(): string;
        set phase1_fast_provisioning(val: string);
        /**
         * Enables or disables in-line provisioning of EAP-FAST credentials when
         * FAST is specified as the EAP method in the #NMSetting8021x:eap property.
         * Recognized values are "0" (disabled), "1" (allow unauthenticated
         * provisioning), "2" (allow authenticated provisioning), and "3" (allow
         * both authenticated and unauthenticated provisioning).  See the
         * wpa_supplicant documentation for more details.
         */
        get phase1FastProvisioning(): string;
        set phase1FastProvisioning(val: string);
        /**
         * Forces use of the new PEAP label during key derivation.  Some RADIUS
         * servers may require forcing the new PEAP label to interoperate with
         * PEAPv1.  Set to "1" to force use of the new PEAP label.  See the
         * wpa_supplicant documentation for more details.
         */
        get phase1_peaplabel(): string;
        set phase1_peaplabel(val: string);
        /**
         * Forces use of the new PEAP label during key derivation.  Some RADIUS
         * servers may require forcing the new PEAP label to interoperate with
         * PEAPv1.  Set to "1" to force use of the new PEAP label.  See the
         * wpa_supplicant documentation for more details.
         */
        get phase1Peaplabel(): string;
        set phase1Peaplabel(val: string);
        /**
         * Forces which PEAP version is used when PEAP is set as the EAP method in
         * the #NMSetting8021x:eap property.  When unset, the version reported by
         * the server will be used.  Sometimes when using older RADIUS servers, it
         * is necessary to force the client to use a particular PEAP version.  To do
         * so, this property may be set to "0" or "1" to force that specific PEAP
         * version.
         */
        get phase1_peapver(): string;
        set phase1_peapver(val: string);
        /**
         * Forces which PEAP version is used when PEAP is set as the EAP method in
         * the #NMSetting8021x:eap property.  When unset, the version reported by
         * the server will be used.  Sometimes when using older RADIUS servers, it
         * is necessary to force the client to use a particular PEAP version.  To do
         * so, this property may be set to "0" or "1" to force that specific PEAP
         * version.
         */
        get phase1Peapver(): string;
        set phase1Peapver(val: string);
        /**
         * List of strings to be matched against the altSubjectName of the
         * certificate presented by the authentication server during the inner
         * "phase 2" authentication. If the list is empty, no verification of the
         * server certificate's altSubjectName is performed.
         */
        get phase2_altsubject_matches(): string[];
        set phase2_altsubject_matches(val: string[]);
        /**
         * List of strings to be matched against the altSubjectName of the
         * certificate presented by the authentication server during the inner
         * "phase 2" authentication. If the list is empty, no verification of the
         * server certificate's altSubjectName is performed.
         */
        get phase2AltsubjectMatches(): string[];
        set phase2AltsubjectMatches(val: string[]);
        /**
         * Specifies the allowed "phase 2" inner authentication method when an EAP
         * method that uses an inner TLS tunnel is specified in the #NMSetting8021x:eap
         * property.  For TTLS this property selects one of the supported non-EAP
         * inner methods: "pap", "chap", "mschap", "mschapv2" while
         * #NMSetting8021x:phase2-autheap selects an EAP inner method.  For PEAP
         * this selects an inner EAP method, one of: "gtc", "otp", "md5" and "tls".
         * Each "phase 2" inner method requires specific parameters for successful
         * authentication; see the wpa_supplicant documentation for more details.
         * Both #NMSetting8021x:phase2-auth and #NMSetting8021x:phase2-autheap cannot
         * be specified.
         */
        get phase2_auth(): string;
        set phase2_auth(val: string);
        /**
         * Specifies the allowed "phase 2" inner authentication method when an EAP
         * method that uses an inner TLS tunnel is specified in the #NMSetting8021x:eap
         * property.  For TTLS this property selects one of the supported non-EAP
         * inner methods: "pap", "chap", "mschap", "mschapv2" while
         * #NMSetting8021x:phase2-autheap selects an EAP inner method.  For PEAP
         * this selects an inner EAP method, one of: "gtc", "otp", "md5" and "tls".
         * Each "phase 2" inner method requires specific parameters for successful
         * authentication; see the wpa_supplicant documentation for more details.
         * Both #NMSetting8021x:phase2-auth and #NMSetting8021x:phase2-autheap cannot
         * be specified.
         */
        get phase2Auth(): string;
        set phase2Auth(val: string);
        /**
         * Specifies the allowed "phase 2" inner EAP-based authentication method
         * when TTLS is specified in the #NMSetting8021x:eap property.  Recognized
         * EAP-based "phase 2" methods are "md5", "mschapv2", "otp", "gtc", and
         * "tls". Each "phase 2" inner method requires specific parameters for
         * successful authentication; see the wpa_supplicant documentation for
         * more details.
         */
        get phase2_autheap(): string;
        set phase2_autheap(val: string);
        /**
         * Specifies the allowed "phase 2" inner EAP-based authentication method
         * when TTLS is specified in the #NMSetting8021x:eap property.  Recognized
         * EAP-based "phase 2" methods are "md5", "mschapv2", "otp", "gtc", and
         * "tls". Each "phase 2" inner method requires specific parameters for
         * successful authentication; see the wpa_supplicant documentation for
         * more details.
         */
        get phase2Autheap(): string;
        set phase2Autheap(val: string);
        /**
         * Contains the "phase 2" CA certificate if used by the EAP method specified
         * in the #NMSetting8021x:phase2-auth or #NMSetting8021x:phase2-autheap
         * properties.
         *
         * Certificate data is specified using a "scheme"; three are currently
         * supported: blob, path and pkcs#11 URL. When using the blob scheme this property
         * should be set to the certificate's DER encoded data. When using the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * certificate, prefixed with the string "file://" and ending with a terminating
         * NUL byte.
         * This property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Note that enabling NMSetting8021x:system-ca-certs will override this
         * setting to use the built-in path, if the built-in path is not a directory.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_ca_cert() function instead.
         */
        get phase2_ca_cert(): GLib.Bytes;
        set phase2_ca_cert(val: GLib.Bytes);
        /**
         * Contains the "phase 2" CA certificate if used by the EAP method specified
         * in the #NMSetting8021x:phase2-auth or #NMSetting8021x:phase2-autheap
         * properties.
         *
         * Certificate data is specified using a "scheme"; three are currently
         * supported: blob, path and pkcs#11 URL. When using the blob scheme this property
         * should be set to the certificate's DER encoded data. When using the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * certificate, prefixed with the string "file://" and ending with a terminating
         * NUL byte.
         * This property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Note that enabling NMSetting8021x:system-ca-certs will override this
         * setting to use the built-in path, if the built-in path is not a directory.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_ca_cert() function instead.
         */
        get phase2CaCert(): GLib.Bytes;
        set phase2CaCert(val: GLib.Bytes);
        /**
         * The password used to access the "phase2" CA certificate stored in
         * #NMSetting8021x:phase2-ca-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get phase2_ca_cert_password(): string;
        set phase2_ca_cert_password(val: string);
        /**
         * The password used to access the "phase2" CA certificate stored in
         * #NMSetting8021x:phase2-ca-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get phase2CaCertPassword(): string;
        set phase2CaCertPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:phase2-ca-cert-password property.
         */
        get phase2_ca_cert_password_flags(): NM.SettingSecretFlags;
        set phase2_ca_cert_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:phase2-ca-cert-password property.
         */
        get phase2CaCertPasswordFlags(): NM.SettingSecretFlags;
        set phase2CaCertPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * UTF-8 encoded path to a directory containing PEM or DER formatted
         * certificates to be added to the verification chain in addition to the
         * certificate specified in the #NMSetting8021x:phase2-ca-cert property.
         *
         * If NMSetting8021x:system-ca-certs is enabled and the built-in CA
         * path is an existing directory, then this setting is ignored.
         */
        get phase2_ca_path(): string;
        set phase2_ca_path(val: string);
        /**
         * UTF-8 encoded path to a directory containing PEM or DER formatted
         * certificates to be added to the verification chain in addition to the
         * certificate specified in the #NMSetting8021x:phase2-ca-cert property.
         *
         * If NMSetting8021x:system-ca-certs is enabled and the built-in CA
         * path is an existing directory, then this setting is ignored.
         */
        get phase2CaPath(): string;
        set phase2CaPath(val: string);
        /**
         * Contains the "phase 2" client certificate if used by the EAP method
         * specified in the #NMSetting8021x:phase2-auth or
         * #NMSetting8021x:phase2-autheap properties.
         *
         * Certificate data is specified using a "scheme"; two are currently
         * supported: blob and path. When using the blob scheme (which is backwards
         * compatible with NM 0.7.x) this property should be set to the
         * certificate's DER encoded data. When using the path scheme, this property
         * should be set to the full UTF-8 encoded path of the certificate, prefixed
         * with the string "file://" and ending with a terminating NUL byte. This
         * property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_client_cert() function instead.
         */
        get phase2_client_cert(): GLib.Bytes;
        set phase2_client_cert(val: GLib.Bytes);
        /**
         * Contains the "phase 2" client certificate if used by the EAP method
         * specified in the #NMSetting8021x:phase2-auth or
         * #NMSetting8021x:phase2-autheap properties.
         *
         * Certificate data is specified using a "scheme"; two are currently
         * supported: blob and path. When using the blob scheme (which is backwards
         * compatible with NM 0.7.x) this property should be set to the
         * certificate's DER encoded data. When using the path scheme, this property
         * should be set to the full UTF-8 encoded path of the certificate, prefixed
         * with the string "file://" and ending with a terminating NUL byte. This
         * property can be unset even if the EAP method supports CA certificates,
         * but this allows man-in-the-middle attacks and is NOT recommended.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_client_cert() function instead.
         */
        get phase2ClientCert(): GLib.Bytes;
        set phase2ClientCert(val: GLib.Bytes);
        /**
         * The password used to access the "phase2" client certificate stored in
         * #NMSetting8021x:phase2-client-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get phase2_client_cert_password(): string;
        set phase2_client_cert_password(val: string);
        /**
         * The password used to access the "phase2" client certificate stored in
         * #NMSetting8021x:phase2-client-cert property. Only makes sense if the certificate
         * is stored on a PKCS#<!-- -->11 token that requires a login.
         */
        get phase2ClientCertPassword(): string;
        set phase2ClientCertPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:phase2-client-cert-password property.
         */
        get phase2_client_cert_password_flags(): NM.SettingSecretFlags;
        set phase2_client_cert_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:phase2-client-cert-password property.
         */
        get phase2ClientCertPasswordFlags(): NM.SettingSecretFlags;
        set phase2ClientCertPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * Constraint for server domain name. If set, this list of FQDNs is used as
         * a match requirement for dNSName element(s) of the certificate presented
         * by the authentication server during the inner "phase 2" authentication.
         * If a matching dNSName is found, this constraint is met.  If no dNSName
         * values are present, this constraint is matched against SubjectName CN
         * using the same comparison.
         * Multiple valid FQDNs can be passed as a ";" delimited list.
         */
        get phase2_domain_match(): string;
        set phase2_domain_match(val: string);
        /**
         * Constraint for server domain name. If set, this list of FQDNs is used as
         * a match requirement for dNSName element(s) of the certificate presented
         * by the authentication server during the inner "phase 2" authentication.
         * If a matching dNSName is found, this constraint is met.  If no dNSName
         * values are present, this constraint is matched against SubjectName CN
         * using the same comparison.
         * Multiple valid FQDNs can be passed as a ";" delimited list.
         */
        get phase2DomainMatch(): string;
        set phase2DomainMatch(val: string);
        /**
         * Constraint for server domain name. If set, this FQDN is used as a suffix
         * match requirement for dNSName element(s) of the certificate presented by
         * the authentication server during the inner "phase 2" authentication.  If
         * a matching dNSName is found, this constraint is met.  If no dNSName
         * values are present, this constraint is matched against SubjectName CN
         * using same suffix match comparison.
         * Since version 1.24, multiple valid FQDNs can be passed as a ";" delimited
         * list.
         */
        get phase2_domain_suffix_match(): string;
        set phase2_domain_suffix_match(val: string);
        /**
         * Constraint for server domain name. If set, this FQDN is used as a suffix
         * match requirement for dNSName element(s) of the certificate presented by
         * the authentication server during the inner "phase 2" authentication.  If
         * a matching dNSName is found, this constraint is met.  If no dNSName
         * values are present, this constraint is matched against SubjectName CN
         * using same suffix match comparison.
         * Since version 1.24, multiple valid FQDNs can be passed as a ";" delimited
         * list.
         */
        get phase2DomainSuffixMatch(): string;
        set phase2DomainSuffixMatch(val: string);
        /**
         * Contains the "phase 2" inner private key when the
         * #NMSetting8021x:phase2-auth or #NMSetting8021x:phase2-autheap property is
         * set to "tls".
         *
         * Key data is specified using a "scheme"; two are currently supported: blob
         * and path. When using the blob scheme and private keys, this property
         * should be set to the key's encrypted PEM encoded data. When using private
         * keys with the path scheme, this property should be set to the full UTF-8
         * encoded path of the key, prefixed with the string "file://" and ending
         * with a terminating NUL byte. When using PKCS#<!-- -->12 format private
         * keys and the blob scheme, this property should be set to the
         * PKCS#<!-- -->12 data and the #NMSetting8021x:phase2-private-key-password
         * property must be set to password used to decrypt the PKCS#<!-- -->12
         * certificate and key. When using PKCS#<!-- -->12 files and the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * key, prefixed with the string "file://" and ending with a terminating
         * NUL byte, and as with the blob scheme the
         * #NMSetting8021x:phase2-private-key-password property must be set to the
         * password used to decode the PKCS#<!-- -->12 private key and certificate.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_private_key() function instead.
         */
        get phase2_private_key(): GLib.Bytes;
        set phase2_private_key(val: GLib.Bytes);
        /**
         * Contains the "phase 2" inner private key when the
         * #NMSetting8021x:phase2-auth or #NMSetting8021x:phase2-autheap property is
         * set to "tls".
         *
         * Key data is specified using a "scheme"; two are currently supported: blob
         * and path. When using the blob scheme and private keys, this property
         * should be set to the key's encrypted PEM encoded data. When using private
         * keys with the path scheme, this property should be set to the full UTF-8
         * encoded path of the key, prefixed with the string "file://" and ending
         * with a terminating NUL byte. When using PKCS#<!-- -->12 format private
         * keys and the blob scheme, this property should be set to the
         * PKCS#<!-- -->12 data and the #NMSetting8021x:phase2-private-key-password
         * property must be set to password used to decrypt the PKCS#<!-- -->12
         * certificate and key. When using PKCS#<!-- -->12 files and the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * key, prefixed with the string "file://" and ending with a terminating
         * NUL byte, and as with the blob scheme the
         * #NMSetting8021x:phase2-private-key-password property must be set to the
         * password used to decode the PKCS#<!-- -->12 private key and certificate.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_phase2_private_key() function instead.
         */
        get phase2PrivateKey(): GLib.Bytes;
        set phase2PrivateKey(val: GLib.Bytes);
        /**
         * The password used to decrypt the "phase 2" private key specified in the
         * #NMSetting8021x:phase2-private-key property when the private key either
         * uses the path scheme, or is a PKCS#<!-- -->12 format key.  Setting this
         * property directly is not generally necessary except when returning
         * secrets to NetworkManager; it is generally set automatically when setting
         * the private key by the nm_setting_802_1x_set_phase2_private_key()
         * function.
         */
        get phase2_private_key_password(): string;
        set phase2_private_key_password(val: string);
        /**
         * The password used to decrypt the "phase 2" private key specified in the
         * #NMSetting8021x:phase2-private-key property when the private key either
         * uses the path scheme, or is a PKCS#<!-- -->12 format key.  Setting this
         * property directly is not generally necessary except when returning
         * secrets to NetworkManager; it is generally set automatically when setting
         * the private key by the nm_setting_802_1x_set_phase2_private_key()
         * function.
         */
        get phase2PrivateKeyPassword(): string;
        set phase2PrivateKeyPassword(val: string);
        /**
         * Flags indicating how to handle the
         * #NMSetting8021x:phase2-private-key-password property.
         */
        get phase2_private_key_password_flags(): NM.SettingSecretFlags;
        set phase2_private_key_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the
         * #NMSetting8021x:phase2-private-key-password property.
         */
        get phase2PrivateKeyPasswordFlags(): NM.SettingSecretFlags;
        set phase2PrivateKeyPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * Substring to be matched against the subject of the certificate presented
         * by the authentication server during the inner "phase 2"
         * authentication. When unset, no verification of the authentication server
         * certificate's subject is performed. This property provides little security,
         * if any, and should not be used.
         */
        get phase2_subject_match(): string;
        set phase2_subject_match(val: string);
        /**
         * Substring to be matched against the subject of the certificate presented
         * by the authentication server during the inner "phase 2"
         * authentication. When unset, no verification of the authentication server
         * certificate's subject is performed. This property provides little security,
         * if any, and should not be used.
         */
        get phase2SubjectMatch(): string;
        set phase2SubjectMatch(val: string);
        /**
         * PIN used for EAP authentication methods.
         */
        get pin(): string;
        set pin(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:pin property.
         */
        get pin_flags(): NM.SettingSecretFlags;
        set pin_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:pin property.
         */
        get pinFlags(): NM.SettingSecretFlags;
        set pinFlags(val: NM.SettingSecretFlags);
        /**
         * Contains the private key when the #NMSetting8021x:eap property is set to
         * "tls".
         *
         * Key data is specified using a "scheme"; two are currently supported: blob
         * and path. When using the blob scheme and private keys, this property
         * should be set to the key's encrypted PEM encoded data. When using private
         * keys with the path scheme, this property should be set to the full UTF-8
         * encoded path of the key, prefixed with the string "file://" and ending
         * with a terminating NUL byte. When using PKCS#<!-- -->12 format private
         * keys and the blob scheme, this property should be set to the
         * PKCS#<!-- -->12 data and the #NMSetting8021x:private-key-password
         * property must be set to password used to decrypt the PKCS#<!-- -->12
         * certificate and key. When using PKCS#<!-- -->12 files and the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * key, prefixed with the string "file://" and ending with a terminating
         * NUL byte, and as with the blob scheme the "private-key-password" property
         * must be set to the password used to decode the PKCS#<!-- -->12 private
         * key and certificate.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_private_key() function instead.
         *
         * WARNING: #NMSetting8021x:private-key is not a "secret" property, and thus
         * unencrypted private key data using the BLOB scheme may be readable by
         * unprivileged users.  Private keys should always be encrypted with a
         * private key password to prevent unauthorized access to unencrypted
         * private key data.
         */
        get private_key(): GLib.Bytes;
        set private_key(val: GLib.Bytes);
        /**
         * Contains the private key when the #NMSetting8021x:eap property is set to
         * "tls".
         *
         * Key data is specified using a "scheme"; two are currently supported: blob
         * and path. When using the blob scheme and private keys, this property
         * should be set to the key's encrypted PEM encoded data. When using private
         * keys with the path scheme, this property should be set to the full UTF-8
         * encoded path of the key, prefixed with the string "file://" and ending
         * with a terminating NUL byte. When using PKCS#<!-- -->12 format private
         * keys and the blob scheme, this property should be set to the
         * PKCS#<!-- -->12 data and the #NMSetting8021x:private-key-password
         * property must be set to password used to decrypt the PKCS#<!-- -->12
         * certificate and key. When using PKCS#<!-- -->12 files and the path
         * scheme, this property should be set to the full UTF-8 encoded path of the
         * key, prefixed with the string "file://" and ending with a terminating
         * NUL byte, and as with the blob scheme the "private-key-password" property
         * must be set to the password used to decode the PKCS#<!-- -->12 private
         * key and certificate.
         *
         * Setting this property directly is discouraged; use the
         * nm_setting_802_1x_set_private_key() function instead.
         *
         * WARNING: #NMSetting8021x:private-key is not a "secret" property, and thus
         * unencrypted private key data using the BLOB scheme may be readable by
         * unprivileged users.  Private keys should always be encrypted with a
         * private key password to prevent unauthorized access to unencrypted
         * private key data.
         */
        get privateKey(): GLib.Bytes;
        set privateKey(val: GLib.Bytes);
        /**
         * The password used to decrypt the private key specified in the
         * #NMSetting8021x:private-key property when the private key either uses the
         * path scheme, or if the private key is a PKCS#<!-- -->12 format key.  Setting this
         * property directly is not generally necessary except when returning
         * secrets to NetworkManager; it is generally set automatically when setting
         * the private key by the nm_setting_802_1x_set_private_key() function.
         */
        get private_key_password(): string;
        set private_key_password(val: string);
        /**
         * The password used to decrypt the private key specified in the
         * #NMSetting8021x:private-key property when the private key either uses the
         * path scheme, or if the private key is a PKCS#<!-- -->12 format key.  Setting this
         * property directly is not generally necessary except when returning
         * secrets to NetworkManager; it is generally set automatically when setting
         * the private key by the nm_setting_802_1x_set_private_key() function.
         */
        get privateKeyPassword(): string;
        set privateKeyPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSetting8021x:private-key-password
         * property.
         */
        get private_key_password_flags(): NM.SettingSecretFlags;
        set private_key_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSetting8021x:private-key-password
         * property.
         */
        get privateKeyPasswordFlags(): NM.SettingSecretFlags;
        set privateKeyPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * Substring to be matched against the subject of the certificate presented
         * by the authentication server. When unset, no verification of the
         * authentication server certificate's subject is performed. This property
         * provides little security, if any, and should not be used.
         */
        get subject_match(): string;
        set subject_match(val: string);
        /**
         * Substring to be matched against the subject of the certificate presented
         * by the authentication server. When unset, no verification of the
         * authentication server certificate's subject is performed. This property
         * provides little security, if any, and should not be used.
         */
        get subjectMatch(): string;
        set subjectMatch(val: string);
        /**
         * When %TRUE, overrides the #NMSetting8021x:ca-path and
         * #NMSetting8021x:phase2-ca-path properties using the system CA directory
         * specified at configure time with the --system-ca-path switch.  The
         * certificates in this directory are added to the verification chain in
         * addition to any certificates specified by the #NMSetting8021x:ca-cert and
         * #NMSetting8021x:phase2-ca-cert properties. If the path provided with
         * --system-ca-path is rather a file name (bundle of trusted CA certificates),
         * it overrides #NMSetting8021x:ca-cert and #NMSetting8021x:phase2-ca-cert
         * properties instead (sets ca_cert/ca_cert2 options for wpa_supplicant).
         */
        get system_ca_certs(): boolean;
        set system_ca_certs(val: boolean);
        /**
         * When %TRUE, overrides the #NMSetting8021x:ca-path and
         * #NMSetting8021x:phase2-ca-path properties using the system CA directory
         * specified at configure time with the --system-ca-path switch.  The
         * certificates in this directory are added to the verification chain in
         * addition to any certificates specified by the #NMSetting8021x:ca-cert and
         * #NMSetting8021x:phase2-ca-cert properties. If the path provided with
         * --system-ca-path is rather a file name (bundle of trusted CA certificates),
         * it overrides #NMSetting8021x:ca-cert and #NMSetting8021x:phase2-ca-cert
         * properties instead (sets ca_cert/ca_cert2 options for wpa_supplicant).
         */
        get systemCaCerts(): boolean;
        set systemCaCerts(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.Setting8021x.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.Setting8021x.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.Setting8021x;
        // Signals
        connect<K extends keyof NM.Setting8021x.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting8021x.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.Setting8021x.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.Setting8021x.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.Setting8021x.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.Setting8021x.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Determines and verifies the blob type.
         * When setting certificate properties of NMSetting8021x
         * the blob must be not UNKNOWN (or NULL).
         *
         * @param pdata the data pointer
         * @param length the length of the data
         */
        static check_cert_scheme(pdata: any | null, length: number): NM.Setting8021xCKScheme;
        // Methods
        /**
         * Adds an allowed alternate subject name match.  Until at least one
         * match is added, the altSubjectName of the remote authentication
         * server is not verified.
         *
         * @returns %TRUE if the alternative subject name match was
         *  successfully added, %FALSE if it was already allowed.
         * @param altsubject_match the altSubjectName to allow for this connection
         */
        add_altsubject_match(altsubject_match: string): boolean;
        /**
         * Adds an allowed EAP method.  The setting is not valid until at least one
         * EAP method has been added.  See #NMSetting8021x:eap property for a list of
         * allowed EAP methods.
         *
         * @returns %TRUE if the EAP method was successfully added, %FALSE if it was
         *  not a valid method or if it was already allowed.
         * @param eap the name of the EAP method to allow for this connection
         */
        add_eap_method(eap: string): boolean;
        /**
         * Adds an allowed alternate subject name match for "phase 2".  Until
         * at least one match is added, the altSubjectName of the "phase 2"
         * remote authentication server is not verified.
         *
         * @returns %TRUE if the "phase 2" alternative subject name match was
         *  successfully added, %FALSE if it was already allowed.
         * @param _phase2_altsubject_match the "phase 2" altSubjectName to allow for this
         * connection
         */
        add_phase2_altsubject_match(_phase2_altsubject_match: string): boolean;
        /**
         * Clears all altSubjectName matches.
         */
        clear_altsubject_matches(): void;
        /**
         * Clears all allowed EAP methods.
         */
        clear_eap_methods(): void;
        /**
         * Clears all "phase 2" altSubjectName matches.
         */
        clear_phase2_altsubject_matches(): void;
        /**
         * Returns the altSubjectName match at index `i`.
         *
         * @returns the altSubjectName match at index `i`
         * @param i the zero-based index of the array of altSubjectName matches
         */
        get_altsubject_match(i: number): string;
        /**
         * Returns the anonymous identifier used by some EAP methods (like TTLS) to
         * authenticate the user in the outer unencrypted "phase 1" authentication.  The
         * inner "phase 2" authentication will use the #NMSetting8021x:identity in
         * a secure form, if applicable for that EAP method.
         *
         * @returns the anonymous identifier
         */
        get_anonymous_identity(): string;
        /**
         * Returns the value contained in the #NMSetting8021x:auth-timeout property.
         *
         * @returns the configured authentication timeout in seconds. Zero means the
         * global default value.
         */
        get_auth_timeout(): number;
        /**
         * Returns the CA certificate blob if the CA certificate is stored using the
         * %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme.  Not all EAP methods use a
         * CA certificate (LEAP for example), and those that can take advantage of the
         * CA certificate allow it to be unset.  Note that lack of a CA certificate
         * reduces security by allowing man-in-the-middle attacks, because the identity
         * of the network cannot be confirmed by the client.
         *
         * @returns the CA certificate data
         */
        get_ca_cert_blob(): GLib.Bytes;
        get_ca_cert_password(): string;
        get_ca_cert_password_flags(): NM.SettingSecretFlags;
        /**
         * Returns the CA certificate path if the CA certificate is stored using the
         * %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.  Not all EAP methods use a
         * CA certificate (LEAP for example), and those that can take advantage of the
         * CA certificate allow it to be unset.  Note that lack of a CA certificate
         * reduces security by allowing man-in-the-middle attacks, because the identity
         * of the network cannot be confirmed by the client.
         *
         * @returns path to the CA certificate file
         */
        get_ca_cert_path(): string;
        /**
         * Returns the scheme used to store the CA certificate.  If the returned scheme
         * is %NM_SETTING_802_1X_CK_SCHEME_BLOB, use nm_setting_802_1x_get_ca_cert_blob();
         * if %NM_SETTING_802_1X_CK_SCHEME_PATH, use nm_setting_802_1x_get_ca_cert_path();
         * if %NM_SETTING_802_1X_CK_SCHEME_PKCS11, use nm_setting_802_1x_get_ca_cert_uri().
         *
         * @returns scheme used to store the CA certificate (blob or path)
         */
        get_ca_cert_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the CA certificate URI analogously to
         * nm_setting_802_1x_get_ca_cert_blob() and
         * nm_setting_802_1x_get_ca_cert_path().
         *
         * Currently, it's limited to PKCS#11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_ca_cert_uri(): string;
        /**
         * Returns the path of the CA certificate directory if previously set.  Systems
         * will often have a directory that contains multiple individual CA certificates
         * which the supplicant can then add to the verification chain.  This may be
         * used in addition to the #NMSetting8021x:ca-cert property to add more CA
         * certificates for verifying the network to client.
         *
         * @returns the CA certificate directory path
         */
        get_ca_path(): string;
        /**
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns the client certificate data
         */
        get_client_cert_blob(): GLib.Bytes;
        get_client_cert_password(): string;
        get_client_cert_password_flags(): NM.SettingSecretFlags;
        /**
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns path to the client certificate file
         */
        get_client_cert_path(): string;
        /**
         * Returns the scheme used to store the client certificate.  If the returned scheme
         * is %NM_SETTING_802_1X_CK_SCHEME_BLOB, use nm_setting_802_1x_get_client_cert_blob();
         * if %NM_SETTING_802_1X_CK_SCHEME_PATH, use nm_setting_802_1x_get_client_cert_path();
         * if %NM_SETTING_802_1X_CK_SCHEME_PKCS11, use nm_setting_802_1x_get_client_cert_uri().
         *
         * @returns scheme used to store the client certificate (blob or path)
         */
        get_client_cert_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the client certificate URI analogously to
         * nm_setting_802_1x_get_client_cert_blob() and
         * nm_setting_802_1x_get_client_cert_path().
         *
         * Currently, it's limited to PKCS#11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_client_cert_uri(): string;
        get_domain_match(): string;
        get_domain_suffix_match(): string;
        /**
         * Returns the name of the allowed EAP method at index `i`.
         *
         * @returns the name of the allowed EAP method at index `i`
         * @param i the index of the EAP method name to return
         */
        get_eap_method(i: number): string;
        /**
         * Returns the identifier used by some EAP methods (like TLS) to
         * authenticate the user.  Often this is a username or login name.
         *
         * @returns the user identifier
         */
        get_identity(): string;
        /**
         * Returns the number of entries in the
         * #NMSetting8021x:altsubject-matches property of this setting.
         *
         * @returns the number of altsubject-matches entries.
         */
        get_num_altsubject_matches(): number;
        /**
         * Returns the number of eap methods allowed for use when connecting to the
         * network.  Generally only one EAP method is used.  Use the functions
         * nm_setting_802_1x_get_eap_method(), nm_setting_802_1x_add_eap_method(),
         * and nm_setting_802_1x_remove_eap_method() for adding, removing, and retrieving
         * allowed EAP methods.
         *
         * @returns the number of allowed EAP methods
         */
        get_num_eap_methods(): number;
        /**
         * Returns the number of entries in the
         * #NMSetting8021x:phase2-altsubject-matches property of this setting.
         *
         * @returns the number of phase2-altsubject-matches entries.
         */
        get_num_phase2_altsubject_matches(): number;
        /**
         * Returns the openssl_ciphers configuration for wpa_supplicant.
         *
         * @returns cipher string for tls setup in wpa_supplicant.
         */
        get_openssl_ciphers(): string;
        /**
         * Returns the value contained in the #NMSetting8021x:optional property.
         *
         * @returns %TRUE if the activation should proceed even when the 802.1X
         *     authentication fails; %FALSE otherwise
         */
        get_optional(): boolean;
        /**
         * Returns the file containing PAC credentials used by EAP-FAST method.
         *
         * @returns the PAC file
         */
        get_pac_file(): string;
        get_password(): string;
        get_password_flags(): NM.SettingSecretFlags;
        get_password_raw(): GLib.Bytes;
        get_password_raw_flags(): NM.SettingSecretFlags;
        get_phase1_auth_flags(): NM.Setting8021xAuthFlags;
        get_phase1_fast_provisioning(): string;
        get_phase1_peaplabel(): string;
        get_phase1_peapver(): string;
        /**
         * Returns the "phase 2" altSubjectName match at index `i`.
         *
         * @returns the "phase 2" altSubjectName match at index `i`
         * @param i the zero-based index of the array of "phase 2" altSubjectName matches
         */
        get_phase2_altsubject_match(i: number): string;
        get_phase2_auth(): string;
        get_phase2_autheap(): string;
        /**
         * Returns the "phase 2" CA certificate blob if the CA certificate is stored
         * using the %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme.  Not all EAP methods use
         * a CA certificate (LEAP for example), and those that can take advantage of the
         * CA certificate allow it to be unset.  Note that lack of a CA certificate
         * reduces security by allowing man-in-the-middle attacks, because the identity
         * of the network cannot be confirmed by the client.
         *
         * @returns the "phase 2" CA certificate data
         */
        get_phase2_ca_cert_blob(): GLib.Bytes;
        get_phase2_ca_cert_password(): string;
        get_phase2_ca_cert_password_flags(): NM.SettingSecretFlags;
        /**
         * Returns the "phase 2" CA certificate path if the CA certificate is stored
         * using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.  Not all EAP methods use
         * a CA certificate (LEAP for example), and those that can take advantage of the
         * CA certificate allow it to be unset.  Note that lack of a CA certificate
         * reduces security by allowing man-in-the-middle attacks, because the identity
         * of the network cannot be confirmed by the client.
         *
         * @returns path to the "phase 2" CA certificate file
         */
        get_phase2_ca_cert_path(): string;
        /**
         * Returns the scheme used to store the "phase 2" CA certificate.  If the
         * returned scheme is %NM_SETTING_802_1X_CK_SCHEME_BLOB, use
         * nm_setting_802_1x_get_ca_cert_blob(); if %NM_SETTING_802_1X_CK_SCHEME_PATH,
         * use nm_setting_802_1x_get_ca_cert_path(); if %NM_SETTING_802_1X_CK_SCHEME_PKCS11,
         * use nm_setting_802_1x_get_ca_cert_uri().
         *
         * @returns scheme used to store the "phase 2" CA certificate (blob or path)
         */
        get_phase2_ca_cert_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the "phase 2" CA certificate URI analogously to
         * nm_setting_802_1x_get_phase2_ca_cert_blob() and
         * nm_setting_802_1x_get_phase2_ca_cert_path().
         *
         * Currently, it's limited to PKCS#<!-- -->11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_phase2_ca_cert_uri(): string;
        /**
         * Returns the path of the "phase 2" CA certificate directory if previously set.
         * Systems will often have a directory that contains multiple individual CA
         * certificates which the supplicant can then add to the verification chain.
         * This may be used in addition to the #NMSetting8021x:phase2-ca-cert property
         * to add more CA certificates for verifying the network to client.
         *
         * @returns the "phase 2" CA certificate directory path
         */
        get_phase2_ca_path(): string;
        /**
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns the "phase 2" client certificate data
         */
        get_phase2_client_cert_blob(): GLib.Bytes;
        get_phase2_client_cert_password(): string;
        get_phase2_client_cert_password_flags(): NM.SettingSecretFlags;
        /**
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns path to the "phase 2" client certificate file
         */
        get_phase2_client_cert_path(): string;
        /**
         * Returns the scheme used to store the "phase 2" client certificate.  If the
         * returned scheme is %NM_SETTING_802_1X_CK_SCHEME_BLOB, use
         * nm_setting_802_1x_get_client_cert_blob(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PATH, use
         * nm_setting_802_1x_get_client_cert_path(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PKCS11, use
         * nm_setting_802_1x_get_client_cert_uri().
         *
         * @returns scheme used to store the "phase 2" client certificate (blob or path)
         */
        get_phase2_client_cert_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the "phase 2" client certificate URI analogously to
         * nm_setting_802_1x_get_phase2_ca_cert_blob() and
         * nm_setting_802_1x_get_phase2_ca_cert_path().
         *
         * Currently, it's limited to PKCS#<!-- -->11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_phase2_client_cert_uri(): string;
        get_phase2_domain_match(): string;
        get_phase2_domain_suffix_match(): string;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * WARNING: the phase2 private key property is not a "secret" property, and thus
         * unencrypted private key data may be readable by unprivileged users.  Private
         * keys should always be encrypted with a private key password.
         *
         * @returns the "phase 2" private key data
         */
        get_phase2_private_key_blob(): GLib.Bytes;
        get_phase2_private_key_format(): NM.Setting8021xCKFormat;
        get_phase2_private_key_password(): string;
        get_phase2_private_key_password_flags(): NM.SettingSecretFlags;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns path to the "phase 2" private key file
         */
        get_phase2_private_key_path(): string;
        /**
         * Returns the scheme used to store the "phase 2" private key.  If the returned
         * scheme is %NM_SETTING_802_1X_CK_SCHEME_BLOB, use
         * nm_setting_802_1x_get_client_cert_blob(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PATH, use
         * nm_setting_802_1x_get_client_cert_path(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PKCS11, use
         * nm_setting_802_1x_get_client_cert_uri().
         *
         * @returns scheme used to store the "phase 2" private key (blob or path)
         */
        get_phase2_private_key_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the "phase 2" private key URI analogously to
         * nm_setting_802_1x_get_phase2_private_key_blob() and
         * nm_setting_802_1x_get_phase2_private_key_path().
         *
         * Currently, it's limited to PKCS#<!-- -->11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_phase2_private_key_uri(): string;
        get_phase2_subject_match(): string;
        get_pin(): string;
        get_pin_flags(): NM.SettingSecretFlags;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * WARNING: the private key property is not a "secret" property, and thus
         * unencrypted private key data may be readable by unprivileged users.  Private
         * keys should always be encrypted with a private key password.
         *
         * @returns the private key data
         */
        get_private_key_blob(): GLib.Bytes;
        get_private_key_format(): NM.Setting8021xCKFormat;
        get_private_key_password(): string;
        get_private_key_password_flags(): NM.SettingSecretFlags;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns path to the private key file
         */
        get_private_key_path(): string;
        /**
         * Returns the scheme used to store the private key.  If the returned scheme is
         * %NM_SETTING_802_1X_CK_SCHEME_BLOB, use
         * nm_setting_802_1x_get_client_cert_blob(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PATH, use
         * nm_setting_802_1x_get_client_cert_path(); if
         * %NM_SETTING_802_1X_CK_SCHEME_PKCS11, use
         * nm_setting_802_1x_get_client_cert_uri().
         *
         * @returns scheme used to store the private key (blob or path)
         */
        get_private_key_scheme(): NM.Setting8021xCKScheme;
        /**
         * Returns the private key URI analogously to
         * nm_setting_802_1x_get_private_key_blob() and
         * nm_setting_802_1x_get_private_key_path().
         *
         * Currently, it's limited to PKCS#<!-- -->11 URIs ('pkcs11' scheme as defined by RFC
         * 7512), but may be extended to other schemes in future (such as 'file' URIs
         * for local files and 'data' URIs for inline certificate data).
         *
         * @returns the URI string
         */
        get_private_key_uri(): string;
        get_subject_match(): string;
        /**
         * Sets the #NMSetting8021x:system-ca-certs property. The
         * #NMSetting8021x:ca-path and #NMSetting8021x:phase2-ca-path
         * properties are ignored if the #NMSetting8021x:system-ca-certs property is
         * %TRUE, in which case a system-wide CA certificate directory specified at
         * compile time (using the --system-ca-path configure option) is used in place
         * of these properties.
         *
         * @returns %TRUE if a system CA certificate path should be used, %FALSE if not
         */
        get_system_ca_certs(): boolean;
        /**
         * Removes the allowed altSubjectName at the specified index.
         *
         * @param i the index of the altSubjectName match to remove
         */
        remove_altsubject_match(i: number): void;
        /**
         * Removes the allowed altSubjectName `altsubject_match`.
         *
         * @returns %TRUE if the alternative subject name match was found and removed,
         *          %FALSE if it was not.
         * @param altsubject_match the altSubjectName to remove
         */
        remove_altsubject_match_by_value(altsubject_match: string): boolean;
        /**
         * Removes the allowed EAP method at the specified index.
         *
         * @param i the index of the EAP method to remove
         */
        remove_eap_method(i: number): void;
        /**
         * Removes the allowed EAP method `method`.
         *
         * @returns %TRUE if the EAP method was founs and removed, %FALSE if it was not.
         * @param eap the name of the EAP method to remove
         */
        remove_eap_method_by_value(eap: string): boolean;
        /**
         * Removes the allowed "phase 2" altSubjectName at the specified index.
         *
         * @param i the index of the "phase 2" altSubjectName match to remove
         */
        remove_phase2_altsubject_match(i: number): void;
        /**
         * Removes the allowed "phase 2" altSubjectName `phase2`_altsubject_match.
         *
         * @returns %TRUE if the alternative subject name match for "phase 2" was found and removed,
         *          %FALSE if it was not.
         * @param _phase2_altsubject_match the "phase 2" altSubjectName to remove
         */
        remove_phase2_altsubject_match_by_value(_phase2_altsubject_match: string): boolean;
        /**
         * Reads a certificate from disk and sets the #NMSetting8021x:ca-cert property
         * with the raw certificate data if using the %NM_SETTING_802_1X_CK_SCHEME_BLOB
         * scheme, or with the path to the certificate file if using the
         * %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH
         *   or %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the CA certificate
         *   file (PEM or DER format).  The path must be UTF-8 encoded; use
         *   g_filename_to_utf8() to convert if needed.  Passing %NULL with any `scheme`
         *   clears the CA certificate.
         * @param scheme desired storage scheme for the certificate
         * @param out_format on successful return, the type of the certificate added
         */
        set_ca_cert(
            value: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
        /**
         * Reads a certificate from disk and sets the #NMSetting8021x:client-cert
         * property with the raw certificate data if using the
         * %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme, or with the path to the certificate
         * file if using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH
         *   or %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the client
         *   certificate file (PEM, DER, or PKCS#<!-- -->12 format).  The path must be UTF-8
         *   encoded; use g_filename_to_utf8() to convert if needed.  Passing %NULL with
         *   any `scheme` clears the client certificate.
         * @param scheme desired storage scheme for the certificate
         * @param out_format on successful return, the type of the certificate added
         */
        set_client_cert(
            value: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
        /**
         * Reads a certificate from disk and sets the #NMSetting8021x:phase2-ca-cert
         * property with the raw certificate data if using the
         * %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme, or with the path to the certificate
         * file if using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH
         *   or %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the "phase2" CA
         *   certificate file (PEM or DER format).  The path must be UTF-8 encoded; use
         *   g_filename_to_utf8() to convert if needed.  Passing %NULL with any `scheme`
         *   clears the "phase2" CA certificate.
         * @param scheme desired storage scheme for the certificate
         * @param out_format on successful return, the type of the certificate added
         */
        set_phase2_ca_cert(
            value: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
        /**
         * Reads a certificate from disk and sets the #NMSetting8021x:phase2-client-cert
         * property with the raw certificate data if using the
         * %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme, or with the path to the certificate
         * file if using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * Client certificates are used to identify the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH
         *   or %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the "phase2" client
         *   certificate file (PEM, DER, or PKCS#<!-- -->12 format).  The path must be UTF-8
         *   encoded; use g_filename_to_utf8() to convert if needed.  Passing %NULL with
         *   any `scheme` clears the "phase2" client certificate.
         * @param scheme desired storage scheme for the certificate
         * @param out_format on successful return, the type of the certificate added
         */
        set_phase2_client_cert(
            value: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * This function reads a private key from disk and sets the
         * #NMSetting8021x:phase2-private-key property with the private key file data if
         * using the %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme, or with the path to the
         * private key file if using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * If `password` is given, this function attempts to decrypt the private key to
         * verify that `password` is correct, and if it is, updates the
         * #NMSetting8021x:phase2-private-key-password property with the given
         * `password`.  If the decryption is unsuccessful, %FALSE is returned, `error` is
         * set, and no internal data is changed.  If no `password` is given, the private
         * key is assumed to be valid, no decryption is performed, and the password may
         * be set at a later time.
         *
         * WARNING: the "phase2" private key property is not a "secret" property, and
         * thus unencrypted private key data using the BLOB scheme may be readable by
         * unprivileged users.  Private keys should always be encrypted with a private
         * key password to prevent unauthorized access to unencrypted private key data.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH or
         *   %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the "phase2" private
         *   key file (PEM, DER, or PKCS#<!-- -->12 format).  The path must be UTF-8 encoded;
         *   use g_filename_to_utf8() to convert if needed.  Passing %NULL with any
         *   `scheme` clears the private key.
         * @param password password used to decrypt the private key, or %NULL if the password
         *   is unknown.  If the password is given but fails to decrypt the private key,
         *   an error is returned.
         * @param scheme desired storage scheme for the private key
         * @param out_format on successful return, the type of the private key added
         */
        set_phase2_private_key(
            value: string,
            password: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
        /**
         * Private keys are used to authenticate the connecting client to the network
         * when EAP-TLS is used as either the "phase 1" or "phase 2" 802.1x
         * authentication method.
         *
         * This function reads a private key from disk and sets the
         * #NMSetting8021x:private-key property with the private key file data if using
         * the %NM_SETTING_802_1X_CK_SCHEME_BLOB scheme, or with the path to the private
         * key file if using the %NM_SETTING_802_1X_CK_SCHEME_PATH scheme.
         *
         * If `password` is given, this function attempts to decrypt the private key to
         * verify that `password` is correct, and if it is, updates the
         * #NMSetting8021x:private-key-password property with the given `password`.  If
         * the decryption is unsuccessful, %FALSE is returned, `error` is set, and no
         * internal data is changed.  If no `password` is given, the private key is
         * assumed to be valid, no decryption is performed, and the password may be set
         * at a later time.
         *
         * WARNING: the private key property is not a "secret" property, and thus
         * unencrypted private key data using the BLOB scheme may be readable by
         * unprivileged users.  Private keys should always be encrypted with a private
         * key password to prevent unauthorized access to unencrypted private key data.
         *
         * @returns %TRUE if the operation succeeded, %FALSE if it was unsuccessful
         * @param value when `scheme` is set to either %NM_SETTING_802_1X_CK_SCHEME_PATH or
         *   %NM_SETTING_802_1X_CK_SCHEME_BLOB, pass the path of the private key file
         *   (PEM, DER, or PKCS#<!-- -->12 format).  The path must be UTF-8 encoded; use
         *   g_filename_to_utf8() to convert if needed.  Passing %NULL with any `scheme`
         *   clears the private key.
         * @param password password used to decrypt the private key, or %NULL if the password
         *   is unknown.  If the password is given but fails to decrypt the private key,
         *   an error is returned.
         * @param scheme desired storage scheme for the private key
         * @param out_format on successful return, the type of the private key added
         */
        set_private_key(
            value: string,
            password: string,
            scheme: NM.Setting8021xCKScheme | null,
            out_format: NM.Setting8021xCKFormat | null,
        ): boolean;
    }
    class SettingAdsl extends Setting {
        static '$gtype': GObject.GType<SettingAdsl>;
        // Properties
        /**
         * Encapsulation of ADSL connection.  Can be "vcmux" or "llc".
         */
        get encapsulation(): string;
        set encapsulation(val: string);
        /**
         * Password used to authenticate with the ADSL service.
         */
        get password(): string;
        set password(val: string);
        /**
         * Flags indicating how to handle the #NMSettingAdsl:password property.
         */
        get password_flags(): NM.SettingSecretFlags;
        set password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingAdsl:password property.
         */
        get passwordFlags(): NM.SettingSecretFlags;
        set passwordFlags(val: NM.SettingSecretFlags);
        /**
         * ADSL connection protocol.  Can be "pppoa", "pppoe" or "ipoatm".
         */
        get protocol(): string;
        set protocol(val: string);
        /**
         * Username used to authenticate with the ADSL service.
         */
        get username(): string;
        set username(val: string);
        /**
         * VCI of ADSL connection
         */
        get vci(): number;
        set vci(val: number);
        /**
         * VPI of ADSL connection
         */
        get vpi(): number;
        set vpi(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingAdsl.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingAdsl.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingAdsl;
        // Signals
        connect<K extends keyof NM.SettingAdsl.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingAdsl.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingAdsl.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingAdsl.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingAdsl.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingAdsl.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_encapsulation(): string;
        get_password(): string;
        get_password_flags(): NM.SettingSecretFlags;
        get_protocol(): string;
        get_username(): string;
        get_vci(): number;
        get_vpi(): number;
    }
    class SettingBluetooth extends Setting {
        static '$gtype': GObject.GType<SettingBluetooth>;
        // Properties
        /**
         * The Bluetooth address of the device.
         */
        get bdaddr(): string;
        set bdaddr(val: string);
        /**
         * Either "dun" for Dial-Up Networking connections or "panu" for Personal
         * Area Networking connections to devices supporting the NAP profile.
         */
        get type(): string;
        set type(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingBluetooth.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingBluetooth.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingBluetooth;
        // Signals
        connect<K extends keyof NM.SettingBluetooth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBluetooth.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingBluetooth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBluetooth.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingBluetooth.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingBluetooth.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the Bluetooth address of the remote device which this setting
         * describes a connection to.
         *
         * @returns the Bluetooth address
         */
        get_bdaddr(): string;
        /**
         * Returns the connection method for communicating with the remote device (i.e.
         * either DUN to a DUN-capable device or PANU to a NAP-capable device).
         *
         * @returns the type, either %NM_SETTING_BLUETOOTH_TYPE_PANU,
         * %NM_SETTING_BLUETOOTH_TYPE_NAP or %NM_SETTING_BLUETOOTH_TYPE_DUN
         */
        get_connection_type(): string;
    }
    class SettingBond extends Setting {
        static '$gtype': GObject.GType<SettingBond>;
        // Properties
        /**
         * Dictionary of key/value pairs of bonding options.  Both keys and values
         * must be strings. Option names must contain only alphanumeric characters
         * (ie, [a-zA-Z0-9]).
         */
        get options(): GLib.HashTable<string, string>;
        set options(val: GLib.HashTable<string, string>);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingBond.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingBond.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingBond;
        // Signals
        connect<K extends keyof NM.SettingBond.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBond.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingBond.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBond.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingBond.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingBond.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Checks whether `name` is a valid bond option and `value` is a valid value for
         * the `name`. If `value` is %NULL, the function only validates the option name.
         *
         * @param name the name of the option to validate
         * @param value the value of the option to validate.
         */
        static validate_option(name: string, value?: string | null): boolean;
        // Methods
        /**
         * Add an option to the table. Adding a new name replaces any existing name/value pair
         * that may already exist.
         *
         * @returns returns %FALSE if either `name` or `value` is %NULL, in that case
         * the option is not set. Otherwise, the function does not fail and does not validate
         * the arguments. All validation happens via nm_connection_verify() or do basic validation
         * yourself with nm_setting_bond_validate_option().
         *
         * Note: Before 1.30, libnm would perform basic validation of the name and the value
         * via nm_setting_bond_validate_option() and reject the request by returning FALSE.
         * Since 1.30, libnm no longer rejects any values as the setter is not supposed
         * to perform validation.
         * @param name name for the option
         * @param value value for the option
         */
        add_option(name: string, value: string): boolean;
        /**
         * Returns the number of options that should be set for this bond when it
         * is activated. This can be used to retrieve each option individually
         * using nm_setting_bond_get_option().
         *
         * @returns the number of bonding options
         */
        get_num_options(): number;
        /**
         * Given an index, return the value of the bonding option at that index.  Indexes
         * are *not* guaranteed to be static across modifications to options done by
         * nm_setting_bond_add_option() and nm_setting_bond_remove_option(),
         * and should not be used to refer to options except for short periods of time
         * such as during option iteration.
         *
         * @returns %TRUE on success if the index was valid and an option was found,
         * %FALSE if the index was invalid (ie, greater than the number of options
         * currently held by the setting)
         * @param idx index of the desired option, from 0 to
         * nm_setting_bond_get_num_options() - 1
         */
        get_option(idx: number): [boolean, string, string];
        /**
         * Returns the value associated with the bonding option specified by
         * `name,` if it exists.
         *
         * @returns the value, or %NULL if the key/value pair was never added to the
         * setting; the value is owned by the setting and must not be modified
         * @param name the option name for which to retrieve the value
         */
        get_option_by_name(name: string): string;
        get_option_default(name: string): string;
        get_option_normalized(name: string): string;
        /**
         * Returns a list of valid bond options.
         *
         * The `setting` argument is unused and may be passed as %NULL.
         *
         * @returns a %NULL-terminated array of strings of valid bond options.
         */
        get_valid_options(): string[] | null;
        /**
         * Remove the bonding option referenced by `name` from the internal option
         * list.
         *
         * @returns %TRUE if the option was found and removed from the internal option
         * list, %FALSE if it was not.
         * @param name name of the option to remove
         */
        remove_option(name: string): boolean;
    }
    class SettingBondPort extends Setting {
        static '$gtype': GObject.GType<SettingBondPort>;
        // Properties
        /**
         * The port priority for bond active port re-selection during failover. A
         * higher number means a higher priority in selection. The primary port has
         * the highest priority. This option is only compatible with active-backup,
         * balance-tlb and balance-alb modes.
         */
        get prio(): number;
        set prio(val: number);
        /**
         * The queue ID of this bond port. The maximum value of queue ID is
         * the number of TX queues currently active in device.
         */
        get queue_id(): number;
        set queue_id(val: number);
        /**
         * The queue ID of this bond port. The maximum value of queue ID is
         * the number of TX queues currently active in device.
         */
        get queueId(): number;
        set queueId(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingBondPort.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingBondPort.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingBondPort;
        // Signals
        connect<K extends keyof NM.SettingBondPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBondPort.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingBondPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBondPort.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingBondPort.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingBondPort.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_prio(): number;
        get_queue_id(): number;
    }
    class SettingBridge extends Setting {
        static '$gtype': GObject.GType<SettingBridge>;
        // Properties
        /**
         * The Ethernet MAC address aging time, in seconds.
         */
        get ageing_time(): number;
        set ageing_time(val: number);
        /**
         * The Ethernet MAC address aging time, in seconds.
         */
        get ageingTime(): number;
        set ageingTime(val: number);
        /**
         * The Spanning Tree Protocol (STP) forwarding delay, in seconds.
         */
        get forward_delay(): number;
        set forward_delay(val: number);
        /**
         * The Spanning Tree Protocol (STP) forwarding delay, in seconds.
         */
        get forwardDelay(): number;
        set forwardDelay(val: number);
        /**
         * If specified, The MAC address of the multicast group this bridge uses for STP.
         *
         * The address must be a link-local address in standard Ethernet MAC address format,
         * ie an address of the form 01:80:C2:00:00:0X, with X in [0, 4..F].
         * If not specified the default value is 01:80:C2:00:00:00.
         */
        get group_address(): string;
        set group_address(val: string);
        /**
         * If specified, The MAC address of the multicast group this bridge uses for STP.
         *
         * The address must be a link-local address in standard Ethernet MAC address format,
         * ie an address of the form 01:80:C2:00:00:0X, with X in [0, 4..F].
         * If not specified the default value is 01:80:C2:00:00:00.
         */
        get groupAddress(): string;
        set groupAddress(val: string);
        /**
         * A mask of group addresses to forward. Usually, group addresses in
         * the range from 01:80:C2:00:00:00 to 01:80:C2:00:00:0F are not
         * forwarded according to standards. This property is a mask of 16 bits,
         * each corresponding to a group address in that range that must be
         * forwarded. The mask can't have bits 0, 1 or 2 set because they are
         * used for STP, MAC pause frames and LACP.
         */
        get group_forward_mask(): number;
        set group_forward_mask(val: number);
        /**
         * A mask of group addresses to forward. Usually, group addresses in
         * the range from 01:80:C2:00:00:00 to 01:80:C2:00:00:0F are not
         * forwarded according to standards. This property is a mask of 16 bits,
         * each corresponding to a group address in that range that must be
         * forwarded. The mask can't have bits 0, 1 or 2 set because they are
         * used for STP, MAC pause frames and LACP.
         */
        get groupForwardMask(): number;
        set groupForwardMask(val: number);
        /**
         * The Spanning Tree Protocol (STP) hello time, in seconds.
         */
        get hello_time(): number;
        set hello_time(val: number);
        /**
         * The Spanning Tree Protocol (STP) hello time, in seconds.
         */
        get helloTime(): number;
        set helloTime(val: number);
        /**
         * If specified, the MAC address of bridge. When creating a new bridge, this
         * MAC address will be set.
         *
         * If this field is left unspecified, the "ethernet.cloned-mac-address" is
         * referred instead to generate the initial MAC address. Note that setting
         * "ethernet.cloned-mac-address" anyway overwrites the MAC address of
         * the bridge later while activating the bridge.
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, the MAC address of bridge. When creating a new bridge, this
         * MAC address will be set.
         *
         * If this field is left unspecified, the "ethernet.cloned-mac-address" is
         * referred instead to generate the initial MAC address. Note that setting
         * "ethernet.cloned-mac-address" anyway overwrites the MAC address of
         * the bridge later while activating the bridge.
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * The Spanning Tree Protocol (STP) maximum message age, in seconds.
         */
        get max_age(): number;
        set max_age(val: number);
        /**
         * The Spanning Tree Protocol (STP) maximum message age, in seconds.
         */
        get maxAge(): number;
        set maxAge(val: number);
        /**
         * Set maximum size of multicast hash table (value must be a power of 2).
         */
        get multicast_hash_max(): number;
        set multicast_hash_max(val: number);
        /**
         * Set maximum size of multicast hash table (value must be a power of 2).
         */
        get multicastHashMax(): number;
        set multicastHashMax(val: number);
        /**
         * Set the number of queries the bridge will send before
         * stopping forwarding a multicast group after a "leave"
         * message has been received.
         */
        get multicast_last_member_count(): number;
        set multicast_last_member_count(val: number);
        /**
         * Set the number of queries the bridge will send before
         * stopping forwarding a multicast group after a "leave"
         * message has been received.
         */
        get multicastLastMemberCount(): number;
        set multicastLastMemberCount(val: number);
        /**
         * Set interval (in deciseconds) between queries to find remaining
         * members of a group, after a "leave" message is received.
         */
        get multicast_last_member_interval(): number;
        set multicast_last_member_interval(val: number);
        /**
         * Set interval (in deciseconds) between queries to find remaining
         * members of a group, after a "leave" message is received.
         */
        get multicastLastMemberInterval(): number;
        set multicastLastMemberInterval(val: number);
        /**
         * Set delay (in deciseconds) after which the bridge will
         * leave a group, if no membership reports for this
         * group are received.
         */
        get multicast_membership_interval(): number;
        set multicast_membership_interval(val: number);
        /**
         * Set delay (in deciseconds) after which the bridge will
         * leave a group, if no membership reports for this
         * group are received.
         */
        get multicastMembershipInterval(): number;
        set multicastMembershipInterval(val: number);
        /**
         * Enable or disable sending of multicast queries by the bridge.
         * If not specified the option is disabled.
         */
        get multicast_querier(): boolean;
        set multicast_querier(val: boolean);
        /**
         * Enable or disable sending of multicast queries by the bridge.
         * If not specified the option is disabled.
         */
        get multicastQuerier(): boolean;
        set multicastQuerier(val: boolean);
        /**
         * If no queries are seen after this delay (in deciseconds) has passed,
         * the bridge will start to send its own queries.
         */
        get multicast_querier_interval(): number;
        set multicast_querier_interval(val: number);
        /**
         * If no queries are seen after this delay (in deciseconds) has passed,
         * the bridge will start to send its own queries.
         */
        get multicastQuerierInterval(): number;
        set multicastQuerierInterval(val: number);
        /**
         * Interval (in deciseconds) between queries sent
         * by the bridge after the end of the startup phase.
         */
        get multicast_query_interval(): number;
        set multicast_query_interval(val: number);
        /**
         * Interval (in deciseconds) between queries sent
         * by the bridge after the end of the startup phase.
         */
        get multicastQueryInterval(): number;
        set multicastQueryInterval(val: number);
        /**
         * Set the Max Response Time/Max Response Delay
         * (in deciseconds) for IGMP/MLD queries sent by the bridge.
         */
        get multicast_query_response_interval(): number;
        set multicast_query_response_interval(val: number);
        /**
         * Set the Max Response Time/Max Response Delay
         * (in deciseconds) for IGMP/MLD queries sent by the bridge.
         */
        get multicastQueryResponseInterval(): number;
        set multicastQueryResponseInterval(val: number);
        /**
         * If enabled the bridge's own IP address is used as
         * the source address for IGMP queries otherwise
         * the default of 0.0.0.0 is used.
         */
        get multicast_query_use_ifaddr(): boolean;
        set multicast_query_use_ifaddr(val: boolean);
        /**
         * If enabled the bridge's own IP address is used as
         * the source address for IGMP queries otherwise
         * the default of 0.0.0.0 is used.
         */
        get multicastQueryUseIfaddr(): boolean;
        set multicastQueryUseIfaddr(val: boolean);
        /**
         * Sets bridge's multicast router. Multicast-snooping must be enabled
         * for this option to work.
         *
         * Supported values are: 'auto', 'disabled', 'enabled' to which kernel
         * assigns the numbers 1, 0, and 2, respectively.
         * If not specified the default value is 'auto' (1).
         */
        get multicast_router(): string;
        set multicast_router(val: string);
        /**
         * Sets bridge's multicast router. Multicast-snooping must be enabled
         * for this option to work.
         *
         * Supported values are: 'auto', 'disabled', 'enabled' to which kernel
         * assigns the numbers 1, 0, and 2, respectively.
         * If not specified the default value is 'auto' (1).
         */
        get multicastRouter(): string;
        set multicastRouter(val: string);
        /**
         * Controls whether IGMP snooping is enabled for this bridge.
         * Note that if snooping was automatically disabled due to hash collisions,
         * the system may refuse to enable the feature until the collisions are
         * resolved.
         */
        get multicast_snooping(): boolean;
        set multicast_snooping(val: boolean);
        /**
         * Controls whether IGMP snooping is enabled for this bridge.
         * Note that if snooping was automatically disabled due to hash collisions,
         * the system may refuse to enable the feature until the collisions are
         * resolved.
         */
        get multicastSnooping(): boolean;
        set multicastSnooping(val: boolean);
        /**
         * Set the number of IGMP queries to send during startup phase.
         */
        get multicast_startup_query_count(): number;
        set multicast_startup_query_count(val: number);
        /**
         * Set the number of IGMP queries to send during startup phase.
         */
        get multicastStartupQueryCount(): number;
        set multicastStartupQueryCount(val: number);
        /**
         * Sets the time (in deciseconds) between queries sent out
         * at startup to determine membership information.
         */
        get multicast_startup_query_interval(): number;
        set multicast_startup_query_interval(val: number);
        /**
         * Sets the time (in deciseconds) between queries sent out
         * at startup to determine membership information.
         */
        get multicastStartupQueryInterval(): number;
        set multicastStartupQueryInterval(val: number);
        /**
         * Sets the Spanning Tree Protocol (STP) priority for this bridge.  Lower
         * values are "better"; the lowest priority bridge will be elected the root
         * bridge.
         */
        get priority(): number;
        set priority(val: number);
        /**
         * Controls whether Spanning Tree Protocol (STP) is enabled for this bridge.
         */
        get stp(): boolean;
        set stp(val: boolean);
        /**
         * The default PVID for the ports of the bridge, that is the VLAN id
         * assigned to incoming untagged frames.
         */
        get vlan_default_pvid(): number;
        set vlan_default_pvid(val: number);
        /**
         * The default PVID for the ports of the bridge, that is the VLAN id
         * assigned to incoming untagged frames.
         */
        get vlanDefaultPvid(): number;
        set vlanDefaultPvid(val: number);
        /**
         * Control whether VLAN filtering is enabled on the bridge.
         */
        get vlan_filtering(): boolean;
        set vlan_filtering(val: boolean);
        /**
         * Control whether VLAN filtering is enabled on the bridge.
         */
        get vlanFiltering(): boolean;
        set vlanFiltering(val: boolean);
        /**
         * If specified, the protocol used for VLAN filtering.
         *
         * Supported values are: '802.1Q', '802.1ad'.
         * If not specified the default value is '802.1Q'.
         */
        get vlan_protocol(): string;
        set vlan_protocol(val: string);
        /**
         * If specified, the protocol used for VLAN filtering.
         *
         * Supported values are: '802.1Q', '802.1ad'.
         * If not specified the default value is '802.1Q'.
         */
        get vlanProtocol(): string;
        set vlanProtocol(val: string);
        /**
         * Controls whether per-VLAN stats accounting is enabled.
         */
        get vlan_stats_enabled(): boolean;
        set vlan_stats_enabled(val: boolean);
        /**
         * Controls whether per-VLAN stats accounting is enabled.
         */
        get vlanStatsEnabled(): boolean;
        set vlanStatsEnabled(val: boolean);
        /**
         * Array of bridge VLAN objects. In addition to the VLANs
         * specified here, the bridge will also have the default-pvid
         * VLAN configured  by the bridge.vlan-default-pvid property.
         *
         * In nmcli the VLAN list can be specified with the following
         * syntax:
         *
         *  $vid [pvid] [untagged] [, $vid [pvid] [untagged]]...
         *
         * where $vid is either a single id between 1 and 4094 or a
         * range, represented as a couple of ids separated by a dash.
         */
        get vlans(): NM.BridgeVlan[];
        set vlans(val: NM.BridgeVlan[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingBridge.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingBridge.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingBridge;
        // Signals
        connect<K extends keyof NM.SettingBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBridge.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBridge.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingBridge.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingBridge.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new vlan and associated information to the setting.  The
         * given vlan gets sealed and a reference to it is added.
         *
         * @param vlan the vlan to add
         */
        add_vlan(vlan: NM.BridgeVlan): void;
        /**
         * Removes all configured VLANs.
         */
        clear_vlans(): void;
        get_ageing_time(): number;
        get_forward_delay(): number;
        get_group_address(): string;
        get_group_forward_mask(): number;
        get_hello_time(): number;
        get_mac_address(): string;
        get_max_age(): number;
        get_multicast_hash_max(): number;
        get_multicast_last_member_count(): number;
        get_multicast_last_member_interval(): number;
        get_multicast_membership_interval(): number;
        get_multicast_querier(): boolean;
        get_multicast_querier_interval(): number;
        get_multicast_query_interval(): number;
        get_multicast_query_response_interval(): number;
        get_multicast_query_use_ifaddr(): boolean;
        get_multicast_router(): string;
        get_multicast_snooping(): boolean;
        get_multicast_startup_query_count(): number;
        get_multicast_startup_query_interval(): number;
        get_num_vlans(): number;
        get_priority(): number;
        get_stp(): boolean;
        get_vlan(idx: number): NM.BridgeVlan;
        get_vlan_default_pvid(): number;
        get_vlan_filtering(): boolean;
        get_vlan_protocol(): string;
        get_vlan_stats_enabled(): boolean;
        /**
         * Removes the vlan at index `idx`.
         *
         * @param idx index number of the VLAN.
         */
        remove_vlan(idx: number): void;
        /**
         * Remove the VLAN with range `vid_start` to `vid_end`.
         * If `vid_end` is zero, it is assumed to be equal to `vid_start`
         * and so the single-id VLAN with id `vid_start` is removed.
         *
         * @returns %TRUE if the vlan was found and removed; %FALSE otherwise
         * @param vid_start the vlan start index
         * @param vid_end the vlan end index
         */
        remove_vlan_by_vid(vid_start: number, vid_end: number): boolean;
    }
    class SettingBridgePort extends Setting {
        static '$gtype': GObject.GType<SettingBridgePort>;
        // Properties
        /**
         * Enables or disables "hairpin mode" for the port, which allows frames to
         * be sent back out through the port the frame was received on.
         */
        get hairpin_mode(): boolean;
        set hairpin_mode(val: boolean);
        /**
         * Enables or disables "hairpin mode" for the port, which allows frames to
         * be sent back out through the port the frame was received on.
         */
        get hairpinMode(): boolean;
        set hairpinMode(val: boolean);
        /**
         * The Spanning Tree Protocol (STP) port cost for destinations via this
         * port.
         */
        get path_cost(): number;
        set path_cost(val: number);
        /**
         * The Spanning Tree Protocol (STP) port cost for destinations via this
         * port.
         */
        get pathCost(): number;
        set pathCost(val: number);
        /**
         * The Spanning Tree Protocol (STP) priority of this bridge port.
         */
        get priority(): number;
        set priority(val: number);
        /**
         * Array of bridge VLAN objects. In addition to the VLANs
         * specified here, the port will also have the default-pvid
         * VLAN configured on the bridge by the bridge.vlan-default-pvid
         * property.
         *
         * In nmcli the VLAN list can be specified with the following
         * syntax:
         *
         *  $vid [pvid] [untagged] [, $vid [pvid] [untagged]]...
         *
         * where $vid is either a single id between 1 and 4094 or a
         * range, represented as a couple of ids separated by a dash.
         */
        get vlans(): NM.BridgeVlan[];
        set vlans(val: NM.BridgeVlan[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingBridgePort.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingBridgePort.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingBridgePort;
        // Signals
        connect<K extends keyof NM.SettingBridgePort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBridgePort.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingBridgePort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingBridgePort.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingBridgePort.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingBridgePort.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new vlan and associated information to the setting.  The
         * given vlan gets sealed and a reference to it is added.
         *
         * @param vlan the vlan to add
         */
        add_vlan(vlan: NM.BridgeVlan): void;
        /**
         * Removes all configured VLANs.
         */
        clear_vlans(): void;
        get_hairpin_mode(): boolean;
        get_num_vlans(): number;
        get_path_cost(): number;
        get_priority(): number;
        get_vlan(idx: number): NM.BridgeVlan;
        /**
         * Removes the vlan at index `idx`.
         *
         * @param idx index number of the VLAN.
         */
        remove_vlan(idx: number): void;
        /**
         * Remove the VLAN with range `vid_start` to `vid_end`.
         * If `vid_end` is zero, it is assumed to be equal to `vid_start`
         * and so the single-id VLAN with id `vid_start` is removed.
         *
         * @returns %TRUE if the vlan was found and removed; %FALSE otherwise
         * @param vid_start the vlan start index
         * @param vid_end the vlan end index
         */
        remove_vlan_by_vid(vid_start: number, vid_end: number): boolean;
    }
    class SettingCdma extends Setting {
        static '$gtype': GObject.GType<SettingCdma>;
        // Properties
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * The number to dial to establish the connection to the CDMA-based mobile
         * broadband network, if any.  If not specified, the default number (#777)
         * is used when required.
         */
        get number(): string;
        set number(val: string);
        /**
         * The password used to authenticate with the network, if required.  Many
         * providers do not require a password, or accept any password.  But if a
         * password is required, it is specified here.
         */
        get password(): string;
        set password(val: string);
        /**
         * Flags indicating how to handle the #NMSettingCdma:password property.
         */
        get password_flags(): NM.SettingSecretFlags;
        set password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingCdma:password property.
         */
        get passwordFlags(): NM.SettingSecretFlags;
        set passwordFlags(val: NM.SettingSecretFlags);
        /**
         * The username used to authenticate with the network, if required.  Many
         * providers do not require a username, or accept any username.  But if a
         * username is required, it is specified here.
         */
        get username(): string;
        set username(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingCdma.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingCdma.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingCdma;
        // Signals
        connect<K extends keyof NM.SettingCdma.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingCdma.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingCdma.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingCdma.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingCdma.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingCdma.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_mtu(): number;
        get_number(): string;
        get_password(): string;
        get_password_flags(): NM.SettingSecretFlags;
        get_username(): string;
    }
    class SettingConnection extends Setting {
        static '$gtype': GObject.GType<SettingConnection>;
        // Properties
        /**
         * The number of retries for the authentication. Zero means to try indefinitely; -1 means
         * to use a global default. If the global default is not set, the authentication
         * retries for 3 times before failing the connection.
         *
         * Currently, this only applies to 802-1x authentication.
         */
        get auth_retries(): number;
        set auth_retries(val: number);
        /**
         * The number of retries for the authentication. Zero means to try indefinitely; -1 means
         * to use a global default. If the global default is not set, the authentication
         * retries for 3 times before failing the connection.
         *
         * Currently, this only applies to 802-1x authentication.
         */
        get authRetries(): number;
        set authRetries(val: number);
        /**
         * Whether or not the connection should be automatically connected by
         * NetworkManager when the resources for the connection are available.
         * %TRUE to automatically activate the connection, %FALSE to require manual
         * intervention to activate the connection.
         *
         * Autoconnect happens when the circumstances are suitable. That means for
         * example that the device is currently managed and not active. Autoconnect
         * thus never replaces or competes with an already active profile.
         *
         * Note that autoconnect is not implemented for VPN profiles. See
         * #NMSettingConnection:secondaries as an alternative to automatically
         * connect VPN profiles.
         *
         * If multiple profiles are ready to autoconnect on the same device,
         * the one with the better "connection.autoconnect-priority" is chosen. If
         * the priorities are equal, then the most recently connected profile is activated.
         * If the profiles were not connected earlier or their
         * "connection.timestamp" is identical, the choice is undefined.
         *
         * Depending on "connection.multi-connect", a profile can (auto)connect only
         * once at a time or multiple times.
         */
        get autoconnect(): boolean;
        set autoconnect(val: boolean);
        /**
         * Whether or not ports of this connection should be automatically brought up
         * when NetworkManager activates this connection. This only has a real effect
         * for controller connections. The properties #NMSettingConnection:autoconnect,
         * #NMSettingConnection:autoconnect-priority and #NMSettingConnection:autoconnect-retries
         * are unrelated to this setting.
         * The permitted values are: 0: leave port connections untouched,
         * 1: activate all the port connections with this connection, -1: default.
         * If -1 (default) is set, global connection.autoconnect-ports is read to
         * determine the real value. If it is default as well, this fallbacks to 0.
         */
        get autoconnect_ports(): number;
        set autoconnect_ports(val: number);
        /**
         * Whether or not ports of this connection should be automatically brought up
         * when NetworkManager activates this connection. This only has a real effect
         * for controller connections. The properties #NMSettingConnection:autoconnect,
         * #NMSettingConnection:autoconnect-priority and #NMSettingConnection:autoconnect-retries
         * are unrelated to this setting.
         * The permitted values are: 0: leave port connections untouched,
         * 1: activate all the port connections with this connection, -1: default.
         * If -1 (default) is set, global connection.autoconnect-ports is read to
         * determine the real value. If it is default as well, this fallbacks to 0.
         */
        get autoconnectPorts(): number;
        set autoconnectPorts(val: number);
        /**
         * The autoconnect priority in range -999 to 999. If the connection is set
         * to autoconnect, connections with higher priority will be preferred.
         * The higher number means higher priority. Defaults to 0.
         * Note that this property only matters if there are more than one candidate
         * profile to select for autoconnect. In case of equal priority, the profile
         * used most recently is chosen.
         */
        get autoconnect_priority(): number;
        set autoconnect_priority(val: number);
        /**
         * The autoconnect priority in range -999 to 999. If the connection is set
         * to autoconnect, connections with higher priority will be preferred.
         * The higher number means higher priority. Defaults to 0.
         * Note that this property only matters if there are more than one candidate
         * profile to select for autoconnect. In case of equal priority, the profile
         * used most recently is chosen.
         */
        get autoconnectPriority(): number;
        set autoconnectPriority(val: number);
        /**
         * The number of times a connection should be tried when autoactivating before
         * giving up. Zero means forever, -1 means the global default (4 times if not
         * overridden). Setting this to 1 means to try activation only once before
         * blocking autoconnect. Note that after a timeout, NetworkManager will try
         * to autoconnect again.
         */
        get autoconnect_retries(): number;
        set autoconnect_retries(val: number);
        /**
         * The number of times a connection should be tried when autoactivating before
         * giving up. Zero means forever, -1 means the global default (4 times if not
         * overridden). Setting this to 1 means to try activation only once before
         * blocking autoconnect. Note that after a timeout, NetworkManager will try
         * to autoconnect again.
         */
        get autoconnectRetries(): number;
        set autoconnectRetries(val: number);
        /**
         * Whether or not ports of this connection should be automatically brought up
         * when NetworkManager activates this connection. This only has a real effect
         * for controller connections. The properties #NMSettingConnection:autoconnect,
         * #NMSettingConnection:autoconnect-priority and #NMSettingConnection:autoconnect-retries
         * are unrelated to this setting.
         * The permitted values are: 0: leave port connections untouched,
         * 1: activate all the port connections with this connection, -1: default.
         * If -1 (default) is set, global connection.autoconnect-slaves is read to
         * determine the real value. If it is default as well, this fallbacks to 0.
         *
         * Deprecated 1.46. Use #NMSettingConnection:autoconnect-ports instead, this is just an alias.
         */
        get autoconnect_slaves(): NM.SettingConnectionAutoconnectSlaves;
        set autoconnect_slaves(val: NM.SettingConnectionAutoconnectSlaves);
        /**
         * Whether or not ports of this connection should be automatically brought up
         * when NetworkManager activates this connection. This only has a real effect
         * for controller connections. The properties #NMSettingConnection:autoconnect,
         * #NMSettingConnection:autoconnect-priority and #NMSettingConnection:autoconnect-retries
         * are unrelated to this setting.
         * The permitted values are: 0: leave port connections untouched,
         * 1: activate all the port connections with this connection, -1: default.
         * If -1 (default) is set, global connection.autoconnect-slaves is read to
         * determine the real value. If it is default as well, this fallbacks to 0.
         *
         * Deprecated 1.46. Use #NMSettingConnection:autoconnect-ports instead, this is just an alias.
         */
        get autoconnectSlaves(): NM.SettingConnectionAutoconnectSlaves;
        set autoconnectSlaves(val: NM.SettingConnectionAutoconnectSlaves);
        /**
         * Interface name of the controller device or UUID of the controller connection.
         */
        get controller(): string;
        set controller(val: string);
        /**
         * Whether DNSOverTls (dns-over-tls) is enabled for the connection.
         * DNSOverTls is a technology which uses TLS to encrypt dns traffic.
         *
         * The permitted values are: "yes" (2) use DNSOverTls and disabled fallback,
         * "opportunistic" (1) use DNSOverTls but allow fallback to unencrypted resolution,
         * "no" (0) don't ever use DNSOverTls.
         * If unspecified "default" depends on the plugin used. Systemd-resolved
         * uses global setting.
         *
         * This feature requires a plugin which supports DNSOverTls. Otherwise, the
         * setting has no effect. One such plugin is dns-systemd-resolved.
         */
        get dns_over_tls(): number;
        set dns_over_tls(val: number);
        /**
         * Whether DNSOverTls (dns-over-tls) is enabled for the connection.
         * DNSOverTls is a technology which uses TLS to encrypt dns traffic.
         *
         * The permitted values are: "yes" (2) use DNSOverTls and disabled fallback,
         * "opportunistic" (1) use DNSOverTls but allow fallback to unencrypted resolution,
         * "no" (0) don't ever use DNSOverTls.
         * If unspecified "default" depends on the plugin used. Systemd-resolved
         * uses global setting.
         *
         * This feature requires a plugin which supports DNSOverTls. Otherwise, the
         * setting has no effect. One such plugin is dns-systemd-resolved.
         */
        get dnsOverTls(): number;
        set dnsOverTls(val: number);
        /**
         * Whether the connection will be brought down before the system is powered
         * off.  The default value is %NM_SETTING_CONNECTION_DOWN_ON_POWEROFF_DEFAULT. When
         * the default value is specified, then the global value from
         * NetworkManager configuration is looked up, if not set, it is considered
         * as %NM_SETTING_CONNECTION_DOWN_ON_POWEROFF_NO.
         */
        get down_on_poweroff(): number;
        set down_on_poweroff(val: number);
        /**
         * Whether the connection will be brought down before the system is powered
         * off.  The default value is %NM_SETTING_CONNECTION_DOWN_ON_POWEROFF_DEFAULT. When
         * the default value is specified, then the global value from
         * NetworkManager configuration is looked up, if not set, it is considered
         * as %NM_SETTING_CONNECTION_DOWN_ON_POWEROFF_NO.
         */
        get downOnPoweroff(): number;
        set downOnPoweroff(val: number);
        /**
         * If greater than zero, delay success of IP addressing until either the
         * timeout is reached, or an IP gateway replies to a ping.
         */
        get gateway_ping_timeout(): number;
        set gateway_ping_timeout(val: number);
        /**
         * If greater than zero, delay success of IP addressing until either the
         * timeout is reached, or an IP gateway replies to a ping.
         */
        get gatewayPingTimeout(): number;
        set gatewayPingTimeout(val: number);
        /**
         * A human readable unique identifier for the connection, like "Work Wi-Fi"
         * or "T-Mobile 3G".
         */
        get id(): string;
        set id(val: string);
        /**
         * The name of the network interface this connection is bound to. If not
         * set, then the connection can be attached to any interface of the
         * appropriate type (subject to restrictions imposed by other settings).
         *
         * For software devices this specifies the name of the created device.
         *
         * For connection types where interface names cannot easily be made
         * persistent (e.g. mobile broadband or USB Ethernet), this property should
         * not be used. Setting this property restricts the interfaces a connection
         * can be used with, and if interface names change or are reordered the
         * connection may be applied to the wrong interface.
         */
        get interface_name(): string;
        set interface_name(val: string);
        /**
         * The name of the network interface this connection is bound to. If not
         * set, then the connection can be attached to any interface of the
         * appropriate type (subject to restrictions imposed by other settings).
         *
         * For software devices this specifies the name of the created device.
         *
         * For connection types where interface names cannot easily be made
         * persistent (e.g. mobile broadband or USB Ethernet), this property should
         * not be used. Setting this property restricts the interfaces a connection
         * can be used with, and if interface names change or are reordered the
         * connection may be applied to the wrong interface.
         */
        get interfaceName(): string;
        set interfaceName(val: string);
        /**
         * The property specifies a list of target IP addresses for pinging.
         * When multiple targets are set, NetworkManager will start multiple ping processes
         * in parallel. This property can only be set if connection.ip-ping-timeout is
         * set. The ip-ping-timeout is used to delay the success of IP addressing until
         * either the specified timeout (in seconds) is reached, or an target IP address replies
         * to a ping. Configuring #NMSettingConnection:ip-ping-addresses may delay reaching the
         * systemd's network-online.target due to waiting for the ping operations to complete or timeout.
         */
        get ip_ping_addresses(): string[];
        set ip_ping_addresses(val: string[]);
        /**
         * The property specifies a list of target IP addresses for pinging.
         * When multiple targets are set, NetworkManager will start multiple ping processes
         * in parallel. This property can only be set if connection.ip-ping-timeout is
         * set. The ip-ping-timeout is used to delay the success of IP addressing until
         * either the specified timeout (in seconds) is reached, or an target IP address replies
         * to a ping. Configuring #NMSettingConnection:ip-ping-addresses may delay reaching the
         * systemd's network-online.target due to waiting for the ping operations to complete or timeout.
         */
        get ipPingAddresses(): string[];
        set ipPingAddresses(val: string[]);
        /**
         * The property determines whether it is sufficient for any ping check
         * to succeed among #NMSettingConnection:ip-ping-addresses, or if all
         * ping checks must succeed for #NMSettingConnection:ip-ping-addresses.
         */
        get ip_ping_addresses_require_all(): number;
        set ip_ping_addresses_require_all(val: number);
        /**
         * The property determines whether it is sufficient for any ping check
         * to succeed among #NMSettingConnection:ip-ping-addresses, or if all
         * ping checks must succeed for #NMSettingConnection:ip-ping-addresses.
         */
        get ipPingAddressesRequireAll(): number;
        set ipPingAddressesRequireAll(val: number);
        /**
         * If greater than zero, delay success of IP addressing until either the specified
         * timeout (in seconds) is reached, or a target IP address replies to a ping. The
         * property specifies the timeout for the #NMSettingConnection:ip-ping-addresses.
         * This property is incompatible with #NMSettingConnection:gateway-ping-timeout,
         * you cannot set these two properties at the same time.
         */
        get ip_ping_timeout(): number;
        set ip_ping_timeout(val: number);
        /**
         * If greater than zero, delay success of IP addressing until either the specified
         * timeout (in seconds) is reached, or a target IP address replies to a ping. The
         * property specifies the timeout for the #NMSettingConnection:ip-ping-addresses.
         * This property is incompatible with #NMSettingConnection:gateway-ping-timeout,
         * you cannot set these two properties at the same time.
         */
        get ipPingTimeout(): number;
        set ipPingTimeout(val: number);
        /**
         * Whether LLDP is enabled for the connection.
         */
        get lldp(): number;
        set lldp(val: number);
        /**
         * Whether Link-Local Multicast Name Resolution (LLMNR) is enabled
         * for the connection. LLMNR is a protocol based on the Domain Name
         * System (DNS) packet format that allows both IPv4 and IPv6 hosts
         * to perform name resolution for hosts on the same local link.
         *
         * The permitted values are: "yes" (2) register hostname and resolving
         * for the connection, "no" (0) disable LLMNR for the interface, "resolve"
         * (1) do not register hostname but allow resolving of LLMNR host names
         * If unspecified, "default" ultimately depends on the DNS plugin (which
         * for systemd-resolved currently means "yes").
         *
         * This feature requires a plugin which supports LLMNR. Otherwise, the
         * setting has no effect. One such plugin is dns-systemd-resolved.
         */
        get llmnr(): number;
        set llmnr(val: number);
        /**
         * Interface name of the controller device or UUID of the controller connection.
         *
         * Deprecated 1.46. Use #NMSettingConnection:controller instead, this is just an alias.
         */
        get master(): string;
        set master(val: string);
        /**
         * Whether mDNS is enabled for the connection.
         *
         * The permitted values are: "yes" (2) register hostname and resolving
         * for the connection, "no" (0) disable mDNS for the interface, "resolve"
         * (1) do not register hostname but allow resolving of mDNS host names
         * and "default" (-1) to allow lookup of a global default in NetworkManager.conf.
         * If unspecified, "default" ultimately depends on the DNS plugin.
         *
         * This feature requires a plugin which supports mDNS. Otherwise, the
         * setting has no effect. Currently the only supported DNS plugin is
         * systemd-resolved. For systemd-resolved, the default is configurable via
         * MulticastDNS= setting in resolved.conf.
         */
        get mdns(): number;
        set mdns(val: number);
        /**
         * Whether the connection is metered.
         *
         * When updating this property on a currently activated connection,
         * the change takes effect immediately.
         */
        get metered(): NM.Metered;
        set metered(val: NM.Metered);
        /**
         * Whether to configure MPTCP endpoints and the address flags.
         * If MPTCP is enabled in NetworkManager, it will configure the
         * addresses of the interface as MPTCP endpoints. Note that
         * IPv4 loopback addresses (127.0.0.0/8), IPv4 link local
         * addresses (169.254.0.0/16), the IPv6 loopback address (::1),
         * IPv6 link local addresses (fe80::/10), IPv6 unique
         * local addresses (ULA, fc00::/7) and IPv6 privacy extension addresses
         * (rfc3041, ipv6.ip6-privacy) will be excluded from being
         * configured as endpoints.
         *
         * If "disabled" (0x1), MPTCP handling for the interface is disabled and
         * no endpoints are registered.
         *
         * The "enabled" (0x2) flag means that MPTCP handling is enabled.
         * This flag can also be implied from the presence of other flags.
         *
         * Even when enabled, MPTCP handling will by default still be disabled
         * unless "/proc/sys/net/mptcp/enabled" sysctl is on. NetworkManager
         * does not change the sysctl and this is up to the administrator
         * or distribution. To configure endpoints even if the sysctl is
         * disabled, "also-without-sysctl" (0x4) flag can be used. In that case,
         * NetworkManager doesn't look at the sysctl and configures endpoints
         * regardless.
         *
         * Even when enabled, NetworkManager will only configure MPTCP endpoints
         * for a certain address family, if there is a unicast default route (0.0.0.0/0
         * or ::/0) in the main routing table. The flag "also-without-default-route"
         * (0x8) can override that.
         *
         * When MPTCP handling is enabled then endpoints are configured with
         * the specified address flags "signal" (0x10), "subflow" (0x20), "backup" (0x40),
         * "fullmesh" (0x80). See ip-mptcp(8) manual for additional information about the flags.
         *
         * If the flags are zero (0x0), the global connection default from NetworkManager.conf is
         * honored. If still unspecified, the fallback is "enabled,subflow".
         * Note that this means that MPTCP is by default done depending on the
         * "/proc/sys/net/mptcp/enabled" sysctl.
         *
         * NetworkManager does not change the MPTCP limits nor enable MPTCP via
         * "/proc/sys/net/mptcp/enabled". That is a host configuration which the
         * admin can change via sysctl and ip-mptcp.
         *
         * Strict reverse path filtering (rp_filter) breaks many MPTCP use cases, so when
         * MPTCP handling for IPv4 addresses on the interface is enabled, NetworkManager would
         * loosen the strict reverse path filtering (1) to the loose setting (2).
         */
        get mptcp_flags(): number;
        set mptcp_flags(val: number);
        /**
         * Whether to configure MPTCP endpoints and the address flags.
         * If MPTCP is enabled in NetworkManager, it will configure the
         * addresses of the interface as MPTCP endpoints. Note that
         * IPv4 loopback addresses (127.0.0.0/8), IPv4 link local
         * addresses (169.254.0.0/16), the IPv6 loopback address (::1),
         * IPv6 link local addresses (fe80::/10), IPv6 unique
         * local addresses (ULA, fc00::/7) and IPv6 privacy extension addresses
         * (rfc3041, ipv6.ip6-privacy) will be excluded from being
         * configured as endpoints.
         *
         * If "disabled" (0x1), MPTCP handling for the interface is disabled and
         * no endpoints are registered.
         *
         * The "enabled" (0x2) flag means that MPTCP handling is enabled.
         * This flag can also be implied from the presence of other flags.
         *
         * Even when enabled, MPTCP handling will by default still be disabled
         * unless "/proc/sys/net/mptcp/enabled" sysctl is on. NetworkManager
         * does not change the sysctl and this is up to the administrator
         * or distribution. To configure endpoints even if the sysctl is
         * disabled, "also-without-sysctl" (0x4) flag can be used. In that case,
         * NetworkManager doesn't look at the sysctl and configures endpoints
         * regardless.
         *
         * Even when enabled, NetworkManager will only configure MPTCP endpoints
         * for a certain address family, if there is a unicast default route (0.0.0.0/0
         * or ::/0) in the main routing table. The flag "also-without-default-route"
         * (0x8) can override that.
         *
         * When MPTCP handling is enabled then endpoints are configured with
         * the specified address flags "signal" (0x10), "subflow" (0x20), "backup" (0x40),
         * "fullmesh" (0x80). See ip-mptcp(8) manual for additional information about the flags.
         *
         * If the flags are zero (0x0), the global connection default from NetworkManager.conf is
         * honored. If still unspecified, the fallback is "enabled,subflow".
         * Note that this means that MPTCP is by default done depending on the
         * "/proc/sys/net/mptcp/enabled" sysctl.
         *
         * NetworkManager does not change the MPTCP limits nor enable MPTCP via
         * "/proc/sys/net/mptcp/enabled". That is a host configuration which the
         * admin can change via sysctl and ip-mptcp.
         *
         * Strict reverse path filtering (rp_filter) breaks many MPTCP use cases, so when
         * MPTCP handling for IPv4 addresses on the interface is enabled, NetworkManager would
         * loosen the strict reverse path filtering (1) to the loose setting (2).
         */
        get mptcpFlags(): number;
        set mptcpFlags(val: number);
        /**
         * If configured, set to a Manufacturer Usage Description (MUD) URL that points
         * to manufacturer-recommended network policies for IoT devices. It is transmitted
         * as a DHCPv4 or DHCPv6 option. The value must be a valid URL starting with "https://".
         *
         * The special value "none" is allowed to indicate that no MUD URL is used.
         *
         * If the per-profile value is unspecified (the default), a global connection default gets
         * consulted. If still unspecified, the ultimate default is "none".
         */
        get mud_url(): string;
        set mud_url(val: string);
        /**
         * If configured, set to a Manufacturer Usage Description (MUD) URL that points
         * to manufacturer-recommended network policies for IoT devices. It is transmitted
         * as a DHCPv4 or DHCPv6 option. The value must be a valid URL starting with "https://".
         *
         * The special value "none" is allowed to indicate that no MUD URL is used.
         *
         * If the per-profile value is unspecified (the default), a global connection default gets
         * consulted. If still unspecified, the ultimate default is "none".
         */
        get mudUrl(): string;
        set mudUrl(val: string);
        /**
         * Specifies whether the profile can be active multiple times at a particular
         * moment. The value is of type #NMConnectionMultiConnect.
         */
        get multi_connect(): number;
        set multi_connect(val: number);
        /**
         * Specifies whether the profile can be active multiple times at a particular
         * moment. The value is of type #NMConnectionMultiConnect.
         */
        get multiConnect(): number;
        set multiConnect(val: number);
        /**
         * An array of strings defining what access a given user has to this
         * connection.  If this is %NULL or empty, all users are allowed to access
         * this connection; otherwise users are allowed if and only if they are in
         * this list.  When this is not empty, the connection can be active only when
         * one of the specified users is logged into an active session.  Each entry
         * is of the form "[type]:[id]:[reserved]"; for example, "user:dcbw:blah".
         *
         * At this time only the "user" [type] is allowed.  Any other values are
         * ignored and reserved for future use.  [id] is the username that this
         * permission refers to, which may not contain the ":" character. Any
         * [reserved] information present must be ignored and is reserved for future
         * use.  All of [type], [id], and [reserved] must be valid UTF-8.
         */
        get permissions(): string[];
        set permissions(val: string[]);
        /**
         * Setting name of the device type of this port's controller connection (eg,
         * %NM_SETTING_BOND_SETTING_NAME), or %NULL if this connection is not a
         * port.
         */
        get port_type(): string;
        set port_type(val: string);
        /**
         * Setting name of the device type of this port's controller connection (eg,
         * %NM_SETTING_BOND_SETTING_NAME), or %NULL if this connection is not a
         * port.
         */
        get portType(): string;
        set portType(val: string);
        /**
         * This property is deprecated and has no meaning.
         */
        get read_only(): boolean;
        set read_only(val: boolean);
        /**
         * This property is deprecated and has no meaning.
         */
        get readOnly(): boolean;
        set readOnly(val: boolean);
        /**
         * List of connection UUIDs that should be activated when the base
         * connection itself is activated. Currently, only VPN connections are
         * supported.
         */
        get secondaries(): string[];
        set secondaries(val: string[]);
        /**
         * Setting name of the device type of this port's controller connection (eg,
         * %NM_SETTING_BOND_SETTING_NAME), or %NULL if this connection is not a
         * port.
         *
         * Deprecated 1.46. Use #NMSettingConnection:port-type instead, this is just an alias.
         */
        get slave_type(): string;
        set slave_type(val: string);
        /**
         * Setting name of the device type of this port's controller connection (eg,
         * %NM_SETTING_BOND_SETTING_NAME), or %NULL if this connection is not a
         * port.
         *
         * Deprecated 1.46. Use #NMSettingConnection:port-type instead, this is just an alias.
         */
        get slaveType(): string;
        set slaveType(val: string);
        /**
         * This represents the identity of the connection used for various purposes.
         * It allows configuring multiple profiles to share the identity. Also,
         * the stable-id can contain placeholders that are substituted dynamically and
         * deterministically depending on the context.
         *
         * The stable-id is used for generating IPv6 stable private addresses with
         * ipv6.addr-gen-mode=stable-privacy. It is also used to seed the generated
         * cloned MAC address for ethernet.cloned-mac-address=stable and
         * wifi.cloned-mac-address=stable. It is also used to derive the DHCP
         * client identifier with ipv4.dhcp-client-id=stable, the DHCPv6 DUID with
         * ipv6.dhcp-duid=stable-[llt,ll,uuid] and the DHCP IAID with
         * ipv4.iaid=stable and ipv6.iaid=stable.
         *
         * Note that depending on the context where it is used, other parameters are
         * also seeded into the generation algorithm. For example, a per-host key
         * is commonly also included, so that different systems end up generating
         * different IDs. Or with ipv6.addr-gen-mode=stable-privacy, also the device's
         * name is included, so that different interfaces yield different addresses.
         * The per-host key is the identity of your machine and stored in /var/lib/NetworkManager/secret_key.
         * See NetworkManager(8) manual about the secret-key and the host identity.
         *
         * The '$' character is treated special to perform dynamic substitutions at
         * activation time. Currently, supported are "${CONNECTION}", "${DEVICE}",
         * "${MAC}", "${NETWORK_SSID}", "${BOOT}", "${RANDOM}".  These effectively
         * create unique IDs per-connection, per-device, per-SSID, per-boot, or
         * every time.  The "${CONNECTION}" uses the profile's connection.uuid, the
         * "${DEVICE}" uses the interface name of the device and "${MAC}" the
         * permanent MAC address of the device. "${NETWORK_SSID}" uses the SSID for
         * Wi-Fi networks and falls back to "${CONNECTION}" on other networks. Any
         * unrecognized patterns following '$' are treated verbatim, however are
         * reserved for future use. You are thus advised to avoid '$' or escape it
         * as "$$".  For example, set it to "${CONNECTION}-${BOOT}-${DEVICE}" to
         * create a unique id for this connection that changes with every reboot
         * and differs depending on the interface where the profile activates.
         *
         * If the value is unset, a global connection default is consulted. If the
         * value is still unset, the default is "default${CONNECTION}" go generate
         * an ID unique per connection profile.
         */
        get stable_id(): string;
        set stable_id(val: string);
        /**
         * This represents the identity of the connection used for various purposes.
         * It allows configuring multiple profiles to share the identity. Also,
         * the stable-id can contain placeholders that are substituted dynamically and
         * deterministically depending on the context.
         *
         * The stable-id is used for generating IPv6 stable private addresses with
         * ipv6.addr-gen-mode=stable-privacy. It is also used to seed the generated
         * cloned MAC address for ethernet.cloned-mac-address=stable and
         * wifi.cloned-mac-address=stable. It is also used to derive the DHCP
         * client identifier with ipv4.dhcp-client-id=stable, the DHCPv6 DUID with
         * ipv6.dhcp-duid=stable-[llt,ll,uuid] and the DHCP IAID with
         * ipv4.iaid=stable and ipv6.iaid=stable.
         *
         * Note that depending on the context where it is used, other parameters are
         * also seeded into the generation algorithm. For example, a per-host key
         * is commonly also included, so that different systems end up generating
         * different IDs. Or with ipv6.addr-gen-mode=stable-privacy, also the device's
         * name is included, so that different interfaces yield different addresses.
         * The per-host key is the identity of your machine and stored in /var/lib/NetworkManager/secret_key.
         * See NetworkManager(8) manual about the secret-key and the host identity.
         *
         * The '$' character is treated special to perform dynamic substitutions at
         * activation time. Currently, supported are "${CONNECTION}", "${DEVICE}",
         * "${MAC}", "${NETWORK_SSID}", "${BOOT}", "${RANDOM}".  These effectively
         * create unique IDs per-connection, per-device, per-SSID, per-boot, or
         * every time.  The "${CONNECTION}" uses the profile's connection.uuid, the
         * "${DEVICE}" uses the interface name of the device and "${MAC}" the
         * permanent MAC address of the device. "${NETWORK_SSID}" uses the SSID for
         * Wi-Fi networks and falls back to "${CONNECTION}" on other networks. Any
         * unrecognized patterns following '$' are treated verbatim, however are
         * reserved for future use. You are thus advised to avoid '$' or escape it
         * as "$$".  For example, set it to "${CONNECTION}-${BOOT}-${DEVICE}" to
         * create a unique id for this connection that changes with every reboot
         * and differs depending on the interface where the profile activates.
         *
         * If the value is unset, a global connection default is consulted. If the
         * value is still unset, the default is "default${CONNECTION}" go generate
         * an ID unique per connection profile.
         */
        get stableId(): string;
        set stableId(val: string);
        /**
         * The time, in seconds since the Unix Epoch, that the connection was last
         * _successfully_ fully activated.
         *
         * NetworkManager updates the connection timestamp periodically when the
         * connection is active to ensure that an active connection has the latest
         * timestamp. The property is only meant for reading (changes to this
         * property will not be preserved).
         */
        get timestamp(): number;
        set timestamp(val: number);
        /**
         * Base type of the connection. For hardware-dependent connections, should
         * contain the setting name of the hardware-type specific setting (ie,
         * "802-3-ethernet" or "802-11-wireless" or "bluetooth", etc), and for
         * non-hardware dependent connections like VPN or otherwise, should contain
         * the setting name of that setting type (ie, "vpn" or "bridge", etc).
         */
        get type(): string;
        set type(val: string);
        /**
         * A universally unique identifier for the connection, for example generated
         * with libuuid.  It should be assigned when the connection is created, and
         * never changed as long as the connection still applies to the same
         * network.  For example, it should not be changed when the
         * #NMSettingConnection:id property or #NMSettingIP4Config changes, but
         * might need to be re-created when the Wi-Fi SSID, mobile broadband network
         * provider, or #NMSettingConnection:type property changes.
         *
         * The UUID must be in the format "2815492f-7e56-435e-b2e9-246bd7cdc664"
         * (ie, contains only hexadecimal characters and "-").  A suitable UUID may
         * be generated by nm_utils_uuid_generate() or
         * nm_uuid_generate_from_string_str().
         */
        get uuid(): string;
        set uuid(val: string);
        /**
         * Time in milliseconds to wait for connection to be considered activated.
         * The wait will start after the pre-up dispatcher event.
         *
         * The value 0 means no wait time. The default value is -1, which
         * currently has the same meaning as no wait time.
         */
        get wait_activation_delay(): number;
        set wait_activation_delay(val: number);
        /**
         * Time in milliseconds to wait for connection to be considered activated.
         * The wait will start after the pre-up dispatcher event.
         *
         * The value 0 means no wait time. The default value is -1, which
         * currently has the same meaning as no wait time.
         */
        get waitActivationDelay(): number;
        set waitActivationDelay(val: number);
        /**
         * Timeout in milliseconds to wait for device at startup.
         * During boot, devices may take a while to be detected by the driver.
         * This property will cause to delay NetworkManager-wait-online.service
         * and nm-online to give the device a chance to appear. This works by
         * waiting for the given timeout until a compatible device for the
         * profile is available and managed.
         *
         * The value 0 means no wait time. The default value is -1, which
         * currently has the same meaning as no wait time.
         */
        get wait_device_timeout(): number;
        set wait_device_timeout(val: number);
        /**
         * Timeout in milliseconds to wait for device at startup.
         * During boot, devices may take a while to be detected by the driver.
         * This property will cause to delay NetworkManager-wait-online.service
         * and nm-online to give the device a chance to appear. This works by
         * waiting for the given timeout until a compatible device for the
         * profile is available and managed.
         *
         * The value 0 means no wait time. The default value is -1, which
         * currently has the same meaning as no wait time.
         */
        get waitDeviceTimeout(): number;
        set waitDeviceTimeout(val: number);
        /**
         * The trust level of a the connection.  Free form case-insensitive string
         * (for example "Home", "Work", "Public").  %NULL or unspecified zone means
         * the connection will be placed in the default zone as defined by the
         * firewall.
         *
         * When updating this property on a currently activated connection,
         * the change takes effect immediately.
         */
        get zone(): string;
        set zone(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingConnection.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingConnection.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingConnection;
        // Signals
        connect<K extends keyof NM.SettingConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingConnection.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingConnection.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingConnection.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingConnection.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a new IP address string to the ip-ping-addresses.
         *
         * @returns %TRUE if the new IP address was added; %FALSE if the IP address
         * was already present
         * @param address the IP address string to add
         */
        add_ip_ping_address(address: string): boolean;
        /**
         * Adds a permission to the connection's permission list.  At this time, only
         * the "user" permission type is supported, and `pitem` must be a username. See
         * #NMSettingConnection:permissions: for more details.
         *
         * @returns %TRUE if the permission was unique and was successfully added to the
         * list, %FALSE if `ptype` or `pitem` was invalid.
         * If the permission was already present in the list, it will not be added
         * a second time but %TRUE will be returned. Note that before 1.28, in this
         * case %FALSE would be returned.
         * @param ptype the permission type; at this time only "user" is supported
         * @param pitem the permission item formatted as required for `ptype`
         * @param detail unused at this time; must be %NULL
         */
        add_permission(ptype: string, pitem: string, detail?: string | null): boolean;
        /**
         * Adds a new secondary connection UUID to the setting.
         *
         * @returns %TRUE if the secondary connection UUID was added; %FALSE if the UUID
         * was already present
         * @param sec_uuid the secondary connection UUID to add
         */
        add_secondary(sec_uuid: string): boolean;
        /**
         * Removes all configured ip-ping-addresses.
         */
        clear_ip_ping_addresses(): void;
        /**
         * Returns the value contained in the #NMSettingConnection:auth-retries property.
         *
         * @returns the configured authentication retries. Zero means
         * infinity and -1 means a global default value.
         */
        get_auth_retries(): number;
        /**
         * Returns the #NMSettingConnection:autoconnect property of the connection.
         *
         * @returns the connection's autoconnect behavior
         */
        get_autoconnect(): boolean;
        /**
         * Returns the #NMSettingConnection:autoconnect-ports property of the connection.
         *
         * @returns whether ports of the connection should be activated together
         *          with the connection.
         */
        get_autoconnect_ports(): NM.Ternary;
        /**
         * Returns the #NMSettingConnection:autoconnect-priority property of the connection.
         * The higher number, the higher priority.
         *
         * @returns the connection's autoconnect priority
         */
        get_autoconnect_priority(): number;
        /**
         * Returns the #NMSettingConnection:autoconnect-retries property of the connection.
         * Zero means infinite, -1 means the global default value.
         *
         * @returns the connection's autoconnect retries
         */
        get_autoconnect_retries(): number;
        /**
         * Returns the #NMSettingConnection:autoconnect-slaves property of the connection.
         *
         * @returns whether ports of the connection should be activated together
         *          with the connection.
         */
        get_autoconnect_slaves(): NM.SettingConnectionAutoconnectSlaves;
        /**
         * Returns the #NMSettingConnection:type property of the connection.
         *
         * @returns the connection type
         */
        get_connection_type(): string;
        /**
         * Returns the #NMSettingConnection:controller property of the connection.
         *
         * @returns interface name of the controller device or UUID of the controller
         * connection.
         */
        get_controller(): string;
        get_dns_over_tls(): NM.SettingConnectionDnsOverTls;
        /**
         * Returns the %NM_SETTING_CONNECTION_DOWN_ON_POWEROFF property.
         *
         * @returns whether the connection will be brought down before the system
         * is powered off.
         */
        get_down_on_poweroff(): NM.SettingConnectionDownOnPoweroff;
        get_gateway_ping_timeout(): number;
        /**
         * Returns the #NMSettingConnection:id property of the connection.
         *
         * @returns the connection ID
         */
        get_id(): string;
        /**
         * Returns the #NMSettingConnection:interface-name property of the connection.
         *
         * @returns the connection's interface name
         */
        get_interface_name(): string;
        get_ip_ping_address(idx: number): string;
        /**
         * Returns the #NMSettingConnection:ip-ping-addresses-require-all property of the connection.
         *
         * @returns whether all the ip ping addresses pass the connectivity check.
         */
        get_ip_ping_addresses_require_all(): NM.Ternary;
        get_ip_ping_timeout(): number;
        /**
         * Returns the #NMSettingConnection:lldp property of the connection.
         *
         * @returns a %NMSettingConnectionLldp which indicates whether LLDP must be
         * enabled for the connection.
         */
        get_lldp(): NM.SettingConnectionLldp;
        get_llmnr(): NM.SettingConnectionLlmnr;
        /**
         * Returns the #NMSettingConnection:master property of the connection.
         *
         * @returns interface name of the controller device or UUID of the controller
         * connection.
         */
        get_master(): string;
        get_mdns(): NM.SettingConnectionMdns;
        get_metered(): NM.Metered;
        get_mptcp_flags(): NM.MptcpFlags;
        /**
         * Returns the value contained in the #NMSettingConnection:mud-url
         * property.
         */
        get_mud_url(): string;
        get_multi_connect(): NM.ConnectionMultiConnect;
        /**
         * Returns the number of entries in the #NMSettingConnection:permissions
         * property of this setting.
         *
         * @returns the number of permissions entries
         */
        get_num_permissions(): number;
        get_num_secondaries(): number;
        /**
         * Retrieve one of the entries of the #NMSettingConnection:permissions property
         * of this setting.
         *
         * @returns %TRUE if a permission was returned, %FALSE if `idx` was invalid
         * @param idx the zero-based index of the permissions entry
         * @param out_ptype on return, the permission type. This is currently always "user",
         *   unless the entry is invalid, in which case it returns "invalid".
         * @param out_pitem on return, the permission item (formatted according to `ptype,` see
         * #NMSettingConnection:permissions for more detail
         * @param out_detail on return, the permission detail (at this time, always %NULL)
         */
        get_permission(idx: number, out_ptype: string, out_pitem: string, out_detail: string): boolean;
        /**
         * Returns the #NMSettingConnection:port-type property of the connection.
         *
         * @returns the type of port this connection is, if any.
         */
        get_port_type(): string;
        /**
         * Returns the #NMSettingConnection:read-only property of the connection.
         *
         * @returns %TRUE if the connection is read-only, %FALSE if it is not
         */
        get_read_only(): boolean;
        get_secondary(idx: number): string;
        /**
         * Returns the #NMSettingConnection:slave-type property of the connection.
         *
         * @returns the type of port this connection is, if any
         */
        get_slave_type(): string;
        /**
         * Returns the #NMSettingConnection:stable_id property of the connection.
         *
         * @returns the stable-id for the connection
         */
        get_stable_id(): string;
        /**
         * Returns the #NMSettingConnection:timestamp property of the connection.
         *
         * @returns the connection's timestamp
         */
        get_timestamp(): number;
        /**
         * Returns the #NMSettingConnection:uuid property of the connection.
         *
         * @returns the connection UUID
         */
        get_uuid(): string;
        get_wait_activation_delay(): number;
        get_wait_device_timeout(): number;
        /**
         * Returns the #NMSettingConnection:zone property of the connection.
         *
         * @returns the trust level of a connection
         */
        get_zone(): string;
        is_slave_type(type: string): boolean;
        /**
         * Checks whether the given username is allowed to view/access this connection.
         *
         * @returns %TRUE if the requested user is allowed to view this connection,
         * %FALSE if the given user is not allowed to view this connection
         * @param uname the user name to check permissions for
         */
        permissions_user_allowed(uname: string): boolean;
        /**
         * Removes the IP address at index `idx`.
         *
         * @param idx index number of the IP address
         */
        remove_ip_ping_address(idx: number): void;
        /**
         * Removes the IP address `address` from ip-ping-addresses.
         *
         * @returns %TRUE if the IP address was found and removed; %FALSE if it was not.
         * @param address the IP address to remove
         */
        remove_ip_ping_address_by_value(address: string): boolean;
        /**
         * Removes the permission at index `idx` from the connection.
         *
         * @param idx the zero-based index of the permission to remove
         */
        remove_permission(idx: number): void;
        /**
         * Removes the permission from the connection.
         * At this time, only the "user" permission type is supported, and `pitem` must
         * be a username. See #NMSettingConnection:permissions: for more details.
         *
         * @returns %TRUE if the permission was found and removed; %FALSE if it was not.
         * @param ptype the permission type; at this time only "user" is supported
         * @param pitem the permission item formatted as required for `ptype`
         * @param detail unused at this time; must be %NULL
         */
        remove_permission_by_value(ptype: string, pitem: string, detail?: string | null): boolean;
        /**
         * Removes the secondary connection UUID at index `idx`.
         *
         * @param idx index number of the secondary connection UUID
         */
        remove_secondary(idx: number): void;
        /**
         * Removes the secondary connection UUID `sec_uuid`.
         *
         * @returns %TRUE if the secondary connection UUID was found and removed; %FALSE if it was not.
         * @param sec_uuid the secondary connection UUID to remove
         */
        remove_secondary_by_value(sec_uuid: string): boolean;
    }
    class SettingDcb extends Setting {
        static '$gtype': GObject.GType<SettingDcb>;
        // Properties
        /**
         * Specifies the #NMSettingDcbFlags for the DCB FCoE application.  Flags may
         * be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get app_fcoe_flags(): NM.SettingDcbFlags;
        set app_fcoe_flags(val: NM.SettingDcbFlags);
        /**
         * Specifies the #NMSettingDcbFlags for the DCB FCoE application.  Flags may
         * be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get appFcoeFlags(): NM.SettingDcbFlags;
        set appFcoeFlags(val: NM.SettingDcbFlags);
        /**
         * The FCoE controller mode; either %NM_SETTING_DCB_FCOE_MODE_FABRIC
         * or %NM_SETTING_DCB_FCOE_MODE_VN2VN.
         *
         * Since 1.34, %NULL is the default and means %NM_SETTING_DCB_FCOE_MODE_FABRIC.
         * Before 1.34, %NULL was rejected as invalid and the default was %NM_SETTING_DCB_FCOE_MODE_FABRIC.
         */
        get app_fcoe_mode(): string;
        set app_fcoe_mode(val: string);
        /**
         * The FCoE controller mode; either %NM_SETTING_DCB_FCOE_MODE_FABRIC
         * or %NM_SETTING_DCB_FCOE_MODE_VN2VN.
         *
         * Since 1.34, %NULL is the default and means %NM_SETTING_DCB_FCOE_MODE_FABRIC.
         * Before 1.34, %NULL was rejected as invalid and the default was %NM_SETTING_DCB_FCOE_MODE_FABRIC.
         */
        get appFcoeMode(): string;
        set appFcoeMode(val: string);
        /**
         * The highest User Priority (0 - 7) which FCoE frames should use, or -1 for
         * default priority.  Only used when the #NMSettingDcb:app-fcoe-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get app_fcoe_priority(): number;
        set app_fcoe_priority(val: number);
        /**
         * The highest User Priority (0 - 7) which FCoE frames should use, or -1 for
         * default priority.  Only used when the #NMSettingDcb:app-fcoe-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get appFcoePriority(): number;
        set appFcoePriority(val: number);
        /**
         * Specifies the #NMSettingDcbFlags for the DCB FIP application.  Flags may
         * be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get app_fip_flags(): NM.SettingDcbFlags;
        set app_fip_flags(val: NM.SettingDcbFlags);
        /**
         * Specifies the #NMSettingDcbFlags for the DCB FIP application.  Flags may
         * be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get appFipFlags(): NM.SettingDcbFlags;
        set appFipFlags(val: NM.SettingDcbFlags);
        /**
         * The highest User Priority (0 - 7) which FIP frames should use, or -1 for
         * default priority.  Only used when the #NMSettingDcb:app-fip-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get app_fip_priority(): number;
        set app_fip_priority(val: number);
        /**
         * The highest User Priority (0 - 7) which FIP frames should use, or -1 for
         * default priority.  Only used when the #NMSettingDcb:app-fip-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get appFipPriority(): number;
        set appFipPriority(val: number);
        /**
         * Specifies the #NMSettingDcbFlags for the DCB iSCSI application.  Flags
         * may be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get app_iscsi_flags(): NM.SettingDcbFlags;
        set app_iscsi_flags(val: NM.SettingDcbFlags);
        /**
         * Specifies the #NMSettingDcbFlags for the DCB iSCSI application.  Flags
         * may be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get appIscsiFlags(): NM.SettingDcbFlags;
        set appIscsiFlags(val: NM.SettingDcbFlags);
        /**
         * The highest User Priority (0 - 7) which iSCSI frames should use, or -1
         * for default priority. Only used when the #NMSettingDcb:app-iscsi-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get app_iscsi_priority(): number;
        set app_iscsi_priority(val: number);
        /**
         * The highest User Priority (0 - 7) which iSCSI frames should use, or -1
         * for default priority. Only used when the #NMSettingDcb:app-iscsi-flags
         * property includes the %NM_SETTING_DCB_FLAG_ENABLE flag.
         */
        get appIscsiPriority(): number;
        set appIscsiPriority(val: number);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the percentage of bandwidth of
         * the priority's assigned group that the priority may use.  The sum of all
         * percentages for priorities which belong to the same group must total 100
         * percents.
         */
        get priority_bandwidth(): number[];
        set priority_bandwidth(val: number[]);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the percentage of bandwidth of
         * the priority's assigned group that the priority may use.  The sum of all
         * percentages for priorities which belong to the same group must total 100
         * percents.
         */
        get priorityBandwidth(): number[];
        set priorityBandwidth(val: number[]);
        /**
         * An array of 8 boolean values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates whether or not the corresponding
         * priority should transmit priority pause.
         */
        get priority_flow_control(): boolean[];
        set priority_flow_control(val: boolean[]);
        /**
         * An array of 8 boolean values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates whether or not the corresponding
         * priority should transmit priority pause.
         */
        get priorityFlowControl(): boolean[];
        set priorityFlowControl(val: boolean[]);
        /**
         * Specifies the #NMSettingDcbFlags for DCB Priority Flow Control (PFC).
         * Flags may be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get priority_flow_control_flags(): NM.SettingDcbFlags;
        set priority_flow_control_flags(val: NM.SettingDcbFlags);
        /**
         * Specifies the #NMSettingDcbFlags for DCB Priority Flow Control (PFC).
         * Flags may be any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get priorityFlowControlFlags(): NM.SettingDcbFlags;
        set priorityFlowControlFlags(val: NM.SettingDcbFlags);
        /**
         * An array of 8 uint values, where the array index corresponds to the
         * Priority Group ID (0 - 7) and the value indicates the percentage of link
         * bandwidth allocated to that group.  Allowed values are 0 - 100, and the
         * sum of all values must total 100 percents.
         */
        get priority_group_bandwidth(): number[];
        set priority_group_bandwidth(val: number[]);
        /**
         * An array of 8 uint values, where the array index corresponds to the
         * Priority Group ID (0 - 7) and the value indicates the percentage of link
         * bandwidth allocated to that group.  Allowed values are 0 - 100, and the
         * sum of all values must total 100 percents.
         */
        get priorityGroupBandwidth(): number[];
        set priorityGroupBandwidth(val: number[]);
        /**
         * Specifies the #NMSettingDcbFlags for DCB Priority Groups.  Flags may be
         * any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get priority_group_flags(): NM.SettingDcbFlags;
        set priority_group_flags(val: NM.SettingDcbFlags);
        /**
         * Specifies the #NMSettingDcbFlags for DCB Priority Groups.  Flags may be
         * any combination of %NM_SETTING_DCB_FLAG_ENABLE,
         * %NM_SETTING_DCB_FLAG_ADVERTISE, and %NM_SETTING_DCB_FLAG_WILLING.
         */
        get priorityGroupFlags(): NM.SettingDcbFlags;
        set priorityGroupFlags(val: NM.SettingDcbFlags);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the Priority Group ID.  Allowed
         * Priority Group ID values are 0 - 7 or 15 for the unrestricted group.
         */
        get priority_group_id(): number[];
        set priority_group_id(val: number[]);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the Priority Group ID.  Allowed
         * Priority Group ID values are 0 - 7 or 15 for the unrestricted group.
         */
        get priorityGroupId(): number[];
        set priorityGroupId(val: number[]);
        /**
         * An array of 8 boolean values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates whether or not the priority may
         * use all of the bandwidth allocated to its assigned group.
         */
        get priority_strict_bandwidth(): boolean[];
        set priority_strict_bandwidth(val: boolean[]);
        /**
         * An array of 8 boolean values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates whether or not the priority may
         * use all of the bandwidth allocated to its assigned group.
         */
        get priorityStrictBandwidth(): boolean[];
        set priorityStrictBandwidth(val: boolean[]);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the traffic class (0 - 7) to
         * which the priority is mapped.
         */
        get priority_traffic_class(): number[];
        set priority_traffic_class(val: number[]);
        /**
         * An array of 8 uint values, where the array index corresponds to the User
         * Priority (0 - 7) and the value indicates the traffic class (0 - 7) to
         * which the priority is mapped.
         */
        get priorityTrafficClass(): number[];
        set priorityTrafficClass(val: number[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingDcb.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingDcb.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingDcb;
        // Signals
        connect<K extends keyof NM.SettingDcb.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingDcb.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingDcb.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingDcb.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingDcb.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingDcb.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_app_fcoe_flags(): NM.SettingDcbFlags;
        get_app_fcoe_mode(): string;
        get_app_fcoe_priority(): number;
        get_app_fip_flags(): NM.SettingDcbFlags;
        get_app_fip_priority(): number;
        get_app_iscsi_flags(): NM.SettingDcbFlags;
        get_app_iscsi_priority(): number;
        get_priority_bandwidth(user_priority: number): number;
        get_priority_flow_control(user_priority: number): boolean;
        get_priority_flow_control_flags(): NM.SettingDcbFlags;
        get_priority_group_bandwidth(group_id: number): number;
        get_priority_group_flags(): NM.SettingDcbFlags;
        get_priority_group_id(user_priority: number): number;
        get_priority_strict_bandwidth(user_priority: number): boolean;
        get_priority_traffic_class(user_priority: number): number;
        /**
         * These values are only valid when #NMSettingDcb:priority-group-flags includes
         * the %NM_SETTING_DCB_FLAG_ENABLE flag.
         *
         * @param user_priority the User Priority (0 - 7) to set the bandwidth percentage for
         * @param bandwidth_percent the bandwidth percentage (0 - 100) that `user_priority` is
         * allowed to use within its priority group
         */
        set_priority_bandwidth(user_priority: number, bandwidth_percent: number): void;
        /**
         * These values are only valid when #NMSettingDcb:priority-flow-control includes
         * the %NM_SETTING_DCB_FLAG_ENABLE flag.
         *
         * @param user_priority the User Priority (0 - 7) to set flow control for
         * @param enabled %TRUE to enable flow control for this priority, %FALSE to disable it
         */
        set_priority_flow_control(user_priority: number, enabled: boolean): void;
        /**
         * These values are only valid when #NMSettingDcb:priority-group-flags includes
         * the %NM_SETTING_DCB_FLAG_ENABLE flag.
         *
         * @param group_id the priority group (0 - 7) to set the bandwidth percentage for
         * @param bandwidth_percent the bandwidth percentage (0 - 100) to assign to `group_id` to
         */
        set_priority_group_bandwidth(group_id: number, bandwidth_percent: number): void;
        /**
         * These values are only valid when #NMSettingDcb:priority-group-flags includes
         * the %NM_SETTING_DCB_FLAG_ENABLE flag.
         *
         * @param user_priority the User Priority (0 - 7) to set flow control for
         * @param group_id the group (0 - 7) to assign `user_priority` to, or 15 for the
         * unrestricted group.
         */
        set_priority_group_id(user_priority: number, group_id: number): void;
        /**
         * These values are only valid when #NMSettingDcb:priority-group-flags includes
         * the %NM_SETTING_DCB_FLAG_ENABLE flag.
         *
         * @param user_priority the User Priority (0 - 7) to set strict bandwidth for
         * @param strict %TRUE to allow `user_priority` to use all the bandwidth allocated to
         * its priority group, or %FALSE if not
         */
        set_priority_strict_bandwidth(user_priority: number, strict: boolean): void;
        set_priority_traffic_class(user_priority: number, traffic_class: number): void;
    }
    class SettingDummy extends Setting {
        static '$gtype': GObject.GType<SettingDummy>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingDummy.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingDummy.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingDummy;
        // Signals
        connect<K extends keyof NM.SettingDummy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingDummy.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingDummy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingDummy.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingDummy.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingDummy.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
    }
    class SettingEthtool extends Setting {
        static '$gtype': GObject.GType<SettingEthtool>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingEthtool.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingEthtool.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingEthtool;
        // Signals
        connect<K extends keyof NM.SettingEthtool.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingEthtool.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingEthtool.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingEthtool.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingEthtool.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingEthtool.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Clears all offload features settings
         */
        clear_features(): void;
        /**
         * Gets and offload feature setting. Returns %NM_TERNARY_DEFAULT if the
         * feature is not set.
         *
         * Note that `optname` must be a valid name for a feature, according to
         * nm_ethtool_optname_is_feature().
         *
         * @returns a #NMTernary value indicating whether the offload feature
         *   is enabled, disabled, or left untouched.
         * @param optname option name of the offload feature to get
         */
        get_feature(optname: string): NM.Ternary;
        /**
         * This returns all options names that are set. This includes the feature names
         * like %NM_ETHTOOL_OPTNAME_FEATURE_GRO. See nm_ethtool_optname_is_feature() to
         * check whether the option name is valid for offload features.
         *
         * @returns list of set option
         *   names or %NULL if no options are set. The option names are still owned by
         *   `setting` and may get invalidated when `setting` gets modified.
         */
        get_optnames(): [string[], number];
        /**
         * Sets and offload feature setting.
         *
         * Note that `optname` must be a valid name for a feature, according to
         * nm_ethtool_optname_is_feature().
         *
         * @param optname option name of the offload feature to get
         * @param value the new value to set. The special value %NM_TERNARY_DEFAULT
         *   means to clear the offload feature setting.
         */
        set_feature(optname: string, value: NM.Ternary | null): void;
    }
    class SettingGeneric extends Setting {
        static '$gtype': GObject.GType<SettingGeneric>;
        // Properties
        /**
         * Name of the device handler that will be invoked to add and delete
         * the device for this connection. The name can only contain ASCII
         * alphanumeric characters and '-', '_', '.'. It cannot start with '.'.
         *
         * See the NetworkManager-dispatcher(8) man page for more details
         * about how to write the device handler.
         *
         * By setting this property the generic connection becomes "virtual",
         * meaning that it can be activated without an existing device; the device
         * will be created at the time the connection is started by invoking the
         * device-handler.
         */
        get device_handler(): string;
        set device_handler(val: string);
        /**
         * Name of the device handler that will be invoked to add and delete
         * the device for this connection. The name can only contain ASCII
         * alphanumeric characters and '-', '_', '.'. It cannot start with '.'.
         *
         * See the NetworkManager-dispatcher(8) man page for more details
         * about how to write the device handler.
         *
         * By setting this property the generic connection becomes "virtual",
         * meaning that it can be activated without an existing device; the device
         * will be created at the time the connection is started by invoking the
         * device-handler.
         */
        get deviceHandler(): string;
        set deviceHandler(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingGeneric.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingGeneric.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingGeneric;
        // Signals
        connect<K extends keyof NM.SettingGeneric.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingGeneric.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingGeneric.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingGeneric.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingGeneric.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingGeneric.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the #NMSettingGeneric:device-handler property of the connection.
         *
         * @returns the device handler name, or %NULL if no device handler is set
         */
        get_device_handler(): string;
    }
    class SettingGsm extends Setting {
        static '$gtype': GObject.GType<SettingGsm>;
        // Properties
        /**
         * The GPRS Access Point Name specifying the APN used when establishing a
         * data session with the GSM-based network.  The APN often determines how
         * the user will be billed for their network usage and whether the user has
         * access to the Internet or just a provider-specific walled-garden, so it
         * is important to use the correct APN for the user's mobile broadband plan.
         * The APN may only be composed of the characters a-z, 0-9, ., and - per GSM
         * 03.60 Section 14.9.
         *
         * If the APN is unset (the default) then it may be detected based on
         * "auto-config" setting. The property can be explicitly set to the
         * empty string to prevent that and use no APN.
         */
        get apn(): string;
        set apn(val: string);
        /**
         * When %TRUE, the settings such as APN, username, or password will
         * default to values that match the network the modem will register
         * to in the Mobile Broadband Provider database.
         */
        get auto_config(): boolean;
        set auto_config(val: boolean);
        /**
         * When %TRUE, the settings such as APN, username, or password will
         * default to values that match the network the modem will register
         * to in the Mobile Broadband Provider database.
         */
        get autoConfig(): boolean;
        set autoConfig(val: boolean);
        /**
         * The device unique identifier (as given by the WWAN management service)
         * which this connection applies to.  If given, the connection will only
         * apply to the specified device.
         */
        get device_id(): string;
        set device_id(val: string);
        /**
         * The device unique identifier (as given by the WWAN management service)
         * which this connection applies to.  If given, the connection will only
         * apply to the specified device.
         */
        get deviceId(): string;
        set deviceId(val: string);
        /**
         * When %TRUE, only connections to the home network will be allowed.
         * Connections to roaming networks will not be made.
         */
        get home_only(): boolean;
        set home_only(val: boolean);
        /**
         * When %TRUE, only connections to the home network will be allowed.
         * Connections to roaming networks will not be made.
         */
        get homeOnly(): boolean;
        set homeOnly(val: boolean);
        /**
         * For LTE modems, this sets the APN for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initial_eps_bearer_apn(): string;
        set initial_eps_bearer_apn(val: string);
        /**
         * For LTE modems, this sets the APN for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initialEpsBearerApn(): string;
        set initialEpsBearerApn(val: string);
        /**
         * For LTE modems, this setting determines whether the initial EPS bearer
         * shall be configured when bringing up the connection.  It is inferred TRUE
         * if initial-eps-bearer-apn is set.
         */
        get initial_eps_bearer_configure(): boolean;
        set initial_eps_bearer_configure(val: boolean);
        /**
         * For LTE modems, this setting determines whether the initial EPS bearer
         * shall be configured when bringing up the connection.  It is inferred TRUE
         * if initial-eps-bearer-apn is set.
         */
        get initialEpsBearerConfigure(): boolean;
        set initialEpsBearerConfigure(val: boolean);
        /**
         * For LTE modems, this sets NOAUTH authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         * If %TRUE, do not require the other side to authenticate itself to the client.
         * If %FALSE, require authentication from the remote side.  In almost all cases,
         * this should be %TRUE.
         */
        get initial_eps_bearer_noauth(): boolean;
        set initial_eps_bearer_noauth(val: boolean);
        /**
         * For LTE modems, this sets NOAUTH authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         * If %TRUE, do not require the other side to authenticate itself to the client.
         * If %FALSE, require authentication from the remote side.  In almost all cases,
         * this should be %TRUE.
         */
        get initialEpsBearerNoauth(): boolean;
        set initialEpsBearerNoauth(val: boolean);
        /**
         * For LTE modems, this sets the password for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initial_eps_bearer_password(): string;
        set initial_eps_bearer_password(val: string);
        /**
         * For LTE modems, this sets the password for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initialEpsBearerPassword(): string;
        set initialEpsBearerPassword(val: string);
        /**
         * Flags indicating how to handle the #NMSettingGsm:initial-eps-bearer-password property.
         */
        get initial_eps_bearer_password_flags(): NM.SettingSecretFlags;
        set initial_eps_bearer_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingGsm:initial-eps-bearer-password property.
         */
        get initialEpsBearerPasswordFlags(): NM.SettingSecretFlags;
        set initialEpsBearerPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * For LTE modems, this disables CHAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initial_eps_bearer_refuse_chap(): boolean;
        set initial_eps_bearer_refuse_chap(val: boolean);
        /**
         * For LTE modems, this disables CHAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initialEpsBearerRefuseChap(): boolean;
        set initialEpsBearerRefuseChap(val: boolean);
        /**
         * For LTE modems, this disables EAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initial_eps_bearer_refuse_eap(): boolean;
        set initial_eps_bearer_refuse_eap(val: boolean);
        /**
         * For LTE modems, this disables EAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initialEpsBearerRefuseEap(): boolean;
        set initialEpsBearerRefuseEap(val: boolean);
        /**
         * For LTE modems, this disables MSCHAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initial_eps_bearer_refuse_mschap(): boolean;
        set initial_eps_bearer_refuse_mschap(val: boolean);
        /**
         * For LTE modems, this disables MSCHAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initialEpsBearerRefuseMschap(): boolean;
        set initialEpsBearerRefuseMschap(val: boolean);
        /**
         * For LTE modems, this disables MSCHAPV2 authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initial_eps_bearer_refuse_mschapv2(): boolean;
        set initial_eps_bearer_refuse_mschapv2(val: boolean);
        /**
         * For LTE modems, this disables MSCHAPV2 authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initialEpsBearerRefuseMschapv2(): boolean;
        set initialEpsBearerRefuseMschapv2(val: boolean);
        /**
         * For LTE modems, this disables PAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initial_eps_bearer_refuse_pap(): boolean;
        set initial_eps_bearer_refuse_pap(val: boolean);
        /**
         * For LTE modems, this disables PAP authentication method for the initial EPS bearer that is set
         * up when attaching to the network.
         */
        get initialEpsBearerRefusePap(): boolean;
        set initialEpsBearerRefusePap(val: boolean);
        /**
         * For LTE modems, this sets the username for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initial_eps_bearer_username(): string;
        set initial_eps_bearer_username(val: string);
        /**
         * For LTE modems, this sets the username for the initial EPS bearer that is set
         * up when attaching to the network.  Setting this parameter implies
         * initial-eps-bearer-configure to be TRUE.
         */
        get initialEpsBearerUsername(): string;
        set initialEpsBearerUsername(val: string);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * The Network ID (GSM LAI format, ie MCC-MNC) to force specific network
         * registration.  If the Network ID is specified, NetworkManager will
         * attempt to force the device to register only on the specified network.
         * This can be used to ensure that the device does not roam when direct
         * roaming control of the device is not otherwise possible.
         */
        get network_id(): string;
        set network_id(val: string);
        /**
         * The Network ID (GSM LAI format, ie MCC-MNC) to force specific network
         * registration.  If the Network ID is specified, NetworkManager will
         * attempt to force the device to register only on the specified network.
         * This can be used to ensure that the device does not roam when direct
         * roaming control of the device is not otherwise possible.
         */
        get networkId(): string;
        set networkId(val: string);
        /**
         * Legacy setting that used to help establishing PPP data sessions for
         * GSM-based modems.
         */
        get number(): string;
        set number(val: string);
        /**
         * The password used to authenticate with the network, if required.  Many
         * providers do not require a password, or accept any password.  But if a
         * password is required, it is specified here.
         */
        get password(): string;
        set password(val: string);
        /**
         * Flags indicating how to handle the #NMSettingGsm:password property.
         */
        get password_flags(): NM.SettingSecretFlags;
        set password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingGsm:password property.
         */
        get passwordFlags(): NM.SettingSecretFlags;
        set passwordFlags(val: NM.SettingSecretFlags);
        /**
         * If the SIM is locked with a PIN it must be unlocked before any other
         * operations are requested.  Specify the PIN here to allow operation of the
         * device.
         */
        get pin(): string;
        set pin(val: string);
        /**
         * Flags indicating how to handle the #NMSettingGsm:pin property.
         */
        get pin_flags(): NM.SettingSecretFlags;
        set pin_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingGsm:pin property.
         */
        get pinFlags(): NM.SettingSecretFlags;
        set pinFlags(val: NM.SettingSecretFlags);
        /**
         * The SIM card unique identifier (as given by the WWAN management service)
         * which this connection applies to.  If given, the connection will apply
         * to any device also allowed by #NMSettingGsm:device-id which contains a
         * SIM card matching the given identifier.
         */
        get sim_id(): string;
        set sim_id(val: string);
        /**
         * The SIM card unique identifier (as given by the WWAN management service)
         * which this connection applies to.  If given, the connection will apply
         * to any device also allowed by #NMSettingGsm:device-id which contains a
         * SIM card matching the given identifier.
         */
        get simId(): string;
        set simId(val: string);
        /**
         * A MCC/MNC string like "310260" or "21601" identifying the specific
         * mobile network operator which this connection applies to.  If given,
         * the connection will apply to any device also allowed by
         * #NMSettingGsm:device-id and #NMSettingGsm:sim-id which contains a SIM
         * card provisioned by the given operator.
         */
        get sim_operator_id(): string;
        set sim_operator_id(val: string);
        /**
         * A MCC/MNC string like "310260" or "21601" identifying the specific
         * mobile network operator which this connection applies to.  If given,
         * the connection will apply to any device also allowed by
         * #NMSettingGsm:device-id and #NMSettingGsm:sim-id which contains a SIM
         * card provisioned by the given operator.
         */
        get simOperatorId(): string;
        set simOperatorId(val: string);
        /**
         * The username used to authenticate with the network, if required.  Many
         * providers do not require a username, or accept any username.  But if a
         * username is required, it is specified here.
         */
        get username(): string;
        set username(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingGsm.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingGsm.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingGsm;
        // Signals
        connect<K extends keyof NM.SettingGsm.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingGsm.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingGsm.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingGsm.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingGsm.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingGsm.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_apn(): string;
        get_auto_config(): boolean;
        get_device_id(): string;
        get_home_only(): boolean;
        get_initial_eps_apn(): string;
        get_initial_eps_config(): boolean;
        get_initial_eps_noauth(): boolean;
        get_initial_eps_password(): string;
        get_initial_eps_refuse_chap(): boolean;
        get_initial_eps_refuse_eap(): boolean;
        get_initial_eps_refuse_mschap(): boolean;
        get_initial_eps_refuse_mschapv2(): boolean;
        get_initial_eps_refuse_pap(): boolean;
        get_initial_eps_username(): string;
        get_mtu(): number;
        get_network_id(): string;
        get_number(): string;
        get_password(): string;
        get_password_flags(): NM.SettingSecretFlags;
        get_pin(): string;
        get_pin_flags(): NM.SettingSecretFlags;
        get_sim_id(): string;
        get_sim_operator_id(): string;
        get_username(): string;
    }
    class SettingHostname extends Setting {
        static '$gtype': GObject.GType<SettingHostname>;
        // Properties
        /**
         * Whether the system hostname can be determined from DHCP on
         * this connection.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_TRUE.
         */
        get from_dhcp(): NM.Ternary;
        set from_dhcp(val: NM.Ternary);
        /**
         * Whether the system hostname can be determined from DHCP on
         * this connection.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_TRUE.
         */
        get fromDhcp(): NM.Ternary;
        set fromDhcp(val: NM.Ternary);
        /**
         * Whether the system hostname can be determined from reverse
         * DNS lookup of addresses on this device.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_TRUE.
         */
        get from_dns_lookup(): NM.Ternary;
        set from_dns_lookup(val: NM.Ternary);
        /**
         * Whether the system hostname can be determined from reverse
         * DNS lookup of addresses on this device.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_TRUE.
         */
        get fromDnsLookup(): NM.Ternary;
        set fromDnsLookup(val: NM.Ternary);
        /**
         * If set to %NM_TERNARY_TRUE, NetworkManager attempts to get
         * the hostname via DHCPv4/DHCPv6 or reverse DNS lookup on this
         * device only when the device has the default route for the given
         * address family (IPv4/IPv6).
         *
         * If set to %NM_TERNARY_FALSE, the hostname can be set from this
         * device even if it doesn't have the default route.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_FALSE.
         */
        get only_from_default(): NM.Ternary;
        set only_from_default(val: NM.Ternary);
        /**
         * If set to %NM_TERNARY_TRUE, NetworkManager attempts to get
         * the hostname via DHCPv4/DHCPv6 or reverse DNS lookup on this
         * device only when the device has the default route for the given
         * address family (IPv4/IPv6).
         *
         * If set to %NM_TERNARY_FALSE, the hostname can be set from this
         * device even if it doesn't have the default route.
         *
         * When set to %NM_TERNARY_DEFAULT, the value from global configuration
         * is used. If the property doesn't have a value in the global
         * configuration, NetworkManager assumes the value to be %NM_TERNARY_FALSE.
         */
        get onlyFromDefault(): NM.Ternary;
        set onlyFromDefault(val: NM.Ternary);
        /**
         * The relative priority of this connection to determine the
         * system hostname. A lower numerical value is better (higher
         * priority).  A connection with higher priority is considered
         * before connections with lower priority.
         *
         * If the value is zero, it can be overridden by a global value
         * from NetworkManager configuration. If the property doesn't have
         * a value in the global configuration, the value is assumed to be
         * 100.
         *
         * Negative values have the special effect of excluding other
         * connections with a greater numerical priority value; so in
         * presence of at least one negative priority, only connections
         * with the lowest priority value will be used to determine the
         * hostname.
         */
        get priority(): number;
        set priority(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingHostname.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingHostname.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingHostname;
        // Signals
        connect<K extends keyof NM.SettingHostname.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingHostname.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingHostname.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingHostname.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingHostname.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingHostname.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the value contained in the #NMSettingHostname:from-dhcp
         * property.
         *
         * @returns the 'from-dhcp' property value
         */
        get_from_dhcp(): NM.Ternary;
        /**
         * Returns the value contained in the #NMSettingHostname:from-dns-lookup
         * property.
         *
         * @returns the 'from-dns-lookup' property value
         */
        get_from_dns_lookup(): NM.Ternary;
        /**
         * Returns the value contained in the #NMSettingHostname:only-from-default
         * property.
         *
         * @returns the 'only-from-default' property value
         */
        get_only_from_default(): NM.Ternary;
        /**
         * Returns the value contained in the #NMSettingHostname:priority
         * property.
         *
         * @returns the 'priority' property value
         */
        get_priority(): number;
    }
    class SettingHsr extends Setting {
        static '$gtype': GObject.GType<SettingHsr>;
        // Properties
        /**
         * The optional interlink port name of the HSR interface.
         */
        get interlink(): string;
        set interlink(val: string);
        /**
         * The last byte of supervision address.
         */
        get multicast_spec(): number;
        set multicast_spec(val: number);
        /**
         * The last byte of supervision address.
         */
        get multicastSpec(): number;
        set multicastSpec(val: number);
        /**
         * The port1 interface name of the HSR. This property is mandatory.
         */
        get port1(): string;
        set port1(val: string);
        /**
         * The port2 interface name of the HSR. This property is mandatory.
         */
        get port2(): string;
        set port2(val: string);
        /**
         * Configures the protocol version to be used for the HSR/PRP interface.
         * %NM_SETTING_HSR_PROTOCOL_VERSION_DEFAULT sets the protocol version to the default version for the protocol.
         * %NM_SETTING_HSR_PROTOCOL_VERSION_HSR_2010 sets the protocol version to HSRv0 (IEC 62439-3:2010).
         * %NM_SETTING_HSR_PROTOCOL_VERSION_HSR_2012 sets the protocol version to HSRv1 (IEC 62439-3:2012).
         */
        get protocol_version(): number;
        set protocol_version(val: number);
        /**
         * Configures the protocol version to be used for the HSR/PRP interface.
         * %NM_SETTING_HSR_PROTOCOL_VERSION_DEFAULT sets the protocol version to the default version for the protocol.
         * %NM_SETTING_HSR_PROTOCOL_VERSION_HSR_2010 sets the protocol version to HSRv0 (IEC 62439-3:2010).
         * %NM_SETTING_HSR_PROTOCOL_VERSION_HSR_2012 sets the protocol version to HSRv1 (IEC 62439-3:2012).
         */
        get protocolVersion(): number;
        set protocolVersion(val: number);
        /**
         * The protocol used by the interface, whether it is PRP or HSR.
         */
        get prp(): boolean;
        set prp(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingHsr.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingHsr.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingHsr;
        // Signals
        connect<K extends keyof NM.SettingHsr.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingHsr.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingHsr.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingHsr.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingHsr.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingHsr.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_interlink(): string;
        get_multicast_spec(): number;
        get_port1(): string;
        get_port2(): string;
        get_protocol_version(): NM.SettingHsrProtocolVersion;
        get_prp(): boolean;
    }
    class SettingIP4Config extends SettingIPConfig {
        static '$gtype': GObject.GType<SettingIP4Config>;
        // Properties
        /**
         * A string sent to the DHCP server to identify the local machine which the
         * DHCP server may use to customize the DHCP lease and options.
         * When the property is a hex string ('aa:bb:cc') it is interpreted as a
         * binary client ID, in which case the first byte is assumed to be the
         * 'type' field as per RFC 2132 section 9.14 and the remaining bytes may be
         * an hardware address (e.g. '01:xx:xx:xx:xx:xx:xx' where 1 is the Ethernet
         * ARP type and the rest is a MAC address).
         * If the property is not a hex string it is considered as a
         * non-hardware-address client ID and the 'type' field is set to 0.
         *
         * The special values "mac" and "perm-mac" are supported, which use the
         * current or permanent MAC address of the device to generate a client identifier
         * with type ethernet (01). Currently, these options only work for ethernet
         * type of links.
         *
         * The special value "ipv6-duid" uses the DUID from "ipv6.dhcp-duid" property as
         * an RFC4361-compliant client identifier. As IAID it uses "ipv4.dhcp-iaid"
         * and falls back to "ipv6.dhcp-iaid" if unset.
         *
         * The special value "duid" generates a RFC4361-compliant client identifier based
         * on "ipv4.dhcp-iaid" and uses a DUID generated by hashing /etc/machine-id.
         *
         * The special value "stable" is supported to generate a type 0 client identifier based
         * on the stable-id (see connection.stable-id) and a per-host key. If you set the
         * stable-id, you may want to include the "${DEVICE}" or "${MAC}" specifier to get a
         * per-device key.
         *
         * The special value "none" prevents any client identifier from being sent. Note that
         * this is normally not recommended.
         *
         * If unset, a globally configured default from NetworkManager.conf is
         * used. If still unset, the default depends on the DHCP plugin. The
         * internal dhcp client will default to "mac" and the dhclient plugin will
         * try to use one from its config file if present, or won't sent any
         * client-id otherwise.
         */
        get dhcp_client_id(): string;
        set dhcp_client_id(val: string);
        /**
         * A string sent to the DHCP server to identify the local machine which the
         * DHCP server may use to customize the DHCP lease and options.
         * When the property is a hex string ('aa:bb:cc') it is interpreted as a
         * binary client ID, in which case the first byte is assumed to be the
         * 'type' field as per RFC 2132 section 9.14 and the remaining bytes may be
         * an hardware address (e.g. '01:xx:xx:xx:xx:xx:xx' where 1 is the Ethernet
         * ARP type and the rest is a MAC address).
         * If the property is not a hex string it is considered as a
         * non-hardware-address client ID and the 'type' field is set to 0.
         *
         * The special values "mac" and "perm-mac" are supported, which use the
         * current or permanent MAC address of the device to generate a client identifier
         * with type ethernet (01). Currently, these options only work for ethernet
         * type of links.
         *
         * The special value "ipv6-duid" uses the DUID from "ipv6.dhcp-duid" property as
         * an RFC4361-compliant client identifier. As IAID it uses "ipv4.dhcp-iaid"
         * and falls back to "ipv6.dhcp-iaid" if unset.
         *
         * The special value "duid" generates a RFC4361-compliant client identifier based
         * on "ipv4.dhcp-iaid" and uses a DUID generated by hashing /etc/machine-id.
         *
         * The special value "stable" is supported to generate a type 0 client identifier based
         * on the stable-id (see connection.stable-id) and a per-host key. If you set the
         * stable-id, you may want to include the "${DEVICE}" or "${MAC}" specifier to get a
         * per-device key.
         *
         * The special value "none" prevents any client identifier from being sent. Note that
         * this is normally not recommended.
         *
         * If unset, a globally configured default from NetworkManager.conf is
         * used. If still unset, the default depends on the DHCP plugin. The
         * internal dhcp client will default to "mac" and the dhclient plugin will
         * try to use one from its config file if present, or won't sent any
         * client-id otherwise.
         */
        get dhcpClientId(): string;
        set dhcpClientId(val: string);
        /**
         * If the #NMSettingIPConfig:dhcp-send-hostname property is %TRUE, then the
         * specified FQDN will be sent to the DHCP server when acquiring a lease. This
         * property and #NMSettingIPConfig:dhcp-hostname are mutually exclusive and
         * cannot be set at the same time.
         */
        get dhcp_fqdn(): string;
        set dhcp_fqdn(val: string);
        /**
         * If the #NMSettingIPConfig:dhcp-send-hostname property is %TRUE, then the
         * specified FQDN will be sent to the DHCP server when acquiring a lease. This
         * property and #NMSettingIPConfig:dhcp-hostname are mutually exclusive and
         * cannot be set at the same time.
         */
        get dhcpFqdn(): string;
        set dhcpFqdn(val: string);
        /**
         * Controls the "IPv6-Only Preferred" DHCPv4 option (RFC 8925).
         *
         * When set to %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_YES, the host adds the
         * option to the parameter request list; if the DHCP server sends the option back,
         * the host stops the DHCP client for the time interval specified in the option.
         *
         * Enable this feature if the host supports an IPv6-only mode, i.e. either all
         * applications are IPv6-only capable or there is a form of 464XLAT deployed.
         *
         * When set to %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_DEFAULT, the actual value
         * is looked up in the global configuration; if not specified, it defaults to
         * %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_NO.
         *
         * If the connection has IPv6 method set to "disabled", this property does not
         * have effect and the "IPv6-Only Preferred" option is always disabled.
         */
        get dhcp_ipv6_only_preferred(): number;
        set dhcp_ipv6_only_preferred(val: number);
        /**
         * Controls the "IPv6-Only Preferred" DHCPv4 option (RFC 8925).
         *
         * When set to %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_YES, the host adds the
         * option to the parameter request list; if the DHCP server sends the option back,
         * the host stops the DHCP client for the time interval specified in the option.
         *
         * Enable this feature if the host supports an IPv6-only mode, i.e. either all
         * applications are IPv6-only capable or there is a form of 464XLAT deployed.
         *
         * When set to %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_DEFAULT, the actual value
         * is looked up in the global configuration; if not specified, it defaults to
         * %NM_SETTING_IP4_DHCP_IPV6_ONLY_PREFERRED_NO.
         *
         * If the connection has IPv6 method set to "disabled", this property does not
         * have effect and the "IPv6-Only Preferred" option is always disabled.
         */
        get dhcpIpv6OnlyPreferred(): number;
        set dhcpIpv6OnlyPreferred(val: number);
        /**
         * The Vendor Class Identifier DHCP option (60).
         * Special characters in the data string may be escaped using C-style escapes,
         * nevertheless this property cannot contain nul bytes.
         * If the per-profile value is unspecified (the default),
         * a global connection default gets consulted.
         * If still unspecified, the DHCP option is not sent to the server.
         */
        get dhcp_vendor_class_identifier(): string;
        set dhcp_vendor_class_identifier(val: string);
        /**
         * The Vendor Class Identifier DHCP option (60).
         * Special characters in the data string may be escaped using C-style escapes,
         * nevertheless this property cannot contain nul bytes.
         * If the per-profile value is unspecified (the default),
         * a global connection default gets consulted.
         * If still unspecified, the DHCP option is not sent to the server.
         */
        get dhcpVendorClassIdentifier(): string;
        set dhcpVendorClassIdentifier(val: string);
        /**
         * Enable and disable the IPv4 link-local configuration independently of the
         * ipv4.method configuration. This allows a link-local address (169.254.x.y/16)
         * to be obtained in addition to other addresses, such as those manually
         * configured or obtained from a DHCP server.
         *
         * When set to "auto", the value is dependent on "ipv4.method".
         * When set to "default", it honors the global connection default, before
         * falling back to "auto". Note that if "ipv4.method" is "disabled", then
         * link local addressing is always disabled too. The default is "default".
         * Since 1.52, when set to "fallback", a link-local address is obtained
         * if no other IPv4 address is set.
         */
        get link_local(): number;
        set link_local(val: number);
        /**
         * Enable and disable the IPv4 link-local configuration independently of the
         * ipv4.method configuration. This allows a link-local address (169.254.x.y/16)
         * to be obtained in addition to other addresses, such as those manually
         * configured or obtained from a DHCP server.
         *
         * When set to "auto", the value is dependent on "ipv4.method".
         * When set to "default", it honors the global connection default, before
         * falling back to "auto". Note that if "ipv4.method" is "disabled", then
         * link local addressing is always disabled too. The default is "default".
         * Since 1.52, when set to "fallback", a link-local address is obtained
         * if no other IPv4 address is set.
         */
        get linkLocal(): number;
        set linkLocal(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingIP4Config.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingIP4Config.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingIP4Config;
        // Signals
        connect<K extends keyof NM.SettingIP4Config.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIP4Config.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingIP4Config.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIP4Config.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingIP4Config.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingIP4Config.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the value contained in the #NMSettingIP4Config:dhcp-client-id
         * property.
         *
         * @returns the configured Client ID to send to the DHCP server when requesting
         * addresses via DHCP.
         */
        get_dhcp_client_id(): string;
        /**
         * Returns the value contained in the #NMSettingIP4Config:dhcp-fqdn
         * property.
         *
         * @returns the configured FQDN to send to the DHCP server
         */
        get_dhcp_fqdn(): string;
        /**
         * Returns the value in the #NMSettingIP4Config:dhcp-ipv6-only-preferred
         * property.
         *
         * @returns the DHCP IPv6-only preferred property value
         */
        get_dhcp_ipv6_only_preferred(): NM.SettingIP4DhcpIpv6OnlyPreferred;
        /**
         * Returns the value contained in the #NMSettingIP4Config:dhcp_vendor_class_identifier
         * property.
         *
         * @returns the vendor class identifier option to send to the DHCP server
         */
        get_dhcp_vendor_class_identifier(): string;
        /**
         * Returns the value contained in the #NMSettingIP4Config:link_local
         * property.
         *
         * @returns the link-local configuration
         */
        get_link_local(): NM.SettingIP4LinkLocal;
    }
    class SettingIP6Config extends SettingIPConfig {
        static '$gtype': GObject.GType<SettingIP6Config>;
        // Properties
        /**
         * Configure the method for creating the IPv6 interface identifier of
         * addresses for RFC4862 IPv6 Stateless Address Autoconfiguration and IPv6
         * Link Local.
         *
         * The permitted values are: %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_EUI64,
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_STABLE_PRIVACY.
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_DEFAULT_OR_EUI64 or
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_DEFAULT.
         *
         * If the property is set to "eui64", the addresses will be generated using
         * the interface token derived from the hardware address. This makes the
         * host part of the address constant, making it possible to track the
         * host's presence when it changes networks. The address changes when the
         * interface hardware is replaced. If a duplicate address is detected,
         * there is no fallback to generate another address. When configured, the
         * "ipv6.token" is used instead of the MAC address to generate addresses
         * for stateless autoconfiguration.
         *
         * If the property is set to "stable-privacy", the interface identifier is
         * generated as specified by RFC7217. This works by hashing a host specific
         * key (see NetworkManager(8) manual), the interface name, the connection's
         * "connection.stable-id" property and the address prefix.  This improves
         * privacy by making it harder to use the address to track the host's
         * presence as every prefix and network has a different identifier. Also,
         * the address is stable when the network interface hardware is replaced.
         *
         * The special values "default" and "default-or-eui64" will fallback to the
         * global connection default as documented in the NetworkManager.conf(5)
         * manual. If the global default is not specified, the fallback value is
         * "stable-privacy" or "eui64", respectively.
         *
         * For libnm, the property defaults to "default" since 1.40.  Previously it
         * used to default to "stable-privacy".  On D-Bus, the absence of an
         * addr-gen-mode setting equals "default". For keyfile plugin, the absence
         * of the setting on disk means "default-or-eui64" so that the property
         * doesn't change on upgrade from older versions.
         *
         * Note that this setting is distinct from the Privacy Extensions as
         * configured by "ip6-privacy" property and it does not affect the
         * temporary addresses configured with this option.
         */
        get addr_gen_mode(): number;
        set addr_gen_mode(val: number);
        /**
         * Configure the method for creating the IPv6 interface identifier of
         * addresses for RFC4862 IPv6 Stateless Address Autoconfiguration and IPv6
         * Link Local.
         *
         * The permitted values are: %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_EUI64,
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_STABLE_PRIVACY.
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_DEFAULT_OR_EUI64 or
         * %NM_SETTING_IP6_CONFIG_ADDR_GEN_MODE_DEFAULT.
         *
         * If the property is set to "eui64", the addresses will be generated using
         * the interface token derived from the hardware address. This makes the
         * host part of the address constant, making it possible to track the
         * host's presence when it changes networks. The address changes when the
         * interface hardware is replaced. If a duplicate address is detected,
         * there is no fallback to generate another address. When configured, the
         * "ipv6.token" is used instead of the MAC address to generate addresses
         * for stateless autoconfiguration.
         *
         * If the property is set to "stable-privacy", the interface identifier is
         * generated as specified by RFC7217. This works by hashing a host specific
         * key (see NetworkManager(8) manual), the interface name, the connection's
         * "connection.stable-id" property and the address prefix.  This improves
         * privacy by making it harder to use the address to track the host's
         * presence as every prefix and network has a different identifier. Also,
         * the address is stable when the network interface hardware is replaced.
         *
         * The special values "default" and "default-or-eui64" will fallback to the
         * global connection default as documented in the NetworkManager.conf(5)
         * manual. If the global default is not specified, the fallback value is
         * "stable-privacy" or "eui64", respectively.
         *
         * For libnm, the property defaults to "default" since 1.40.  Previously it
         * used to default to "stable-privacy".  On D-Bus, the absence of an
         * addr-gen-mode setting equals "default". For keyfile plugin, the absence
         * of the setting on disk means "default-or-eui64" so that the property
         * doesn't change on upgrade from older versions.
         *
         * Note that this setting is distinct from the Privacy Extensions as
         * configured by "ip6-privacy" property and it does not affect the
         * temporary addresses configured with this option.
         */
        get addrGenMode(): number;
        set addrGenMode(val: number);
        /**
         * A string containing the DHCPv6 Unique Identifier (DUID) used by the dhcp
         * client to identify itself to DHCPv6 servers (RFC 3315). The DUID is carried
         * in the Client Identifier option.
         * If the property is a hex string ('aa:bb:cc') it is interpreted as a binary
         * DUID and filled as an opaque value in the Client Identifier option.
         *
         * The special value "lease" will retrieve the DUID previously used from the
         * lease file belonging to the connection. If no DUID is found and "dhclient"
         * is the configured dhcp client, the DUID is searched in the system-wide
         * dhclient lease file. If still no DUID is found, or another dhcp client is
         * used, a global and permanent DUID-UUID (RFC 6355) will be generated based
         * on the machine-id.
         *
         * The special values "llt" and "ll" will generate a DUID of type LLT or LL
         * (see RFC 3315) based on the current MAC address of the device. In order to
         * try providing a stable DUID-LLT, the time field will contain a constant
         * timestamp that is used globally (for all profiles) and persisted to disk.
         *
         * The special values "stable-llt", "stable-ll" and "stable-uuid" will generate
         * a DUID of the corresponding type, derived from the connection's stable-id and
         * a per-host unique key. You may want to include the "${DEVICE}" or "${MAC}" specifier
         * in the stable-id, in case this profile gets activated on multiple devices.
         * So, the link-layer address of "stable-ll" and "stable-llt" will be a generated
         * address derived from the stable id. The DUID-LLT time value in the "stable-llt"
         * option will be picked among a static timespan of three years (the upper bound
         * of the interval is the same constant timestamp used in "llt").
         *
         * When the property is unset, the global value provided for "ipv6.dhcp-duid" is
         * used. If no global value is provided, the default "lease" value is assumed.
         */
        get dhcp_duid(): string;
        set dhcp_duid(val: string);
        /**
         * A string containing the DHCPv6 Unique Identifier (DUID) used by the dhcp
         * client to identify itself to DHCPv6 servers (RFC 3315). The DUID is carried
         * in the Client Identifier option.
         * If the property is a hex string ('aa:bb:cc') it is interpreted as a binary
         * DUID and filled as an opaque value in the Client Identifier option.
         *
         * The special value "lease" will retrieve the DUID previously used from the
         * lease file belonging to the connection. If no DUID is found and "dhclient"
         * is the configured dhcp client, the DUID is searched in the system-wide
         * dhclient lease file. If still no DUID is found, or another dhcp client is
         * used, a global and permanent DUID-UUID (RFC 6355) will be generated based
         * on the machine-id.
         *
         * The special values "llt" and "ll" will generate a DUID of type LLT or LL
         * (see RFC 3315) based on the current MAC address of the device. In order to
         * try providing a stable DUID-LLT, the time field will contain a constant
         * timestamp that is used globally (for all profiles) and persisted to disk.
         *
         * The special values "stable-llt", "stable-ll" and "stable-uuid" will generate
         * a DUID of the corresponding type, derived from the connection's stable-id and
         * a per-host unique key. You may want to include the "${DEVICE}" or "${MAC}" specifier
         * in the stable-id, in case this profile gets activated on multiple devices.
         * So, the link-layer address of "stable-ll" and "stable-llt" will be a generated
         * address derived from the stable id. The DUID-LLT time value in the "stable-llt"
         * option will be picked among a static timespan of three years (the upper bound
         * of the interval is the same constant timestamp used in "llt").
         *
         * When the property is unset, the global value provided for "ipv6.dhcp-duid" is
         * used. If no global value is provided, the default "lease" value is assumed.
         */
        get dhcpDuid(): string;
        set dhcpDuid(val: string);
        /**
         * A IPv6 address followed by a slash and a prefix length. If set, the value is
         * sent to the DHCPv6 server as hint indicating the prefix delegation (IA_PD) we
         * want to receive.
         * To only hint a prefix length without prefix, set the address part to the
         * zero address (for example "::/60").
         */
        get dhcp_pd_hint(): string;
        set dhcp_pd_hint(val: string);
        /**
         * A IPv6 address followed by a slash and a prefix length. If set, the value is
         * sent to the DHCPv6 server as hint indicating the prefix delegation (IA_PD) we
         * want to receive.
         * To only hint a prefix length without prefix, set the address part to the
         * zero address (for example "::/60").
         */
        get dhcpPdHint(): string;
        set dhcpPdHint(val: string);
        /**
         * Configure IPv6 Privacy Extensions for SLAAC, described in RFC4941.  If
         * enabled, it makes the kernel generate a temporary IPv6 address in
         * addition to the public one generated from MAC address via modified
         * EUI-64.  This enhances privacy, but could cause problems in some
         * applications, on the other hand.  The permitted values are: -1: unknown,
         * 0: disabled, 1: enabled (prefer public address), 2: enabled (prefer temporary
         * addresses).
         *
         * Having a per-connection setting set to "-1" (default) means fallback to
         * global configuration "ipv6.ip6-privacy". If it's also unspecified or set
         * to "-1", fallback to read "/proc/sys/net/ipv6/conf/default/use_tempaddr".
         *
         * Note that this setting is distinct from the Stable Privacy addresses
         * that can be enabled with the "addr-gen-mode" property's "stable-privacy"
         * setting as another way of avoiding host tracking with IPv6 addresses.
         */
        get ip6_privacy(): NM.SettingIP6ConfigPrivacy;
        set ip6_privacy(val: NM.SettingIP6ConfigPrivacy);
        /**
         * Configure IPv6 Privacy Extensions for SLAAC, described in RFC4941.  If
         * enabled, it makes the kernel generate a temporary IPv6 address in
         * addition to the public one generated from MAC address via modified
         * EUI-64.  This enhances privacy, but could cause problems in some
         * applications, on the other hand.  The permitted values are: -1: unknown,
         * 0: disabled, 1: enabled (prefer public address), 2: enabled (prefer temporary
         * addresses).
         *
         * Having a per-connection setting set to "-1" (default) means fallback to
         * global configuration "ipv6.ip6-privacy". If it's also unspecified or set
         * to "-1", fallback to read "/proc/sys/net/ipv6/conf/default/use_tempaddr".
         *
         * Note that this setting is distinct from the Stable Privacy addresses
         * that can be enabled with the "addr-gen-mode" property's "stable-privacy"
         * setting as another way of avoiding host tracking with IPv6 addresses.
         */
        get ip6Privacy(): NM.SettingIP6ConfigPrivacy;
        set ip6Privacy(val: NM.SettingIP6ConfigPrivacy);
        /**
         * Maximum transmission unit size, in bytes. If zero (the default), the MTU
         * is set automatically from router advertisements or is left equal to the
         * link-layer MTU. If greater than the link-layer MTU, or greater than zero
         * but less than the minimum IPv6 MTU of 1280, this value has no effect.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * A timeout for waiting Router Advertisements in seconds. If zero (the default), a
         * globally configured default is used. If still unspecified, the timeout depends on the
         * sysctl settings of the device.
         *
         * Set to 2147483647 (MAXINT32) for infinity.
         */
        get ra_timeout(): number;
        set ra_timeout(val: number);
        /**
         * A timeout for waiting Router Advertisements in seconds. If zero (the default), a
         * globally configured default is used. If still unspecified, the timeout depends on the
         * sysctl settings of the device.
         *
         * Set to 2147483647 (MAXINT32) for infinity.
         */
        get raTimeout(): number;
        set raTimeout(val: number);
        /**
         * The preferred lifetime of autogenerated temporary addresses, in seconds.
         *
         * Having a per-connection setting set to "0" (default) means fallback to
         * global configuration "ipv6.temp-preferred-lifetime" setting". If it's also
         * unspecified or set to "0", fallback to read
         * "/proc/sys/net/ipv6/conf/default/temp_prefered_lft".
         */
        get temp_preferred_lifetime(): number;
        set temp_preferred_lifetime(val: number);
        /**
         * The preferred lifetime of autogenerated temporary addresses, in seconds.
         *
         * Having a per-connection setting set to "0" (default) means fallback to
         * global configuration "ipv6.temp-preferred-lifetime" setting". If it's also
         * unspecified or set to "0", fallback to read
         * "/proc/sys/net/ipv6/conf/default/temp_prefered_lft".
         */
        get tempPreferredLifetime(): number;
        set tempPreferredLifetime(val: number);
        /**
         * The valid lifetime of autogenerated temporary addresses, in seconds.
         *
         * Having a per-connection setting set to "0" (default) means fallback to
         * global configuration "ipv6.temp-valid-lifetime" setting". If it's also
         * unspecified or set to "0", fallback to read
         * "/proc/sys/net/ipv6/conf/default/temp_valid_lft".
         */
        get temp_valid_lifetime(): number;
        set temp_valid_lifetime(val: number);
        /**
         * The valid lifetime of autogenerated temporary addresses, in seconds.
         *
         * Having a per-connection setting set to "0" (default) means fallback to
         * global configuration "ipv6.temp-valid-lifetime" setting". If it's also
         * unspecified or set to "0", fallback to read
         * "/proc/sys/net/ipv6/conf/default/temp_valid_lft".
         */
        get tempValidLifetime(): number;
        set tempValidLifetime(val: number);
        /**
         * Configure the token for draft-chown-6man-tokenised-ipv6-identifiers-02
         * IPv6 tokenized interface identifiers. Useful with eui64 addr-gen-mode.
         *
         * When set, the token is used as IPv6 interface identifier instead of the
         * hardware address. This only applies to addresses from stateless
         * autoconfiguration, not to IPv6 link local addresses.
         */
        get token(): string;
        set token(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingIP6Config.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingIP6Config.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingIP6Config;
        // Signals
        connect<K extends keyof NM.SettingIP6Config.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIP6Config.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingIP6Config.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIP6Config.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingIP6Config.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingIP6Config.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the value contained in the #NMSettingIP6Config:addr-gen-mode
         * property.
         *
         * @returns IPv6 Address Generation Mode.
         */
        get_addr_gen_mode(): NM.SettingIP6ConfigAddrGenMode;
        /**
         * Returns the value contained in the #NMSettingIP6Config:dhcp-duid
         * property.
         *
         * @returns The configured DUID value to be included in the DHCPv6 requests
         * sent to the DHCPv6 servers.
         */
        get_dhcp_duid(): string;
        /**
         * Returns the value contained in the #NMSettingIP6Config:dhcp-pd-hint
         * property.
         *
         * @returns a string containing an address and prefix length to be used
         * as hint for DHCPv6 prefix delegation.
         */
        get_dhcp_pd_hint(): string;
        /**
         * Returns the value contained in the #NMSettingIP6Config:ip6-privacy
         * property.
         *
         * @returns IPv6 Privacy Extensions configuration value (#NMSettingIP6ConfigPrivacy).
         */
        get_ip6_privacy(): NM.SettingIP6ConfigPrivacy;
        get_mtu(): number;
        get_ra_timeout(): number;
        /**
         * Returns the value contained in the #NMSettingIP6Config:temp-preferred-lifetime
         * property.
         *
         * @returns The preferred lifetime of autogenerated temporary addresses.
         */
        get_temp_preferred_lifetime(): number;
        /**
         * Returns the value contained in the #NMSettingIP6Config:temp-valid-lifetime
         * property.
         *
         * @returns The valid lifetime of autogenerated temporary addresses.
         */
        get_temp_valid_lifetime(): number;
        /**
         * Returns the value contained in the #NMSettingIP6Config:token
         * property.
         *
         * @returns A string.
         */
        get_token(): string;
    }
    abstract class SettingIPConfig extends Setting {
        static '$gtype': GObject.GType<SettingIPConfig>;
        // Properties
        /**
         * Array of IP addresses.
         */
        get addresses(): NM.IPAddress[];
        set addresses(val: NM.IPAddress[]);
        /**
         * VPN connections will default to add the route automatically unless this
         * setting is set to %FALSE.
         *
         * For other connection types, adding such an automatic route is currently
         * not supported and setting this to %TRUE has no effect.
         */
        get auto_route_ext_gw(): NM.Ternary;
        set auto_route_ext_gw(val: NM.Ternary);
        /**
         * VPN connections will default to add the route automatically unless this
         * setting is set to %FALSE.
         *
         * For other connection types, adding such an automatic route is currently
         * not supported and setting this to %TRUE has no effect.
         */
        get autoRouteExtGw(): NM.Ternary;
        set autoRouteExtGw(val: NM.Ternary);
        /**
         * Maximum timeout in milliseconds used to check for the presence of duplicate
         * IP addresses on the network.  If an address conflict is detected, the
         * activation will fail. The property is currently implemented only for IPv4.
         *
         * A zero value means that no duplicate address detection is performed, -1 means
         * the default value (either the value configured globally in NetworkManger.conf
         * or 200ms).  A value greater than zero is a timeout in milliseconds.  Note that
         * the time intervals are subject to randomization as per RFC 5227 and so the
         * actual duration can be between half and the full time specified in this
         * property.
         */
        get dad_timeout(): number;
        set dad_timeout(val: number);
        /**
         * Maximum timeout in milliseconds used to check for the presence of duplicate
         * IP addresses on the network.  If an address conflict is detected, the
         * activation will fail. The property is currently implemented only for IPv4.
         *
         * A zero value means that no duplicate address detection is performed, -1 means
         * the default value (either the value configured globally in NetworkManger.conf
         * or 200ms).  A value greater than zero is a timeout in milliseconds.  Note that
         * the time intervals are subject to randomization as per RFC 5227 and so the
         * actual duration can be between half and the full time specified in this
         * property.
         */
        get dadTimeout(): number;
        set dadTimeout(val: number);
        /**
         * Specifies the value for the DSCP field (traffic class) of the IP header. When
         * empty, the global default value is used; if no global default is specified, it is
         * assumed to be "CS0". Allowed values are: "CS0", "CS4" and "CS6".
         *
         * The property is currently valid only for IPv4, and it is supported only by the
         * "internal" DHCP plugin.
         */
        get dhcp_dscp(): string;
        set dhcp_dscp(val: string);
        /**
         * Specifies the value for the DSCP field (traffic class) of the IP header. When
         * empty, the global default value is used; if no global default is specified, it is
         * assumed to be "CS0". Allowed values are: "CS0", "CS4" and "CS6".
         *
         * The property is currently valid only for IPv4, and it is supported only by the
         * "internal" DHCP plugin.
         */
        get dhcpDscp(): string;
        set dhcpDscp(val: string);
        /**
         * If the #NMSettingIPConfig:dhcp-send-hostname property is %TRUE, then the
         * specified name will be sent to the DHCP server when acquiring a lease.
         * This property and #NMSettingIP4Config:dhcp-fqdn are mutually exclusive and
         * cannot be set at the same time.
         */
        get dhcp_hostname(): string;
        set dhcp_hostname(val: string);
        /**
         * If the #NMSettingIPConfig:dhcp-send-hostname property is %TRUE, then the
         * specified name will be sent to the DHCP server when acquiring a lease.
         * This property and #NMSettingIP4Config:dhcp-fqdn are mutually exclusive and
         * cannot be set at the same time.
         */
        get dhcpHostname(): string;
        set dhcpHostname(val: string);
        /**
         * Flags for the DHCP hostname and FQDN.
         *
         * Currently, this property only includes flags to control the FQDN flags
         * set in the DHCP FQDN option. Supported FQDN flags are
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE,
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_ENCODED and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_NO_UPDATE.  When no FQDN flag is set and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_CLEAR_FLAGS is set, the DHCP FQDN option will
         * contain no flag. Otherwise, if no FQDN flag is set and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_CLEAR_FLAGS is not set, the standard FQDN flags
         * are set in the request:
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE,
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_ENCODED for IPv4 and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE for IPv6.
         *
         * When this property is set to the default value %NM_DHCP_HOSTNAME_FLAG_NONE,
         * a global default is looked up in NetworkManager configuration. If that value
         * is unset or also %NM_DHCP_HOSTNAME_FLAG_NONE, then the standard FQDN flags
         * described above are sent in the DHCP requests.
         */
        get dhcp_hostname_flags(): number;
        set dhcp_hostname_flags(val: number);
        /**
         * Flags for the DHCP hostname and FQDN.
         *
         * Currently, this property only includes flags to control the FQDN flags
         * set in the DHCP FQDN option. Supported FQDN flags are
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE,
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_ENCODED and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_NO_UPDATE.  When no FQDN flag is set and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_CLEAR_FLAGS is set, the DHCP FQDN option will
         * contain no flag. Otherwise, if no FQDN flag is set and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_CLEAR_FLAGS is not set, the standard FQDN flags
         * are set in the request:
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE,
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_ENCODED for IPv4 and
         * %NM_DHCP_HOSTNAME_FLAG_FQDN_SERV_UPDATE for IPv6.
         *
         * When this property is set to the default value %NM_DHCP_HOSTNAME_FLAG_NONE,
         * a global default is looked up in NetworkManager configuration. If that value
         * is unset or also %NM_DHCP_HOSTNAME_FLAG_NONE, then the standard FQDN flags
         * described above are sent in the DHCP requests.
         */
        get dhcpHostnameFlags(): number;
        set dhcpHostnameFlags(val: number);
        /**
         * A string containing the "Identity Association Identifier" (IAID) used by
         * the DHCP client. The string can be a 32-bit number (either decimal,
         * hexadecimal or as colon separated hexadecimal numbers). Alternatively
         * it can be set to the special values "mac", "perm-mac", "ifname" or
         * "stable". When set to "mac" (or "perm-mac"), the last 4 bytes of the
         * current (or permanent) MAC address are used as IAID. When set to
         * "ifname", the IAID is computed by hashing the interface name. The
         * special value "stable" can be used to generate an IAID based on the
         * stable-id (see connection.stable-id), a per-host key and the interface
         * name. When the property is unset, the value from global configuration is
         * used; if no global default is set then the IAID is assumed to be
         * "ifname".
         *
         * For DHCPv4, the IAID is only used with "ipv4.dhcp-client-id"
         * values "duid" and "ipv6-duid" to generate the client-id.
         *
         * For DHCPv6, note that at the moment this property is
         * only supported by the "internal" DHCPv6 plugin. The "dhclient" DHCPv6
         * plugin always derives the IAID from the MAC address.
         *
         * The actually used DHCPv6 IAID for a currently activated interface is
         * exposed in the lease information of the device.
         */
        get dhcp_iaid(): string;
        set dhcp_iaid(val: string);
        /**
         * A string containing the "Identity Association Identifier" (IAID) used by
         * the DHCP client. The string can be a 32-bit number (either decimal,
         * hexadecimal or as colon separated hexadecimal numbers). Alternatively
         * it can be set to the special values "mac", "perm-mac", "ifname" or
         * "stable". When set to "mac" (or "perm-mac"), the last 4 bytes of the
         * current (or permanent) MAC address are used as IAID. When set to
         * "ifname", the IAID is computed by hashing the interface name. The
         * special value "stable" can be used to generate an IAID based on the
         * stable-id (see connection.stable-id), a per-host key and the interface
         * name. When the property is unset, the value from global configuration is
         * used; if no global default is set then the IAID is assumed to be
         * "ifname".
         *
         * For DHCPv4, the IAID is only used with "ipv4.dhcp-client-id"
         * values "duid" and "ipv6-duid" to generate the client-id.
         *
         * For DHCPv6, note that at the moment this property is
         * only supported by the "internal" DHCPv6 plugin. The "dhclient" DHCPv6
         * plugin always derives the IAID from the MAC address.
         *
         * The actually used DHCPv6 IAID for a currently activated interface is
         * exposed in the lease information of the device.
         */
        get dhcpIaid(): string;
        set dhcpIaid(val: string);
        /**
         * Array of servers from which DHCP offers must be rejected. This property
         * is useful to avoid getting a lease from misconfigured or rogue servers.
         *
         * For DHCPv4, each element must be an IPv4 address, optionally
         * followed by a slash and a prefix length (e.g. "192.168.122.0/24").
         *
         * This property is currently not implemented for DHCPv6.
         */
        get dhcp_reject_servers(): string[];
        set dhcp_reject_servers(val: string[]);
        /**
         * Array of servers from which DHCP offers must be rejected. This property
         * is useful to avoid getting a lease from misconfigured or rogue servers.
         *
         * For DHCPv4, each element must be an IPv4 address, optionally
         * followed by a slash and a prefix length (e.g. "192.168.122.0/24").
         *
         * This property is currently not implemented for DHCPv6.
         */
        get dhcpRejectServers(): string[];
        set dhcpRejectServers(val: string[]);
        /**
         * Since 1.52 this property is deprecated and is only used as fallback value
         * for #NMSettingIPConfig:dhcp-send-hostname-v2 if it's set to 'default'.
         * This is only done to avoid breaking existing configurations, the new
         * property should be used from now on.
         */
        get dhcp_send_hostname(): boolean;
        set dhcp_send_hostname(val: boolean);
        /**
         * Since 1.52 this property is deprecated and is only used as fallback value
         * for #NMSettingIPConfig:dhcp-send-hostname-v2 if it's set to 'default'.
         * This is only done to avoid breaking existing configurations, the new
         * property should be used from now on.
         */
        get dhcpSendHostname(): boolean;
        set dhcpSendHostname(val: boolean);
        /**
         * If %TRUE, a hostname is sent to the DHCP server when acquiring a lease.
         * Some DHCP servers use this hostname to update DNS databases, essentially
         * providing a static hostname for the computer.  If the
         * #NMSettingIPConfig:dhcp-hostname property is %NULL and this property is
         * %TRUE, the current persistent hostname of the computer is sent.
         *
         * The default value is %NM_TERNARY_DEFAULT. In this case the global value
         * from NetworkManager configuration is looked up. If it's not set, the value
         * from #NMSettingIPConfig:dhcp-send-hostname, which defaults to %TRUE, is
         * used for backwards compatibility. In the future this will change and, in
         * absence of a global default, it will always fallback to %TRUE.
         */
        get dhcp_send_hostname_v2(): number;
        set dhcp_send_hostname_v2(val: number);
        /**
         * If %TRUE, a hostname is sent to the DHCP server when acquiring a lease.
         * Some DHCP servers use this hostname to update DNS databases, essentially
         * providing a static hostname for the computer.  If the
         * #NMSettingIPConfig:dhcp-hostname property is %NULL and this property is
         * %TRUE, the current persistent hostname of the computer is sent.
         *
         * The default value is %NM_TERNARY_DEFAULT. In this case the global value
         * from NetworkManager configuration is looked up. If it's not set, the value
         * from #NMSettingIPConfig:dhcp-send-hostname, which defaults to %TRUE, is
         * used for backwards compatibility. In the future this will change and, in
         * absence of a global default, it will always fallback to %TRUE.
         */
        get dhcpSendHostnameV2(): number;
        set dhcpSendHostnameV2(val: number);
        /**
         * Whether the DHCP client will send RELEASE message when
         * bringing the connection down. The default value is %NM_TERNARY_DEFAULT.
         * When the default value is specified, then the global value from NetworkManager
         * configuration is looked up, if not set, it is considered as %FALSE.
         */
        get dhcp_send_release(): NM.Ternary;
        set dhcp_send_release(val: NM.Ternary);
        /**
         * Whether the DHCP client will send RELEASE message when
         * bringing the connection down. The default value is %NM_TERNARY_DEFAULT.
         * When the default value is specified, then the global value from NetworkManager
         * configuration is looked up, if not set, it is considered as %FALSE.
         */
        get dhcpSendRelease(): NM.Ternary;
        set dhcpSendRelease(val: NM.Ternary);
        /**
         * A timeout for a DHCP transaction in seconds. If zero (the default), a
         * globally configured default is used. If still unspecified, a device specific
         * timeout is used (usually 45 seconds).
         *
         * Set to 2147483647 (MAXINT32) for infinity.
         */
        get dhcp_timeout(): number;
        set dhcp_timeout(val: number);
        /**
         * A timeout for a DHCP transaction in seconds. If zero (the default), a
         * globally configured default is used. If still unspecified, a device specific
         * timeout is used (usually 45 seconds).
         *
         * Set to 2147483647 (MAXINT32) for infinity.
         */
        get dhcpTimeout(): number;
        set dhcpTimeout(val: number);
        /**
         * Array of DNS servers.
         *
         * Each server can be specified either as a plain IP address (optionally followed
         * by a "#" and the SNI server name for DNS over TLS) or with a URI syntax.
         *
         * When it is specified as an URI, the following forms are supported:
         * dns+udp://ADDRESS[:PORT], dns+tls://ADDRESS[:PORT][#SERVERNAME] .
         *
         * When using the URI syntax, IPv6 addresses must be enclosed in square
         * brackets ('[', ']').
         */
        get dns(): string[];
        set dns(val: string[]);
        /**
         * Array of DNS options to be added to resolv.conf.
         *
         * %NULL means that the options are unset and left at the default.
         * In this case NetworkManager will use default options. This is
         * distinct from an empty list of properties.
         *
         * The following options are directly added to resolv.conf: "attempts",
         *  "debug", "edns0",
         * "inet6", "ip6-bytestring", "ip6-dotint", "ndots", "no-aaaa",
         * "no-check-names", "no-ip6-dotint", "no-reload", "no-tld-query",
         * "rotate", "single-request", "single-request-reopen", "timeout",
         * "trust-ad", "use-vc". See the resolv.conf(5) man page for a
         * detailed description of these options.
         *
         * In addition, NetworkManager supports the special options "_no-add-edns0"
         * and "_no-add-trust-ad". They are not added to resolv.conf, and can be
         * used to prevent the automatic addition of options "edns0" and "trust-ad"
         * when using caching DNS plugins (see below).
         *
         * The "trust-ad" setting is only honored if the profile contributes
         * name servers to resolv.conf, and if all contributing profiles have
         * "trust-ad" enabled.
         *
         * When using a caching DNS plugin (dnsmasq or systemd-resolved in
         * NetworkManager.conf) then "edns0" and "trust-ad" are automatically
         * added, unless "_no-add-edns0" and "_no-add-trust-ad" are present.
         */
        get dns_options(): string[];
        set dns_options(val: string[]);
        /**
         * Array of DNS options to be added to resolv.conf.
         *
         * %NULL means that the options are unset and left at the default.
         * In this case NetworkManager will use default options. This is
         * distinct from an empty list of properties.
         *
         * The following options are directly added to resolv.conf: "attempts",
         *  "debug", "edns0",
         * "inet6", "ip6-bytestring", "ip6-dotint", "ndots", "no-aaaa",
         * "no-check-names", "no-ip6-dotint", "no-reload", "no-tld-query",
         * "rotate", "single-request", "single-request-reopen", "timeout",
         * "trust-ad", "use-vc". See the resolv.conf(5) man page for a
         * detailed description of these options.
         *
         * In addition, NetworkManager supports the special options "_no-add-edns0"
         * and "_no-add-trust-ad". They are not added to resolv.conf, and can be
         * used to prevent the automatic addition of options "edns0" and "trust-ad"
         * when using caching DNS plugins (see below).
         *
         * The "trust-ad" setting is only honored if the profile contributes
         * name servers to resolv.conf, and if all contributing profiles have
         * "trust-ad" enabled.
         *
         * When using a caching DNS plugin (dnsmasq or systemd-resolved in
         * NetworkManager.conf) then "edns0" and "trust-ad" are automatically
         * added, unless "_no-add-edns0" and "_no-add-trust-ad" are present.
         */
        get dnsOptions(): string[];
        set dnsOptions(val: string[]);
        /**
         * DNS servers priority.
         *
         * The relative priority for DNS servers specified by this setting.  A lower
         * numerical value is better (higher priority).
         *
         * Negative values have the special effect of excluding other configurations
         * with a greater numerical priority value; so in presence of at least one negative
         * priority, only DNS servers from connections with the lowest priority value will be used.
         * To avoid all DNS leaks, set the priority of the profile that should be used
         * to the most negative value of all active connections profiles.
         *
         * Zero selects a globally configured default value. If the latter is missing
         * or zero too, it defaults to 50 for VPNs (including WireGuard) and 100 for
         * other connections.
         *
         * Note that the priority is to order DNS settings for multiple active
         * connections.  It does not disambiguate multiple DNS servers within the
         * same connection profile.
         *
         * When multiple devices have configurations with the same priority, VPNs will be
         * considered first, then devices with the best (lowest metric) default
         * route and then all other devices.
         *
         * When using dns=default, servers with higher priority will be on top of
         * resolv.conf. To prioritize a given server over another one within the
         * same connection, just specify them in the desired order.
         * Note that commonly the resolver tries name servers in /etc/resolv.conf
         * in the order listed, proceeding with the next server in the list
         * on failure. See for example the "rotate" option of the dns-options setting.
         * If there are any negative DNS priorities, then only name servers from
         * the devices with that lowest priority will be considered.
         *
         * When using a DNS resolver that supports Conditional Forwarding or
         * Split DNS (with dns=dnsmasq or dns=systemd-resolved settings), each connection
         * is used to query domains in its search list. The search domains determine which
         * name servers to ask, and the DNS priority is used to prioritize
         * name servers based on the domain.  Queries for domains not present in any
         * search list are routed through connections having the '~.' special wildcard
         * domain, which is added automatically to connections with the default route
         * (or can be added manually).  When multiple connections specify the same domain, the
         * one with the best priority (lowest numerical value) wins.  If a sub domain
         * is configured on another interface it will be accepted regardless the priority,
         * unless parent domain on the other interface has a negative priority, which causes
         * the sub domain to be shadowed.
         * With Split DNS one can avoid undesired DNS leaks by properly configuring
         * DNS priorities and the search domains, so that only name servers of the desired
         * interface are configured.
         */
        get dns_priority(): number;
        set dns_priority(val: number);
        /**
         * DNS servers priority.
         *
         * The relative priority for DNS servers specified by this setting.  A lower
         * numerical value is better (higher priority).
         *
         * Negative values have the special effect of excluding other configurations
         * with a greater numerical priority value; so in presence of at least one negative
         * priority, only DNS servers from connections with the lowest priority value will be used.
         * To avoid all DNS leaks, set the priority of the profile that should be used
         * to the most negative value of all active connections profiles.
         *
         * Zero selects a globally configured default value. If the latter is missing
         * or zero too, it defaults to 50 for VPNs (including WireGuard) and 100 for
         * other connections.
         *
         * Note that the priority is to order DNS settings for multiple active
         * connections.  It does not disambiguate multiple DNS servers within the
         * same connection profile.
         *
         * When multiple devices have configurations with the same priority, VPNs will be
         * considered first, then devices with the best (lowest metric) default
         * route and then all other devices.
         *
         * When using dns=default, servers with higher priority will be on top of
         * resolv.conf. To prioritize a given server over another one within the
         * same connection, just specify them in the desired order.
         * Note that commonly the resolver tries name servers in /etc/resolv.conf
         * in the order listed, proceeding with the next server in the list
         * on failure. See for example the "rotate" option of the dns-options setting.
         * If there are any negative DNS priorities, then only name servers from
         * the devices with that lowest priority will be considered.
         *
         * When using a DNS resolver that supports Conditional Forwarding or
         * Split DNS (with dns=dnsmasq or dns=systemd-resolved settings), each connection
         * is used to query domains in its search list. The search domains determine which
         * name servers to ask, and the DNS priority is used to prioritize
         * name servers based on the domain.  Queries for domains not present in any
         * search list are routed through connections having the '~.' special wildcard
         * domain, which is added automatically to connections with the default route
         * (or can be added manually).  When multiple connections specify the same domain, the
         * one with the best priority (lowest numerical value) wins.  If a sub domain
         * is configured on another interface it will be accepted regardless the priority,
         * unless parent domain on the other interface has a negative priority, which causes
         * the sub domain to be shadowed.
         * With Split DNS one can avoid undesired DNS leaks by properly configuring
         * DNS priorities and the search domains, so that only name servers of the desired
         * interface are configured.
         */
        get dnsPriority(): number;
        set dnsPriority(val: number);
        /**
         * List of DNS search domains. Domains starting with a tilde ('~')
         * are considered 'routing' domains and are used only to decide the
         * interface over which a query must be forwarded; they are not used
         * to complete unqualified host names.
         *
         * When using a DNS plugin that supports Conditional Forwarding or
         * Split DNS, then the search domains specify which name servers to
         * query. This makes the behavior different from running with plain
         * /etc/resolv.conf. For more information see also the dns-priority setting.
         *
         * When set on a profile that also enabled DHCP, the DNS search list
         * received automatically (option 119 for DHCPv4 and option 24 for DHCPv6)
         * gets merged with the manual list. This can be prevented by setting
         * "ignore-auto-dns". Note that if no DNS searches are configured, the
         * fallback will be derived from the domain from DHCP (option 15).
         */
        get dns_search(): string[];
        set dns_search(val: string[]);
        /**
         * List of DNS search domains. Domains starting with a tilde ('~')
         * are considered 'routing' domains and are used only to decide the
         * interface over which a query must be forwarded; they are not used
         * to complete unqualified host names.
         *
         * When using a DNS plugin that supports Conditional Forwarding or
         * Split DNS, then the search domains specify which name servers to
         * query. This makes the behavior different from running with plain
         * /etc/resolv.conf. For more information see also the dns-priority setting.
         *
         * When set on a profile that also enabled DHCP, the DNS search list
         * received automatically (option 119 for DHCPv4 and option 24 for DHCPv6)
         * gets merged with the manual list. This can be prevented by setting
         * "ignore-auto-dns". Note that if no DNS searches are configured, the
         * fallback will be derived from the domain from DHCP (option 15).
         */
        get dnsSearch(): string[];
        set dnsSearch(val: string[]);
        /**
         * Whether to configure sysctl interface-specific forwarding. When enabled, the interface
         * will act as a router to forward the packet from one interface to another. When set to
         * %NM_SETTING_IP_CONFIG_FORWARDING_DEFAULT, the value from global configuration is used;
         * if no global default is defined, %NM_SETTING_IP_CONFIG_FORWARDING_AUTO will be used.
         * The #NMSettingIPConfig:forwarding property is ignored when #NMSettingIPConfig:method
         * is set to "shared", because forwarding is always enabled in this case.
         * The accepted values are:
         *   %NM_SETTING_IP_CONFIG_FORWARDING_DEFAULT: use global default.
         *   %NM_SETTING_IP_CONFIG_FORWARDING_NO: disabled.
         *   %NM_SETTING_IP_CONFIG_FORWARDING_YES: enabled.
         *   %NM_SETTING_IP_CONFIG_FORWARDING_AUTO: enable if any shared connection is active,
         *        use kernel default otherwise.
         */
        get forwarding(): number;
        set forwarding(val: number);
        /**
         * The gateway associated with this configuration. This is only meaningful
         * if #NMSettingIPConfig:addresses is also set.
         *
         * Setting the gateway causes NetworkManager to configure a standard default route
         * with the gateway as next hop. This is ignored if #NMSettingIPConfig:never-default
         * is set. An alternative is to configure the default route explicitly with a manual
         * route and /0 as prefix length.
         *
         * Note that the gateway usually conflicts with routing that NetworkManager configures
         * for WireGuard interfaces, so usually it should not be set in that case. See
         * #NMSettingWireGuard:ip4-auto-default-route.
         */
        get gateway(): string;
        set gateway(val: string);
        /**
         * When #NMSettingIPConfig:method is set to "auto" and this property to
         * %TRUE, automatically configured name servers and search domains are
         * ignored and only name servers and search domains specified in the
         * #NMSettingIPConfig:dns and #NMSettingIPConfig:dns-search properties, if
         * any, are used.
         */
        get ignore_auto_dns(): boolean;
        set ignore_auto_dns(val: boolean);
        /**
         * When #NMSettingIPConfig:method is set to "auto" and this property to
         * %TRUE, automatically configured name servers and search domains are
         * ignored and only name servers and search domains specified in the
         * #NMSettingIPConfig:dns and #NMSettingIPConfig:dns-search properties, if
         * any, are used.
         */
        get ignoreAutoDns(): boolean;
        set ignoreAutoDns(val: boolean);
        /**
         * When #NMSettingIPConfig:method is set to "auto" and this property to
         * %TRUE, automatically configured routes are ignored and only routes
         * specified in the #NMSettingIPConfig:routes property, if any, are used.
         */
        get ignore_auto_routes(): boolean;
        set ignore_auto_routes(val: boolean);
        /**
         * When #NMSettingIPConfig:method is set to "auto" and this property to
         * %TRUE, automatically configured routes are ignored and only routes
         * specified in the #NMSettingIPConfig:routes property, if any, are used.
         */
        get ignoreAutoRoutes(): boolean;
        set ignoreAutoRoutes(val: boolean);
        /**
         * If %TRUE, allow overall network configuration to proceed even if the
         * configuration specified by this property times out.  Note that at least
         * one IP configuration must succeed or overall network configuration will
         * still fail.  For example, in IPv6-only networks, setting this property to
         * %TRUE on the #NMSettingIP4Config allows the overall network configuration
         * to succeed if IPv4 configuration fails but IPv6 configuration completes
         * successfully.
         */
        get may_fail(): boolean;
        set may_fail(val: boolean);
        /**
         * If %TRUE, allow overall network configuration to proceed even if the
         * configuration specified by this property times out.  Note that at least
         * one IP configuration must succeed or overall network configuration will
         * still fail.  For example, in IPv6-only networks, setting this property to
         * %TRUE on the #NMSettingIP4Config allows the overall network configuration
         * to succeed if IPv4 configuration fails but IPv6 configuration completes
         * successfully.
         */
        get mayFail(): boolean;
        set mayFail(val: boolean);
        /**
         * IP configuration method.
         *
         * #NMSettingIP4Config and #NMSettingIP6Config both support "disabled",
         * "auto", "manual", and "link-local". See the subclass-specific
         * documentation for other values.
         *
         * In general, for the "auto" method, properties such as
         * #NMSettingIPConfig:dns and #NMSettingIPConfig:routes specify information
         * that is added on to the information returned from automatic
         * configuration.  The #NMSettingIPConfig:ignore-auto-routes and
         * #NMSettingIPConfig:ignore-auto-dns properties modify this behavior.
         *
         * For methods that imply no upstream network, such as "shared" or
         * "link-local", these properties must be empty.
         *
         * For IPv4 method "shared", the IP subnet can be configured by adding one
         * manual IPv4 address or otherwise 10.42.x.0/24 is chosen. Note that the
         * shared method must be configured on the interface which shares the internet
         * to a subnet, not on the uplink which is shared.
         */
        get method(): string;
        set method(val: string);
        /**
         * If %TRUE, this connection will never be the default connection for this
         * IP type, meaning it will never be assigned the default route by
         * NetworkManager.
         */
        get never_default(): boolean;
        set never_default(val: boolean);
        /**
         * If %TRUE, this connection will never be the default connection for this
         * IP type, meaning it will never be assigned the default route by
         * NetworkManager.
         */
        get neverDefault(): boolean;
        set neverDefault(val: boolean);
        /**
         * Connections will default to keep the autogenerated priority 0 local rule
         * unless this setting is set to %TRUE.
         */
        get replace_local_rule(): NM.Ternary;
        set replace_local_rule(val: NM.Ternary);
        /**
         * Connections will default to keep the autogenerated priority 0 local rule
         * unless this setting is set to %TRUE.
         */
        get replaceLocalRule(): NM.Ternary;
        set replaceLocalRule(val: NM.Ternary);
        /**
         * The minimum time interval in milliseconds for which dynamic IP configuration
         * should be tried before the connection succeeds.
         *
         * This property is useful for example if both IPv4 and IPv6 are enabled and
         * are allowed to fail. Normally the connection succeeds as soon as one of
         * the two address families completes; by setting a required timeout for
         * e.g. IPv4, one can ensure that even if IP6 succeeds earlier than IPv4,
         * NetworkManager waits some time for IPv4 before the connection becomes
         * active.
         *
         * Note that if #NMSettingIPConfig:may-fail is FALSE for the same address
         * family, this property has no effect as NetworkManager needs to wait for
         * the full DHCP timeout.
         *
         * A zero value means that no required timeout is present, -1 means the
         * default value (either configuration ipvx.required-timeout override or
         * zero).
         */
        get required_timeout(): number;
        set required_timeout(val: number);
        /**
         * The minimum time interval in milliseconds for which dynamic IP configuration
         * should be tried before the connection succeeds.
         *
         * This property is useful for example if both IPv4 and IPv6 are enabled and
         * are allowed to fail. Normally the connection succeeds as soon as one of
         * the two address families completes; by setting a required timeout for
         * e.g. IPv4, one can ensure that even if IP6 succeeds earlier than IPv4,
         * NetworkManager waits some time for IPv4 before the connection becomes
         * active.
         *
         * Note that if #NMSettingIPConfig:may-fail is FALSE for the same address
         * family, this property has no effect as NetworkManager needs to wait for
         * the full DHCP timeout.
         *
         * A zero value means that no required timeout is present, -1 means the
         * default value (either configuration ipvx.required-timeout override or
         * zero).
         */
        get requiredTimeout(): number;
        set requiredTimeout(val: number);
        /**
         * The default metric for routes that don't explicitly specify a metric.
         * The default value -1 means that the metric is chosen automatically
         * based on the device type.
         * The metric applies to dynamic routes, manual (static) routes that
         * don't have an explicit metric setting, address prefix routes, and
         * the default route.
         * Note that for IPv6, the kernel accepts zero (0) but coerces it to
         * 1024 (user default). Hence, setting this property to zero effectively
         * mean setting it to 1024.
         * For IPv4, zero is a regular value for the metric.
         */
        get route_metric(): number;
        set route_metric(val: number);
        /**
         * The default metric for routes that don't explicitly specify a metric.
         * The default value -1 means that the metric is chosen automatically
         * based on the device type.
         * The metric applies to dynamic routes, manual (static) routes that
         * don't have an explicit metric setting, address prefix routes, and
         * the default route.
         * Note that for IPv6, the kernel accepts zero (0) but coerces it to
         * 1024 (user default). Hence, setting this property to zero effectively
         * mean setting it to 1024.
         * For IPv4, zero is a regular value for the metric.
         */
        get routeMetric(): number;
        set routeMetric(val: number);
        /**
         * Enable policy routing (source routing) and set the routing table used when adding routes.
         *
         * This affects all routes, including device-routes, IPv4LL, DHCP, SLAAC, default-routes
         * and static routes. But note that static routes can individually overwrite the setting
         * by explicitly specifying a non-zero routing table.
         *
         * If the table setting is left at zero, it is eligible to be overwritten via global
         * configuration. If the property is zero even after applying the global configuration
         * value, policy routing is disabled for the address family of this connection.
         *
         * Policy routing disabled means that NetworkManager will add all routes to the main
         * table (except static routes that explicitly configure a different table). Additionally,
         * NetworkManager will not delete any extraneous routes from tables except the main table.
         * This is to preserve backward compatibility for users who manage routing tables outside
         * of NetworkManager.
         */
        get route_table(): number;
        set route_table(val: number);
        /**
         * Enable policy routing (source routing) and set the routing table used when adding routes.
         *
         * This affects all routes, including device-routes, IPv4LL, DHCP, SLAAC, default-routes
         * and static routes. But note that static routes can individually overwrite the setting
         * by explicitly specifying a non-zero routing table.
         *
         * If the table setting is left at zero, it is eligible to be overwritten via global
         * configuration. If the property is zero even after applying the global configuration
         * value, policy routing is disabled for the address family of this connection.
         *
         * Policy routing disabled means that NetworkManager will add all routes to the main
         * table (except static routes that explicitly configure a different table). Additionally,
         * NetworkManager will not delete any extraneous routes from tables except the main table.
         * This is to preserve backward compatibility for users who manage routing tables outside
         * of NetworkManager.
         */
        get routeTable(): number;
        set routeTable(val: number);
        /**
         * Whether to add routes for DNS servers. When enabled, NetworkManager adds a route
         * for each DNS server that is associated with this connection either statically
         * (defined in the connection profile) or dynamically (for example, retrieved via
         * DHCP). The route guarantees that the DNS server is reached via this interface. When
         * set to %NM_SETTING_IP_CONFIG_ROUTED_DNS_DEFAULT, the value from global
         * configuration is used; if no global default is defined, this feature is disabled.
         */
        get routed_dns(): number;
        set routed_dns(val: number);
        /**
         * Whether to add routes for DNS servers. When enabled, NetworkManager adds a route
         * for each DNS server that is associated with this connection either statically
         * (defined in the connection profile) or dynamically (for example, retrieved via
         * DHCP). The route guarantees that the DNS server is reached via this interface. When
         * set to %NM_SETTING_IP_CONFIG_ROUTED_DNS_DEFAULT, the value from global
         * configuration is used; if no global default is defined, this feature is disabled.
         */
        get routedDns(): number;
        set routedDns(val: number);
        /**
         * Array of IP routes.
         */
        get routes(): NM.IPRoute[];
        set routes(val: NM.IPRoute[]);
        /**
         * This option allows you to specify a custom DHCP lease time for the shared connection
         * method in seconds. The value should be either a number between 120 and 31536000 (one year)
         * If this option is not specified, 3600 (one hour) is used.
         *
         * Special values are 0 for default value of 1 hour and 2147483647 (MAXINT32) for infinite lease time.
         */
        get shared_dhcp_lease_time(): number;
        set shared_dhcp_lease_time(val: number);
        /**
         * This option allows you to specify a custom DHCP lease time for the shared connection
         * method in seconds. The value should be either a number between 120 and 31536000 (one year)
         * If this option is not specified, 3600 (one hour) is used.
         *
         * Special values are 0 for default value of 1 hour and 2147483647 (MAXINT32) for infinite lease time.
         */
        get sharedDhcpLeaseTime(): number;
        set sharedDhcpLeaseTime(val: number);
        /**
         * This option allows you to specify a custom DHCP range for the shared connection
         * method. The value is expected to be in `<START_ADDRESS>,<END_ADDRESS>` format.
         * The range should be part of network set by ipv4.address option and it should
         * not contain network address or broadcast address. If this option is not specified,
         * the DHCP range will be automatically determined based on the interface address.
         * The range will be selected to be adjacent to the interface address, either before
         * or after it, with the larger possible range being preferred. The range will be
         * adjusted to fill the available address space, except for networks with a prefix
         * length greater than 24, which will be treated as if they have a prefix length of 24.
         */
        get shared_dhcp_range(): string;
        set shared_dhcp_range(val: string);
        /**
         * This option allows you to specify a custom DHCP range for the shared connection
         * method. The value is expected to be in `<START_ADDRESS>,<END_ADDRESS>` format.
         * The range should be part of network set by ipv4.address option and it should
         * not contain network address or broadcast address. If this option is not specified,
         * the DHCP range will be automatically determined based on the interface address.
         * The range will be selected to be adjacent to the interface address, either before
         * or after it, with the larger possible range being preferred. The range will be
         * adjusted to fill the available address space, except for networks with a prefix
         * length greater than 24, which will be treated as if they have a prefix length of 24.
         */
        get sharedDhcpRange(): string;
        set sharedDhcpRange(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingIPConfig.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingIPConfig.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.SettingIPConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIPConfig.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingIPConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIPConfig.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingIPConfig.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingIPConfig.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a new IP address and associated information to the setting.  The
         * given address is duplicated internally and is not changed by this function.
         *
         * @returns %TRUE if the address was added; %FALSE if the address was already
         * known.
         * @param address the new address to add
         */
        add_address(address: NM.IPAddress): boolean;
        /**
         * Adds a new DHCP reject server to the setting.
         *
         * @param server the DHCP reject server to add
         */
        add_dhcp_reject_server(server: string): void;
        /**
         * Adds a new DNS server to the setting.
         *
         * @returns %TRUE if the DNS server was added; %FALSE if the server was already
         * known
         *
         * Before 1.42, setting `dns` to an invalid string was treated as user-error.
         * Now, also invalid DNS values can be set, but will be rejected later during
         * nm_connection_verify().
         * @param dns the IP address of the DNS server to add
         */
        add_dns(dns: string): boolean;
        /**
         * Adds a new DNS option to the setting.
         *
         * @returns %TRUE if the DNS option was added; %FALSE otherwise
         * @param dns_option the DNS option to add
         */
        add_dns_option(dns_option: string): boolean;
        /**
         * Adds a new DNS search domain to the setting.
         *
         * @returns %TRUE if the DNS search domain was added; %FALSE if the search
         * domain was already known
         * @param dns_search the search domain to add
         */
        add_dns_search(dns_search: string): boolean;
        /**
         * Appends a new route and associated information to the setting.  The
         * given route is duplicated internally and is not changed by this function.
         * If an identical route (considering attributes as well) already exists, the
         * route is not added and the function returns %FALSE.
         *
         * Note that before 1.10, this function would not consider route attributes
         * and not add a route that has an existing route with same dest/prefix,next_hop,metric
         * parameters.
         *
         * @returns %TRUE if the route was added; %FALSE if the route was already known.
         * @param route the route to add
         */
        add_route(route: NM.IPRoute): boolean;
        /**
         * Appends a new routing-rule and associated information to the setting. The
         * given routing rules gets sealed and the reference count is incremented.
         * The function does not check whether an identical rule already exists
         * and always appends the rule to the end of the list.
         *
         * @param routing_rule the #NMIPRoutingRule to add. The address family
         *   of the added rule must be compatible with the setting.
         */
        add_routing_rule(routing_rule: NM.IPRoutingRule): void;
        /**
         * Removes all configured addresses.
         */
        clear_addresses(): void;
        /**
         * Removes all configured DHCP reject servers.
         */
        clear_dhcp_reject_servers(): void;
        /**
         * Removes all configured DNS servers.
         */
        clear_dns(): void;
        /**
         * Removes all configured DNS options.
         *
         * @param is_set the dns-options can be either empty or unset (default).
         *   Specify how to clear the options.
         */
        clear_dns_options(is_set: boolean): void;
        /**
         * Removes all configured DNS search domains.
         */
        clear_dns_searches(): void;
        /**
         * Removes all configured routes.
         */
        clear_routes(): void;
        /**
         * Removes all configured routing rules.
         */
        clear_routing_rules(): void;
        get_address(idx: number): NM.IPAddress;
        get_auto_route_ext_gw(): NM.Ternary;
        get_dad_timeout(): number;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-dscp
         * property.
         *
         * @returns the value for the DSCP field for DHCP
         */
        get_dhcp_dscp(): string;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-hostname
         * property.
         *
         * @returns the configured hostname to send to the DHCP server
         */
        get_dhcp_hostname(): string;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-hostname-flags
         * property.
         *
         * @returns flags for the DHCP hostname and FQDN
         */
        get_dhcp_hostname_flags(): NM.DhcpHostnameFlags;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-iaid
         * property.
         *
         * @returns the configured DHCP IAID (Identity Association Identifier)
         */
        get_dhcp_iaid(): string;
        get_dhcp_reject_servers(): string[];
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-send-hostname
         * property.
         *
         * @returns %TRUE if NetworkManager should send the machine hostname to the
         * DHCP server when requesting addresses to allow the server to automatically
         * update DNS information for this machine.
         */
        get_dhcp_send_hostname(): boolean;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-send-hostname-v2
         * property.
         *
         * @returns the #NMSettingIPConfig:dhcp-send-hostname-v2 property of the setting
         */
        get_dhcp_send_hostname_v2(): NM.Ternary;
        get_dhcp_send_release(): NM.Ternary;
        /**
         * Returns the value contained in the #NMSettingIPConfig:dhcp-timeout
         * property.
         *
         * @returns the configured DHCP timeout in seconds. 0 = default for
         * the particular kind of device.
         */
        get_dhcp_timeout(): number;
        get_dns(idx: number): string;
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the DNS option at index `idx`
         * @param idx index number of the DNS option
         */
        get_dns_option(idx: number): string;
        get_dns_priority(): number;
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the DNS search domain at index `idx`
         * @param idx index number of the DNS search domain to return
         */
        get_dns_search(idx: number): string;
        get_forwarding(): NM.SettingIPConfigForwarding;
        get_gateway(): string;
        /**
         * Returns the value contained in the #NMSettingIPConfig:ignore-auto-dns
         * property.
         *
         * @returns %TRUE if automatically configured (ie via DHCP) DNS information
         * should be ignored.
         */
        get_ignore_auto_dns(): boolean;
        /**
         * Returns the value contained in the #NMSettingIPConfig:ignore-auto-routes
         * property.
         *
         * @returns %TRUE if automatically configured (ie via DHCP) routes should be
         * ignored.
         */
        get_ignore_auto_routes(): boolean;
        /**
         * Returns the value contained in the #NMSettingIPConfig:may-fail
         * property.
         *
         * @returns %TRUE if this connection doesn't require this type of IP
         * addressing to complete for the connection to succeed.
         */
        get_may_fail(): boolean;
        get_method(): string;
        /**
         * Returns the value contained in the #NMSettingIPConfig:never-default
         * property.
         *
         * @returns %TRUE if this connection should never be the default
         *   connection
         */
        get_never_default(): boolean;
        get_num_addresses(): number;
        get_num_dns(): number;
        get_num_dns_options(): number;
        get_num_dns_searches(): number;
        get_num_routes(): number;
        get_num_routing_rules(): number;
        get_replace_local_rule(): NM.Ternary;
        /**
         * Returns the value contained in the #NMSettingIPConfig:required-timeout
         * property.
         *
         * @returns the required timeout for the address family
         */
        get_required_timeout(): number;
        get_route(idx: number): NM.IPRoute;
        /**
         * Returns the value contained in the #NMSettingIPConfig:route-metric
         * property.
         *
         * @returns the route metric that is used for routes that don't explicitly
         * specify a metric. See #NMSettingIPConfig:route-metric for more details.
         */
        get_route_metric(): number;
        /**
         * Returns the value contained in the #NMSettingIPConfig:route-table
         * property.
         *
         * @returns the configured route-table.
         */
        get_route_table(): number;
        get_routed_dns(): NM.SettingIPConfigRoutedDns;
        get_routing_rule(idx: number): NM.IPRoutingRule;
        /**
         * Returns the value contained in the #NMSettingIPConfig:shared-dhcp-lease-time
         * property.
         *
         * @returns the configured DHCP server lease time
         */
        get_shared_dhcp_lease_time(): number;
        /**
         * Returns the value contained in the #NMSettingIPConfig:shared-dhcp-range
         * property.
         *
         * @returns the configured DHCP server range
         */
        get_shared_dhcp_range(): string;
        /**
         * NMSettingIPConfig can have a list of dns-options. If the list
         * is empty, there are two similar (but differentiated) states.
         * Either the options are explicitly set to have no values,
         * or the options are left undefined. The latter means to use
         * a default configuration, while the former explicitly means "no-options".
         *
         * @returns whether DNS options are initialized or left unset (the default).
         */
        has_dns_options(): boolean;
        /**
         * Removes the address at index `idx`.
         *
         * @param idx index number of the address to remove
         */
        remove_address(idx: number): void;
        /**
         * Removes the address `address`.
         *
         * @returns %TRUE if the address was found and removed; %FALSE if it was not.
         * @param address the IP address to remove
         */
        remove_address_by_value(address: NM.IPAddress): boolean;
        /**
         * Removes the DHCP reject server at index `idx`.
         *
         * @param idx index number of the DHCP reject server
         */
        remove_dhcp_reject_server(idx: number): void;
        /**
         * Removes the DNS server at index `idx`.
         *
         * @param idx index number of the DNS server to remove
         */
        remove_dns(idx: number): void;
        /**
         * Removes the DNS server `dns`.
         *
         * @returns %TRUE if the DNS server was found and removed; %FALSE if it was not.
         *
         * Before 1.42, setting `dns` to an invalid string was treated as user-error.
         * @param dns the DNS server to remove
         */
        remove_dns_by_value(dns: string): boolean;
        /**
         * Removes the DNS option at index `idx`.
         *
         * @param idx index number of the DNS option
         */
        remove_dns_option(idx: number): void;
        /**
         * Removes the DNS option `dns_option`.
         *
         * @returns %TRUE if the DNS option was found and removed; %FALSE if it was not.
         * @param dns_option the DNS option to remove
         */
        remove_dns_option_by_value(dns_option: string): boolean;
        /**
         * Removes the DNS search domain at index `idx`.
         *
         * @param idx index number of the DNS search domain
         */
        remove_dns_search(idx: number): void;
        /**
         * Removes the DNS search domain `dns_search`.
         *
         * @returns %TRUE if the DNS search domain was found and removed; %FALSE if it was not.
         * @param dns_search the search domain to remove
         */
        remove_dns_search_by_value(dns_search: string): boolean;
        /**
         * Removes the route at index `idx`.
         *
         * @param idx index number of the route
         */
        remove_route(idx: number): void;
        /**
         * Removes the first matching route that matches `route`.
         * Note that before 1.10, this function would only compare dest/prefix,next_hop,metric
         * and ignore route attributes. Now, `route` must match exactly.
         *
         * @returns %TRUE if the route was found and removed; %FALSE if it was not.
         * @param route the route to remove
         */
        remove_route_by_value(route: NM.IPRoute): boolean;
        /**
         * Removes the routing_rule at index `idx`.
         *
         * @param idx index number of the routing_rule
         */
        remove_routing_rule(idx: number): void;
    }
    class SettingIPTunnel extends Setting {
        static '$gtype': GObject.GType<SettingIPTunnel>;
        // Properties
        /**
         * How many additional levels of encapsulation are permitted to be prepended
         * to packets. This property applies only to IPv6 tunnels. To disable this option,
         * add %NM_IP_TUNNEL_FLAG_IP6_IGN_ENCAP_LIMIT to ip-tunnel flags.
         */
        get encapsulation_limit(): number;
        set encapsulation_limit(val: number);
        /**
         * How many additional levels of encapsulation are permitted to be prepended
         * to packets. This property applies only to IPv6 tunnels. To disable this option,
         * add %NM_IP_TUNNEL_FLAG_IP6_IGN_ENCAP_LIMIT to ip-tunnel flags.
         */
        get encapsulationLimit(): number;
        set encapsulationLimit(val: number);
        /**
         * Tunnel flags. Currently, the following values are supported:
         * %NM_IP_TUNNEL_FLAG_IP6_IGN_ENCAP_LIMIT, %NM_IP_TUNNEL_FLAG_IP6_USE_ORIG_TCLASS,
         * %NM_IP_TUNNEL_FLAG_IP6_USE_ORIG_FLOWLABEL, %NM_IP_TUNNEL_FLAG_IP6_MIP6_DEV,
         * %NM_IP_TUNNEL_FLAG_IP6_RCV_DSCP_COPY, %NM_IP_TUNNEL_FLAG_IP6_USE_ORIG_FWMARK.
         * They are valid only for IPv6 tunnels.
         */
        get flags(): number;
        set flags(val: number);
        /**
         * The flow label to assign to tunnel packets. This property applies only to
         * IPv6 tunnels.
         */
        get flow_label(): number;
        set flow_label(val: number);
        /**
         * The flow label to assign to tunnel packets. This property applies only to
         * IPv6 tunnels.
         */
        get flowLabel(): number;
        set flowLabel(val: number);
        /**
         * The fwmark value to assign to tunnel packets. This property can be set
         * to a non zero value only on VTI and VTI6 tunnels.
         */
        get fwmark(): number;
        set fwmark(val: number);
        /**
         * The key used for tunnel input packets; the property is valid only for
         * certain tunnel modes (GRE, IP6GRE). If empty, no key is used.
         */
        get input_key(): string;
        set input_key(val: string);
        /**
         * The key used for tunnel input packets; the property is valid only for
         * certain tunnel modes (GRE, IP6GRE). If empty, no key is used.
         */
        get inputKey(): string;
        set inputKey(val: string);
        /**
         * The local endpoint of the tunnel; the value can be empty, otherwise it
         * must contain an IPv4 or IPv6 address.
         */
        get local(): string;
        set local(val: string);
        /**
         * The tunneling mode. Valid values: %NM_IP_TUNNEL_MODE_IPIP,
         * %NM_IP_TUNNEL_MODE_GRE, %NM_IP_TUNNEL_MODE_SIT, %NM_IP_TUNNEL_MODE_ISATAP,
         * %NM_IP_TUNNEL_MODE_VTI, %NM_IP_TUNNEL_MODE_IP6IP6, %NM_IP_TUNNEL_MODE_IPIP6,
         * %NM_IP_TUNNEL_MODE_IP6GRE, %NM_IP_TUNNEL_MODE_VTI6, %NM_IP_TUNNEL_MODE_GRETAP
         * and %NM_IP_TUNNEL_MODE_IP6GRETAP
         */
        get mode(): number;
        set mode(val: number);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple fragments.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * The key used for tunnel output packets; the property is valid only for
         * certain tunnel modes (GRE, IP6GRE). If empty, no key is used.
         */
        get output_key(): string;
        set output_key(val: string);
        /**
         * The key used for tunnel output packets; the property is valid only for
         * certain tunnel modes (GRE, IP6GRE). If empty, no key is used.
         */
        get outputKey(): string;
        set outputKey(val: string);
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * the new device will be bound to so that tunneled packets will only be
         * routed via that interface.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Whether to enable Path MTU Discovery on this tunnel.
         */
        get path_mtu_discovery(): boolean;
        set path_mtu_discovery(val: boolean);
        /**
         * Whether to enable Path MTU Discovery on this tunnel.
         */
        get pathMtuDiscovery(): boolean;
        set pathMtuDiscovery(val: boolean);
        /**
         * The remote endpoint of the tunnel; the value must contain an IPv4 or IPv6
         * address.
         */
        get remote(): string;
        set remote(val: string);
        /**
         * The type of service (IPv4) or traffic class (IPv6) field to be set on
         * tunneled packets.
         */
        get tos(): number;
        set tos(val: number);
        /**
         * The TTL to assign to tunneled packets. 0 is a special value meaning that
         * packets inherit the TTL value.
         */
        get ttl(): number;
        set ttl(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingIPTunnel.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingIPTunnel.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingIPTunnel;
        // Signals
        connect<K extends keyof NM.SettingIPTunnel.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIPTunnel.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingIPTunnel.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIPTunnel.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingIPTunnel.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingIPTunnel.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the #NMSettingIPTunnel:encapsulation-limit property of the setting.
         *
         * @returns the encapsulation limit value
         */
        get_encapsulation_limit(): number;
        /**
         * Returns the #NMSettingIPTunnel:flags property of the setting.
         *
         * @returns the tunnel flags
         */
        get_flags(): NM.IPTunnelFlags;
        /**
         * Returns the #NMSettingIPTunnel:flow-label property of the setting.
         *
         * @returns the flow label value
         */
        get_flow_label(): number;
        /**
         * Returns the #NMSettingIPTunnel:fwmark property of the setting.
         *
         * @returns the fwmark value
         */
        get_fwmark(): number;
        /**
         * Returns the #NMSettingIPTunnel:input-key property of the setting.
         *
         * @returns the input key
         */
        get_input_key(): string;
        /**
         * Returns the #NMSettingIPTunnel:local property of the setting.
         *
         * @returns the local endpoint
         */
        get_local(): string;
        /**
         * Returns the #NMSettingIPTunnel:mode property of the setting.
         *
         * @returns the tunnel mode
         */
        get_mode(): NM.IPTunnelMode;
        /**
         * Returns the #NMSettingIPTunnel:mtu property of the setting.
         *
         * @returns the MTU
         */
        get_mtu(): number;
        /**
         * Returns the #NMSettingIPTunnel:output-key property of the setting.
         *
         * @returns the output key
         */
        get_output_key(): string;
        /**
         * Returns the #NMSettingIPTunnel:parent property of the setting
         *
         * @returns the parent device
         */
        get_parent(): string;
        /**
         * Returns the #NMSettingIPTunnel:path-mtu-discovery property of the setting.
         *
         * @returns whether path MTU discovery is enabled
         */
        get_path_mtu_discovery(): boolean;
        /**
         * Returns the #NMSettingIPTunnel:remote property of the setting.
         *
         * @returns the remote endpoint
         */
        get_remote(): string;
        /**
         * Returns the #NMSettingIPTunnel:tos property of the setting.
         *
         * @returns the TOS value
         */
        get_tos(): number;
        /**
         * Returns the #NMSettingIPTunnel:ttl property of the setting.
         *
         * @returns the Time-to-live value
         */
        get_ttl(): number;
    }
    class SettingInfiniband extends Setting {
        static '$gtype': GObject.GType<SettingInfiniband>;
        // Properties
        /**
         * If specified, this connection will only apply to the IPoIB device whose
         * permanent MAC address matches. This property does not change the MAC
         * address of the device (i.e. MAC spoofing).
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, this connection will only apply to the IPoIB device whose
         * permanent MAC address matches. This property does not change the MAC
         * address of the device (i.e. MAC spoofing).
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * The InfiniBand p-key to use for this device. A value of -1 means to use
         * the default p-key (aka "the p-key at index 0"). Otherwise, it is a
         * 16-bit unsigned integer, whose high bit 0x8000 is set if it is a "full
         * membership" p-key. The values 0 and 0x8000 are not allowed.
         *
         * With the p-key set, the interface name is always "$parent.$p_key".
         * Setting "connection.interface-name" to another name is not supported.
         *
         * Note that kernel will internally always set the full membership bit,
         * although the interface name does not reflect that. Usually the user
         * would want to configure a full membership p-key with 0x8000 flag set.
         */
        get p_key(): number;
        set p_key(val: number);
        /**
         * The InfiniBand p-key to use for this device. A value of -1 means to use
         * the default p-key (aka "the p-key at index 0"). Otherwise, it is a
         * 16-bit unsigned integer, whose high bit 0x8000 is set if it is a "full
         * membership" p-key. The values 0 and 0x8000 are not allowed.
         *
         * With the p-key set, the interface name is always "$parent.$p_key".
         * Setting "connection.interface-name" to another name is not supported.
         *
         * Note that kernel will internally always set the full membership bit,
         * although the interface name does not reflect that. Usually the user
         * would want to configure a full membership p-key with 0x8000 flag set.
         */
        get pKey(): number;
        set pKey(val: number);
        /**
         * The interface name of the parent device of this device. Normally %NULL,
         * but if the #NMSettingInfiniband:p_key property is set, then you must
         * specify the base device by setting either this property or
         * #NMSettingInfiniband:mac-address.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * The IP-over-InfiniBand transport mode. Either "datagram" or
         * "connected".
         */
        get transport_mode(): string;
        set transport_mode(val: string);
        /**
         * The IP-over-InfiniBand transport mode. Either "datagram" or
         * "connected".
         */
        get transportMode(): string;
        set transportMode(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingInfiniband.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingInfiniband.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingInfiniband;
        // Signals
        connect<K extends keyof NM.SettingInfiniband.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingInfiniband.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingInfiniband.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingInfiniband.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingInfiniband.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingInfiniband.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_mac_address(): string;
        get_mtu(): number;
        /**
         * Returns the P_Key to use for this device. A value of -1 means to
         * use the default P_Key (aka "the P_Key at index 0"). Otherwise, it is
         * a 16-bit unsigned integer.
         *
         * @returns the IPoIB P_Key
         */
        get_p_key(): number;
        /**
         * Returns the parent interface name for this device, if set.
         *
         * @returns the parent interface name
         */
        get_parent(): string;
        /**
         * Returns the transport mode for this device. Either 'datagram' or
         * 'connected'.
         *
         * @returns the IPoIB transport mode
         */
        get_transport_mode(): string;
        /**
         * Returns the interface name created by combining #NMSettingInfiniband:parent
         * and #NMSettingInfiniband:p-key. (If either property is unset, this will
         * return %NULL.)
         *
         * @returns the interface name, or %NULL
         */
        get_virtual_interface_name(): string;
    }
    class SettingIpvlan extends Setting {
        static '$gtype': GObject.GType<SettingIpvlan>;
        // Properties
        /**
         * The IPVLAN mode. Valid values: %NM_SETTING_IPVLAN_MODE_L2,
         * %NM_SETTING_IPVLAN_MODE_L3 and %NM_SETTING_IPVLAN_MODE_L3S.
         */
        get mode(): number;
        set mode(val: number);
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * from which this IPVLAN interface should be created. If this property is
         * not specified, the connection must contain an #NMSettingWired setting
         * with a #NMSettingWired:mac-address property.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Whether the interface should be put in private mode.
         */
        get private(): boolean;
        set private(val: boolean);
        /**
         * Whether the interface should be put in VEPA mode.
         */
        get vepa(): boolean;
        set vepa(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingIpvlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingIpvlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingIpvlan;
        // Signals
        connect<K extends keyof NM.SettingIpvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIpvlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingIpvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingIpvlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingIpvlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingIpvlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_mode(): NM.SettingIpvlanMode;
        get_parent(): string;
        get_private(): boolean;
        get_vepa(): boolean;
    }
    class SettingLink extends Setting {
        static '$gtype': GObject.GType<SettingLink>;
        // Properties
        /**
         * The maximum size of a packet built by the Generic Receive Offload stack for
         * this device. The value must be between 0 and 4294967295. When set to -1, the
         * existing value is preserved.
         */
        get gro_max_size(): number;
        set gro_max_size(val: number);
        /**
         * The maximum size of a packet built by the Generic Receive Offload stack for
         * this device. The value must be between 0 and 4294967295. When set to -1, the
         * existing value is preserved.
         */
        get groMaxSize(): number;
        set groMaxSize(val: number);
        /**
         * The maximum segments of a Generic Segment Offload packet the device should accept.
         * The value must be between 0 and 4294967295. When set to -1, the existing value
         * is preserved.
         */
        get gso_max_segments(): number;
        set gso_max_segments(val: number);
        /**
         * The maximum segments of a Generic Segment Offload packet the device should accept.
         * The value must be between 0 and 4294967295. When set to -1, the existing value
         * is preserved.
         */
        get gsoMaxSegments(): number;
        set gsoMaxSegments(val: number);
        /**
         * The maximum size of a Generic Segment Offload packet the device should accept.
         * The value must be between 0 and 4294967295. When set to -1, the existing value
         * is preserved.
         */
        get gso_max_size(): number;
        set gso_max_size(val: number);
        /**
         * The maximum size of a Generic Segment Offload packet the device should accept.
         * The value must be between 0 and 4294967295. When set to -1, the existing value
         * is preserved.
         */
        get gsoMaxSize(): number;
        set gsoMaxSize(val: number);
        /**
         * The size of the transmit queue for the device, in number of packets. The value
         * must be between 0 and 4294967295. When set to -1, the existing value is preserved.
         */
        get tx_queue_length(): number;
        set tx_queue_length(val: number);
        /**
         * The size of the transmit queue for the device, in number of packets. The value
         * must be between 0 and 4294967295. When set to -1, the existing value is preserved.
         */
        get txQueueLength(): number;
        set txQueueLength(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingLink.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingLink.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingLink;
        // Signals
        connect<K extends keyof NM.SettingLink.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingLink.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingLink.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingLink.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingLink.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingLink.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the value contained in the #NMSettingLink:gro-max-size
         * property.
         *
         * @returns the 'gro-max-size' property value
         */
        get_gro_max_size(): number;
        /**
         * Returns the value contained in the #NMSettingLink:gso-max-segments
         * property.
         *
         * @returns the 'gso-max-segments' property value
         */
        get_gso_max_segments(): number;
        /**
         * Returns the value contained in the #NMSettingLink:gso-max-size
         * property.
         *
         * @returns the 'gso-max-size' property value
         */
        get_gso_max_size(): number;
        /**
         * Returns the value contained in the #NMSettingLink:tx-queue-length
         * property.
         *
         * @returns the 'tx-queue-length' property value
         */
        get_tx_queue_length(): number;
    }
    class SettingLoopback extends Setting {
        static '$gtype': GObject.GType<SettingLoopback>;
        // Properties
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple Ethernet frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingLoopback.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingLoopback.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingLoopback;
        // Signals
        connect<K extends keyof NM.SettingLoopback.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingLoopback.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingLoopback.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingLoopback.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingLoopback.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingLoopback.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_mtu(): number;
    }
    class SettingMacsec extends Setting {
        static '$gtype': GObject.GType<SettingMacsec>;
        // Properties
        /**
         * Whether the transmitted traffic must be encrypted.
         */
        get encrypt(): boolean;
        set encrypt(val: boolean);
        /**
         * The pre-shared CAK (Connectivity Association Key) for MACsec
         * Key Agreement. Must be a string of 32 hexadecimal characters.
         */
        get mka_cak(): string;
        set mka_cak(val: string);
        /**
         * The pre-shared CAK (Connectivity Association Key) for MACsec
         * Key Agreement. Must be a string of 32 hexadecimal characters.
         */
        get mkaCak(): string;
        set mkaCak(val: string);
        /**
         * Flags indicating how to handle the #NMSettingMacsec:mka-cak
         * property.
         */
        get mka_cak_flags(): NM.SettingSecretFlags;
        set mka_cak_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingMacsec:mka-cak
         * property.
         */
        get mkaCakFlags(): NM.SettingSecretFlags;
        set mkaCakFlags(val: NM.SettingSecretFlags);
        /**
         * The pre-shared CKN (Connectivity-association Key Name) for
         * MACsec Key Agreement. Must be a string of hexadecimal characters
         * with a even length between 2 and 64.
         */
        get mka_ckn(): string;
        set mka_ckn(val: string);
        /**
         * The pre-shared CKN (Connectivity-association Key Name) for
         * MACsec Key Agreement. Must be a string of hexadecimal characters
         * with a even length between 2 and 64.
         */
        get mkaCkn(): string;
        set mkaCkn(val: string);
        /**
         * Specifies how the CAK (Connectivity Association Key) for MKA (MACsec Key
         * Agreement) is obtained.
         */
        get mode(): number;
        set mode(val: number);
        /**
         * Specifies the MACsec offload mode.
         *
         * %NM_SETTING_MACSEC_OFFLOAD_OFF disables MACsec offload.
         *
         * %NM_SETTING_MACSEC_OFFLOAD_PHY and %NM_SETTING_MACSEC_OFFLOAD_MAC request offload
         * respectively to the PHY or to the MAC; if the selected mode is not available, the
         * connection will fail.
         *
         * %NM_SETTING_MACSEC_OFFLOAD_DEFAULT uses the global default value specified in
         * NetworkManager configuration; if no global default is defined, the built-in
         * default is %NM_SETTING_MACSEC_OFFLOAD_OFF.
         */
        get offload(): number;
        set offload(val: number);
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * from which this MACSEC interface should be created.  If this property is
         * not specified, the connection must contain an #NMSettingWired setting
         * with a #NMSettingWired:mac-address property.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * The port component of the SCI (Secure Channel Identifier), between 1 and 65534.
         */
        get port(): number;
        set port(val: number);
        /**
         * Specifies whether the SCI (Secure Channel Identifier) is included
         * in every packet.
         */
        get send_sci(): boolean;
        set send_sci(val: boolean);
        /**
         * Specifies whether the SCI (Secure Channel Identifier) is included
         * in every packet.
         */
        get sendSci(): boolean;
        set sendSci(val: boolean);
        /**
         * Specifies the validation mode for incoming frames.
         */
        get validation(): number;
        set validation(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingMacsec.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingMacsec.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingMacsec;
        // Signals
        connect<K extends keyof NM.SettingMacsec.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMacsec.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingMacsec.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMacsec.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingMacsec.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingMacsec.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_encrypt(): boolean;
        get_mka_cak(): string;
        get_mka_cak_flags(): NM.SettingSecretFlags;
        get_mka_ckn(): string;
        get_mode(): NM.SettingMacsecMode;
        get_offload(): NM.SettingMacsecOffload;
        get_parent(): string;
        get_port(): number;
        get_send_sci(): boolean;
        get_validation(): NM.SettingMacsecValidation;
    }
    class SettingMacvlan extends Setting {
        static '$gtype': GObject.GType<SettingMacvlan>;
        // Properties
        /**
         * The macvlan mode, which specifies the communication mechanism between multiple
         * macvlans on the same lower device.
         */
        get mode(): number;
        set mode(val: number);
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * from which this MAC-VLAN interface should be created.  If this property is
         * not specified, the connection must contain an #NMSettingWired setting
         * with a #NMSettingWired:mac-address property.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Whether the parent interface should be put in promiscuous mode (true by default).
         */
        get promiscuous(): boolean;
        set promiscuous(val: boolean);
        /**
         * Whether the interface should be a MACVTAP.
         */
        get tap(): boolean;
        set tap(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingMacvlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingMacvlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingMacvlan;
        // Signals
        connect<K extends keyof NM.SettingMacvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMacvlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingMacvlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMacvlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingMacvlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingMacvlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_mode(): NM.SettingMacvlanMode;
        get_parent(): string;
        get_promiscuous(): boolean;
        get_tap(): boolean;
    }
    class SettingMatch extends Setting {
        static '$gtype': GObject.GType<SettingMatch>;
        // Properties
        /**
         * A list of driver names to match. Each element is a shell wildcard pattern.
         *
         * See NMSettingMatch:interface-name for how special characters '|', '&',
         * '!' and '\\' are used for optional and mandatory matches and inverting the
         * pattern.
         */
        get driver(): string[];
        set driver(val: string[]);
        /**
         * A list of interface names to match. Each element is a shell wildcard
         * pattern.
         *
         * An element can be prefixed with a pipe symbol (|) or an ampersand (&).
         * The former means that the element is optional and the latter means that
         * it is mandatory. If there are any optional elements, than the match
         * evaluates to true if at least one of the optional element matches
         * (logical OR). If there are any mandatory elements, then they all
         * must match (logical AND). By default, an element is optional. This means
         * that an element "foo" behaves the same as "|foo". An element can also be inverted
         * with exclamation mark (!) between the pipe symbol (or the ampersand) and before
         * the pattern. Note that "!foo" is a shortcut for the mandatory match "&!foo". Finally,
         * a backslash can be used at the beginning of the element (after the optional special characters)
         * to escape the start of the pattern. For example, "&\\!a" is an mandatory match for literally "!a".
         */
        get interface_name(): string[];
        set interface_name(val: string[]);
        /**
         * A list of interface names to match. Each element is a shell wildcard
         * pattern.
         *
         * An element can be prefixed with a pipe symbol (|) or an ampersand (&).
         * The former means that the element is optional and the latter means that
         * it is mandatory. If there are any optional elements, than the match
         * evaluates to true if at least one of the optional element matches
         * (logical OR). If there are any mandatory elements, then they all
         * must match (logical AND). By default, an element is optional. This means
         * that an element "foo" behaves the same as "|foo". An element can also be inverted
         * with exclamation mark (!) between the pipe symbol (or the ampersand) and before
         * the pattern. Note that "!foo" is a shortcut for the mandatory match "&!foo". Finally,
         * a backslash can be used at the beginning of the element (after the optional special characters)
         * to escape the start of the pattern. For example, "&\\!a" is an mandatory match for literally "!a".
         */
        get interfaceName(): string[];
        set interfaceName(val: string[]);
        /**
         * A list of kernel command line arguments to match. This may be used to check
         * whether a specific kernel command line option is set (or unset, if prefixed with
         * the exclamation mark). The argument must either be a single word, or
         * an assignment (i.e. two words, joined by "="). In the former case the kernel
         * command line is searched for the word appearing as is, or as left hand side
         * of an assignment. In the latter case, the exact assignment is looked for
         * with right and left hand side matching. Wildcard patterns are not supported.
         *
         * See NMSettingMatch:interface-name for how special characters '|', '&',
         * '!' and '\\' are used for optional and mandatory matches and inverting the
         * match.
         */
        get kernel_command_line(): string[];
        set kernel_command_line(val: string[]);
        /**
         * A list of kernel command line arguments to match. This may be used to check
         * whether a specific kernel command line option is set (or unset, if prefixed with
         * the exclamation mark). The argument must either be a single word, or
         * an assignment (i.e. two words, joined by "="). In the former case the kernel
         * command line is searched for the word appearing as is, or as left hand side
         * of an assignment. In the latter case, the exact assignment is looked for
         * with right and left hand side matching. Wildcard patterns are not supported.
         *
         * See NMSettingMatch:interface-name for how special characters '|', '&',
         * '!' and '\\' are used for optional and mandatory matches and inverting the
         * match.
         */
        get kernelCommandLine(): string[];
        set kernelCommandLine(val: string[]);
        /**
         * A list of paths to match against the ID_PATH udev property of
         * devices. ID_PATH represents the topological persistent path of a
         * device. It typically contains a subsystem string (pci, usb, platform,
         * etc.) and a subsystem-specific identifier.
         *
         * For PCI devices the path has the form
         * "pci-$domain:$bus:$device.$function", where each variable is an
         * hexadecimal value; for example "pci-0000:0a:00.0".
         *
         * The path of a device can be obtained with "udevadm info
         * /sys/class/net/$dev | grep ID_PATH=" or by looking at the "path"
         * property exported by NetworkManager ("nmcli -f general.path device
         * show $dev").
         *
         * Each element of the list is a shell wildcard pattern.
         *
         * See NMSettingMatch:interface-name for how special characters '|', '&',
         * '!' and '\\' are used for optional and mandatory matches and inverting the
         * pattern.
         */
        get path(): string[];
        set path(val: string[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingMatch.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingMatch.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingMatch;
        // Signals
        connect<K extends keyof NM.SettingMatch.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMatch.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingMatch.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingMatch.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingMatch.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingMatch.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a new driver to the setting.
         *
         * @param driver the driver to add
         */
        add_driver(driver: string): void;
        /**
         * Adds a new interface name to the setting.
         *
         * @param interface_name the interface name to add
         */
        add_interface_name(interface_name: string): void;
        /**
         * Adds a new kernel command line argument to the setting.
         *
         * @param kernel_command_line the kernel command line argument to add
         */
        add_kernel_command_line(kernel_command_line: string): void;
        /**
         * Adds a new path to the setting.
         *
         * @param path the path to add
         */
        add_path(path: string): void;
        /**
         * Removes all configured drivers.
         */
        clear_drivers(): void;
        /**
         * Removes all configured interface names.
         */
        clear_interface_names(): void;
        /**
         * Removes all configured kernel command line arguments.
         */
        clear_kernel_command_lines(): void;
        /**
         * Removes all configured paths.
         */
        clear_paths(): void;
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the driver at index `idx`
         * @param idx index number of the driver to return
         */
        get_driver(idx: number): string;
        /**
         * Returns all the drivers.
         *
         * @returns the configured drivers.
         */
        get_drivers(): string[];
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the interface name at index `idx`
         * @param idx index number of the DNS search domain to return
         */
        get_interface_name(idx: number): string;
        /**
         * Returns all the interface names.
         *
         * @returns the NULL terminated list of
         *   configured interface names.
         *
         * Before 1.26, the returned array was not %NULL terminated and you MUST provide a length.
         */
        get_interface_names(): string[];
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the kernel command line argument at index `idx`
         * @param idx index number of the kernel command line argument to return
         */
        get_kernel_command_line(idx: number): string;
        /**
         * Returns all the kernel command line arguments.
         *
         * @returns the configured kernel command
         *    line arguments.
         */
        get_kernel_command_lines(): string[];
        get_num_drivers(): number;
        get_num_interface_names(): number;
        get_num_kernel_command_lines(): number;
        get_num_paths(): number;
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the path at index `idx`
         * @param idx index number of the path to return
         */
        get_path(idx: number): string;
        /**
         * Returns all the paths.
         *
         * @returns the configured paths.
         */
        get_paths(): string[];
        /**
         * Removes the driver at index `idx`.
         *
         * @param idx index number of the driver
         */
        remove_driver(idx: number): void;
        /**
         * Removes `driver`.
         *
         * @returns %TRUE if the driver was found and removed; %FALSE if it was not.
         * @param driver the driver to remove
         */
        remove_driver_by_value(driver: string): boolean;
        /**
         * Removes the interface name at index `idx`.
         *
         * @param idx index number of the interface name
         */
        remove_interface_name(idx: number): void;
        /**
         * Removes `interface_name`.
         *
         * @returns %TRUE if the interface name was found and removed; %FALSE if it was not.
         * @param interface_name the interface name to remove
         */
        remove_interface_name_by_value(interface_name: string): boolean;
        /**
         * Removes the kernel command line argument at index `idx`.
         *
         * @param idx index number of the kernel command line argument
         */
        remove_kernel_command_line(idx: number): void;
        /**
         * Removes `kernel_command_line`.
         *
         * @returns %TRUE if the kernel command line argument was found and removed; %FALSE if it was not.
         * @param kernel_command_line the kernel command line argument name to remove
         */
        remove_kernel_command_line_by_value(kernel_command_line: string): boolean;
        /**
         * Removes the path at index `idx`.
         *
         * @param idx index number of the path
         */
        remove_path(idx: number): void;
        /**
         * Removes `path`.
         *
         * @returns %TRUE if the path was found and removed; %FALSE if it was not.
         * @param path the path to remove
         */
        remove_path_by_value(path: string): boolean;
    }
    class SettingOlpcMesh extends Setting {
        static '$gtype': GObject.GType<SettingOlpcMesh>;
        // Properties
        /**
         * Channel on which the mesh network to join is located.
         */
        get channel(): number;
        set channel(val: number);
        /**
         * Anycast DHCP MAC address used when requesting an IP address via DHCP.
         * The specific anycast address used determines which DHCP server class
         * answers the request.
         *
         * This is currently only implemented by dhclient DHCP plugin.
         */
        get dhcp_anycast_address(): string;
        set dhcp_anycast_address(val: string);
        /**
         * Anycast DHCP MAC address used when requesting an IP address via DHCP.
         * The specific anycast address used determines which DHCP server class
         * answers the request.
         *
         * This is currently only implemented by dhclient DHCP plugin.
         */
        get dhcpAnycastAddress(): string;
        set dhcpAnycastAddress(val: string);
        /**
         * SSID of the mesh network to join.
         */
        get ssid(): GLib.Bytes;
        set ssid(val: GLib.Bytes);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOlpcMesh.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOlpcMesh.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOlpcMesh;
        // Signals
        connect<K extends keyof NM.SettingOlpcMesh.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOlpcMesh.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOlpcMesh.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOlpcMesh.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOlpcMesh.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOlpcMesh.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_channel(): number;
        get_dhcp_anycast_address(): string;
        get_ssid(): GLib.Bytes;
    }
    class SettingOvsBridge extends Setting {
        static '$gtype': GObject.GType<SettingOvsBridge>;
        // Properties
        /**
         * The data path type. One of "system", "netdev" or empty.
         */
        get datapath_type(): string;
        set datapath_type(val: string);
        /**
         * The data path type. One of "system", "netdev" or empty.
         */
        get datapathType(): string;
        set datapathType(val: string);
        /**
         * The bridge failure mode. One of "secure", "standalone" or empty.
         */
        get fail_mode(): string;
        set fail_mode(val: string);
        /**
         * The bridge failure mode. One of "secure", "standalone" or empty.
         */
        get failMode(): string;
        set failMode(val: string);
        /**
         * Enable or disable multicast snooping.
         */
        get mcast_snooping_enable(): boolean;
        set mcast_snooping_enable(val: boolean);
        /**
         * Enable or disable multicast snooping.
         */
        get mcastSnoopingEnable(): boolean;
        set mcastSnoopingEnable(val: boolean);
        /**
         * Enable or disable RSTP.
         */
        get rstp_enable(): boolean;
        set rstp_enable(val: boolean);
        /**
         * Enable or disable RSTP.
         */
        get rstpEnable(): boolean;
        set rstpEnable(val: boolean);
        /**
         * Enable or disable STP.
         */
        get stp_enable(): boolean;
        set stp_enable(val: boolean);
        /**
         * Enable or disable STP.
         */
        get stpEnable(): boolean;
        set stpEnable(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsBridge.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsBridge.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsBridge;
        // Signals
        connect<K extends keyof NM.SettingOvsBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsBridge.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsBridge.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsBridge.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsBridge.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsBridge.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_datapath_type(): string;
        get_fail_mode(): string;
        get_mcast_snooping_enable(): boolean;
        get_rstp_enable(): boolean;
        get_stp_enable(): boolean;
    }
    class SettingOvsDpdk extends Setting {
        static '$gtype': GObject.GType<SettingOvsDpdk>;
        // Properties
        /**
         * Open vSwitch DPDK device arguments.
         */
        get devargs(): string;
        set devargs(val: string);
        /**
         * Configures the Link State Change (LSC) detection mode for the OVS DPDK interface.
         * When set to %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_IGNORE, NetworkManager doesn't
         * change the default value configured by Open vSwitch.
         * %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_ENABLED enables interrupts.
         * %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_DISABLED disables interrupts, thus setting the
         * interface in poll mode.
         */
        get lsc_interrupt(): number;
        set lsc_interrupt(val: number);
        /**
         * Configures the Link State Change (LSC) detection mode for the OVS DPDK interface.
         * When set to %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_IGNORE, NetworkManager doesn't
         * change the default value configured by Open vSwitch.
         * %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_ENABLED enables interrupts.
         * %NM_SETTING_OVS_DPDK_LSC_INTERRUPT_DISABLED disables interrupts, thus setting the
         * interface in poll mode.
         */
        get lscInterrupt(): number;
        set lscInterrupt(val: number);
        /**
         * Open vSwitch DPDK number of rx queues.
         * Defaults to zero which means to leave the parameter in OVS unspecified
         * and effectively configures one queue.
         */
        get n_rxq(): number;
        set n_rxq(val: number);
        /**
         * Open vSwitch DPDK number of rx queues.
         * Defaults to zero which means to leave the parameter in OVS unspecified
         * and effectively configures one queue.
         */
        get nRxq(): number;
        set nRxq(val: number);
        /**
         * The rx queue size (number of rx descriptors) for DPDK ports.
         * Must be zero or a power of 2 between 1 and 4096, and supported
         * by the hardware. Defaults to zero which means to leave the
         * parameter in OVS unspecified and effectively configures 2048
         * descriptors.
         */
        get n_rxq_desc(): number;
        set n_rxq_desc(val: number);
        /**
         * The rx queue size (number of rx descriptors) for DPDK ports.
         * Must be zero or a power of 2 between 1 and 4096, and supported
         * by the hardware. Defaults to zero which means to leave the
         * parameter in OVS unspecified and effectively configures 2048
         * descriptors.
         */
        get nRxqDesc(): number;
        set nRxqDesc(val: number);
        /**
         * The tx queue size (number of tx descriptors) for DPDK ports.
         * Must be zero or a power of 2 between 1 and 4096, and supported
         * by the hardware. Defaults to zero which means to leave the
         * parameter in OVS unspecified and effectively configures 2048
         * descriptors.
         */
        get n_txq_desc(): number;
        set n_txq_desc(val: number);
        /**
         * The tx queue size (number of tx descriptors) for DPDK ports.
         * Must be zero or a power of 2 between 1 and 4096, and supported
         * by the hardware. Defaults to zero which means to leave the
         * parameter in OVS unspecified and effectively configures 2048
         * descriptors.
         */
        get nTxqDesc(): number;
        set nTxqDesc(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsDpdk.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsDpdk.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsDpdk;
        // Signals
        connect<K extends keyof NM.SettingOvsDpdk.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsDpdk.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsDpdk.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsDpdk.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsDpdk.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsDpdk.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_devargs(): string;
        get_lsc_interrupt(): NM.SettingOvsDpdkLscInterrupt;
        get_n_rxq(): number;
        get_n_rxq_desc(): number;
        get_n_txq_desc(): number;
    }
    class SettingOvsExternalIDs extends Setting {
        static '$gtype': GObject.GType<SettingOvsExternalIDs>;
        // Properties
        /**
         * A dictionary of key/value pairs with external-ids for OVS.
         */
        get data(): GLib.HashTable<string, string>;
        set data(val: GLib.HashTable<string, string>);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsExternalIDs.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsExternalIDs.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsExternalIDs;
        // Signals
        connect<K extends keyof NM.SettingOvsExternalIDs.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsExternalIDs.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsExternalIDs.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsExternalIDs.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsExternalIDs.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsExternalIDs.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Checks whether `key` is a valid key for OVS' external-ids.
         * This means, the key cannot be %NULL, not too large and valid ASCII.
         * Also, only digits and numbers are allowed with a few special
         * characters. They key must also not start with "NM.".
         *
         * @param key the key to check
         */
        static check_key(key?: string | null): boolean;
        /**
         * Checks whether `val` is a valid user data value. This means,
         * value is not %NULL, not too large and valid UTF-8.
         *
         * @param val the value to check
         */
        static check_val(val?: string | null): boolean;
        // Methods
        get_data(key: string): string;
        // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;
        get_data_keys(): string[];
        set_data(key: string, val?: string | null): void;
        // Conflicted with GObject.Object.set_data
        set_data(...args: never[]): any;
    }
    class SettingOvsInterface extends Setting {
        static '$gtype': GObject.GType<SettingOvsInterface>;
        // Properties
        /**
         * Open vSwitch openflow port number.
         * Defaults to zero which means that port number will not be specified
         * and it will be chosen randomly by ovs. OpenFlow ports are the network interfaces
         * for passing packets between OpenFlow processing and the rest of the network.
         * OpenFlow switches connect logically to each other via their OpenFlow ports.
         */
        get ofport_request(): number;
        set ofport_request(val: number);
        /**
         * Open vSwitch openflow port number.
         * Defaults to zero which means that port number will not be specified
         * and it will be chosen randomly by ovs. OpenFlow ports are the network interfaces
         * for passing packets between OpenFlow processing and the rest of the network.
         * OpenFlow switches connect logically to each other via their OpenFlow ports.
         */
        get ofportRequest(): number;
        set ofportRequest(val: number);
        /**
         * The interface type. Either "internal", "system", "patch", "dpdk", or empty.
         */
        get type(): string;
        set type(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsInterface.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsInterface.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsInterface;
        // Signals
        connect<K extends keyof NM.SettingOvsInterface.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsInterface.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsInterface.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsInterface.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsInterface.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsInterface.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_interface_type(): string;
        get_ofport_request(): number;
    }
    class SettingOvsOtherConfig extends Setting {
        static '$gtype': GObject.GType<SettingOvsOtherConfig>;
        // Properties
        /**
         * A dictionary of key/value pairs with other_config settings for OVS.
         * See also "other_config" in the "ovs-vswitchd.conf.db" manual for the keys
         * that OVS supports.
         */
        get data(): GLib.HashTable<string, string>;
        set data(val: GLib.HashTable<string, string>);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsOtherConfig.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsOtherConfig.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsOtherConfig;
        // Signals
        connect<K extends keyof NM.SettingOvsOtherConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsOtherConfig.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsOtherConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsOtherConfig.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsOtherConfig.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsOtherConfig.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_data(key: string): string;
        // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;
        get_data_keys(): string[];
        set_data(key: string, val?: string | null): void;
        // Conflicted with GObject.Object.set_data
        set_data(...args: never[]): any;
    }
    class SettingOvsPatch extends Setting {
        static '$gtype': GObject.GType<SettingOvsPatch>;
        // Properties
        /**
         * Specifies the name of the interface for the other side of the patch.
         * The patch on the other side must also set this interface as peer.
         */
        get peer(): string;
        set peer(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsPatch.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsPatch.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsPatch;
        // Signals
        connect<K extends keyof NM.SettingOvsPatch.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsPatch.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsPatch.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsPatch.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsPatch.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsPatch.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_peer(): string;
    }
    class SettingOvsPort extends Setting {
        static '$gtype': GObject.GType<SettingOvsPort>;
        // Properties
        /**
         * The time port must be inactive in order to be considered down.
         */
        get bond_downdelay(): number;
        set bond_downdelay(val: number);
        /**
         * The time port must be inactive in order to be considered down.
         */
        get bondDowndelay(): number;
        set bondDowndelay(val: number);
        /**
         * Bonding mode. One of "active-backup", "balance-slb", or "balance-tcp".
         */
        get bond_mode(): string;
        set bond_mode(val: string);
        /**
         * Bonding mode. One of "active-backup", "balance-slb", or "balance-tcp".
         */
        get bondMode(): string;
        set bondMode(val: string);
        /**
         * The time port must be active before it starts forwarding traffic.
         */
        get bond_updelay(): number;
        set bond_updelay(val: number);
        /**
         * The time port must be active before it starts forwarding traffic.
         */
        get bondUpdelay(): number;
        set bondUpdelay(val: number);
        /**
         * LACP mode. One of "active", "off", or "passive".
         */
        get lacp(): string;
        set lacp(val: string);
        /**
         * The VLAN tag in the range 0-4095.
         */
        get tag(): number;
        set tag(val: number);
        /**
         * A list of VLAN ranges that this port trunks.
         *
         * The property is valid only for ports with mode "trunk",
         * "native-tagged", or "native-untagged port".
         * If it is empty, the port trunks all VLANs.
         */
        get trunks(): NM.Range[];
        set trunks(val: NM.Range[]);
        /**
         * The VLAN mode. One of "access", "native-tagged", "native-untagged",
         * "trunk", "dot1q-tunnel" or unset.
         */
        get vlan_mode(): string;
        set vlan_mode(val: string);
        /**
         * The VLAN mode. One of "access", "native-tagged", "native-untagged",
         * "trunk", "dot1q-tunnel" or unset.
         */
        get vlanMode(): string;
        set vlanMode(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingOvsPort.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingOvsPort.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingOvsPort;
        // Signals
        connect<K extends keyof NM.SettingOvsPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsPort.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingOvsPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingOvsPort.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingOvsPort.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingOvsPort.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new trunk range to the setting.
         * This takes a reference to `trunk`.
         *
         * @param trunk the trunk to add
         */
        add_trunk(trunk: NM.Range): void;
        /**
         * Removes all configured trunk ranges.
         */
        clear_trunks(): void;
        get_bond_downdelay(): number;
        get_bond_mode(): string;
        get_bond_updelay(): number;
        get_lacp(): string;
        get_num_trunks(): number;
        get_tag(): number;
        get_trunk(idx: number): NM.Range;
        get_vlan_mode(): string;
        /**
         * Removes the trunk range at index `idx`.
         *
         * @param idx index number of the trunk range.
         */
        remove_trunk(idx: number): void;
        /**
         * Remove the trunk range with range `start` to `end`.
         *
         * @returns %TRUE if the trunk range was found and removed; %FALSE otherwise
         * @param start the trunk range start index
         * @param end the trunk range end index
         */
        remove_trunk_by_value(start: number, end: number): boolean;
    }
    class SettingPpp extends Setting {
        static '$gtype': GObject.GType<SettingPpp>;
        // Properties
        /**
         * If non-zero, instruct pppd to set the serial port to the specified
         * baudrate.  This value should normally be left as 0 to automatically
         * choose the speed.
         */
        get baud(): number;
        set baud(val: number);
        /**
         * If %TRUE, specify that pppd should set the serial port to use hardware
         * flow control with RTS and CTS signals.  This value should normally be set
         * to %FALSE.
         */
        get crtscts(): boolean;
        set crtscts(val: boolean);
        /**
         * If non-zero, instruct pppd to presume the connection to the peer has
         * failed if the specified number of LCP echo-requests go unanswered by the
         * peer.  The "lcp-echo-interval" property must also be set to a non-zero
         * value if this property is used.
         */
        get lcp_echo_failure(): number;
        set lcp_echo_failure(val: number);
        /**
         * If non-zero, instruct pppd to presume the connection to the peer has
         * failed if the specified number of LCP echo-requests go unanswered by the
         * peer.  The "lcp-echo-interval" property must also be set to a non-zero
         * value if this property is used.
         */
        get lcpEchoFailure(): number;
        set lcpEchoFailure(val: number);
        /**
         * If non-zero, instruct pppd to send an LCP echo-request frame to the peer
         * every n seconds (where n is the specified value).  Note that some PPP
         * peers will respond to echo requests and some will not, and it is not
         * possible to autodetect this.
         */
        get lcp_echo_interval(): number;
        set lcp_echo_interval(val: number);
        /**
         * If non-zero, instruct pppd to send an LCP echo-request frame to the peer
         * every n seconds (where n is the specified value).  Note that some PPP
         * peers will respond to echo requests and some will not, and it is not
         * possible to autodetect this.
         */
        get lcpEchoInterval(): number;
        set lcpEchoInterval(val: number);
        /**
         * If %TRUE, stateful MPPE is used.  See pppd documentation for more
         * information on stateful MPPE.
         */
        get mppe_stateful(): boolean;
        set mppe_stateful(val: boolean);
        /**
         * If %TRUE, stateful MPPE is used.  See pppd documentation for more
         * information on stateful MPPE.
         */
        get mppeStateful(): boolean;
        set mppeStateful(val: boolean);
        /**
         * If non-zero, instruct pppd to request that the peer send packets no
         * larger than the specified size.  If non-zero, the MRU should be between
         * 128 and 16384.
         */
        get mru(): number;
        set mru(val: number);
        /**
         * If non-zero, instruct pppd to send packets no larger than the specified
         * size.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * If %TRUE, Van Jacobsen TCP header compression will not be requested.
         */
        get no_vj_comp(): boolean;
        set no_vj_comp(val: boolean);
        /**
         * If %TRUE, Van Jacobsen TCP header compression will not be requested.
         */
        get noVjComp(): boolean;
        set noVjComp(val: boolean);
        /**
         * If %TRUE, do not require the other side (usually the PPP server) to
         * authenticate itself to the client.  If %FALSE, require authentication
         * from the remote side.  In almost all cases, this should be %TRUE.
         */
        get noauth(): boolean;
        set noauth(val: boolean);
        /**
         * If %TRUE, BSD compression will not be requested.
         */
        get nobsdcomp(): boolean;
        set nobsdcomp(val: boolean);
        /**
         * If %TRUE, "deflate" compression will not be requested.
         */
        get nodeflate(): boolean;
        set nodeflate(val: boolean);
        /**
         * If %TRUE, the CHAP authentication method will not be used.
         */
        get refuse_chap(): boolean;
        set refuse_chap(val: boolean);
        /**
         * If %TRUE, the CHAP authentication method will not be used.
         */
        get refuseChap(): boolean;
        set refuseChap(val: boolean);
        /**
         * If %TRUE, the EAP authentication method will not be used.
         */
        get refuse_eap(): boolean;
        set refuse_eap(val: boolean);
        /**
         * If %TRUE, the EAP authentication method will not be used.
         */
        get refuseEap(): boolean;
        set refuseEap(val: boolean);
        /**
         * If %TRUE, the MSCHAP authentication method will not be used.
         */
        get refuse_mschap(): boolean;
        set refuse_mschap(val: boolean);
        /**
         * If %TRUE, the MSCHAP authentication method will not be used.
         */
        get refuseMschap(): boolean;
        set refuseMschap(val: boolean);
        /**
         * If %TRUE, the MSCHAPv2 authentication method will not be used.
         */
        get refuse_mschapv2(): boolean;
        set refuse_mschapv2(val: boolean);
        /**
         * If %TRUE, the MSCHAPv2 authentication method will not be used.
         */
        get refuseMschapv2(): boolean;
        set refuseMschapv2(val: boolean);
        /**
         * If %TRUE, the PAP authentication method will not be used.
         */
        get refuse_pap(): boolean;
        set refuse_pap(val: boolean);
        /**
         * If %TRUE, the PAP authentication method will not be used.
         */
        get refusePap(): boolean;
        set refusePap(val: boolean);
        /**
         * If %TRUE, MPPE (Microsoft Point-to-Point Encryption) will be required for
         * the PPP session.  If either 64-bit or 128-bit MPPE is not available the
         * session will fail.  Note that MPPE is not used on mobile broadband
         * connections.
         */
        get require_mppe(): boolean;
        set require_mppe(val: boolean);
        /**
         * If %TRUE, MPPE (Microsoft Point-to-Point Encryption) will be required for
         * the PPP session.  If either 64-bit or 128-bit MPPE is not available the
         * session will fail.  Note that MPPE is not used on mobile broadband
         * connections.
         */
        get requireMppe(): boolean;
        set requireMppe(val: boolean);
        /**
         * If %TRUE, 128-bit MPPE (Microsoft Point-to-Point Encryption) will be
         * required for the PPP session, and the "require-mppe" property must also
         * be set to %TRUE.  If 128-bit MPPE is not available the session will fail.
         */
        get require_mppe_128(): boolean;
        set require_mppe_128(val: boolean);
        /**
         * If %TRUE, 128-bit MPPE (Microsoft Point-to-Point Encryption) will be
         * required for the PPP session, and the "require-mppe" property must also
         * be set to %TRUE.  If 128-bit MPPE is not available the session will fail.
         */
        get requireMppe128(): boolean;
        set requireMppe128(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingPpp.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingPpp.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingPpp;
        // Signals
        connect<K extends keyof NM.SettingPpp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPpp.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingPpp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPpp.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingPpp.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingPpp.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_baud(): number;
        get_crtscts(): boolean;
        get_lcp_echo_failure(): number;
        get_lcp_echo_interval(): number;
        get_mppe_stateful(): boolean;
        get_mru(): number;
        get_mtu(): number;
        get_no_vj_comp(): boolean;
        get_noauth(): boolean;
        get_nobsdcomp(): boolean;
        get_nodeflate(): boolean;
        get_refuse_chap(): boolean;
        get_refuse_eap(): boolean;
        get_refuse_mschap(): boolean;
        get_refuse_mschapv2(): boolean;
        get_refuse_pap(): boolean;
        get_require_mppe(): boolean;
        get_require_mppe_128(): boolean;
    }
    class SettingPppoe extends Setting {
        static '$gtype': GObject.GType<SettingPppoe>;
        // Properties
        /**
         * If given, specifies the parent interface name on which this PPPoE
         * connection should be created.  If this property is not specified,
         * the connection is activated on the interface specified in
         * #NMSettingConnection:interface-name of #NMSettingConnection.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Password used to authenticate with the PPPoE service.
         */
        get password(): string;
        set password(val: string);
        /**
         * Flags indicating how to handle the #NMSettingPppoe:password property.
         */
        get password_flags(): NM.SettingSecretFlags;
        set password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingPppoe:password property.
         */
        get passwordFlags(): NM.SettingSecretFlags;
        set passwordFlags(val: NM.SettingSecretFlags);
        /**
         * If specified, instruct PPPoE to only initiate sessions with access
         * concentrators that provide the specified service.  For most providers,
         * this should be left blank.  It is only required if there are multiple
         * access concentrators or a specific service is known to be required.
         */
        get service(): string;
        set service(val: string);
        /**
         * Username used to authenticate with the PPPoE service.
         */
        get username(): string;
        set username(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingPppoe.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingPppoe.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingPppoe;
        // Signals
        connect<K extends keyof NM.SettingPppoe.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPppoe.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingPppoe.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPppoe.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingPppoe.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingPppoe.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_parent(): string;
        get_password(): string;
        get_password_flags(): NM.SettingSecretFlags;
        get_service(): string;
        get_username(): string;
    }
    class SettingPrefixDelegation extends Setting {
        static '$gtype': GObject.GType<SettingPrefixDelegation>;
        // Properties
        /**
         * The subnet ID to use on the interface from the prefix delegation received via
         * an upstream interface. Set to a value between 0 and 0xffffffff (2^32 - 1)
         * to indicate a specific subnet ID; or set to -1 to automatically choose
         * an available subnet ID.
         */
        get subnet_id(): number;
        set subnet_id(val: number);
        /**
         * The subnet ID to use on the interface from the prefix delegation received via
         * an upstream interface. Set to a value between 0 and 0xffffffff (2^32 - 1)
         * to indicate a specific subnet ID; or set to -1 to automatically choose
         * an available subnet ID.
         */
        get subnetId(): number;
        set subnetId(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingPrefixDelegation.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingPrefixDelegation.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingPrefixDelegation;
        // Signals
        connect<K extends keyof NM.SettingPrefixDelegation.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPrefixDelegation.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingPrefixDelegation.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingPrefixDelegation.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingPrefixDelegation.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingPrefixDelegation.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_subnet_id(): number;
    }
    class SettingProxy extends Setting {
        static '$gtype': GObject.GType<SettingProxy>;
        // Properties
        /**
         * Whether the proxy configuration is for browser only.
         */
        get browser_only(): boolean;
        set browser_only(val: boolean);
        /**
         * Whether the proxy configuration is for browser only.
         */
        get browserOnly(): boolean;
        set browserOnly(val: boolean);
        /**
         * Method for proxy configuration, Default is %NM_SETTING_PROXY_METHOD_NONE
         */
        get method(): number;
        set method(val: number);
        /**
         * PAC script for the connection. This is an UTF-8 encoded javascript code
         * that defines a FindProxyForURL() function.
         */
        get pac_script(): string;
        set pac_script(val: string);
        /**
         * PAC script for the connection. This is an UTF-8 encoded javascript code
         * that defines a FindProxyForURL() function.
         */
        get pacScript(): string;
        set pacScript(val: string);
        /**
         * PAC URL for obtaining PAC file.
         */
        get pac_url(): string;
        set pac_url(val: string);
        /**
         * PAC URL for obtaining PAC file.
         */
        get pacUrl(): string;
        set pacUrl(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingProxy.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingProxy.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingProxy;
        // Signals
        connect<K extends keyof NM.SettingProxy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingProxy.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingProxy.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingProxy.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingProxy.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingProxy.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_browser_only(): boolean;
        /**
         * Returns the proxy configuration method. By default the value is %NM_SETTING_PROXY_METHOD_NONE.
         * %NM_SETTING_PROXY_METHOD_NONE should be selected for a connection intended for direct network
         * access.
         *
         * @returns the proxy configuration method
         */
        get_method(): NM.SettingProxyMethod;
        get_pac_script(): string;
        get_pac_url(): string;
    }
    class SettingSerial extends Setting {
        static '$gtype': GObject.GType<SettingSerial>;
        // Properties
        /**
         * Speed to use for communication over the serial port.  Note that this
         * value usually has no effect for mobile broadband modems as they generally
         * ignore speed settings and use the highest available speed.
         */
        get baud(): number;
        set baud(val: number);
        /**
         * Byte-width of the serial communication. The 8 in "8n1" for example.
         */
        get bits(): number;
        set bits(val: number);
        /**
         * Parity setting of the serial port.
         */
        get parity(): NM.SettingSerialParity;
        set parity(val: NM.SettingSerialParity);
        /**
         * Time to delay between each byte sent to the modem, in microseconds.
         */
        get send_delay(): number;
        set send_delay(val: number);
        /**
         * Time to delay between each byte sent to the modem, in microseconds.
         */
        get sendDelay(): number;
        set sendDelay(val: number);
        /**
         * Number of stop bits for communication on the serial port.  Either 1 or 2.
         * The 1 in "8n1" for example.
         */
        get stopbits(): number;
        set stopbits(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingSerial.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingSerial.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingSerial;
        // Signals
        connect<K extends keyof NM.SettingSerial.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingSerial.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingSerial.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingSerial.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingSerial.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingSerial.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_baud(): number;
        get_bits(): number;
        get_parity(): NM.SettingSerialParity;
        get_send_delay(): number;
        get_stopbits(): number;
    }
    class SettingSriov extends Setting {
        static '$gtype': GObject.GType<SettingSriov>;
        // Properties
        /**
         * Whether to autoprobe virtual functions by a compatible driver.
         *
         * If set to %NM_TERNARY_TRUE, the kernel will try to bind VFs to
         * a compatible driver and if this succeeds a new network
         * interface will be instantiated for each VF.
         *
         * If set to %NM_TERNARY_FALSE, VFs will not be claimed and no
         * network interfaces will be created for them.
         *
         * When set to %NM_TERNARY_DEFAULT, the global default is used; in
         * case the global default is unspecified it is assumed to be
         * %NM_TERNARY_TRUE.
         */
        get autoprobe_drivers(): NM.Ternary;
        set autoprobe_drivers(val: NM.Ternary);
        /**
         * Whether to autoprobe virtual functions by a compatible driver.
         *
         * If set to %NM_TERNARY_TRUE, the kernel will try to bind VFs to
         * a compatible driver and if this succeeds a new network
         * interface will be instantiated for each VF.
         *
         * If set to %NM_TERNARY_FALSE, VFs will not be claimed and no
         * network interfaces will be created for them.
         *
         * When set to %NM_TERNARY_DEFAULT, the global default is used; in
         * case the global default is unspecified it is assumed to be
         * %NM_TERNARY_TRUE.
         */
        get autoprobeDrivers(): NM.Ternary;
        set autoprobeDrivers(val: NM.Ternary);
        /**
         * Select the eswitch encapsulation support.
         *
         * Currently it's only supported for PCI PF devices, and only if the eswitch device
         * is managed from the same PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_ENCAP_MODE_PRESERVE (default) the eswitch encap-mode
         * won't be modified by NetworkManager.
         */
        get eswitch_encap_mode(): number;
        set eswitch_encap_mode(val: number);
        /**
         * Select the eswitch encapsulation support.
         *
         * Currently it's only supported for PCI PF devices, and only if the eswitch device
         * is managed from the same PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_ENCAP_MODE_PRESERVE (default) the eswitch encap-mode
         * won't be modified by NetworkManager.
         */
        get eswitchEncapMode(): number;
        set eswitchEncapMode(val: number);
        /**
         * Select the eswitch inline-mode of the device. Some HWs need the VF driver to put
         * part of the packet headers on the TX descriptor so the e-switch can do proper
         * matching and steering.
         *
         * Currently it's only supported for PCI PF devices, and only if the eswitch device
         * is managed from the same PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_INLINE_MODE_PRESERVE (default) the eswitch inline-mode
         * won't be modified by NetworkManager.
         */
        get eswitch_inline_mode(): number;
        set eswitch_inline_mode(val: number);
        /**
         * Select the eswitch inline-mode of the device. Some HWs need the VF driver to put
         * part of the packet headers on the TX descriptor so the e-switch can do proper
         * matching and steering.
         *
         * Currently it's only supported for PCI PF devices, and only if the eswitch device
         * is managed from the same PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_INLINE_MODE_PRESERVE (default) the eswitch inline-mode
         * won't be modified by NetworkManager.
         */
        get eswitchInlineMode(): number;
        set eswitchInlineMode(val: number);
        /**
         * Select the eswitch mode of the device. Currently it's only supported for
         * PCI PF devices, and only if the eswitch device is managed from the same
         * PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_MODE_PRESERVE (default) the eswitch mode won't be
         * modified by NetworkManager.
         */
        get eswitch_mode(): number;
        set eswitch_mode(val: number);
        /**
         * Select the eswitch mode of the device. Currently it's only supported for
         * PCI PF devices, and only if the eswitch device is managed from the same
         * PCI address than the PF.
         *
         * If set to %NM_SRIOV_ESWITCH_MODE_PRESERVE (default) the eswitch mode won't be
         * modified by NetworkManager.
         */
        get eswitchMode(): number;
        set eswitchMode(val: number);
        /**
         * This controls whether NetworkManager preserves the SR-IOV parameters set on
         * the device when the connection is deactivated, or whether it resets them to
         * their default value. The SR-IOV parameters are those specified in this setting
         * (the "sriov" setting), like the number of VFs to create, the eswitch
         * configuration, etc.
         *
         * If set to %NM_SRIOV_PRESERVE_ON_DOWN_NO, NetworkManager resets the SR-IOV
         * parameters when the connection is deactivated. When set to
         * %NM_SRIOV_PRESERVE_ON_DOWN_YES, NetworkManager preserves those parameters
         * on the device. If the value is %NM_SRIOV_PRESERVE_ON_DOWN_DEFAULT, NetworkManager
         * looks up a global default value in the configuration; in case no such value is
         * defined, it uses %NM_SRIOV_PRESERVE_ON_DOWN_NO as fallback.
         */
        get preserve_on_down(): number;
        set preserve_on_down(val: number);
        /**
         * This controls whether NetworkManager preserves the SR-IOV parameters set on
         * the device when the connection is deactivated, or whether it resets them to
         * their default value. The SR-IOV parameters are those specified in this setting
         * (the "sriov" setting), like the number of VFs to create, the eswitch
         * configuration, etc.
         *
         * If set to %NM_SRIOV_PRESERVE_ON_DOWN_NO, NetworkManager resets the SR-IOV
         * parameters when the connection is deactivated. When set to
         * %NM_SRIOV_PRESERVE_ON_DOWN_YES, NetworkManager preserves those parameters
         * on the device. If the value is %NM_SRIOV_PRESERVE_ON_DOWN_DEFAULT, NetworkManager
         * looks up a global default value in the configuration; in case no such value is
         * defined, it uses %NM_SRIOV_PRESERVE_ON_DOWN_NO as fallback.
         */
        get preserveOnDown(): number;
        set preserveOnDown(val: number);
        /**
         * The total number of virtual functions to create.
         *
         * Note that when the sriov setting is present NetworkManager
         * enforces the number of virtual functions on the interface
         * (also when it is zero) during activation and resets it
         * upon deactivation. To prevent any changes to SR-IOV
         * parameters don't add a sriov setting to the connection.
         */
        get total_vfs(): number;
        set total_vfs(val: number);
        /**
         * The total number of virtual functions to create.
         *
         * Note that when the sriov setting is present NetworkManager
         * enforces the number of virtual functions on the interface
         * (also when it is zero) during activation and resets it
         * upon deactivation. To prevent any changes to SR-IOV
         * parameters don't add a sriov setting to the connection.
         */
        get totalVfs(): number;
        set totalVfs(val: number);
        /**
         * Array of virtual function descriptors.
         *
         * Each VF descriptor is a dictionary mapping attribute names
         * to GVariant values. The 'index' entry is mandatory for
         * each VF.
         *
         * When represented as string a VF is in the form:
         *
         *   "INDEX [ATTR=VALUE[ ATTR=VALUE]...]".
         *
         * for example:
         *
         *   "2 mac=00:11:22:33:44:55 spoof-check=true".
         *
         * Multiple VFs can be specified using a comma as separator.
         * Currently, the following attributes are supported: mac,
         * spoof-check, trust, min-tx-rate, max-tx-rate, vlans.
         *
         * The "vlans" attribute is represented as a semicolon-separated
         * list of VLAN descriptors, where each descriptor has the form
         *
         *   "ID[.PRIORITY[.PROTO]]".
         *
         * PROTO can be either 'q' for 802.1Q (the default) or 'ad' for
         * 802.1ad.
         */
        get vfs(): NM.SriovVF[];
        set vfs(val: NM.SriovVF[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingSriov.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingSriov.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingSriov;
        // Signals
        connect<K extends keyof NM.SettingSriov.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingSriov.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingSriov.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingSriov.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingSriov.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingSriov.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new VF and associated information to the setting.  The
         * given VF is duplicated internally and is not changed by this function.
         *
         * @param vf the VF to add
         */
        add_vf(vf: NM.SriovVF): void;
        /**
         * Removes all configured VFs.
         */
        clear_vfs(): void;
        /**
         * Returns the value contained in the #NMSettingSriov:autoprobe-drivers
         * property.
         *
         * @returns the autoprobe-drivers property value
         */
        get_autoprobe_drivers(): NM.Ternary;
        get_eswitch_encap_mode(): NM.SriovEswitchEncapMode;
        get_eswitch_inline_mode(): NM.SriovEswitchInlineMode;
        get_eswitch_mode(): NM.SriovEswitchMode;
        get_num_vfs(): number;
        get_preserve_on_down(): NM.SriovPreserveOnDown;
        /**
         * Returns the value contained in the #NMSettingSriov:total-vfs
         * property.
         *
         * @returns the total number of SR-IOV virtual functions to create
         */
        get_total_vfs(): number;
        get_vf(idx: number): NM.SriovVF;
        /**
         * Removes the VF at index `idx`.
         *
         * @param idx index number of the VF
         */
        remove_vf(idx: number): void;
        /**
         * Removes the VF with VF index `index`.
         *
         * @returns %TRUE if the VF was found and removed; %FALSE if it was not
         * @param index the VF index of the VF to remove
         */
        remove_vf_by_index(index: number): boolean;
    }
    class SettingTCConfig extends Setting {
        static '$gtype': GObject.GType<SettingTCConfig>;
        // Properties
        /**
         * Array of TC queueing disciplines.
         *
         * When the #NMSettingTCConfig setting is present, qdiscs from this
         * property are applied upon activation. If the property is empty,
         * all qdiscs are removed and the device will only
         * have the default qdisc assigned by kernel according to the
         * "net.core.default_qdisc" sysctl.
         *
         * If the #NMSettingTCConfig setting is not present, NetworkManager
         * doesn't touch the qdiscs present on the interface.
         */
        get qdiscs(): NM.TCQdisc[];
        set qdiscs(val: NM.TCQdisc[]);
        /**
         * Array of TC traffic filters.
         *
         * When the #NMSettingTCConfig setting is present, filters from this
         * property are applied upon activation. If the property is empty,
         * NetworkManager removes all the filters.
         *
         * If the #NMSettingTCConfig setting is not present, NetworkManager
         * doesn't touch the filters present on the interface.
         */
        get tfilters(): NM.TCTfilter[];
        set tfilters(val: NM.TCTfilter[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingTCConfig.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingTCConfig.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingTCConfig;
        // Signals
        connect<K extends keyof NM.SettingTCConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTCConfig.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingTCConfig.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTCConfig.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingTCConfig.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingTCConfig.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new qdisc and associated information to the setting.  The
         * given qdisc is duplicated internally and is not changed by this function.
         * If an identical qdisc (considering attributes as well) already exists, the
         * qdisc is not added and the function returns %FALSE.
         *
         * @returns %TRUE if the qdisc was added; %FALSE if the qdisc was already known.
         * @param qdisc the qdisc to add
         */
        add_qdisc(qdisc: NM.TCQdisc): boolean;
        /**
         * Appends a new tfilter and associated information to the setting.  The
         * given tfilter is duplicated internally and is not changed by this function.
         * If an identical tfilter (considering attributes as well) already exists, the
         * tfilter is not added and the function returns %FALSE.
         *
         * @returns %TRUE if the tfilter was added; %FALSE if the tfilter was already known.
         * @param tfilter the tfilter to add
         */
        add_tfilter(tfilter: NM.TCTfilter): boolean;
        /**
         * Removes all configured queueing disciplines.
         */
        clear_qdiscs(): void;
        /**
         * Removes all configured queueing disciplines.
         */
        clear_tfilters(): void;
        get_num_qdiscs(): number;
        get_num_tfilters(): number;
        get_qdisc(idx: number): NM.TCQdisc;
        get_tfilter(idx: number): NM.TCTfilter;
        /**
         * Removes the qdisc at index `idx`.
         *
         * @param idx index number of the qdisc
         */
        remove_qdisc(idx: number): void;
        /**
         * Removes the first matching qdisc that matches `qdisc`.
         *
         * @returns %TRUE if the qdisc was found and removed; %FALSE if it was not.
         * @param qdisc the qdisc to remove
         */
        remove_qdisc_by_value(qdisc: NM.TCQdisc): boolean;
        /**
         * Removes the tfilter at index `idx`.
         *
         * @param idx index number of the tfilter
         */
        remove_tfilter(idx: number): void;
        /**
         * Removes the first matching tfilter that matches `tfilter`.
         *
         * @returns %TRUE if the tfilter was found and removed; %FALSE if it was not.
         * @param tfilter the tfilter to remove
         */
        remove_tfilter_by_value(tfilter: NM.TCTfilter): boolean;
    }
    class SettingTeam extends Setting {
        static '$gtype': GObject.GType<SettingTeam>;
        // Properties
        /**
         * The JSON configuration for the team network interface.  The property
         * should contain raw JSON configuration data suitable for teamd, because
         * the value is passed directly to teamd. If not specified, the default
         * configuration is used.  See man teamd.conf for the format details.
         */
        get config(): string;
        set config(val: string);
        /**
         * Link watchers configuration for the connection: each link watcher is
         * defined by a dictionary, whose keys depend upon the selected link
         * watcher. Available link watchers are 'ethtool', 'nsna_ping' and
         * 'arp_ping' and it is specified in the dictionary with the key 'name'.
         * Available keys are:   ethtool: 'delay-up', 'delay-down', 'init-wait';
         * nsna_ping: 'init-wait', 'interval', 'missed-max', 'target-host';
         * arp_ping: all the ones in nsna_ping and 'source-host', 'validate-active',
         * 'validate-inactive', 'send-always'. See teamd.conf man for more details.
         */
        get link_watchers(): NM.TeamLinkWatcher[];
        set link_watchers(val: NM.TeamLinkWatcher[]);
        /**
         * Link watchers configuration for the connection: each link watcher is
         * defined by a dictionary, whose keys depend upon the selected link
         * watcher. Available link watchers are 'ethtool', 'nsna_ping' and
         * 'arp_ping' and it is specified in the dictionary with the key 'name'.
         * Available keys are:   ethtool: 'delay-up', 'delay-down', 'init-wait';
         * nsna_ping: 'init-wait', 'interval', 'missed-max', 'target-host';
         * arp_ping: all the ones in nsna_ping and 'source-host', 'validate-active',
         * 'validate-inactive', 'send-always'. See teamd.conf man for more details.
         */
        get linkWatchers(): NM.TeamLinkWatcher[];
        set linkWatchers(val: NM.TeamLinkWatcher[]);
        /**
         * Corresponds to the teamd mcast_rejoin.count.
         */
        get mcast_rejoin_count(): number;
        set mcast_rejoin_count(val: number);
        /**
         * Corresponds to the teamd mcast_rejoin.count.
         */
        get mcastRejoinCount(): number;
        set mcastRejoinCount(val: number);
        /**
         * Corresponds to the teamd mcast_rejoin.interval.
         */
        get mcast_rejoin_interval(): number;
        set mcast_rejoin_interval(val: number);
        /**
         * Corresponds to the teamd mcast_rejoin.interval.
         */
        get mcastRejoinInterval(): number;
        set mcastRejoinInterval(val: number);
        /**
         * Corresponds to the teamd notify_peers.count.
         */
        get notify_peers_count(): number;
        set notify_peers_count(val: number);
        /**
         * Corresponds to the teamd notify_peers.count.
         */
        get notifyPeersCount(): number;
        set notifyPeersCount(val: number);
        /**
         * Corresponds to the teamd notify_peers.interval.
         */
        get notify_peers_interval(): number;
        set notify_peers_interval(val: number);
        /**
         * Corresponds to the teamd notify_peers.interval.
         */
        get notifyPeersInterval(): number;
        set notifyPeersInterval(val: number);
        /**
         * Corresponds to the teamd runner.name.
         * Permitted values are: "roundrobin", "broadcast", "activebackup",
         * "loadbalance", "lacp", "random".
         */
        get runner(): string;
        set runner(val: string);
        /**
         * Corresponds to the teamd runner.active.
         */
        get runner_active(): boolean;
        set runner_active(val: boolean);
        /**
         * Corresponds to the teamd runner.active.
         */
        get runnerActive(): boolean;
        set runnerActive(val: boolean);
        /**
         * Corresponds to the teamd runner.agg_select_policy.
         */
        get runner_agg_select_policy(): string;
        set runner_agg_select_policy(val: string);
        /**
         * Corresponds to the teamd runner.agg_select_policy.
         */
        get runnerAggSelectPolicy(): string;
        set runnerAggSelectPolicy(val: string);
        /**
         * Corresponds to the teamd runner.fast_rate.
         */
        get runner_fast_rate(): boolean;
        set runner_fast_rate(val: boolean);
        /**
         * Corresponds to the teamd runner.fast_rate.
         */
        get runnerFastRate(): boolean;
        set runnerFastRate(val: boolean);
        /**
         * Corresponds to the teamd runner.hwaddr_policy.
         */
        get runner_hwaddr_policy(): string;
        set runner_hwaddr_policy(val: string);
        /**
         * Corresponds to the teamd runner.hwaddr_policy.
         */
        get runnerHwaddrPolicy(): string;
        set runnerHwaddrPolicy(val: string);
        /**
         * Corresponds to the teamd runner.min_ports.
         */
        get runner_min_ports(): number;
        set runner_min_ports(val: number);
        /**
         * Corresponds to the teamd runner.min_ports.
         */
        get runnerMinPorts(): number;
        set runnerMinPorts(val: number);
        /**
         * Corresponds to the teamd runner.sys_prio.
         */
        get runner_sys_prio(): number;
        set runner_sys_prio(val: number);
        /**
         * Corresponds to the teamd runner.sys_prio.
         */
        get runnerSysPrio(): number;
        set runnerSysPrio(val: number);
        /**
         * Corresponds to the teamd runner.tx_balancer.name.
         */
        get runner_tx_balancer(): string;
        set runner_tx_balancer(val: string);
        /**
         * Corresponds to the teamd runner.tx_balancer.name.
         */
        get runnerTxBalancer(): string;
        set runnerTxBalancer(val: string);
        /**
         * Corresponds to the teamd runner.tx_balancer.interval.
         */
        get runner_tx_balancer_interval(): number;
        set runner_tx_balancer_interval(val: number);
        /**
         * Corresponds to the teamd runner.tx_balancer.interval.
         */
        get runnerTxBalancerInterval(): number;
        set runnerTxBalancerInterval(val: number);
        /**
         * Corresponds to the teamd runner.tx_hash.
         */
        get runner_tx_hash(): string[];
        set runner_tx_hash(val: string[]);
        /**
         * Corresponds to the teamd runner.tx_hash.
         */
        get runnerTxHash(): string[];
        set runnerTxHash(val: string[]);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingTeam.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingTeam.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingTeam;
        // Signals
        connect<K extends keyof NM.SettingTeam.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTeam.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingTeam.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTeam.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingTeam.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingTeam.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new link watcher to the setting.
         *
         * @returns %TRUE if the link watcher is added; %FALSE if an identical link
         * watcher was already there.
         * @param link_watcher the link watcher to add
         */
        add_link_watcher(link_watcher: NM.TeamLinkWatcher): boolean;
        /**
         * Adds a new txhash element to the setting.
         *
         * @returns %TRUE if the txhash element was added; %FALSE if the element
         * was already knnown.
         * @param txhash the element to add to txhash
         */
        add_runner_tx_hash(txhash: string): boolean;
        /**
         * Removes all configured link watchers.
         */
        clear_link_watchers(): void;
        get_config(): string;
        get_link_watcher(idx: number): NM.TeamLinkWatcher;
        get_mcast_rejoin_count(): number;
        get_mcast_rejoin_interval(): number;
        get_notify_peers_count(): number;
        get_notify_peers_interval(): number;
        get_num_link_watchers(): number;
        get_num_runner_tx_hash(): number;
        get_runner(): string;
        get_runner_active(): boolean;
        get_runner_agg_select_policy(): string;
        get_runner_fast_rate(): boolean;
        get_runner_hwaddr_policy(): string;
        get_runner_min_ports(): number;
        get_runner_sys_prio(): number;
        get_runner_tx_balancer(): string;
        get_runner_tx_balancer_interval(): number;
        get_runner_tx_hash(idx: number): string;
        /**
         * Removes the link watcher at index #idx.
         *
         * @param idx index number of the link watcher to remove
         */
        remove_link_watcher(idx: number): void;
        /**
         * Removes the link watcher entry matching link_watcher.
         *
         * @returns %TRUE if the link watcher was found and removed, %FALSE otherwise.
         * @param link_watcher the link watcher to remove
         */
        remove_link_watcher_by_value(link_watcher: NM.TeamLinkWatcher): boolean;
        /**
         * Removes the txhash element at index `idx`.
         *
         * @param idx index number of the element to remove from txhash
         */
        remove_runner_tx_hash(idx: number): void;
        /**
         * Removes the txhash element #txhash
         *
         * @returns %TRUE if the txhash element was found and removed; %FALSE if it was not.
         * @param txhash the txhash element to remove
         */
        remove_runner_tx_hash_by_value(txhash: string): boolean;
    }
    class SettingTeamPort extends Setting {
        static '$gtype': GObject.GType<SettingTeamPort>;
        // Properties
        /**
         * The JSON configuration for the team port. The property should contain raw
         * JSON configuration data suitable for teamd, because the value is passed
         * directly to teamd. If not specified, the default configuration is
         * used. See man teamd.conf for the format details.
         */
        get config(): string;
        set config(val: string);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.lacp_key.
         */
        get lacp_key(): number;
        set lacp_key(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.lacp_key.
         */
        get lacpKey(): number;
        set lacpKey(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.lacp_prio.
         */
        get lacp_prio(): number;
        set lacp_prio(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.lacp_prio.
         */
        get lacpPrio(): number;
        set lacpPrio(val: number);
        /**
         * Link watchers configuration for the connection: each link watcher is
         * defined by a dictionary, whose keys depend upon the selected link
         * watcher. Available link watchers are 'ethtool', 'nsna_ping' and
         * 'arp_ping' and it is specified in the dictionary with the key 'name'.
         * Available keys are:   ethtool: 'delay-up', 'delay-down', 'init-wait';
         * nsna_ping: 'init-wait', 'interval', 'missed-max', 'target-host';
         * arp_ping: all the ones in nsna_ping and 'source-host', 'validate-active',
         * 'validate-inactive', 'send-always'. See teamd.conf man for more details.
         */
        get link_watchers(): NM.TeamLinkWatcher[];
        set link_watchers(val: NM.TeamLinkWatcher[]);
        /**
         * Link watchers configuration for the connection: each link watcher is
         * defined by a dictionary, whose keys depend upon the selected link
         * watcher. Available link watchers are 'ethtool', 'nsna_ping' and
         * 'arp_ping' and it is specified in the dictionary with the key 'name'.
         * Available keys are:   ethtool: 'delay-up', 'delay-down', 'init-wait';
         * nsna_ping: 'init-wait', 'interval', 'missed-max', 'target-host';
         * arp_ping: all the ones in nsna_ping and 'source-host', 'validate-active',
         * 'validate-inactive', 'send-always'. See teamd.conf man for more details.
         */
        get linkWatchers(): NM.TeamLinkWatcher[];
        set linkWatchers(val: NM.TeamLinkWatcher[]);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.prio.
         */
        get prio(): number;
        set prio(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.queue_id.
         * When set to -1 means the parameter is skipped from the json config.
         */
        get queue_id(): number;
        set queue_id(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.queue_id.
         * When set to -1 means the parameter is skipped from the json config.
         */
        get queueId(): number;
        set queueId(val: number);
        /**
         * Corresponds to the teamd ports.PORTIFNAME.sticky.
         */
        get sticky(): boolean;
        set sticky(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingTeamPort.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingTeamPort.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingTeamPort;
        // Signals
        connect<K extends keyof NM.SettingTeamPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTeamPort.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingTeamPort.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTeamPort.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingTeamPort.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingTeamPort.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Appends a new link watcher to the setting.
         *
         * @returns %TRUE if the link watcher is added; %FALSE if an identical link
         * watcher was already there.
         * @param link_watcher the link watcher to add
         */
        add_link_watcher(link_watcher: NM.TeamLinkWatcher): boolean;
        /**
         * Removes all configured link watchers.
         */
        clear_link_watchers(): void;
        get_config(): string;
        get_lacp_key(): number;
        get_lacp_prio(): number;
        get_link_watcher(idx: number): NM.TeamLinkWatcher;
        get_num_link_watchers(): number;
        get_prio(): number;
        get_queue_id(): number;
        get_sticky(): boolean;
        /**
         * Removes the link watcher at index #idx.
         *
         * @param idx index number of the link watcher to remove
         */
        remove_link_watcher(idx: number): void;
        /**
         * Removes the link watcher entry matching link_watcher.
         *
         * @returns %TRUE if the link watcher was found and removed, %FALSE otherwise.
         * @param link_watcher the link watcher to remove
         */
        remove_link_watcher_by_value(link_watcher: NM.TeamLinkWatcher): boolean;
    }
    class SettingTun extends Setting {
        static '$gtype': GObject.GType<SettingTun>;
        // Properties
        /**
         * The group ID which will own the device. If set to %NULL everyone
         * will be able to use the device.
         */
        get group(): string;
        set group(val: string);
        /**
         * The operating mode of the virtual device. Allowed values are
         * %NM_SETTING_TUN_MODE_TUN to create a layer 3 device and
         * %NM_SETTING_TUN_MODE_TAP to create an Ethernet-like layer 2
         * one.
         */
        get mode(): number;
        set mode(val: number);
        /**
         * If the property is set to %TRUE, the interface will support
         * multiple file descriptors (queues) to parallelize packet
         * sending or receiving. Otherwise, the interface will only
         * support a single queue.
         */
        get multi_queue(): boolean;
        set multi_queue(val: boolean);
        /**
         * If the property is set to %TRUE, the interface will support
         * multiple file descriptors (queues) to parallelize packet
         * sending or receiving. Otherwise, the interface will only
         * support a single queue.
         */
        get multiQueue(): boolean;
        set multiQueue(val: boolean);
        /**
         * The user ID which will own the device. If set to %NULL everyone
         * will be able to use the device.
         */
        get owner(): string;
        set owner(val: string);
        /**
         * If %TRUE the interface will prepend a 4 byte header describing the
         * physical interface to the packets.
         */
        get pi(): boolean;
        set pi(val: boolean);
        /**
         * If %TRUE the IFF_VNET_HDR the tunnel packets will include a virtio
         * network header.
         */
        get vnet_hdr(): boolean;
        set vnet_hdr(val: boolean);
        /**
         * If %TRUE the IFF_VNET_HDR the tunnel packets will include a virtio
         * network header.
         */
        get vnetHdr(): boolean;
        set vnetHdr(val: boolean);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingTun.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingTun.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingTun;
        // Signals
        connect<K extends keyof NM.SettingTun.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTun.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingTun.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingTun.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingTun.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingTun.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_group(): string;
        get_mode(): NM.SettingTunMode;
        get_multi_queue(): boolean;
        get_owner(): string;
        get_pi(): boolean;
        get_vnet_hdr(): boolean;
    }
    class SettingUser extends Setting {
        static '$gtype': GObject.GType<SettingUser>;
        // Properties
        /**
         * A dictionary of key/value pairs with user data. This data is ignored by NetworkManager
         * and can be used at the users discretion. The keys only support a strict ascii format,
         * but the values can be arbitrary UTF8 strings up to a certain length.
         */
        get data(): GLib.HashTable<string, string>;
        set data(val: GLib.HashTable<string, string>);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingUser.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingUser.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingUser;
        // Signals
        connect<K extends keyof NM.SettingUser.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingUser.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingUser.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingUser.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingUser.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingUser.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Checks whether `key` is a valid user data key. This means,
         * key is not %NULL, not too large and valid ASCII. Also,
         * only digits and numbers are allowed with a few special
         * characters. The key must contain at least one '.' and
         * look like a fully qualified DNS name.
         *
         * @param key the key to check
         */
        static check_key(key: string): boolean;
        /**
         * Checks whether `val` is a valid user data value. This means,
         * value is not %NULL, not too large and valid UTF-8.
         *
         * @param val the value to check
         */
        static check_val(val: string): boolean;
        // Methods
        get_data(key: string): string;
        // Conflicted with GObject.Object.get_data
        get_data(...args: never[]): any;
        get_keys(): string[];
        set_data(key: string, val?: string | null): boolean;
        // Conflicted with GObject.Object.set_data
        set_data(...args: never[]): any;
    }
    class SettingVeth extends Setting {
        static '$gtype': GObject.GType<SettingVeth>;
        // Properties
        /**
         * This property specifies the peer interface name of the veth. This
         * property is mandatory.
         */
        get peer(): string;
        set peer(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingVeth.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingVeth.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingVeth;
        // Signals
        connect<K extends keyof NM.SettingVeth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVeth.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingVeth.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVeth.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingVeth.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingVeth.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_peer(): string;
    }
    class SettingVlan extends Setting {
        static '$gtype': GObject.GType<SettingVlan>;
        // Properties
        /**
         * For outgoing packets, a list of mappings from Linux SKB priorities to
         * 802.1p priorities.  The mapping is given in the format "from:to" where
         * both "from" and "to" are unsigned integers, ie "7:3".
         */
        get egress_priority_map(): string[];
        set egress_priority_map(val: string[]);
        /**
         * For outgoing packets, a list of mappings from Linux SKB priorities to
         * 802.1p priorities.  The mapping is given in the format "from:to" where
         * both "from" and "to" are unsigned integers, ie "7:3".
         */
        get egressPriorityMap(): string[];
        set egressPriorityMap(val: string[]);
        /**
         * One or more flags which control the behavior and features of the VLAN
         * interface.  Flags include %NM_VLAN_FLAG_REORDER_HEADERS (reordering of
         * output packet headers), %NM_VLAN_FLAG_GVRP (use of the GVRP protocol),
         * and %NM_VLAN_FLAG_LOOSE_BINDING (loose binding of the interface to its
         * controller device's operating state). %NM_VLAN_FLAG_MVRP (use of the MVRP
         * protocol).
         *
         * The default value of this property is NM_VLAN_FLAG_REORDER_HEADERS,
         * but it used to be 0. To preserve backward compatibility, the default-value
         * in the D-Bus API continues to be 0 and a missing property on D-Bus
         * is still considered as 0.
         */
        get flags(): NM.VlanFlags;
        set flags(val: NM.VlanFlags);
        /**
         * The VLAN identifier that the interface created by this connection should
         * be assigned. The valid range is from 0 to 4094, without the reserved id 4095.
         */
        get id(): number;
        set id(val: number);
        /**
         * For incoming packets, a list of mappings from 802.1p priorities to Linux
         * SKB priorities.  The mapping is given in the format "from:to" where both
         * "from" and "to" are unsigned integers, ie "7:3".
         */
        get ingress_priority_map(): string[];
        set ingress_priority_map(val: string[]);
        /**
         * For incoming packets, a list of mappings from 802.1p priorities to Linux
         * SKB priorities.  The mapping is given in the format "from:to" where both
         * "from" and "to" are unsigned integers, ie "7:3".
         */
        get ingressPriorityMap(): string[];
        set ingressPriorityMap(val: string[]);
        /**
         * If given, specifies the parent interface name or parent connection UUID
         * from which this VLAN interface should be created.  If this property is
         * not specified, the connection must contain an #NMSettingWired setting
         * with a #NMSettingWired:mac-address property.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Specifies the VLAN protocol to use for encapsulation.
         *
         * Supported values are: '802.1Q', '802.1ad'. If not specified the default
         * value is '802.1Q'.
         */
        get protocol(): string;
        set protocol(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingVlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingVlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingVlan;
        // Signals
        connect<K extends keyof NM.SettingVlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingVlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingVlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingVlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a priority mapping to the #NMSettingVlan:ingress_priority_map or
         * #NMSettingVlan:egress_priority_map properties of the setting. If `from` is
         * already in the given priority map, this function will overwrite the
         * existing entry with the new `to`.
         *
         * If `map` is #NM_VLAN_INGRESS_MAP then `from` is the incoming 802.1q VLAN
         * Priority Code Point (PCP) value, and `to` is the Linux SKB priority value.
         *
         * If `map` is #NM_VLAN_EGRESS_MAP then `from` is the Linux SKB priority value and
         * `to` is the outgoing 802.1q VLAN Priority Code Point (PCP) value.
         *
         * @returns %TRUE.
         * @param map the type of priority map
         * @param from the priority to map to `to`
         * @param to the priority to map `from` to
         */
        add_priority(map: NM.VlanPriorityMap | null, from: number, to: number): boolean;
        /**
         * Adds a priority map entry into either the #NMSettingVlan:ingress_priority_map
         * or the #NMSettingVlan:egress_priority_map properties.  The priority map maps
         * the Linux SKB priorities to 802.1p priorities.
         *
         * @returns %TRUE if the entry was successfully added to the list, or it
         * overwrote the old value, %FALSE if `str` is not a valid mapping.
         * @param map the type of priority map
         * @param str the string which contains a priority map, like "3:7"
         */
        add_priority_str(map: NM.VlanPriorityMap | null, str: string): boolean;
        /**
         * Clear all the entries from #NMSettingVlan:ingress_priority_map or
         * #NMSettingVlan:egress_priority_map properties.
         *
         * @param map the type of priority map
         */
        clear_priorities(map: NM.VlanPriorityMap | null): void;
        get_flags(): number;
        get_id(): number;
        /**
         * Returns the number of entries in the
         * #NMSettingVlan:ingress_priority_map or #NMSettingVlan:egress_priority_map
         * properties of this setting.
         *
         * @returns return the number of ingress/egress priority entries.
         * @param map the type of priority map
         */
        get_num_priorities(map: NM.VlanPriorityMap | null): number;
        get_parent(): string;
        /**
         * Retrieve one of the entries of the #NMSettingVlan:ingress_priority_map
         * or #NMSettingVlan:egress_priority_map properties of this setting.
         *
         * @returns returns %TRUE if `idx` is in range. Otherwise, %FALSE.
         * @param map the type of priority map
         * @param idx the zero-based index of the ingress/egress priority map entry
         */
        get_priority(map: NM.VlanPriorityMap | null, idx: number): [boolean, number, number];
        get_protocol(): string;
        /**
         * Removes the priority map at index `idx` from the
         * #NMSettingVlan:ingress_priority_map or #NMSettingVlan:egress_priority_map
         * properties.
         *
         * @param map the type of priority map
         * @param idx the zero-based index of the priority map to remove
         */
        remove_priority(map: NM.VlanPriorityMap | null, idx: number): void;
        /**
         * Removes the priority map `form:``to` from the #NMSettingVlan:ingress_priority_map
         * or #NMSettingVlan:egress_priority_map (according to `map` argument)
         * properties.
         *
         * @returns %TRUE if the priority mapping was found and removed; %FALSE if it was not.
         * @param map the type of priority map
         * @param from the priority to map to `to`
         * @param to the priority to map `from` to
         */
        remove_priority_by_value(map: NM.VlanPriorityMap | null, from: number, to: number): boolean;
        /**
         * Removes the priority map `str` from the #NMSettingVlan:ingress_priority_map
         * or #NMSettingVlan:egress_priority_map (according to `map` argument)
         * properties.
         *
         * @returns %TRUE if the priority mapping was found and removed; %FALSE if it was not.
         * @param map the type of priority map
         * @param str the string which contains a priority map, like "3:7"
         */
        remove_priority_str_by_value(map: NM.VlanPriorityMap | null, str: string): boolean;
    }
    class SettingVpn extends Setting {
        static '$gtype': GObject.GType<SettingVpn>;
        // Properties
        /**
         * Dictionary of key/value pairs of VPN plugin specific data.  Both keys and
         * values must be strings.
         */
        get data(): GLib.HashTable<string, string>;
        set data(val: GLib.HashTable<string, string>);
        /**
         * If the VPN service supports persistence, and this property is %TRUE,
         * the VPN will attempt to stay connected across link changes and outages,
         * until explicitly disconnected.
         */
        get persistent(): boolean;
        set persistent(val: boolean);
        /**
         * Dictionary of key/value pairs of VPN plugin specific secrets like
         * passwords or private keys.  Both keys and values must be strings.
         */
        get secrets(): GLib.HashTable<string, string>;
        set secrets(val: GLib.HashTable<string, string>);
        /**
         * D-Bus service name of the VPN plugin that this setting uses to connect to
         * its network.  i.e. org.freedesktop.NetworkManager.vpnc for the vpnc
         * plugin.
         */
        get service_type(): string;
        set service_type(val: string);
        /**
         * D-Bus service name of the VPN plugin that this setting uses to connect to
         * its network.  i.e. org.freedesktop.NetworkManager.vpnc for the vpnc
         * plugin.
         */
        get serviceType(): string;
        set serviceType(val: string);
        /**
         * Timeout for the VPN service to establish the connection. Some services
         * may take quite a long time to connect.
         * Value of 0 means a default timeout, which is 60 seconds (unless overridden
         * by vpn.timeout in configuration file). Values greater than zero mean
         * timeout in seconds.
         */
        get timeout(): number;
        set timeout(val: number);
        /**
         * If the VPN connection requires a user name for authentication, that name
         * should be provided here.  If the connection is available to more than one
         * user, and the VPN requires each user to supply a different name, then
         * leave this property empty.  If this property is empty, NetworkManager
         * will automatically supply the username of the user which requested the
         * VPN connection.
         */
        get user_name(): string;
        set user_name(val: string);
        /**
         * If the VPN connection requires a user name for authentication, that name
         * should be provided here.  If the connection is available to more than one
         * user, and the VPN requires each user to supply a different name, then
         * leave this property empty.  If this property is empty, NetworkManager
         * will automatically supply the username of the user which requested the
         * VPN connection.
         */
        get userName(): string;
        set userName(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingVpn.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingVpn.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingVpn;
        // Signals
        connect<K extends keyof NM.SettingVpn.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVpn.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingVpn.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVpn.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingVpn.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingVpn.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Establishes a relationship between `key` and `item` internally in the
         * setting which may be retrieved later.  Should not be used to store passwords
         * or other secrets, which is what nm_setting_vpn_add_secret() is for.
         *
         * Before 1.24, `item` must not be %NULL and not an empty string. Since 1.24,
         * `item` can be set to an empty string. It can also be set to %NULL to unset
         * the key. In that case, the behavior is as if calling nm_setting_vpn_remove_data_item().
         *
         * @param key a name that uniquely identifies the given value `item`
         * @param item the value to be referenced by `key`
         */
        add_data_item(key: string, item?: string | null): void;
        /**
         * Establishes a relationship between `key` and `secret` internally in the
         * setting which may be retrieved later.
         *
         * Before 1.24, `secret` must not be %NULL and not an empty string. Since 1.24,
         * `secret` can be set to an empty string. It can also be set to %NULL to unset
         * the key. In that case, the behavior is as if calling nm_setting_vpn_remove_secret().
         *
         * @param key a name that uniquely identifies the given secret `secret`
         * @param secret the secret to be referenced by `key`
         */
        add_secret(key: string, secret?: string | null): void;
        /**
         * Iterates all data items stored in this setting.  It is safe to add, remove,
         * and modify data items inside `func,` though any additions or removals made
         * during iteration will not be part of the iteration.
         *
         * @param func an user provided function
         */
        foreach_data_item(func: NM.VpnIterFunc): void;
        /**
         * Iterates all secrets stored in this setting.  It is safe to add, remove,
         * and modify secrets inside `func,` though any additions or removals made during
         * iteration will not be part of the iteration.
         *
         * @param func an user provided function
         */
        foreach_secret(func: NM.VpnIterFunc): void;
        /**
         * Retrieves the data item of a key/value relationship previously established
         * by nm_setting_vpn_add_data_item().
         *
         * @returns the data item, if any
         * @param key the name of the data item to retrieve
         */
        get_data_item(key: string): string;
        /**
         * Retrieves every data key inside `setting,` as an array.
         *
         * @returns a
         *   %NULL-terminated array containing each data key or %NULL if
         *   there are no data items.
         */
        get_data_keys(): string[] | null;
        /**
         * Gets number of key/value pairs of VPN configuration data.
         *
         * @returns the number of VPN plugin specific configuration data items
         */
        get_num_data_items(): number;
        /**
         * Gets number of VPN plugin specific secrets in the setting.
         *
         * @returns the number of VPN plugin specific secrets
         */
        get_num_secrets(): number;
        get_persistent(): boolean;
        /**
         * Retrieves the secret of a key/value relationship previously established
         * by nm_setting_vpn_add_secret().
         *
         * @returns the secret, if any
         * @param key the name of the secret to retrieve
         */
        get_secret(key: string): string;
        /**
         * Retrieves every secret key inside `setting,` as an array.
         *
         * @returns a
         *   %NULL-terminated array containing each secret key or %NULL if
         *   there are no secrets.
         */
        get_secret_keys(): string[] | null;
        /**
         * Returns the service name of the VPN, which identifies the specific VPN
         * plugin that should be used to connect to this VPN.
         *
         * @returns the VPN plugin's service name
         */
        get_service_type(): string;
        get_timeout(): number;
        get_user_name(): string;
        /**
         * Deletes a key/value relationship previously established by
         * nm_setting_vpn_add_data_item().
         *
         * @returns %TRUE if the data item was found and removed from the internal list,
         * %FALSE if it was not.
         * @param key the name of the data item to remove
         */
        remove_data_item(key: string): boolean;
        /**
         * Deletes a key/value relationship previously established by
         * nm_setting_vpn_add_secret().
         *
         * @returns %TRUE if the secret was found and removed from the internal list,
         * %FALSE if it was not.
         * @param key the name of the secret to remove
         */
        remove_secret(key: string): boolean;
    }
    class SettingVrf extends Setting {
        static '$gtype': GObject.GType<SettingVrf>;
        // Properties
        /**
         * The routing table for this VRF.
         */
        get table(): number;
        set table(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingVrf.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingVrf.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingVrf;
        // Signals
        connect<K extends keyof NM.SettingVrf.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVrf.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingVrf.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVrf.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingVrf.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingVrf.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_table(): number;
    }
    class SettingVxlan extends Setting {
        static '$gtype': GObject.GType<SettingVxlan>;
        // Properties
        /**
         * Specifies the lifetime in seconds of FDB entries learnt by the kernel.
         */
        get ageing(): number;
        set ageing(val: number);
        /**
         * Specifies the UDP destination port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get destination_port(): number;
        set destination_port(val: number);
        /**
         * Specifies the UDP destination port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get destinationPort(): number;
        set destinationPort(val: number);
        /**
         * Specifies the VXLAN Network Identifier (or VXLAN Segment Identifier) to
         * use.
         */
        get id(): number;
        set id(val: number);
        /**
         * Specifies whether netlink LL ADDR miss notifications are generated.
         */
        get l2_miss(): boolean;
        set l2_miss(val: boolean);
        /**
         * Specifies whether netlink LL ADDR miss notifications are generated.
         */
        get l2Miss(): boolean;
        set l2Miss(val: boolean);
        /**
         * Specifies whether netlink IP ADDR miss notifications are generated.
         */
        get l3_miss(): boolean;
        set l3_miss(val: boolean);
        /**
         * Specifies whether netlink IP ADDR miss notifications are generated.
         */
        get l3Miss(): boolean;
        set l3Miss(val: boolean);
        /**
         * Specifies whether unknown source link layer addresses and IP addresses
         * are entered into the VXLAN device forwarding database.
         */
        get learning(): boolean;
        set learning(val: boolean);
        /**
         * Specifies the maximum number of FDB entries. A value of zero means that
         * the kernel will store unlimited entries.
         */
        get limit(): number;
        set limit(val: number);
        /**
         * If given, specifies the source IP address to use in outgoing packets.
         */
        get local(): string;
        set local(val: string);
        /**
         * If given, specifies the parent interface name or parent connection UUID.
         */
        get parent(): string;
        set parent(val: string);
        /**
         * Specifies whether ARP proxy is turned on.
         */
        get proxy(): boolean;
        set proxy(val: boolean);
        /**
         * Specifies the unicast destination IP address to use in outgoing packets
         * when the destination link layer address is not known in the VXLAN device
         * forwarding database, or the multicast IP address to join.
         */
        get remote(): string;
        set remote(val: string);
        /**
         * Specifies whether route short circuit is turned on.
         */
        get rsc(): boolean;
        set rsc(val: boolean);
        /**
         * Specifies the maximum UDP source port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get source_port_max(): number;
        set source_port_max(val: number);
        /**
         * Specifies the maximum UDP source port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get sourcePortMax(): number;
        set sourcePortMax(val: number);
        /**
         * Specifies the minimum UDP source port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get source_port_min(): number;
        set source_port_min(val: number);
        /**
         * Specifies the minimum UDP source port to communicate to the remote VXLAN
         * tunnel endpoint.
         */
        get sourcePortMin(): number;
        set sourcePortMin(val: number);
        /**
         * Specifies the TOS value to use in outgoing packets.
         */
        get tos(): number;
        set tos(val: number);
        /**
         * Specifies the time-to-live value to use in outgoing packets.
         */
        get ttl(): number;
        set ttl(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingVxlan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingVxlan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingVxlan;
        // Signals
        connect<K extends keyof NM.SettingVxlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVxlan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingVxlan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingVxlan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingVxlan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingVxlan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_ageing(): number;
        get_destination_port(): number;
        get_id(): number;
        get_l2_miss(): boolean;
        get_l3_miss(): boolean;
        get_learning(): boolean;
        get_limit(): number;
        get_local(): string;
        get_parent(): string;
        get_proxy(): boolean;
        get_remote(): string;
        get_rsc(): boolean;
        get_source_port_max(): number;
        get_source_port_min(): number;
        get_tos(): number;
        get_ttl(): number;
    }
    class SettingWifiP2P extends Setting {
        static '$gtype': GObject.GType<SettingWifiP2P>;
        // Properties
        /**
         * The P2P device that should be connected to. Currently, this is the only
         * way to create or join a group.
         */
        get peer(): string;
        set peer(val: string);
        /**
         * The Wi-Fi Display (WFD) Information Elements (IEs) to set.
         *
         * Wi-Fi Display requires a protocol specific information element to be
         * set in certain Wi-Fi frames. These can be specified here for the
         * purpose of establishing a connection.
         * This setting is only useful when implementing a Wi-Fi Display client.
         */
        get wfd_ies(): GLib.Bytes;
        set wfd_ies(val: GLib.Bytes);
        /**
         * The Wi-Fi Display (WFD) Information Elements (IEs) to set.
         *
         * Wi-Fi Display requires a protocol specific information element to be
         * set in certain Wi-Fi frames. These can be specified here for the
         * purpose of establishing a connection.
         * This setting is only useful when implementing a Wi-Fi Display client.
         */
        get wfdIes(): GLib.Bytes;
        set wfdIes(val: GLib.Bytes);
        /**
         * Flags indicating which mode of WPS is to be used.
         *
         * There's little point in changing the default setting as NetworkManager will
         * automatically determine the best method to use.
         */
        get wps_method(): number;
        set wps_method(val: number);
        /**
         * Flags indicating which mode of WPS is to be used.
         *
         * There's little point in changing the default setting as NetworkManager will
         * automatically determine the best method to use.
         */
        get wpsMethod(): number;
        set wpsMethod(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWifiP2P.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWifiP2P.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWifiP2P;
        // Signals
        connect<K extends keyof NM.SettingWifiP2P.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWifiP2P.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWifiP2P.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWifiP2P.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWifiP2P.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWifiP2P.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_peer(): string;
        get_wfd_ies(): GLib.Bytes;
        get_wps_method(): NM.SettingWirelessSecurityWpsMethod;
    }
    class SettingWimax extends Setting {
        static '$gtype': GObject.GType<SettingWimax>;
        // Properties
        /**
         * If specified, this connection will only apply to the WiMAX device whose
         * MAC address matches. This property does not change the MAC address of the
         * device (known as MAC spoofing).
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, this connection will only apply to the WiMAX device whose
         * MAC address matches. This property does not change the MAC address of the
         * device (known as MAC spoofing).
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * Network Service Provider (NSP) name of the WiMAX network this connection
         * should use.
         */
        get network_name(): string;
        set network_name(val: string);
        /**
         * Network Service Provider (NSP) name of the WiMAX network this connection
         * should use.
         */
        get networkName(): string;
        set networkName(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWimax.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWimax.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWimax;
        // Signals
        connect<K extends keyof NM.SettingWimax.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWimax.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWimax.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWimax.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWimax.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWimax.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Returns the MAC address of a WiMAX device which this connection is locked
         * to.
         *
         * @returns the MAC address
         */
        get_mac_address(): string;
        /**
         * Returns the WiMAX NSP name (ex "Sprint" or "CLEAR") which identifies the
         * specific WiMAX network this setting describes a connection to.
         *
         * @returns the WiMAX NSP name
         */
        get_network_name(): string;
    }
    class SettingWireGuard extends Setting {
        static '$gtype': GObject.GType<SettingWireGuard>;
        // Properties
        /**
         * The use of fwmark is optional and is by default off. Setting it to 0
         * disables it. Otherwise, it is a 32-bit fwmark for outgoing packets.
         *
         * Note that "ip4-auto-default-route" or "ip6-auto-default-route" enabled,
         * implies to automatically choose a fwmark.
         */
        get fwmark(): number;
        set fwmark(val: number);
        /**
         * Whether to enable special handling of the IPv4 default route.
         * If enabled, the IPv4 default route from wireguard.peer-routes
         * will be placed to a dedicated routing-table and two policy routing rules
         * will be added. The fwmark number is also used as routing-table for the default-route,
         * and if fwmark is zero, an unused fwmark/table is chosen automatically.
         * This corresponds to what wg-quick does with Table=auto and what WireGuard
         * calls "Improved Rule-based Routing".
         *
         * Note that for this automatism to work, you usually don't want to set
         * ipv4.gateway, because that will result in a conflicting default route.
         *
         * Leaving this at the default will enable this option automatically
         * if ipv4.never-default is not set and there are any peers that use
         * a default-route as allowed-ips. Since this automatism only makes
         * sense if you also have a peer with an /0 allowed-ips, it is usually
         * not necessary to enable this explicitly. However, you can disable
         * it if you want to configure your own routing and rules.
         */
        get ip4_auto_default_route(): NM.Ternary;
        set ip4_auto_default_route(val: NM.Ternary);
        /**
         * Whether to enable special handling of the IPv4 default route.
         * If enabled, the IPv4 default route from wireguard.peer-routes
         * will be placed to a dedicated routing-table and two policy routing rules
         * will be added. The fwmark number is also used as routing-table for the default-route,
         * and if fwmark is zero, an unused fwmark/table is chosen automatically.
         * This corresponds to what wg-quick does with Table=auto and what WireGuard
         * calls "Improved Rule-based Routing".
         *
         * Note that for this automatism to work, you usually don't want to set
         * ipv4.gateway, because that will result in a conflicting default route.
         *
         * Leaving this at the default will enable this option automatically
         * if ipv4.never-default is not set and there are any peers that use
         * a default-route as allowed-ips. Since this automatism only makes
         * sense if you also have a peer with an /0 allowed-ips, it is usually
         * not necessary to enable this explicitly. However, you can disable
         * it if you want to configure your own routing and rules.
         */
        get ip4AutoDefaultRoute(): NM.Ternary;
        set ip4AutoDefaultRoute(val: NM.Ternary);
        /**
         * Like ip4-auto-default-route, but for the IPv6 default route.
         */
        get ip6_auto_default_route(): NM.Ternary;
        set ip6_auto_default_route(val: NM.Ternary);
        /**
         * Like ip4-auto-default-route, but for the IPv6 default route.
         */
        get ip6AutoDefaultRoute(): NM.Ternary;
        set ip6AutoDefaultRoute(val: NM.Ternary);
        /**
         * The listen-port. If listen-port is not specified, the port will be chosen
         * randomly when the interface comes up.
         */
        get listen_port(): number;
        set listen_port(val: number);
        /**
         * The listen-port. If listen-port is not specified, the port will be chosen
         * randomly when the interface comes up.
         */
        get listenPort(): number;
        set listenPort(val: number);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple fragments.
         *
         * If zero a default MTU is used. Note that contrary to wg-quick's MTU
         * setting, this does not take into account the current routes at the
         * time of activation.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * Whether to automatically add routes for the AllowedIPs ranges
         * of the peers. If %TRUE (the default), NetworkManager will automatically
         * add routes in the routing tables according to ipv4.route-table and
         * ipv6.route-table. Usually you want this automatism enabled.
         * If %FALSE, no such routes are added automatically. In this case, the
         * user may want to configure static routes in ipv4.routes and ipv6.routes,
         * respectively.
         *
         * Note that if the peer's AllowedIPs is "0.0.0.0/0" or "::/0" and the profile's
         * ipv4.never-default or ipv6.never-default setting is enabled, the peer route for
         * this peer won't be added automatically.
         */
        get peer_routes(): boolean;
        set peer_routes(val: boolean);
        /**
         * Whether to automatically add routes for the AllowedIPs ranges
         * of the peers. If %TRUE (the default), NetworkManager will automatically
         * add routes in the routing tables according to ipv4.route-table and
         * ipv6.route-table. Usually you want this automatism enabled.
         * If %FALSE, no such routes are added automatically. In this case, the
         * user may want to configure static routes in ipv4.routes and ipv6.routes,
         * respectively.
         *
         * Note that if the peer's AllowedIPs is "0.0.0.0/0" or "::/0" and the profile's
         * ipv4.never-default or ipv6.never-default setting is enabled, the peer route for
         * this peer won't be added automatically.
         */
        get peerRoutes(): boolean;
        set peerRoutes(val: boolean);
        /**
         * The 256 bit private-key in base64 encoding.
         */
        get private_key(): string;
        set private_key(val: string);
        /**
         * The 256 bit private-key in base64 encoding.
         */
        get privateKey(): string;
        set privateKey(val: string);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:private-key
         * property.
         */
        get private_key_flags(): NM.SettingSecretFlags;
        set private_key_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:private-key
         * property.
         */
        get privateKeyFlags(): NM.SettingSecretFlags;
        set privateKeyFlags(val: NM.SettingSecretFlags);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWireGuard.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWireGuard.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWireGuard;
        // Signals
        connect<K extends keyof NM.SettingWireGuard.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWireGuard.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWireGuard.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWireGuard.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWireGuard.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWireGuard.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * If a peer with the same public-key already exists, that
         * one is replaced by `peer`. The new `peer` is always appended
         * (or moved to) the end, so in case a peer is replaced, the
         * indexes are shifted and the number of peers stays unchanged.
         *
         * @param peer the #NMWireGuardPeer instance to append.
         *   This seals `peer` and keeps a reference on the
         *   instance.
         */
        append_peer(peer: NM.WireGuardPeer): void;
        clear_peers(): number;
        get_fwmark(): number;
        get_ip4_auto_default_route(): NM.Ternary;
        get_ip6_auto_default_route(): NM.Ternary;
        get_listen_port(): number;
        get_mtu(): number;
        get_peer(idx: number): NM.WireGuardPeer;
        get_peer_by_public_key(public_key: string): [NM.WireGuardPeer | null, number];
        get_peer_routes(): boolean;
        get_peers_len(): number;
        get_private_key(): string;
        get_private_key_flags(): NM.SettingSecretFlags;
        remove_peer(idx: number): boolean;
        /**
         * If `idx` is one past the last peer, the behavior is the same
         * as nm_setting_wireguard_append_peer().
         * Otherwise, the peer will be at `idx` and replace the peer
         * instance at that index. Note that if a peer with the same
         * public-key exists on another index, then that peer will also
         * be replaced. In that case, the number of peers will shrink
         * by one (because the one at `idx` got replace and then one
         * with the same public-key got removed). This also means,
         * that the resulting index afterwards may be one less than
         * `idx` (if another peer with a lower index was dropped).
         *
         * @param peer the #NMWireGuardPeer instance to set.
         *   This seals `peer` and keeps a reference on the
         *   instance.
         * @param idx the index, in the range of 0 to the number of
         *   peers (including). That means, if `idx` is one past
         *   the end of the number of peers, this is the same as
         *   nm_setting_wireguard_append_peer(). Otherwise, the
         *   peer at this index is replaced.
         */
        set_peer(peer: NM.WireGuardPeer, idx: number): void;
    }
    class SettingWired extends Setting {
        static '$gtype': GObject.GType<SettingWired>;
        // Properties
        /**
         * When %TRUE, setup the interface to accept packets for all MAC addresses.
         * This is enabling the kernel interface flag IFF_PROMISC.
         * When %FALSE, the interface will only accept the packets with the
         * interface destination mac address or broadcast.
         */
        get accept_all_mac_addresses(): NM.Ternary;
        set accept_all_mac_addresses(val: NM.Ternary);
        /**
         * When %TRUE, setup the interface to accept packets for all MAC addresses.
         * This is enabling the kernel interface flag IFF_PROMISC.
         * When %FALSE, the interface will only accept the packets with the
         * interface destination mac address or broadcast.
         */
        get acceptAllMacAddresses(): NM.Ternary;
        set acceptAllMacAddresses(val: NM.Ternary);
        /**
         * When %TRUE, enforce auto-negotiation of speed and duplex mode.
         * If "speed" and "duplex" properties are both specified, only that
         * single mode will be advertised and accepted during the link
         * auto-negotiation process: this works only for BASE-T 802.3 specifications
         * and is useful for enforcing gigabits modes, as in these cases link
         * negotiation is mandatory.
         * When %FALSE, "speed" and "duplex" properties should be both set or
         * link configuration will be skipped.
         */
        get auto_negotiate(): boolean;
        set auto_negotiate(val: boolean);
        /**
         * When %TRUE, enforce auto-negotiation of speed and duplex mode.
         * If "speed" and "duplex" properties are both specified, only that
         * single mode will be advertised and accepted during the link
         * auto-negotiation process: this works only for BASE-T 802.3 specifications
         * and is useful for enforcing gigabits modes, as in these cases link
         * negotiation is mandatory.
         * When %FALSE, "speed" and "duplex" properties should be both set or
         * link configuration will be skipped.
         */
        get autoNegotiate(): boolean;
        set autoNegotiate(val: boolean);
        /**
         * If specified, request that the device use this MAC address instead.
         * This is known as MAC cloning or spoofing.
         *
         * Beside explicitly specifying a MAC address, the special values "preserve", "permanent",
         * "random" and "stable" are supported.
         * "preserve" means not to touch the MAC address on activation.
         * "permanent" means to use the permanent hardware address if the device
         * has one (otherwise this is treated as "preserve").
         * "random" creates a random MAC address on each connect.
         * "stable" creates a hashed MAC address based on connection.stable-id and a
         * machine dependent key.
         *
         * If unspecified, the value can be overwritten via global defaults, see manual
         * of NetworkManager.conf. If still unspecified, it defaults to "preserve"
         * (older versions of NetworkManager may use a different default value).
         *
         * On D-Bus, this field is expressed as "assigned-mac-address" or the deprecated
         * "cloned-mac-address".
         */
        get cloned_mac_address(): string;
        set cloned_mac_address(val: string);
        /**
         * If specified, request that the device use this MAC address instead.
         * This is known as MAC cloning or spoofing.
         *
         * Beside explicitly specifying a MAC address, the special values "preserve", "permanent",
         * "random" and "stable" are supported.
         * "preserve" means not to touch the MAC address on activation.
         * "permanent" means to use the permanent hardware address if the device
         * has one (otherwise this is treated as "preserve").
         * "random" creates a random MAC address on each connect.
         * "stable" creates a hashed MAC address based on connection.stable-id and a
         * machine dependent key.
         *
         * If unspecified, the value can be overwritten via global defaults, see manual
         * of NetworkManager.conf. If still unspecified, it defaults to "preserve"
         * (older versions of NetworkManager may use a different default value).
         *
         * On D-Bus, this field is expressed as "assigned-mac-address" or the deprecated
         * "cloned-mac-address".
         */
        get clonedMacAddress(): string;
        set clonedMacAddress(val: string);
        /**
         * When a value is set, either "half" or "full", configures the device
         * to use the specified duplex mode. If "auto-negotiate" is "yes" the
         * specified duplex mode will be the only one advertised during link
         * negotiation: this works only for BASE-T 802.3 specifications and is
         * useful for enforcing gigabits modes, as in these cases link negotiation
         * is mandatory.
         * If the value is unset (the default), the link configuration will be
         * either skipped (if "auto-negotiate" is "no", the default) or will
         * be auto-negotiated (if "auto-negotiate" is "yes") and the local device
         * will advertise all the supported duplex modes.
         * Must be set together with the "speed" property if specified.
         * Before specifying a duplex mode be sure your device supports it.
         */
        get duplex(): string;
        set duplex(val: string);
        /**
         * With #NMSettingWired:cloned-mac-address setting "random" or "stable",
         * by default all bits of the MAC address are scrambled and a locally-administered,
         * unicast MAC address is created. This property allows one to specify that certain bits
         * are fixed. Note that the least significant bit of the first MAC address will
         * always be unset to create a unicast MAC address.
         *
         * If the property is %NULL, it is eligible to be overwritten by a default
         * connection setting. If the value is still %NULL or an empty string, the
         * default is to create a locally-administered, unicast MAC address.
         *
         * If the value contains one MAC address, this address is used as mask. The set
         * bits of the mask are to be filled with the current MAC address of the device,
         * while the unset bits are subject to randomization.
         * Setting "FE:FF:FF:00:00:00" means to preserve the OUI of the current MAC address
         * and only randomize the lower 3 bytes using the "random" or "stable" algorithm.
         *
         * If the value contains one additional MAC address after the mask,
         * this address is used instead of the current MAC address to fill the bits
         * that shall not be randomized. For example, a value of
         * "FE:FF:FF:00:00:00 68:F7:28:00:00:00" will set the OUI of the MAC address
         * to 68:F7:28, while the lower bits are randomized. A value of
         * "02:00:00:00:00:00 00:00:00:00:00:00" will create a fully scrambled
         * globally-administered, burned-in MAC address.
         *
         * If the value contains more than one additional MAC addresses, one of
         * them is chosen randomly. For example, "02:00:00:00:00:00 00:00:00:00:00:00 02:00:00:00:00:00"
         * will create a fully scrambled MAC address, randomly locally or globally
         * administered.
         */
        get generate_mac_address_mask(): string;
        set generate_mac_address_mask(val: string);
        /**
         * With #NMSettingWired:cloned-mac-address setting "random" or "stable",
         * by default all bits of the MAC address are scrambled and a locally-administered,
         * unicast MAC address is created. This property allows one to specify that certain bits
         * are fixed. Note that the least significant bit of the first MAC address will
         * always be unset to create a unicast MAC address.
         *
         * If the property is %NULL, it is eligible to be overwritten by a default
         * connection setting. If the value is still %NULL or an empty string, the
         * default is to create a locally-administered, unicast MAC address.
         *
         * If the value contains one MAC address, this address is used as mask. The set
         * bits of the mask are to be filled with the current MAC address of the device,
         * while the unset bits are subject to randomization.
         * Setting "FE:FF:FF:00:00:00" means to preserve the OUI of the current MAC address
         * and only randomize the lower 3 bytes using the "random" or "stable" algorithm.
         *
         * If the value contains one additional MAC address after the mask,
         * this address is used instead of the current MAC address to fill the bits
         * that shall not be randomized. For example, a value of
         * "FE:FF:FF:00:00:00 68:F7:28:00:00:00" will set the OUI of the MAC address
         * to 68:F7:28, while the lower bits are randomized. A value of
         * "02:00:00:00:00:00 00:00:00:00:00:00" will create a fully scrambled
         * globally-administered, burned-in MAC address.
         *
         * If the value contains more than one additional MAC addresses, one of
         * them is chosen randomly. For example, "02:00:00:00:00:00 00:00:00:00:00:00 02:00:00:00:00:00"
         * will create a fully scrambled MAC address, randomly locally or globally
         * administered.
         */
        get generateMacAddressMask(): string;
        set generateMacAddressMask(val: string);
        /**
         * If specified, this connection will only apply to the Ethernet device
         * whose permanent MAC address matches. This property does not change the
         * MAC address of the device (i.e. MAC spoofing).
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, this connection will only apply to the Ethernet device
         * whose permanent MAC address matches. This property does not change the
         * MAC address of the device (i.e. MAC spoofing).
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * If specified, this connection will never apply to the Ethernet device
         * whose permanent MAC address matches an address in the list.  Each MAC
         * address is in the standard hex-digits-and-colons notation
         * (00:11:22:33:44:55).
         */
        get mac_address_blacklist(): string[];
        set mac_address_blacklist(val: string[]);
        /**
         * If specified, this connection will never apply to the Ethernet device
         * whose permanent MAC address matches an address in the list.  Each MAC
         * address is in the standard hex-digits-and-colons notation
         * (00:11:22:33:44:55).
         */
        get macAddressBlacklist(): string[];
        set macAddressBlacklist(val: string[]);
        /**
         * If specified, this connection will never apply to the Ethernet device
         * whose permanent MAC address matches an address in the list.  Each MAC
         * address is in the standard hex-digits-and-colons notation
         * (00:11:22:33:44:55).
         */
        get mac_address_denylist(): string[];
        set mac_address_denylist(val: string[]);
        /**
         * If specified, this connection will never apply to the Ethernet device
         * whose permanent MAC address matches an address in the list.  Each MAC
         * address is in the standard hex-digits-and-colons notation
         * (00:11:22:33:44:55).
         */
        get macAddressDenylist(): string[];
        set macAddressDenylist(val: string[]);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple Ethernet frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * Specific port type to use if the device supports multiple
         * attachment methods.  One of "tp" (Twisted Pair), "aui" (Attachment Unit
         * Interface), "bnc" (Thin Ethernet) or "mii" (Media Independent Interface).
         * If the device supports only one port type, this setting is ignored.
         */
        get port(): string;
        set port(val: string);
        /**
         * s390 network device type; one of "qeth", "lcs", or "ctc", representing
         * the different types of virtual network devices available on s390 systems.
         */
        get s390_nettype(): string;
        set s390_nettype(val: string);
        /**
         * s390 network device type; one of "qeth", "lcs", or "ctc", representing
         * the different types of virtual network devices available on s390 systems.
         */
        get s390Nettype(): string;
        set s390Nettype(val: string);
        /**
         * Dictionary of key/value pairs of s390-specific device options.  Both keys
         * and values must be strings.  Allowed keys include "portno", "layer2",
         * "portname", "protocol", among others.  Key names must contain only
         * alphanumeric characters (ie, [a-zA-Z0-9]).
         *
         * Currently, NetworkManager itself does nothing with this information.
         * However, s390utils ships a udev rule which parses this information
         * and applies it to the interface.
         */
        get s390_options(): GLib.HashTable<string, string>;
        set s390_options(val: GLib.HashTable<string, string>);
        /**
         * Dictionary of key/value pairs of s390-specific device options.  Both keys
         * and values must be strings.  Allowed keys include "portno", "layer2",
         * "portname", "protocol", among others.  Key names must contain only
         * alphanumeric characters (ie, [a-zA-Z0-9]).
         *
         * Currently, NetworkManager itself does nothing with this information.
         * However, s390utils ships a udev rule which parses this information
         * and applies it to the interface.
         */
        get s390Options(): GLib.HashTable<string, string>;
        set s390Options(val: GLib.HashTable<string, string>);
        /**
         * Identifies specific subchannels that this network device uses for
         * communication with z/VM or s390 host.  Like the
         * #NMSettingWired:mac-address property for non-z/VM devices, this property
         * can be used to ensure this connection only applies to the network device
         * that uses these subchannels.  The list should contain exactly 3 strings,
         * and each string may only be composed of hexadecimal characters and the
         * period (.) character.
         */
        get s390_subchannels(): string[];
        set s390_subchannels(val: string[]);
        /**
         * Identifies specific subchannels that this network device uses for
         * communication with z/VM or s390 host.  Like the
         * #NMSettingWired:mac-address property for non-z/VM devices, this property
         * can be used to ensure this connection only applies to the network device
         * that uses these subchannels.  The list should contain exactly 3 strings,
         * and each string may only be composed of hexadecimal characters and the
         * period (.) character.
         */
        get s390Subchannels(): string[];
        set s390Subchannels(val: string[]);
        /**
         * When a value greater than 0 is set, configures the device to use
         * the specified speed. If "auto-negotiate" is "yes" the specified
         * speed will be the only one advertised during link negotiation:
         * this works only for BASE-T 802.3 specifications and is useful for
         * enforcing gigabit speeds, as in this case link negotiation is
         * mandatory.
         * If the value is unset (0, the default), the link configuration will be
         * either skipped (if "auto-negotiate" is "no", the default) or will
         * be auto-negotiated (if "auto-negotiate" is "yes") and the local device
         * will advertise all the supported speeds.
         * In Mbit/s, ie 100 == 100Mbit/s.
         * Must be set together with the "duplex" property when non-zero.
         * Before specifying a speed value be sure your device supports it.
         */
        get speed(): number;
        set speed(val: number);
        /**
         * The #NMSettingWiredWakeOnLan options to enable. Not all devices support all options.
         * May be any combination of %NM_SETTING_WIRED_WAKE_ON_LAN_PHY,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_UNICAST, %NM_SETTING_WIRED_WAKE_ON_LAN_MULTICAST,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_BROADCAST, %NM_SETTING_WIRED_WAKE_ON_LAN_ARP,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_MAGIC or the special values
         * %NM_SETTING_WIRED_WAKE_ON_LAN_DEFAULT (to use global settings) and
         * %NM_SETTING_WIRED_WAKE_ON_LAN_IGNORE (to disable management of Wake-on-LAN in
         * NetworkManager).
         */
        get wake_on_lan(): number;
        set wake_on_lan(val: number);
        /**
         * The #NMSettingWiredWakeOnLan options to enable. Not all devices support all options.
         * May be any combination of %NM_SETTING_WIRED_WAKE_ON_LAN_PHY,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_UNICAST, %NM_SETTING_WIRED_WAKE_ON_LAN_MULTICAST,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_BROADCAST, %NM_SETTING_WIRED_WAKE_ON_LAN_ARP,
         * %NM_SETTING_WIRED_WAKE_ON_LAN_MAGIC or the special values
         * %NM_SETTING_WIRED_WAKE_ON_LAN_DEFAULT (to use global settings) and
         * %NM_SETTING_WIRED_WAKE_ON_LAN_IGNORE (to disable management of Wake-on-LAN in
         * NetworkManager).
         */
        get wakeOnLan(): number;
        set wakeOnLan(val: number);
        /**
         * If specified, the password used with magic-packet-based
         * Wake-on-LAN, represented as an Ethernet MAC address.  If %NULL,
         * no password will be required.
         */
        get wake_on_lan_password(): string;
        set wake_on_lan_password(val: string);
        /**
         * If specified, the password used with magic-packet-based
         * Wake-on-LAN, represented as an Ethernet MAC address.  If %NULL,
         * no password will be required.
         */
        get wakeOnLanPassword(): string;
        set wakeOnLanPassword(val: string);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWired.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWired.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWired;
        // Signals
        connect<K extends keyof NM.SettingWired.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWired.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWired.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWired.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWired.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWired.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a new MAC address to the #NMSettingWired:mac-address-blacklist property.
         *
         * @returns %TRUE if the MAC address was added; %FALSE if the MAC address
         * is invalid or was already present
         * @param mac the MAC address string (hex-digits-and-colons notation) to blacklist
         */
        add_mac_blacklist_item(mac: string): boolean;
        /**
         * Adds a new MAC address to the #NMSettingWired:mac-address-denylist property.
         *
         * @returns %TRUE if the MAC address was added; %FALSE if the MAC address
         * is invalid or was already present
         * @param mac the MAC address string (hex-digits-and-colons notation) to denylist
         */
        add_mac_denylist_item(mac: string): boolean;
        /**
         * Add an option to the table. If the key already exists, the value gets
         * replaced.
         *
         * Before 1.32, the function would assert that the key is valid. Since then,
         * an invalid key gets silently added but renders the profile as invalid.
         *
         * @returns since 1.32 this always returns %TRUE.
         * @param key key name for the option
         * @param value value for the option
         */
        add_s390_option(key: string, value: string): boolean;
        /**
         * Removes all blacklisted MAC addresses.
         */
        clear_mac_blacklist_items(): void;
        /**
         * Removes all denylisted MAC addresses.
         */
        clear_mac_denylist_items(): void;
        get_accept_all_mac_addresses(): NM.Ternary;
        get_auto_negotiate(): boolean;
        get_cloned_mac_address(): string;
        get_duplex(): string;
        get_generate_mac_address_mask(): string;
        get_mac_address(): string;
        get_mac_address_blacklist(): string[];
        get_mac_address_denylist(): string[];
        /**
         * Since 1.48, access at index "len" is allowed and returns NULL.
         *
         * @returns the blacklisted MAC address string (hex-digits-and-colons notation)
         * at index `idx`
         * @param idx the zero-based index of the MAC address entry
         */
        get_mac_blacklist_item(idx: number): string;
        get_mac_denylist_item(idx: number): string;
        get_mtu(): number;
        get_num_mac_blacklist_items(): number;
        get_num_mac_denylist_items(): number;
        /**
         * Returns the number of s390-specific options that should be set for this
         * device when it is activated.  This can be used to retrieve each s390
         * option individually using nm_setting_wired_get_s390_option().
         *
         * @returns the number of s390-specific device options
         */
        get_num_s390_options(): number;
        get_port(): string;
        /**
         * Returns the s390 device type this connection should apply to.  Will be one
         * of 'qeth', 'lcs', or 'ctc'.
         *
         * @returns the s390 device type
         */
        get_s390_nettype(): string;
        /**
         * Given an index, return the value of the s390 option at that index.  indexes
         * are *not* guaranteed to be static across modifications to options done by
         * nm_setting_wired_add_s390_option() and nm_setting_wired_remove_s390_option(),
         * and should not be used to refer to options except for short periods of time
         * such as during option iteration.
         *
         * @returns %TRUE on success if the index was valid and an option was found,
         * %FALSE if the index was invalid (ie, greater than the number of options
         * currently held by the setting)
         * @param idx index of the desired option, from 0 to
         * nm_setting_wired_get_num_s390_options() - 1
         */
        get_s390_option(idx: number): [boolean, string, string];
        /**
         * Returns the value associated with the s390-specific option specified by
         * `key,` if it exists.
         *
         * @returns the value, or %NULL if the key/value pair was never added to the
         * setting; the value is owned by the setting and must not be modified
         * @param key the key for which to retrieve the value
         */
        get_s390_option_by_key(key: string): string;
        /**
         * Return the list of s390 subchannels that identify the device that this
         * connection is applicable to.  The connection should only be used in
         * conjunction with that device.
         *
         * @returns array of strings, each specifying
         *   one subchannel the s390 device uses to communicate to the host.
         */
        get_s390_subchannels(): string[];
        get_speed(): number;
        /**
         * Returns a list of valid s390 options.
         *
         * The `setting` argument is unused and %NULL may be passed instead.
         *
         * @returns a %NULL-terminated array of strings of valid s390 options.
         */
        get_valid_s390_options(): string[];
        /**
         * Returns the Wake-on-LAN options enabled for the connection
         *
         * @returns the Wake-on-LAN options
         */
        get_wake_on_lan(): NM.SettingWiredWakeOnLan;
        /**
         * Returns the Wake-on-LAN password. This only applies to
         * %NM_SETTING_WIRED_WAKE_ON_LAN_MAGIC.
         *
         * @returns the Wake-on-LAN setting password, or %NULL if there is no password.
         */
        get_wake_on_lan_password(): string;
        /**
         * Removes the MAC address at index `idx` from the blacklist.
         *
         * @param idx index number of the MAC address
         */
        remove_mac_blacklist_item(idx: number): void;
        /**
         * Removes the MAC address `mac` from the blacklist.
         *
         * @returns %TRUE if the MAC address was found and removed; %FALSE if it was not.
         * @param mac the MAC address string (hex-digits-and-colons notation) to remove from
         * the blacklist
         */
        remove_mac_blacklist_item_by_value(mac: string): boolean;
        /**
         * Removes the MAC address at index `idx` from the denylist.
         *
         * @param idx index number of the MAC address
         */
        remove_mac_denylist_item(idx: number): void;
        /**
         * Removes the MAC address `mac` from the denylist.
         *
         * @returns %TRUE if the MAC address was found and removed; %FALSE if it was not.
         * @param mac the MAC address string (hex-digits-and-colons notation) to remove from
         * the denylist
         */
        remove_mac_denylist_item_by_value(mac: string): boolean;
        /**
         * Remove the s390-specific option referenced by `key` from the internal option
         * list.
         *
         * @returns %TRUE if the option was found and removed from the internal option
         * list, %FALSE if it was not.
         * @param key key name for the option to remove
         */
        remove_s390_option(key: string): boolean;
    }
    class SettingWireless extends Setting {
        static '$gtype': GObject.GType<SettingWireless>;
        // Properties
        /**
         * Configures AP isolation, which prevents communication between
         * wireless devices connected to this AP. This property can be set
         * to a value different from %NM_TERNARY_DEFAULT only when the
         * interface is configured in AP mode.
         *
         * If set to %NM_TERNARY_TRUE, devices are not able to communicate
         * with each other. This increases security because it protects
         * devices against attacks from other clients in the network. At
         * the same time, it prevents devices to access resources on the
         * same wireless networks as file shares, printers, etc.
         *
         * If set to %NM_TERNARY_FALSE, devices can talk to each other.
         *
         * When set to %NM_TERNARY_DEFAULT, the global default is used; in
         * case the global default is unspecified it is assumed to be
         * %NM_TERNARY_FALSE.
         */
        get ap_isolation(): NM.Ternary;
        set ap_isolation(val: NM.Ternary);
        /**
         * Configures AP isolation, which prevents communication between
         * wireless devices connected to this AP. This property can be set
         * to a value different from %NM_TERNARY_DEFAULT only when the
         * interface is configured in AP mode.
         *
         * If set to %NM_TERNARY_TRUE, devices are not able to communicate
         * with each other. This increases security because it protects
         * devices against attacks from other clients in the network. At
         * the same time, it prevents devices to access resources on the
         * same wireless networks as file shares, printers, etc.
         *
         * If set to %NM_TERNARY_FALSE, devices can talk to each other.
         *
         * When set to %NM_TERNARY_DEFAULT, the global default is used; in
         * case the global default is unspecified it is assumed to be
         * %NM_TERNARY_FALSE.
         */
        get apIsolation(): NM.Ternary;
        set apIsolation(val: NM.Ternary);
        /**
         * 802.11 frequency band of the network.  One of "a" for 5GHz 802.11a or
         * "bg" for 2.4GHz 802.11.  This will lock associations to the Wi-Fi network
         * to the specific band, i.e. if "a" is specified, the device will not
         * associate with the same network in the 2.4GHz band even if the network's
         * settings are compatible.  This setting depends on specific driver
         * capability and may not work with all drivers.
         */
        get band(): string;
        set band(val: string);
        /**
         * If specified, directs the device to only associate with the given access
         * point.  This capability is highly driver dependent and not supported by
         * all devices.  Note: this property does not control the BSSID used when
         * creating an Ad-Hoc network and is unlikely to in the future.
         *
         * Locking a client profile to a certain BSSID will prevent roaming and also
         * disable background scanning. That can be useful, if there is only one access
         * point for the SSID.
         */
        get bssid(): string;
        set bssid(val: string);
        /**
         * Wireless channel to use for the Wi-Fi connection.  The device will only
         * join (or create for Ad-Hoc networks) a Wi-Fi network on the specified
         * channel.  Because channel numbers overlap between bands, this property
         * also requires the "band" property to be set.
         */
        get channel(): number;
        set channel(val: number);
        /**
         * Specifies width of the wireless channel in Access Point (AP) mode.
         *
         * When set to %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO (the default), the
         * channel width is automatically determined. At the moment, this means that
         * the safest (smallest) width is chosen.
         *
         * If the value is not %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO, then the
         * 'channel' property must also be set. When using the 2.4GHz band, the width
         * can be at most 40MHz.
         *
         * This property can be set to a value different from
         * %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO only when the interface is configured
         * in AP mode.
         */
        get channel_width(): number;
        set channel_width(val: number);
        /**
         * Specifies width of the wireless channel in Access Point (AP) mode.
         *
         * When set to %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO (the default), the
         * channel width is automatically determined. At the moment, this means that
         * the safest (smallest) width is chosen.
         *
         * If the value is not %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO, then the
         * 'channel' property must also be set. When using the 2.4GHz band, the width
         * can be at most 40MHz.
         *
         * This property can be set to a value different from
         * %NM_SETTING_WIRELESS_CHANNEL_WIDTH_AUTO only when the interface is configured
         * in AP mode.
         */
        get channelWidth(): number;
        set channelWidth(val: number);
        /**
         * If specified, request that the device use this MAC address instead.
         * This is known as MAC cloning or spoofing.
         *
         * Beside explicitly specifying a MAC address, the special values "preserve", "permanent",
         * "random", "stable" and "stable-ssid" are supported.
         * "preserve" means not to touch the MAC address on activation.
         * "permanent" means to use the permanent hardware address of the device.
         * "random" creates a random MAC address on each connect.
         * "stable" creates a hashed MAC address based on connection.stable-id and a
         * machine dependent key.
         * "stable-ssid" creates a hashed MAC address based on the SSID, the same as setting the
         * stable-id to "${NETWORK_SSID}".
         *
         * If unspecified, the value can be overwritten via global defaults, see manual
         * of NetworkManager.conf. If still unspecified, it defaults to "preserve"
         * (older versions of NetworkManager may use a different default value).
         *
         * On D-Bus, this field is expressed as "assigned-mac-address" or the deprecated
         * "cloned-mac-address".
         */
        get cloned_mac_address(): string;
        set cloned_mac_address(val: string);
        /**
         * If specified, request that the device use this MAC address instead.
         * This is known as MAC cloning or spoofing.
         *
         * Beside explicitly specifying a MAC address, the special values "preserve", "permanent",
         * "random", "stable" and "stable-ssid" are supported.
         * "preserve" means not to touch the MAC address on activation.
         * "permanent" means to use the permanent hardware address of the device.
         * "random" creates a random MAC address on each connect.
         * "stable" creates a hashed MAC address based on connection.stable-id and a
         * machine dependent key.
         * "stable-ssid" creates a hashed MAC address based on the SSID, the same as setting the
         * stable-id to "${NETWORK_SSID}".
         *
         * If unspecified, the value can be overwritten via global defaults, see manual
         * of NetworkManager.conf. If still unspecified, it defaults to "preserve"
         * (older versions of NetworkManager may use a different default value).
         *
         * On D-Bus, this field is expressed as "assigned-mac-address" or the deprecated
         * "cloned-mac-address".
         */
        get clonedMacAddress(): string;
        set clonedMacAddress(val: string);
        /**
         * With #NMSettingWireless:cloned-mac-address setting "random" or "stable",
         * by default all bits of the MAC address are scrambled and a locally-administered,
         * unicast MAC address is created. This property allows one to specify that certain bits
         * are fixed. Note that the least significant bit of the first MAC address will
         * always be unset to create a unicast MAC address.
         *
         * If the property is %NULL, it is eligible to be overwritten by a default
         * connection setting. If the value is still %NULL or an empty string, the
         * default is to create a locally-administered, unicast MAC address.
         *
         * If the value contains one MAC address, this address is used as mask. The set
         * bits of the mask are to be filled with the current MAC address of the device,
         * while the unset bits are subject to randomization.
         * Setting "FE:FF:FF:00:00:00" means to preserve the OUI of the current MAC address
         * and only randomize the lower 3 bytes using the "random" or "stable" algorithm.
         *
         * If the value contains one additional MAC address after the mask,
         * this address is used instead of the current MAC address to fill the bits
         * that shall not be randomized. For example, a value of
         * "FE:FF:FF:00:00:00 68:F7:28:00:00:00" will set the OUI of the MAC address
         * to 68:F7:28, while the lower bits are randomized. A value of
         * "02:00:00:00:00:00 00:00:00:00:00:00" will create a fully scrambled
         * globally-administered, burned-in MAC address.
         *
         * If the value contains more than one additional MAC addresses, one of
         * them is chosen randomly. For example, "02:00:00:00:00:00 00:00:00:00:00:00 02:00:00:00:00:00"
         * will create a fully scrambled MAC address, randomly locally or globally
         * administered.
         */
        get generate_mac_address_mask(): string;
        set generate_mac_address_mask(val: string);
        /**
         * With #NMSettingWireless:cloned-mac-address setting "random" or "stable",
         * by default all bits of the MAC address are scrambled and a locally-administered,
         * unicast MAC address is created. This property allows one to specify that certain bits
         * are fixed. Note that the least significant bit of the first MAC address will
         * always be unset to create a unicast MAC address.
         *
         * If the property is %NULL, it is eligible to be overwritten by a default
         * connection setting. If the value is still %NULL or an empty string, the
         * default is to create a locally-administered, unicast MAC address.
         *
         * If the value contains one MAC address, this address is used as mask. The set
         * bits of the mask are to be filled with the current MAC address of the device,
         * while the unset bits are subject to randomization.
         * Setting "FE:FF:FF:00:00:00" means to preserve the OUI of the current MAC address
         * and only randomize the lower 3 bytes using the "random" or "stable" algorithm.
         *
         * If the value contains one additional MAC address after the mask,
         * this address is used instead of the current MAC address to fill the bits
         * that shall not be randomized. For example, a value of
         * "FE:FF:FF:00:00:00 68:F7:28:00:00:00" will set the OUI of the MAC address
         * to 68:F7:28, while the lower bits are randomized. A value of
         * "02:00:00:00:00:00 00:00:00:00:00:00" will create a fully scrambled
         * globally-administered, burned-in MAC address.
         *
         * If the value contains more than one additional MAC addresses, one of
         * them is chosen randomly. For example, "02:00:00:00:00:00 00:00:00:00:00:00 02:00:00:00:00:00"
         * will create a fully scrambled MAC address, randomly locally or globally
         * administered.
         */
        get generateMacAddressMask(): string;
        set generateMacAddressMask(val: string);
        /**
         * If %TRUE, indicates that the network is a non-broadcasting network that
         * hides its SSID. This works both in infrastructure and AP mode.
         *
         * In infrastructure mode, various workarounds are used for a more reliable
         * discovery of hidden networks, such as probe-scanning the SSID.  However,
         * these workarounds expose inherent insecurities with hidden SSID networks,
         * and thus hidden SSID networks should be used with caution.
         *
         * In AP mode, the created network does not broadcast its SSID.
         *
         * Note that marking the network as hidden may be a privacy issue for you
         * (in infrastructure mode) or client stations (in AP mode), as the explicit
         * probe-scans are distinctly recognizable on the air.
         */
        get hidden(): boolean;
        set hidden(val: boolean);
        /**
         * If specified, this connection will only apply to the Wi-Fi device whose
         * permanent MAC address matches. This property does not change the MAC
         * address of the device (i.e. MAC spoofing).
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, this connection will only apply to the Wi-Fi device whose
         * permanent MAC address matches. This property does not change the MAC
         * address of the device (i.e. MAC spoofing).
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * A list of permanent MAC addresses of Wi-Fi devices to which this
         * connection should never apply.  Each MAC address should be given in the
         * standard hex-digits-and-colons notation (eg "00:11:22:33:44:55").
         */
        get mac_address_blacklist(): string[];
        set mac_address_blacklist(val: string[]);
        /**
         * A list of permanent MAC addresses of Wi-Fi devices to which this
         * connection should never apply.  Each MAC address should be given in the
         * standard hex-digits-and-colons notation (eg "00:11:22:33:44:55").
         */
        get macAddressBlacklist(): string[];
        set macAddressBlacklist(val: string[]);
        /**
         * A list of permanent MAC addresses of Wi-Fi devices to which this
         * connection should never apply.  Each MAC address should be given in the
         * standard hex-digits-and-colons notation (eg "00:11:22:33:44:55").
         */
        get mac_address_denylist(): string[];
        set mac_address_denylist(val: string[]);
        /**
         * A list of permanent MAC addresses of Wi-Fi devices to which this
         * connection should never apply.  Each MAC address should be given in the
         * standard hex-digits-and-colons notation (eg "00:11:22:33:44:55").
         */
        get macAddressDenylist(): string[];
        set macAddressDenylist(val: string[]);
        /**
         * One of %NM_SETTING_MAC_RANDOMIZATION_DEFAULT (never randomize unless
         * the user has set a global default to randomize and the supplicant
         * supports randomization),  %NM_SETTING_MAC_RANDOMIZATION_NEVER (never
         * randomize the MAC address), or %NM_SETTING_MAC_RANDOMIZATION_ALWAYS
         * (always randomize the MAC address).
         */
        get mac_address_randomization(): number;
        set mac_address_randomization(val: number);
        /**
         * One of %NM_SETTING_MAC_RANDOMIZATION_DEFAULT (never randomize unless
         * the user has set a global default to randomize and the supplicant
         * supports randomization),  %NM_SETTING_MAC_RANDOMIZATION_NEVER (never
         * randomize the MAC address), or %NM_SETTING_MAC_RANDOMIZATION_ALWAYS
         * (always randomize the MAC address).
         */
        get macAddressRandomization(): number;
        set macAddressRandomization(val: number);
        /**
         * Wi-Fi network mode; one of "infrastructure", "mesh", "adhoc" or "ap".  If blank,
         * infrastructure is assumed.
         */
        get mode(): string;
        set mode(val: string);
        /**
         * If non-zero, only transmit packets of the specified size or smaller,
         * breaking larger packets up into multiple Ethernet frames.
         */
        get mtu(): number;
        set mtu(val: number);
        /**
         * One of %NM_SETTING_WIRELESS_POWERSAVE_DISABLE (disable Wi-Fi power
         * saving), %NM_SETTING_WIRELESS_POWERSAVE_ENABLE (enable Wi-Fi power
         * saving), %NM_SETTING_WIRELESS_POWERSAVE_IGNORE (don't touch currently
         * configure setting) or %NM_SETTING_WIRELESS_POWERSAVE_DEFAULT (use the
         * globally configured value). All other values are reserved.
         */
        get powersave(): number;
        set powersave(val: number);
        /**
         * This property is not implemented and has no effect.
         */
        get rate(): number;
        set rate(val: number);
        /**
         * A list of BSSIDs (each BSSID formatted as a MAC address like
         * "00:11:22:33:44:55") that have been detected as part of the Wi-Fi
         * network.  NetworkManager internally tracks previously seen BSSIDs. The
         * property is only meant for reading and reflects the BSSID list of
         * NetworkManager. The changes you make to this property will not be
         * preserved.
         *
         * This is not a regular property that the user would configure. Instead,
         * NetworkManager automatically sets the seen BSSIDs and tracks them internally
         * in "/var/lib/NetworkManager/seen-bssids" file.
         */
        get seen_bssids(): string[];
        set seen_bssids(val: string[]);
        /**
         * A list of BSSIDs (each BSSID formatted as a MAC address like
         * "00:11:22:33:44:55") that have been detected as part of the Wi-Fi
         * network.  NetworkManager internally tracks previously seen BSSIDs. The
         * property is only meant for reading and reflects the BSSID list of
         * NetworkManager. The changes you make to this property will not be
         * preserved.
         *
         * This is not a regular property that the user would configure. Instead,
         * NetworkManager automatically sets the seen BSSIDs and tracks them internally
         * in "/var/lib/NetworkManager/seen-bssids" file.
         */
        get seenBssids(): string[];
        set seenBssids(val: string[]);
        /**
         * SSID of the Wi-Fi network. Must be specified.
         */
        get ssid(): GLib.Bytes;
        set ssid(val: GLib.Bytes);
        /**
         * This property is not implemented and has no effect.
         */
        get tx_power(): number;
        set tx_power(val: number);
        /**
         * This property is not implemented and has no effect.
         */
        get txPower(): number;
        set txPower(val: number);
        /**
         * The #NMSettingWirelessWakeOnWLan options to enable. Not all devices support all options.
         * May be any combination of %NM_SETTING_WIRELESS_WAKE_ON_WLAN_ANY,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_DISCONNECT,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_MAGIC,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_GTK_REKEY_FAILURE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_EAP_IDENTITY_REQUEST,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_4WAY_HANDSHAKE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_RFKILL_RELEASE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_TCP or the special values
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_DEFAULT (to use global settings) and
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_IGNORE (to disable management of Wake-on-LAN in
         * NetworkManager).
         */
        get wake_on_wlan(): number;
        set wake_on_wlan(val: number);
        /**
         * The #NMSettingWirelessWakeOnWLan options to enable. Not all devices support all options.
         * May be any combination of %NM_SETTING_WIRELESS_WAKE_ON_WLAN_ANY,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_DISCONNECT,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_MAGIC,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_GTK_REKEY_FAILURE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_EAP_IDENTITY_REQUEST,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_4WAY_HANDSHAKE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_RFKILL_RELEASE,
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_TCP or the special values
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_DEFAULT (to use global settings) and
         * %NM_SETTING_WIRELESS_WAKE_ON_WLAN_IGNORE (to disable management of Wake-on-LAN in
         * NetworkManager).
         */
        get wakeOnWlan(): number;
        set wakeOnWlan(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWireless.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWireless.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWireless;
        // Signals
        connect<K extends keyof NM.SettingWireless.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWireless.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWireless.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWireless.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWireless.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWireless.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds a new MAC address to the #NMSettingWireless:mac-address-denylist property.
         *
         * @returns %TRUE if the MAC address was added; %FALSE if the MAC address
         * is invalid or was already present
         * @param mac the MAC address string (hex-digits-and-colons notation) to denylist
         */
        add_mac_blacklist_item(mac: string): boolean;
        /**
         * Adds a new MAC address to the #NMSettingWireless:mac-address-denylist property.
         *
         * @returns %TRUE if the MAC address was added; %FALSE if the MAC address
         * is invalid or was already present
         * @param mac the MAC address string (hex-digits-and-colons notation) to denylist
         */
        add_mac_denylist_item(mac: string): boolean;
        /**
         * Adds a new Wi-Fi AP's BSSID to the previously seen BSSID list of the setting.
         * NetworkManager now tracks previously seen BSSIDs internally so this function
         * no longer has much use. Actually, changes you make using this function will
         * not be preserved.
         *
         * @returns %TRUE if `bssid` was already known, %FALSE if not
         * @param bssid the new BSSID to add to the list
         */
        add_seen_bssid(bssid: string): boolean;
        /**
         * Given a #NMSettingWireless and an optional #NMSettingWirelessSecurity,
         * determine if the configuration given by the settings is compatible with
         * the security of an access point using that access point's capability flags
         * and mode.  Useful for clients that wish to filter a set of connections
         * against a set of access points and determine which connections are
         * compatible with which access points.
         *
         * @returns %TRUE if the given settings are compatible with the access point's
         * security flags and mode, %FALSE if they are not.
         * @param s_wireless_sec a #NMSettingWirelessSecurity or %NULL
         * @param ap_flags the %NM80211ApFlags of the given access point
         * @param ap_wpa the %NM80211ApSecurityFlags of the given access point's WPA
         * capabilities
         * @param ap_rsn the %NM80211ApSecurityFlags of the given access point's WPA2/RSN
         * capabilities
         * @param ap_mode the 802.11 mode of the AP, either Ad-Hoc or Infrastructure
         */
        ap_security_compatible(
            s_wireless_sec: NM.SettingWirelessSecurity,
            ap_flags: NM.__80211ApFlags,
            ap_wpa: NM.__80211ApSecurityFlags,
            ap_rsn: NM.__80211ApSecurityFlags,
            ap_mode: NM.__80211Mode,
        ): boolean;
        /**
         * Removes all denylisted MAC addresses.
         */
        clear_mac_blacklist_items(): void;
        /**
         * Removes all denylisted MAC addresses.
         */
        clear_mac_denylist_items(): void;
        get_ap_isolation(): NM.Ternary;
        get_band(): string;
        get_bssid(): string;
        get_channel(): number;
        /**
         * Returns the #NMSettingWireless:channel-width property.
         *
         * @returns the channel width
         */
        get_channel_width(): NM.SettingWirelessChannelWidth;
        get_cloned_mac_address(): string;
        get_generate_mac_address_mask(): string;
        get_hidden(): boolean;
        get_mac_address(): string;
        get_mac_address_blacklist(): string[];
        get_mac_address_denylist(): string[];
        get_mac_address_randomization(): NM.SettingMacRandomization;
        /**
         * Since 1.46, access at index "len" is allowed and returns NULL.
         *
         * @returns the denylisted MAC address string (hex-digits-and-colons notation)
         * at index `idx`
         * @param idx the zero-based index of the MAC address entry
         */
        get_mac_blacklist_item(idx: number): string;
        get_mac_denylist_item(idx: number): string;
        get_mode(): string;
        get_mtu(): number;
        get_num_mac_blacklist_items(): number;
        get_num_mac_denylist_items(): number;
        get_num_seen_bssids(): number;
        get_powersave(): number;
        get_rate(): number;
        get_seen_bssid(i: number): string;
        get_ssid(): GLib.Bytes;
        get_tx_power(): number;
        /**
         * Returns the Wake-on-WLAN options enabled for the connection
         *
         * @returns the Wake-on-WLAN options
         */
        get_wake_on_wlan(): NM.SettingWirelessWakeOnWLan;
        /**
         * Removes the MAC address at index `idx` from the denylist.
         *
         * @param idx index number of the MAC address
         */
        remove_mac_blacklist_item(idx: number): void;
        /**
         * Removes the MAC address `mac` from the denylist.
         *
         * @returns %TRUE if the MAC address was found and removed; %FALSE if it was not.
         * @param mac the MAC address string (hex-digits-and-colons notation) to remove from
         * the denylist
         */
        remove_mac_blacklist_item_by_value(mac: string): boolean;
        /**
         * Removes the MAC address at index `idx` from the denylist.
         *
         * @param idx index number of the MAC address
         */
        remove_mac_denylist_item(idx: number): void;
        /**
         * Removes the MAC address `mac` from the denylist.
         *
         * @returns %TRUE if the MAC address was found and removed; %FALSE if it was not.
         * @param mac the MAC address string (hex-digits-and-colons notation) to remove from
         * the denylist
         */
        remove_mac_denylist_item_by_value(mac: string): boolean;
    }
    class SettingWirelessSecurity extends Setting {
        static '$gtype': GObject.GType<SettingWirelessSecurity>;
        // Properties
        /**
         * When WEP is used (ie, key-mgmt = "none" or "ieee8021x") indicate the
         * 802.11 authentication algorithm required by the AP here.  One of "open"
         * for Open System, "shared" for Shared Key, or "leap" for Cisco LEAP.  When
         * using Cisco LEAP (ie, key-mgmt = "ieee8021x" and auth-alg = "leap") the
         * "leap-username" and "leap-password" properties must be specified.
         */
        get auth_alg(): string;
        set auth_alg(val: string);
        /**
         * When WEP is used (ie, key-mgmt = "none" or "ieee8021x") indicate the
         * 802.11 authentication algorithm required by the AP here.  One of "open"
         * for Open System, "shared" for Shared Key, or "leap" for Cisco LEAP.  When
         * using Cisco LEAP (ie, key-mgmt = "ieee8021x" and auth-alg = "leap") the
         * "leap-username" and "leap-password" properties must be specified.
         */
        get authAlg(): string;
        set authAlg(val: string);
        /**
         * Indicates whether Fast Initial Link Setup (802.11ai) must be enabled for
         * the connection.  One of %NM_SETTING_WIRELESS_SECURITY_FILS_DEFAULT (use
         * global default value), %NM_SETTING_WIRELESS_SECURITY_FILS_DISABLE
         * (disable FILS), %NM_SETTING_WIRELESS_SECURITY_FILS_OPTIONAL (enable FILS
         * if the supplicant and the access point support it) or
         * %NM_SETTING_WIRELESS_SECURITY_FILS_REQUIRED (enable FILS and fail if not
         * supported).  When set to %NM_SETTING_WIRELESS_SECURITY_FILS_DEFAULT and
         * no global default is set, FILS will be optionally enabled.
         */
        get fils(): number;
        set fils(val: number);
        /**
         * A list of group/broadcast encryption algorithms which prevents
         * connections to Wi-Fi networks that do not utilize one of the algorithms
         * in the list.  For maximum compatibility leave this property empty.  Each
         * list element may be one of "wep40", "wep104", "tkip", or "ccmp".
         */
        get group(): string[];
        set group(val: string[]);
        /**
         * Key management used for the connection. One of "none" (WEP or no
         * password protection), "ieee8021x" (Dynamic WEP), "owe" (Opportunistic
         * Wireless Encryption), "wpa-psk" (WPA2 + WPA3 personal), "sae" (WPA3
         * personal only), "wpa-eap" (WPA2 + WPA3 enterprise) or
         * "wpa-eap-suite-b-192" (WPA3 enterprise only).
         *
         * This property must be set for any Wi-Fi connection that uses security.
         */
        get key_mgmt(): string;
        set key_mgmt(val: string);
        /**
         * Key management used for the connection. One of "none" (WEP or no
         * password protection), "ieee8021x" (Dynamic WEP), "owe" (Opportunistic
         * Wireless Encryption), "wpa-psk" (WPA2 + WPA3 personal), "sae" (WPA3
         * personal only), "wpa-eap" (WPA2 + WPA3 enterprise) or
         * "wpa-eap-suite-b-192" (WPA3 enterprise only).
         *
         * This property must be set for any Wi-Fi connection that uses security.
         */
        get keyMgmt(): string;
        set keyMgmt(val: string);
        /**
         * The login password for legacy LEAP connections (ie, key-mgmt =
         * "ieee8021x" and auth-alg = "leap").
         */
        get leap_password(): string;
        set leap_password(val: string);
        /**
         * The login password for legacy LEAP connections (ie, key-mgmt =
         * "ieee8021x" and auth-alg = "leap").
         */
        get leapPassword(): string;
        set leapPassword(val: string);
        /**
         * Flags indicating how to handle the
         * #NMSettingWirelessSecurity:leap-password property.
         */
        get leap_password_flags(): NM.SettingSecretFlags;
        set leap_password_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the
         * #NMSettingWirelessSecurity:leap-password property.
         */
        get leapPasswordFlags(): NM.SettingSecretFlags;
        set leapPasswordFlags(val: NM.SettingSecretFlags);
        /**
         * The login username for legacy LEAP connections (ie, key-mgmt =
         * "ieee8021x" and auth-alg = "leap").
         */
        get leap_username(): string;
        set leap_username(val: string);
        /**
         * The login username for legacy LEAP connections (ie, key-mgmt =
         * "ieee8021x" and auth-alg = "leap").
         */
        get leapUsername(): string;
        set leapUsername(val: string);
        /**
         * A list of pairwise encryption algorithms which prevents connections to
         * Wi-Fi networks that do not utilize one of the algorithms in the list.
         * For maximum compatibility leave this property empty.  Each list element
         * may be one of "tkip" or "ccmp".
         */
        get pairwise(): string[];
        set pairwise(val: string[]);
        /**
         * Indicates whether Protected Management Frames (802.11w) must be enabled
         * for the connection.  One of %NM_SETTING_WIRELESS_SECURITY_PMF_DEFAULT
         * (use global default value), %NM_SETTING_WIRELESS_SECURITY_PMF_DISABLE
         * (disable PMF), %NM_SETTING_WIRELESS_SECURITY_PMF_OPTIONAL (enable PMF if
         * the supplicant and the access point support it) or
         * %NM_SETTING_WIRELESS_SECURITY_PMF_REQUIRED (enable PMF and fail if not
         * supported).  When set to %NM_SETTING_WIRELESS_SECURITY_PMF_DEFAULT and no
         * global default is set, PMF will be optionally enabled.
         */
        get pmf(): number;
        set pmf(val: number);
        /**
         * List of strings specifying the allowed WPA protocol versions to use.
         * Each element may be one "wpa" (allow WPA) or "rsn" (allow WPA2/RSN).  If
         * not specified, both WPA and RSN connections are allowed.
         */
        get proto(): string[];
        set proto(val: string[]);
        /**
         * Pre-Shared-Key for WPA networks. For WPA-PSK, it's either an ASCII
         * passphrase of 8 to 63 characters that is (as specified in the 802.11i
         * standard) hashed to derive the actual key, or the key in form of 64
         * hexadecimal character. The WPA3-Personal networks use a passphrase
         * of any length for SAE authentication.
         */
        get psk(): string;
        set psk(val: string);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:psk
         * property.
         */
        get psk_flags(): NM.SettingSecretFlags;
        set psk_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:psk
         * property.
         */
        get pskFlags(): NM.SettingSecretFlags;
        set pskFlags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:wep-key0,
         * #NMSettingWirelessSecurity:wep-key1, #NMSettingWirelessSecurity:wep-key2,
         * and #NMSettingWirelessSecurity:wep-key3 properties.
         */
        get wep_key_flags(): NM.SettingSecretFlags;
        set wep_key_flags(val: NM.SettingSecretFlags);
        /**
         * Flags indicating how to handle the #NMSettingWirelessSecurity:wep-key0,
         * #NMSettingWirelessSecurity:wep-key1, #NMSettingWirelessSecurity:wep-key2,
         * and #NMSettingWirelessSecurity:wep-key3 properties.
         */
        get wepKeyFlags(): NM.SettingSecretFlags;
        set wepKeyFlags(val: NM.SettingSecretFlags);
        /**
         * Controls the interpretation of WEP keys.  Allowed values are
         * %NM_WEP_KEY_TYPE_KEY, in which case the key is either a 10- or
         * 26-character hexadecimal string, or a 5- or 13-character ASCII password;
         * or %NM_WEP_KEY_TYPE_PASSPHRASE, in which case the passphrase is provided
         * as a string and will be hashed using the de-facto MD5 method to derive
         * the actual WEP key.
         */
        get wep_key_type(): NM.WepKeyType;
        set wep_key_type(val: NM.WepKeyType);
        /**
         * Controls the interpretation of WEP keys.  Allowed values are
         * %NM_WEP_KEY_TYPE_KEY, in which case the key is either a 10- or
         * 26-character hexadecimal string, or a 5- or 13-character ASCII password;
         * or %NM_WEP_KEY_TYPE_PASSPHRASE, in which case the passphrase is provided
         * as a string and will be hashed using the de-facto MD5 method to derive
         * the actual WEP key.
         */
        get wepKeyType(): NM.WepKeyType;
        set wepKeyType(val: NM.WepKeyType);
        /**
         * Index 0 WEP key.  This is the WEP key used in most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wep_key0(): string;
        set wep_key0(val: string);
        /**
         * Index 0 WEP key.  This is the WEP key used in most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wepKey0(): string;
        set wepKey0(val: string);
        /**
         * Index 1 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wep_key1(): string;
        set wep_key1(val: string);
        /**
         * Index 1 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wepKey1(): string;
        set wepKey1(val: string);
        /**
         * Index 2 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wep_key2(): string;
        set wep_key2(val: string);
        /**
         * Index 2 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wepKey2(): string;
        set wepKey2(val: string);
        /**
         * Index 3 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wep_key3(): string;
        set wep_key3(val: string);
        /**
         * Index 3 WEP key.  This WEP index is not used by most networks.  See the
         * "wep-key-type" property for a description of how this key is interpreted.
         */
        get wepKey3(): string;
        set wepKey3(val: string);
        /**
         * When static WEP is used (ie, key-mgmt = "none") and a non-default WEP key
         * index is used by the AP, put that WEP key index here.  Valid values are 0
         * (default key) through 3.  Note that some consumer access points (like the
         * Linksys WRT54G) number the keys 1 - 4.
         */
        get wep_tx_keyidx(): number;
        set wep_tx_keyidx(val: number);
        /**
         * When static WEP is used (ie, key-mgmt = "none") and a non-default WEP key
         * index is used by the AP, put that WEP key index here.  Valid values are 0
         * (default key) through 3.  Note that some consumer access points (like the
         * Linksys WRT54G) number the keys 1 - 4.
         */
        get wepTxKeyidx(): number;
        set wepTxKeyidx(val: number);
        /**
         * Flags indicating which mode of WPS is to be used if any.
         *
         * There's little point in changing the default setting as NetworkManager will
         * automatically determine whether it's feasible to start WPS enrollment from
         * the Access Point capabilities.
         *
         * WPS can be disabled by setting this property to a value of 1.
         */
        get wps_method(): number;
        set wps_method(val: number);
        /**
         * Flags indicating which mode of WPS is to be used if any.
         *
         * There's little point in changing the default setting as NetworkManager will
         * automatically determine whether it's feasible to start WPS enrollment from
         * the Access Point capabilities.
         *
         * WPS can be disabled by setting this property to a value of 1.
         */
        get wpsMethod(): number;
        set wpsMethod(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWirelessSecurity.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWirelessSecurity.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWirelessSecurity;
        // Signals
        connect<K extends keyof NM.SettingWirelessSecurity.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWirelessSecurity.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWirelessSecurity.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWirelessSecurity.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWirelessSecurity.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWirelessSecurity.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Adds an encryption algorithm to the list of allowed groupwise encryption
         * algorithms.  If the list is not empty, then only access points that support
         * one or more of the encryption algorithms in the list will be considered
         * compatible with this connection.
         *
         * @returns %TRUE if the algorithm was added to the list, %FALSE if it was
         * already in the list
         * @param group the encryption algorithm to add, one of "wep40", "wep104",
         * "tkip", or "ccmp"
         */
        add_group(group: string): boolean;
        /**
         * Adds an encryption algorithm to the list of allowed pairwise encryption
         * algorithms.  If the list is not empty, then only access points that support
         * one or more of the encryption algorithms in the list will be considered
         * compatible with this connection.
         *
         * @returns %TRUE if the algorithm was added to the list, %FALSE if it was
         * already in the list
         * @param pairwise the encryption algorithm to add, one of "tkip" or "ccmp"
         */
        add_pairwise(pairwise: string): boolean;
        /**
         * Adds a Wi-Fi security protocol (one of "wpa" or "rsn") to the allowed list;
         * only protocols in this list will be used when finding and connecting to
         * the Wi-Fi network specified by this connection.  For example, if the
         * protocol list contains only "wpa" but the access point for the SSID specified
         * by this connection only supports WPA2/RSN, the connection cannot be used
         * with the access point.
         *
         * @returns %TRUE if the protocol was new and was added to the allowed
         * protocol list, or %FALSE if it was already in the list
         * @param proto the protocol to add, one of "wpa" or "rsn"
         */
        add_proto(proto: string): boolean;
        /**
         * Removes all algorithms from the allowed list.  If there are no algorithms
         * specified then all groupwise encryption algorithms are allowed.
         */
        clear_groups(): void;
        /**
         * Removes all algorithms from the allowed list.  If there are no algorithms
         * specified then all pairwise encryption algorithms are allowed.
         */
        clear_pairwise(): void;
        /**
         * Removes all protocols from the allowed list.  If there are no protocols
         * specified then all protocols are allowed.
         */
        clear_protos(): void;
        get_auth_alg(): string;
        get_fils(): NM.SettingWirelessSecurityFils;
        /**
         * Returns the allowed groupwise encryption algorithm from allowed algorithm
         * list.
         *
         * @returns the groupwise encryption algorithm at index `i`
         * @param i index of an item in the allowed groupwise encryption algorithm list
         */
        get_group(i: number): string;
        get_key_mgmt(): string;
        get_leap_password(): string;
        get_leap_password_flags(): NM.SettingSecretFlags;
        get_leap_username(): string;
        get_num_groups(): number;
        get_num_pairwise(): number;
        get_num_protos(): number;
        /**
         * Returns the allowed pairwise encryption algorithm from allowed algorithm
         * list.
         *
         * @returns the pairwise encryption algorithm at index `i`
         * @param i index of an item in the allowed pairwise encryption algorithm list
         */
        get_pairwise(i: number): string;
        get_pmf(): NM.SettingWirelessSecurityPmf;
        get_proto(i: number): string;
        get_psk(): string;
        get_psk_flags(): NM.SettingSecretFlags;
        get_wep_key(idx: number): string;
        get_wep_key_flags(): NM.SettingSecretFlags;
        get_wep_key_type(): NM.WepKeyType;
        get_wep_tx_keyidx(): number;
        get_wps_method(): NM.SettingWirelessSecurityWpsMethod;
        /**
         * Removes an encryption algorithm from the allowed groupwise encryption
         * algorithm list.
         *
         * @param i the index of an item in the allowed groupwise encryption algorithm list
         */
        remove_group(i: number): void;
        /**
         * Removes an encryption algorithm from the allowed groupwise encryption
         * algorithm list.
         *
         * @returns %TRUE if the algorithm was found and removed; %FALSE if it was not.
         * @param group the encryption algorithm to remove, one of "wep40", "wep104",
         * "tkip", or "ccmp"
         */
        remove_group_by_value(group: string): boolean;
        /**
         * Removes an encryption algorithm from the allowed pairwise encryption
         * algorithm list.
         *
         * @param i the index of an item in the allowed pairwise encryption algorithm list
         */
        remove_pairwise(i: number): void;
        /**
         * Removes an encryption algorithm from the allowed pairwise encryption
         * algorithm list.
         *
         * @returns %TRUE if the encryption algorithm was found and removed; %FALSE if it was not.
         * @param pairwise the encryption algorithm to remove, one of "tkip" or "ccmp"
         */
        remove_pairwise_by_value(pairwise: string): boolean;
        /**
         * Removes a protocol from the allowed protocol list.
         *
         * @param i index of the protocol to remove
         */
        remove_proto(i: number): void;
        /**
         * Removes a protocol from the allowed protocol list.
         *
         * @returns %TRUE if the protocol was found and removed; %FALSE if it was not.
         * @param proto the protocol to remove, one of "wpa" or "rsn"
         */
        remove_proto_by_value(proto: string): boolean;
        /**
         * Sets a WEP key in the given index.
         *
         * @param idx the index of the key (0..3 inclusive)
         * @param key the WEP key as a string, in either hexadecimal, ASCII, or passphrase
         * form as determined by the value of the #NMSettingWirelessSecurity:wep-key-type
         * property.
         */
        set_wep_key(idx: number, key: string): void;
    }
    class SettingWpan extends Setting {
        static '$gtype': GObject.GType<SettingWpan>;
        // Properties
        /**
         * IEEE 802.15.4 channel. A positive integer or -1, meaning "do not
         * set, use whatever the device is already set to".
         */
        get channel(): number;
        set channel(val: number);
        /**
         * If specified, this connection will only apply to the IEEE 802.15.4 (WPAN)
         * MAC layer device whose permanent MAC address matches.
         */
        get mac_address(): string;
        set mac_address(val: string);
        /**
         * If specified, this connection will only apply to the IEEE 802.15.4 (WPAN)
         * MAC layer device whose permanent MAC address matches.
         */
        get macAddress(): string;
        set macAddress(val: string);
        /**
         * IEEE 802.15.4 channel page. A positive integer or -1, meaning "do not
         * set, use whatever the device is already set to".
         */
        get page(): number;
        set page(val: number);
        /**
         * IEEE 802.15.4 Personal Area Network (PAN) identifier.
         */
        get pan_id(): number;
        set pan_id(val: number);
        /**
         * IEEE 802.15.4 Personal Area Network (PAN) identifier.
         */
        get panId(): number;
        set panId(val: number);
        /**
         * Short IEEE 802.15.4 address to be used within a restricted environment.
         */
        get short_address(): number;
        set short_address(val: number);
        /**
         * Short IEEE 802.15.4 address to be used within a restricted environment.
         */
        get shortAddress(): number;
        set shortAddress(val: number);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SettingWpan.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SettingWpan.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new(): NM.SettingWpan;
        // Signals
        connect<K extends keyof NM.SettingWpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWpan.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SettingWpan.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SettingWpan.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SettingWpan.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SettingWpan.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        get_channel(): number;
        get_mac_address(): string;
        get_page(): number;
        get_pan_id(): number;
        get_short_address(): number;
    }
    class SimpleConnection extends GObjectClasses.Object implements NM.Connection {
        static '$gtype': GObject.GType<SimpleConnection>;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.SimpleConnection.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.SimpleConnection.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.SimpleConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SimpleConnection.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.SimpleConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.SimpleConnection.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.SimpleConnection.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.SimpleConnection.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Creates a new #NMSimpleConnection object with no #NMSetting objects.
         */
        static new(): NM.Connection;
        /**
         * Clones an #NMConnection as an #NMSimpleConnection.
         *
         * @param connection the #NMConnection to clone
         */
        static new_clone(connection: NM.Connection): NM.Connection;
        /**
         * Creates a new #NMSimpleConnection from a hash table describing the
         * connection and normalize the connection.  See nm_connection_to_dbus() for a
         * description of the expected hash table.
         *
         * @param dict a #GVariant of type %NM_VARIANT_TYPE_CONNECTION describing the connection
         */
        static new_from_dbus(dict: GLib.Variant): NM.Connection;
        // Inherited methods
        /**
         * Adds a #NMSetting to the connection, replacing any previous #NMSetting of the
         * same name which has previously been added to the #NMConnection.  The
         * connection takes ownership of the #NMSetting object and does not increase
         * the setting object's reference count.
         *
         * @param setting the #NMSetting to add to the connection object
         */
        add_setting(setting: NM.Setting): void;
        /**
         * Clears and frees any secrets that may be stored in the connection, to avoid
         * keeping secret data in memory when not needed.
         */
        clear_secrets(): void;
        /**
         * Clears and frees secrets determined by `func`.
         *
         * @param func function to be called to determine whether a
         *     specific secret should be cleared or not. If %NULL, all secrets are cleared.
         */
        clear_secrets_with_flags(func?: NM.SettingClearSecretsWithFlagsFn | null): void;
        /**
         * Deletes all of `connection'`s settings.
         */
        clear_settings(): void;
        /**
         * Compares two #NMConnection objects for similarity, with comparison behavior
         * modified by a set of flags.  See nm_setting_compare() for a description of
         * each flag's behavior.
         *
         * @returns %TRUE if the comparison succeeds, %FALSE if it does not
         * @param b a second #NMConnection to compare with the first
         * @param flags compare flags, e.g. %NM_SETTING_COMPARE_FLAG_EXACT
         */
        compare(b: NM.Connection, flags: NM.SettingCompareFlags | null): boolean;
        /**
         * Print the connection (including secrets!) to stdout. For debugging
         * purposes ONLY, should NOT be used for serialization of the setting,
         * or machine-parsed in any way. The output format is not guaranteed to
         * be stable and may change at any time.
         */
        dump(): void;
        /**
         * Iterates over the properties of each #NMSetting object in the #NMConnection,
         * calling the supplied user function for each property.
         *
         * @param func user-supplied function called for each setting's property
         */
        for_each_setting_value(func: NM.SettingValueIterFn): void;
        /**
         * A shortcut to return the type from the connection's #NMSettingConnection.
         *
         * @returns the type from the connection's 'connection' setting
         */
        get_connection_type(): string;
        /**
         * A shortcut to return the ID from the connection's #NMSettingConnection.
         *
         * @returns the ID from the connection's 'connection' setting
         */
        get_id(): string;
        /**
         * Returns the interface name as stored in NMSettingConnection:interface_name.
         * If the connection contains no NMSettingConnection, it will return %NULL.
         *
         * For hardware devices and software devices created outside of NetworkManager,
         * this name is used to match the device. for software devices created by
         * NetworkManager, this is the name of the created interface.
         *
         * @returns Name of the kernel interface or %NULL
         */
        get_interface_name(): string;
        /**
         * Returns the connection's D-Bus path.
         *
         * @returns the D-Bus path of the connection, previously set by a call to
         * nm_connection_set_path().
         */
        get_path(): string;
        /**
         * Gets the #NMSetting with the given #GType, if one has been previously added
         * to the #NMConnection.
         *
         * @returns the #NMSetting, or %NULL if no setting of that type was previously
         * added to the #NMConnection
         * @param setting_type the #GType of the setting object to return
         */
        get_setting(setting_type: GObject.GType): NM.Setting;
        /**
         * A shortcut to return any #NMSetting8021x the connection might contain.
         *
         * @returns an #NMSetting8021x if the connection contains one, otherwise %NULL
         */
        get_setting_802_1x(): NM.Setting8021x;
        /**
         * A shortcut to return any #NMSettingAdsl the connection might contain.
         *
         * @returns an #NMSettingAdsl if the connection contains one, otherwise %NULL
         */
        get_setting_adsl(): NM.SettingAdsl;
        /**
         * A shortcut to return any #NMSettingBluetooth the connection might contain.
         *
         * @returns an #NMSettingBluetooth if the connection contains one, otherwise %NULL
         */
        get_setting_bluetooth(): NM.SettingBluetooth;
        /**
         * A shortcut to return any #NMSettingBond the connection might contain.
         *
         * @returns an #NMSettingBond if the connection contains one, otherwise %NULL
         */
        get_setting_bond(): NM.SettingBond;
        /**
         * A shortcut to return any #NMSettingBridge the connection might contain.
         *
         * @returns an #NMSettingBridge if the connection contains one, otherwise %NULL
         */
        get_setting_bridge(): NM.SettingBridge;
        /**
         * A shortcut to return any #NMSettingBridgePort the connection might contain.
         *
         * @returns an #NMSettingBridgePort if the connection contains one, otherwise %NULL
         */
        get_setting_bridge_port(): NM.SettingBridgePort;
        /**
         * Gets the #NMSetting with the given name, if one has been previously added
         * the #NMConnection.
         *
         * @returns the #NMSetting, or %NULL if no setting with that name was previously
         * added to the #NMConnection
         * @param name a setting name
         */
        get_setting_by_name(name: string): NM.Setting;
        /**
         * A shortcut to return any #NMSettingCdma the connection might contain.
         *
         * @returns an #NMSettingCdma if the connection contains one, otherwise %NULL
         */
        get_setting_cdma(): NM.SettingCdma;
        /**
         * A shortcut to return any #NMSettingConnection the connection might contain.
         *
         * @returns an #NMSettingConnection if the connection contains one, otherwise %NULL
         */
        get_setting_connection(): NM.SettingConnection;
        /**
         * A shortcut to return any #NMSettingDcb the connection might contain.
         *
         * @returns an #NMSettingDcb if the connection contains one, otherwise NULL
         */
        get_setting_dcb(): NM.SettingDcb;
        /**
         * A shortcut to return any #NMSettingDummy the connection might contain.
         *
         * @returns an #NMSettingDummy if the connection contains one, otherwise %NULL
         */
        get_setting_dummy(): NM.SettingDummy;
        /**
         * A shortcut to return any #NMSettingGeneric the connection might contain.
         *
         * @returns an #NMSettingGeneric if the connection contains one, otherwise NULL
         */
        get_setting_generic(): NM.SettingGeneric;
        /**
         * A shortcut to return any #NMSettingGsm the connection might contain.
         *
         * @returns an #NMSettingGsm if the connection contains one, otherwise %NULL
         */
        get_setting_gsm(): NM.SettingGsm;
        /**
         * A shortcut to return any #NMSettingInfiniband the connection might contain.
         *
         * @returns an #NMSettingInfiniband if the connection contains one, otherwise %NULL
         */
        get_setting_infiniband(): NM.SettingInfiniband;
        /**
         * A shortcut to return any #NMSettingIP4Config the connection might contain.
         *
         * Note that it returns the value as type #NMSettingIPConfig, since the vast
         * majority of IPv4-setting-related methods are on that type, not
         * #NMSettingIP4Config.
         *
         * @returns an #NMSettingIP4Config if the
         * connection contains one, otherwise %NULL
         */
        get_setting_ip4_config(): NM.SettingIP4Config;
        /**
         * A shortcut to return any #NMSettingIP6Config the connection might contain.
         *
         * Note that it returns the value as type #NMSettingIPConfig, since the vast
         * majority of IPv6-setting-related methods are on that type, not
         * #NMSettingIP6Config.
         *
         * @returns an #NMSettingIP6Config if the
         * connection contains one, otherwise %NULL
         */
        get_setting_ip6_config(): NM.SettingIP6Config;
        /**
         * A shortcut to return any #NMSettingIPTunnel the connection might contain.
         *
         * @returns an #NMSettingIPTunnel if the connection contains one, otherwise %NULL
         */
        get_setting_ip_tunnel(): NM.SettingIPTunnel;
        /**
         * A shortcut to return any #NMSettingMacsec the connection might contain.
         *
         * @returns an #NMSettingMacsec if the connection contains one, otherwise %NULL
         */
        get_setting_macsec(): NM.SettingMacsec;
        /**
         * A shortcut to return any #NMSettingMacvlan the connection might contain.
         *
         * @returns an #NMSettingMacvlan if the connection contains one, otherwise %NULL
         */
        get_setting_macvlan(): NM.SettingMacvlan;
        /**
         * A shortcut to return any #NMSettingOlpcMesh the connection might contain.
         *
         * @returns an #NMSettingOlpcMesh if the connection contains one, otherwise %NULL
         */
        get_setting_olpc_mesh(): NM.SettingOlpcMesh;
        /**
         * A shortcut to return any #NMSettingOvsBridge the connection might contain.
         *
         * @returns an #NMSettingOvsBridge if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_bridge(): NM.SettingOvsBridge;
        /**
         * A shortcut to return any #NMSettingOvsInterface the connection might contain.
         *
         * @returns an #NMSettingOvsInterface if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_interface(): NM.SettingOvsInterface;
        /**
         * A shortcut to return any #NMSettingOvsPatch the connection might contain.
         *
         * @returns an #NMSettingOvsPatch if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_patch(): NM.SettingOvsPatch;
        /**
         * A shortcut to return any #NMSettingOvsPort the connection might contain.
         *
         * @returns an #NMSettingOvsPort if the connection contains one, otherwise %NULL
         */
        get_setting_ovs_port(): NM.SettingOvsPort;
        /**
         * A shortcut to return any #NMSettingPpp the connection might contain.
         *
         * @returns an #NMSettingPpp if the connection contains one, otherwise %NULL
         */
        get_setting_ppp(): NM.SettingPpp;
        /**
         * A shortcut to return any #NMSettingPppoe the connection might contain.
         *
         * @returns an #NMSettingPppoe if the connection contains one, otherwise %NULL
         */
        get_setting_pppoe(): NM.SettingPppoe;
        /**
         * A shortcut to return any #NMSettingProxy the connection might contain.
         *
         * @returns an #NMSettingProxy if the connection contains one, otherwise %NULL
         */
        get_setting_proxy(): NM.SettingProxy;
        /**
         * A shortcut to return any #NMSettingSerial the connection might contain.
         *
         * @returns an #NMSettingSerial if the connection contains one, otherwise %NULL
         */
        get_setting_serial(): NM.SettingSerial;
        /**
         * A shortcut to return any #NMSettingTCConfig the connection might contain.
         *
         * @returns an #NMSettingTCConfig if the connection contains one, otherwise %NULL
         */
        get_setting_tc_config(): NM.SettingTCConfig;
        /**
         * A shortcut to return any #NMSettingTeam the connection might contain.
         *
         * @returns an #NMSettingTeam if the connection contains one, otherwise %NULL
         */
        get_setting_team(): NM.SettingTeam;
        /**
         * A shortcut to return any #NMSettingTeamPort the connection might contain.
         *
         * @returns an #NMSettingTeamPort if the connection contains one, otherwise %NULL
         */
        get_setting_team_port(): NM.SettingTeamPort;
        /**
         * A shortcut to return any #NMSettingTun the connection might contain.
         *
         * @returns an #NMSettingTun if the connection contains one, otherwise %NULL
         */
        get_setting_tun(): NM.SettingTun;
        /**
         * A shortcut to return any #NMSettingVlan the connection might contain.
         *
         * @returns an #NMSettingVlan if the connection contains one, otherwise %NULL
         */
        get_setting_vlan(): NM.SettingVlan;
        /**
         * A shortcut to return any #NMSettingVpn the connection might contain.
         *
         * @returns an #NMSettingVpn if the connection contains one, otherwise %NULL
         */
        get_setting_vpn(): NM.SettingVpn;
        /**
         * A shortcut to return any #NMSettingVxlan the connection might contain.
         *
         * @returns an #NMSettingVxlan if the connection contains one, otherwise %NULL
         */
        get_setting_vxlan(): NM.SettingVxlan;
        /**
         * A shortcut to return any #NMSettingWimax the connection might contain.
         *
         * @returns an #NMSettingWimax if the connection contains one, otherwise %NULL
         */
        get_setting_wimax(): NM.SettingWimax;
        /**
         * A shortcut to return any #NMSettingWired the connection might contain.
         *
         * @returns an #NMSettingWired if the connection contains one, otherwise %NULL
         */
        get_setting_wired(): NM.SettingWired;
        /**
         * A shortcut to return any #NMSettingWireless the connection might contain.
         *
         * @returns an #NMSettingWireless if the connection contains one, otherwise %NULL
         */
        get_setting_wireless(): NM.SettingWireless;
        /**
         * A shortcut to return any #NMSettingWirelessSecurity the connection might contain.
         *
         * @returns an #NMSettingWirelessSecurity if the connection contains one, otherwise %NULL
         */
        get_setting_wireless_security(): NM.SettingWirelessSecurity;
        /**
         * Retrieves the settings in `connection`.
         *
         * The returned array is %NULL-terminated.
         *
         * @returns a
         *   %NULL-terminated array containing every setting of `connection`.
         *   If the connection has no settings, %NULL is returned.
         */
        get_settings(): NM.Setting[] | null;
        /**
         * A shortcut to return the UUID from the connection's #NMSettingConnection.
         *
         * @returns the UUID from the connection's 'connection' setting
         */
        get_uuid(): string;
        /**
         * Returns the name that nm_device_disambiguate_names() would
         * return for the virtual device that would be created for `connection`.
         * Eg, "VLAN (eth1.1)".
         *
         * @returns the name of `connection'`s device,
         *   or %NULL if `connection` is not a virtual connection type
         */
        get_virtual_device_description(): string;
        /**
         * A convenience function to check if the given `connection` is a particular
         * type (ie wired, Wi-Fi, ppp, etc). Checks the #NMSettingConnection:type
         * property of the connection and matches that against `type`.
         *
         * @returns %TRUE if the connection is of the given `type,` %FALSE if not
         * @param type a setting name to check the connection's type against (like
         * %NM_SETTING_WIRELESS_SETTING_NAME or %NM_SETTING_WIRED_SETTING_NAME)
         */
        is_type(type: string): boolean;
        /**
         * Checks if `connection` refers to a virtual device (and thus can potentially be
         * activated even if the device it refers to doesn't exist).
         *
         * @returns whether `connection` refers to a virtual device
         */
        is_virtual(): boolean;
        /**
         * Returns the name of the first setting object in the connection which would
         * need secrets to make a successful connection.  The returned hints are only
         * intended as a guide to what secrets may be required, because in some
         * circumstances, there is no way to conclusively determine exactly which
         * secrets are needed.
         *
         * @returns the setting name of the #NMSetting object which has
         *   invalid or missing secrets
         */
        need_secrets(): [string | null, string[] | null];
        /**
         * Does some basic normalization and fixup of well known inconsistencies
         * and deprecated fields. If the connection was modified in any way,
         * the output parameter `modified` is set %TRUE.
         *
         * Finally the connection will be verified and %TRUE returns if the connection
         * is valid. As this function only performs some specific normalization steps
         * it cannot repair all connections. If the connection has errors that
         * cannot be normalized, the connection will not be modified.
         *
         * @returns %TRUE if the connection is valid, %FALSE if it is not
         * @param parameters a #GHashTable with
         * normalization parameters to allow customization of the normalization by providing
         * specific arguments. Unknown arguments will be ignored and the default will be
         * used. The keys must be strings compared with g_str_equal() function.
         * The values are opaque and depend on the parameter name.
         */
        normalize(parameters: GLib.HashTable<string, any> | null): [boolean, boolean];
        /**
         * Removes the #NMSetting with the given #GType from the #NMConnection.  This
         * operation dereferences the #NMSetting object.
         *
         * @param setting_type the #GType of the setting object to remove
         */
        remove_setting(setting_type: GObject.GType): void;
        /**
         * Replaces `connection'`s settings with `new_settings` (which must be
         * syntactically valid, and describe a known type of connection, but does not
         * need to result in a connection that passes nm_connection_verify()).
         *
         * @returns %TRUE if connection was updated, %FALSE if `new_settings` could not
         *   be deserialized (in which case `connection` will be unchanged).
         * @param new_settings a #GVariant of type %NM_VARIANT_TYPE_CONNECTION, with the new settings
         */
        replace_settings(new_settings: GLib.Variant): boolean;
        /**
         * Deep-copies the settings of `new_connection` and replaces the settings of `connection`
         * with the copied settings.
         *
         * @param new_connection a #NMConnection to replace the settings of `connection` with
         */
        replace_settings_from_connection(new_connection: NM.Connection): void;
        /**
         * Sets the D-Bus path of the connection.  This property is not serialized, and
         * is only for the reference of the caller.  Sets the #NMConnection:path
         * property.
         *
         * @param path the D-Bus path of the connection as given by the settings service
         * which provides the connection
         */
        set_path(path: string): void;
        /**
         * Converts the #NMConnection into a #GVariant of type
         * %NM_VARIANT_TYPE_CONNECTION describing the connection, suitable for
         * marshalling over D-Bus or otherwise serializing.
         *
         * @returns a new floating #GVariant describing the connection,
         * its settings, and each setting's properties.
         * @param flags serialization flags, e.g. %NM_CONNECTION_SERIALIZE_ALL
         */
        to_dbus(flags: NM.ConnectionSerializationFlags | null): GLib.Variant;
        /**
         * Update the specified setting's secrets, given a dictionary of secrets
         * intended for that setting (deserialized from D-Bus for example).  Will also
         * extract the given setting's secrets hash if given a connection dictionary.
         * If `setting_name` is %NULL, expects a fully serialized #NMConnection as
         * returned by nm_connection_to_dbus() and will update all secrets from all
         * settings contained in `secrets`.
         *
         * @returns %TRUE if the secrets were successfully updated, %FALSE if the update
         * failed (tried to update secrets for a setting that doesn't exist, etc)
         * @param setting_name the setting object name to which the secrets apply
         * @param secrets a #GVariant of secrets, of type %NM_VARIANT_TYPE_CONNECTION
         *   or %NM_VARIANT_TYPE_SETTING
         */
        update_secrets(setting_name: string, secrets: GLib.Variant): boolean;
        /**
         * Validates the connection and all its settings.  Each setting's properties
         * have allowed values, and some values are dependent on other values.  For
         * example, if a Wi-Fi connection is security enabled, the #NMSettingWireless
         * setting object's 'security' property must contain the setting name of the
         * #NMSettingWirelessSecurity object, which must also be present in the
         * connection for the connection to be valid.  As another example, the
         * #NMSettingWired object's 'mac-address' property must be a validly formatted
         * MAC address.  The returned #GError contains information about which
         * setting and which property failed validation, and how it failed validation.
         *
         * @returns %TRUE if the connection is valid, %FALSE if it is not
         */
        verify(): boolean;
        /**
         * Verifies the secrets in the connection.
         *
         * @returns %TRUE if the secrets are valid, %FALSE if they are not
         */
        verify_secrets(): boolean;
        /**
         * emitted when any change to the connection's settings occurs
         */
        vfunc_changed(): void;
        /**
         * emitted when the connection's secrets are cleared
         */
        vfunc_secrets_cleared(): void;
        /**
         * emitted when the connection's secrets are updated
         *
         * @param setting
         */
        vfunc_secrets_updated(setting: string): void;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Disconnects a handler from an instance so it will not be called during any future or currently ongoing emissions of the signal it has been connected to.
         *
         * @param id Handler ID of the handler to be disconnected
         */
        disconnect(id: number): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    class VpnConnection extends ActiveConnection {
        static '$gtype': GObject.GType<VpnConnection>;
        // Properties
        /**
         * The VPN login banner of the active VPN connection.
         */
        get banner(): string;
        /**
         * The VPN state of the active VPN connection.
         */
        get vpn_state(): NM.VpnConnectionState;
        /**
         * The VPN state of the active VPN connection.
         */
        get vpnState(): NM.VpnConnectionState;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.VpnConnection.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.VpnConnection.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.VpnConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnConnection.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.VpnConnection.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnConnection.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.VpnConnection.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.VpnConnection.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Gets the VPN login banner of the active #NMVpnConnection.
         *
         * @returns the VPN login banner of the VPN connection. This is the internal
         * string used by the connection, and must not be modified.
         */
        get_banner(): string;
        /**
         * Gets the current #NMVpnConnection state.
         *
         * @returns the VPN state of the active VPN connection.
         */
        get_vpn_state(): NM.VpnConnectionState;
    }
    class VpnPluginInfo extends GObjectClasses.Object implements Gio.Initable {
        static '$gtype': GObject.GType<VpnPluginInfo>;
        // Properties
        /**
         * The filename from which the info was loaded.
         * Can be %NULL if the instance was not loaded from
         * a file (i.e. the keyfile instance was passed to the
         * constructor).
         */
        get filename(): string;
        /**
         * The name of the VPN plugin.
         */
        get name(): string;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.VpnPluginInfo.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.VpnPluginInfo.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        static new_from_file(filename: string): NM.VpnPluginInfo;
        static new_search_file(name?: string | null, service?: string | null): NM.VpnPluginInfo;
        static new_with_data(filename: string, keyfile: GLib.KeyFile): NM.VpnPluginInfo;
        // Signals
        connect<K extends keyof NM.VpnPluginInfo.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnPluginInfo.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.VpnPluginInfo.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnPluginInfo.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.VpnPluginInfo.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.VpnPluginInfo.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        static list_add(list: NM.VpnPluginInfo[], plugin_info: NM.VpnPluginInfo): boolean;
        static list_find_by_filename(list: NM.VpnPluginInfo[], filename: string): NM.VpnPluginInfo;
        static list_find_by_name(list: NM.VpnPluginInfo[], name: string): NM.VpnPluginInfo;
        static list_find_by_service(list: NM.VpnPluginInfo[], service: string): NM.VpnPluginInfo;
        /**
         * A VPN plugin provides one or several service-types, like org.freedesktop.NetworkManager.libreswan
         * Certain plugins provide more then one service type, via aliases (org.freedesktop.NetworkManager.openswan).
         * This function looks up a service-type (or an alias) based on a name.
         *
         * Preferably, the name can be a full service-type/alias of an installed
         * plugin. Otherwise, it can be the name of a VPN plugin (in which case, the
         * primary, non-aliased service-type is returned). Otherwise, it can be
         * one of several well known short-names (which is a hard-coded list of
         * types in libnm). On success, this returns a full qualified service-type
         * (or an alias). It doesn't say, that such an plugin is actually available,
         * but it could be retrieved via nm_vpn_plugin_info_list_find_by_service().
         *
         * @param list a possibly empty #GSList of #NMVpnPluginInfo instances
         * @param name a name to lookup the service-type.
         */
        static list_find_service_type(list: NM.VpnPluginInfo[], name: string): string;
        static list_get_service_types(
            list: NM.VpnPluginInfo[],
            only_existing: boolean,
            with_abbreviations: boolean,
        ): string[];
        static list_load(): NM.VpnPluginInfo[];
        /**
         * Remove `plugin_info` from `list`.
         *
         * @param list list of plugins
         * @param plugin_info instance
         */
        static list_remove(list: NM.VpnPluginInfo[], plugin_info: NM.VpnPluginInfo): boolean;
        /**
         * Regular name files have a certain pattern. That basically means
         * they have the file extension "name". Check if `filename`
         * is valid according to that pattern.
         *
         * @param filename the filename to check
         */
        static validate_filename(filename: string): boolean;
        // Methods
        get_aliases(): string[];
        get_auth_dialog(): string;
        get_editor_plugin(): NM.VpnEditorPlugin;
        get_filename(): string;
        get_name(): string;
        get_plugin(): string;
        get_program(): string;
        get_service(): string;
        load_editor_plugin(): NM.VpnEditorPlugin;
        lookup_property(group: string, key: string): string;
        /**
         * Set the internal plugin instance. If %NULL, only clear the previous instance.
         *
         * @param plugin plugin instance
         */
        set_editor_plugin(plugin?: NM.VpnEditorPlugin | null): void;
        supports_hints(): boolean;
        supports_multiple(): boolean;
        supports_safe_private_file_access(): boolean;
        // Inherited methods
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @returns %TRUE if successful. If an error has occurred, this function will
         *     return %FALSE and set `error` appropriately if present.
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        vfunc_init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Disconnects a handler from an instance so it will not be called during any future or currently ongoing emissions of the signal it has been connected to.
         *
         * @param id Handler ID of the handler to be disconnected
         */
        disconnect(id: number): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    abstract class VpnPluginOld extends GObjectClasses.Object implements Gio.Initable {
        static '$gtype': GObject.GType<VpnPluginOld>;
        // Properties
        /**
         * The D-Bus service name of this plugin.
         */
        get service_name(): string;
        /**
         * The D-Bus service name of this plugin.
         */
        get serviceName(): string;
        /**
         * The state of the plugin.
         */
        get state(): NM.VpnServiceState;
        set state(val: NM.VpnServiceState);
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.VpnPluginOld.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.VpnPluginOld.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.VpnPluginOld.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnPluginOld.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.VpnPluginOld.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnPluginOld.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.VpnPluginOld.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.VpnPluginOld.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Given a VPN secret key name, attempts to find the corresponding flags data
         * item in `data`.  If found, converts the flags data item to
         * #NMSettingSecretFlags and returns it.
         *
         * @param data hash table containing VPN key/value pair data items
         * @param secret_name VPN secret key name for which to retrieve flags for
         */
        static get_secret_flags(data: GLib.HashTable<any, any>, secret_name: string): [boolean, NM.SettingSecretFlags];
        /**
         * Parses key/value pairs from a file descriptor (normally stdin) passed by
         * an applet when the applet calls the authentication dialog of the VPN plugin.
         *
         * @param fd file descriptor to read from, usually stdin (0)
         */
        static read_vpn_details(fd: number): [boolean, GLib.HashTable<any, any>, GLib.HashTable<any, any>];
        // Virtual methods
        vfunc_config(config: GLib.Variant): void;
        vfunc_connect(connection: NM.Connection): boolean;
        vfunc_connect_interactive(connection: NM.Connection, details: GLib.Variant): boolean;
        vfunc_disconnect(): boolean;
        vfunc_failure(reason: NM.VpnPluginFailure): void;
        vfunc_ip4_config(_ip4_config: GLib.Variant): void;
        vfunc_ip6_config(config: GLib.Variant): void;
        vfunc_login_banner(banner: string): void;
        vfunc_need_secrets(connection: NM.Connection, setting_name: string): boolean;
        vfunc_new_secrets(connection: NM.Connection): boolean;
        vfunc_quit(): void;
        vfunc_state_changed(state: NM.VpnServiceState): void;
        // Methods
        disconnect(): boolean;
        // Conflicted with GObject.Object.disconnect
        disconnect(...args: never[]): any;
        failure(reason: NM.VpnPluginFailure | null): void;
        get_connection(): Gio.DBusConnection;
        get_state(): NM.VpnServiceState;
        /**
         * Called by VPN plugin implementations to signal to NetworkManager that secrets
         * are required during the connection process.  This signal may be used to
         * request new secrets when the secrets originally provided by NetworkManager
         * are insufficient, or the VPN process indicates that it needs additional
         * information to complete the request.
         *
         * @param message an information message about why secrets are required, if any
         * @param hints VPN specific secret names for required new secrets
         */
        secrets_required(message: string, hints: string): void;
        set_ip4_config(_ip4_config: GLib.Variant): void;
        set_login_banner(banner: string): void;
        set_state(state: NM.VpnServiceState | null): void;
        // Inherited methods
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @returns %TRUE if successful. If an error has occurred, this function will
         *     return %FALSE and set `error` appropriately if present.
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        vfunc_init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    abstract class VpnServicePlugin extends GObjectClasses.Object implements Gio.Initable {
        static '$gtype': GObject.GType<VpnServicePlugin>;
        // Properties
        /**
         * The D-Bus service name of this plugin.
         */
        get service_name(): string;
        /**
         * The D-Bus service name of this plugin.
         */
        get serviceName(): string;
        /**
         * The state of the plugin.
         */
        get state(): NM.VpnServiceState;
        set state(val: NM.VpnServiceState);
        /**
         * Whether to watch for D-Bus peer's changes.
         */
        get watch_peer(): boolean;
        /**
         * Whether to watch for D-Bus peer's changes.
         */
        get watchPeer(): boolean;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.VpnServicePlugin.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.VpnServicePlugin.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.VpnServicePlugin.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnServicePlugin.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.VpnServicePlugin.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.VpnServicePlugin.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.VpnServicePlugin.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.VpnServicePlugin.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Static methods
        /**
         * Given a VPN secret key name, attempts to find the corresponding flags data
         * item in `data`.  If found, converts the flags data item to
         * #NMSettingSecretFlags and returns it.
         *
         * @param data hash table containing VPN key/value pair data items
         * @param secret_name VPN secret key name for which to retrieve flags for
         */
        static get_secret_flags(data: GLib.HashTable<any, any>, secret_name: string): [boolean, NM.SettingSecretFlags];
        /**
         * Parses key/value pairs from a file descriptor (normally stdin) passed by
         * an applet when the applet calls the authentication dialog of the VPN plugin.
         *
         * @param fd file descriptor to read from, usually stdin (0)
         */
        static read_vpn_details(fd: number): [boolean, GLib.HashTable<any, any>, GLib.HashTable<any, any>];
        // Virtual methods
        vfunc_config(config: GLib.Variant): void;
        vfunc_connect(connection: NM.Connection): boolean;
        vfunc_connect_interactive(connection: NM.Connection, details: GLib.Variant): boolean;
        vfunc_disconnect(): boolean;
        vfunc_failure(reason: NM.VpnPluginFailure): void;
        vfunc_ip4_config(_ip4_config: GLib.Variant): void;
        vfunc_ip6_config(config: GLib.Variant): void;
        vfunc_login_banner(banner: string): void;
        vfunc_need_secrets(connection: NM.Connection, setting_name: string): boolean;
        vfunc_new_secrets(connection: NM.Connection): boolean;
        vfunc_quit(): void;
        vfunc_state_changed(state: NM.VpnServiceState): void;
        // Methods
        disconnect(): boolean;
        // Conflicted with GObject.Object.disconnect
        disconnect(...args: never[]): any;
        failure(reason: NM.VpnPluginFailure | null): void;
        get_connection(): Gio.DBusConnection;
        /**
         * Called by VPN plugin implementations to signal to NetworkManager that secrets
         * are required during the connection process.  This signal may be used to
         * request new secrets when the secrets originally provided by NetworkManager
         * are insufficient, or the VPN process indicates that it needs additional
         * information to complete the request.
         *
         * @param message an information message about why secrets are required, if any
         * @param hints VPN specific secret names for required new secrets
         */
        secrets_required(message: string, hints: string): void;
        set_config(config: GLib.Variant): void;
        set_ip4_config(_ip4_config: GLib.Variant): void;
        set_ip6_config(_ip6_config: GLib.Variant): void;
        set_login_banner(banner: string): void;
        /**
         * Shutdown the `plugin` and disconnect from D-Bus. After this,
         * the plugin instance is dead and should no longer be used.
         * It ensures to get no more requests from D-Bus. In principle,
         * you don't need to shutdown the plugin, disposing the instance
         * has the same effect. However, this gives a way to deactivate
         * the plugin before giving up the last reference.
         */
        shutdown(): void;
        // Inherited methods
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @returns %TRUE if successful. If an error has occurred, this function will
         *     return %FALSE and set `error` appropriately if present.
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Initializes the object implementing the interface.
         *
         * This method is intended for language bindings. If writing in C,
         * g_initable_new() should typically be used instead.
         *
         * The object must be initialized before any real use after initial
         * construction, either with this function or g_async_initable_init_async().
         *
         * Implementations may also support cancellation. If `cancellable` is not %NULL,
         * then initialization can be cancelled by triggering the cancellable object
         * from another thread. If the operation was cancelled, the error
         * %G_IO_ERROR_CANCELLED will be returned. If `cancellable` is not %NULL and
         * the object doesn't support cancellable initialization the error
         * %G_IO_ERROR_NOT_SUPPORTED will be returned.
         *
         * If the object is not initialized, or initialization returns with an
         * error, then all operations on the object except g_object_ref() and
         * g_object_unref() are considered to be invalid, and have undefined
         * behaviour. See the [description][iface`Gio`.Initable#description] for more details.
         *
         * Callers should not assume that a class which implements #GInitable can be
         * initialized multiple times, unless the class explicitly documents itself as
         * supporting this. Generally, a class’ implementation of init() can assume
         * (and assert) that it will only be called once. Previously, this documentation
         * recommended all #GInitable implementations should be idempotent; that
         * recommendation was relaxed in GLib 2.54.
         *
         * If a class explicitly supports being initialized multiple times, it is
         * recommended that the method is idempotent: multiple calls with the same
         * arguments should return the same results. Only the first call initializes
         * the object; further calls return the result of the first call.
         *
         * One reason why a class might need to support idempotent initialization is if
         * it is designed to be used via the singleton pattern, with a
         * #GObjectClass.constructor that sometimes returns an existing instance.
         * In this pattern, a caller would expect to be able to call g_initable_init()
         * on the result of g_object_new(), regardless of whether it is in fact a new
         * instance.
         *
         * @param cancellable optional #GCancellable object, %NULL to ignore.
         */
        vfunc_init(cancellable?: Gio.Cancellable | null): boolean;
        /**
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target`.
         *
         * Whenever the `source_property` is changed the `target_property` is
         * updated using the same value. For instance:
         *
         *
         * ```c
         *   g_object_bind_property (action, "active", widget, "sensitive", 0);
         * ```
         *
         *
         * Will result in the "sensitive" property of the widget #GObject instance to be
         * updated with the same value of the "active" property of the action #GObject
         * instance.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well.
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. To remove the binding without affecting the
         * `source` and the `target` you can just call g_object_unref() on the returned
         * #GBinding instance.
         *
         * Removing the binding by calling g_object_unref() on it must only be done if
         * the binding, `source` and `target` are only used from a single thread and it
         * is clear that both `source` and `target` outlive the binding. Especially it
         * is not safe to rely on this if the binding, `source` or `target` can be
         * finalized from different threads. Keep another reference to the binding and
         * use g_binding_unbind() instead to be on the safe side.
         *
         * A #GObject can have multiple bindings.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         */
        bind_property(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
        ): GObject.Binding;
        /**
         * Complete version of g_object_bind_property().
         *
         * Creates a binding between `source_property` on `source` and `target_property`
         * on `target,` allowing you to set the transformation functions to be used by
         * the binding.
         *
         * If `flags` contains %G_BINDING_BIDIRECTIONAL then the binding will be mutual:
         * if `target_property` on `target` changes then the `source_property` on `source`
         * will be updated as well. The `transform_from` function is only used in case
         * of bidirectional bindings, otherwise it will be ignored
         *
         * The binding will automatically be removed when either the `source` or the
         * `target` instances are finalized. This will release the reference that is
         * being held on the #GBinding instance; if you want to hold on to the
         * #GBinding instance, you will need to hold a reference to it.
         *
         * To remove the binding, call g_binding_unbind().
         *
         * A #GObject can have multiple bindings.
         *
         * The same `user_data` parameter will be used for both `transform_to`
         * and `transform_from` transformation functions; the `notify` function will
         * be called once, when the binding is removed. If you need different data
         * for each transformation function, please use
         * g_object_bind_property_with_closures() instead.
         *
         * @returns the #GBinding instance representing the
         *     binding between the two #GObject instances. The binding is released
         *     whenever the #GBinding reference count reaches zero.
         * @param source_property the property on `source` to bind
         * @param target the target #GObject
         * @param target_property the property on `target` to bind
         * @param flags flags to pass to #GBinding
         * @param transform_to the transformation function
         *     from the `source` to the `target,` or %NULL to use the default
         * @param transform_from the transformation function
         *     from the `target` to the `source,` or %NULL to use the default
         * @param notify a function to call when disposing the binding, to free
         *     resources used by the transformation functions, or %NULL if not required
         */
        bind_property_full(
            source_property: string,
            target: GObject.Object,
            target_property: string,
            flags: GObject.BindingFlags | null,
            transform_to?: GObject.BindingTransformFunc | null,
            transform_from?: GObject.BindingTransformFunc | null,
            notify?: GLib.DestroyNotify | null,
        ): GObject.Binding;
        // Conflicted with GObject.Object.bind_property_full
        bind_property_full(...args: never[]): any;
        /**
         * This function is intended for #GObject implementations to re-enforce
         * a [floating][floating-ref] object reference. Doing this is seldom
         * required: all #GInitiallyUnowneds are created with a floating reference
         * which usually just needs to be sunken by calling g_object_ref_sink().
         */
        force_floating(): void;
        /**
         * Increases the freeze count on `object`. If the freeze count is
         * non-zero, the emission of "notify" signals on `object` is
         * stopped. The signals are queued until the freeze count is decreased
         * to zero. Duplicate notifications are squashed so that at most one
         * #GObject::notify signal is emitted for each property modified while the
         * object is frozen.
         *
         * This is necessary for accessors that modify multiple properties to prevent
         * premature notification while the object is still being modified.
         */
        freeze_notify(): void;
        /**
         * Gets a named field from the objects table of associations (see g_object_set_data()).
         *
         * @returns the data if found,
         *          or %NULL if no such data exists.
         * @param key name of the key for that association
         */
        get_data(key: string): any | null;
        /**
         * Gets a property of an object.
         *
         * The value can be:
         * - an empty GObject.Value initialized by G_VALUE_INIT, which will be automatically initialized with the expected type of the property (since GLib 2.60)
         * - a GObject.Value initialized with the expected type of the property
         * - a GObject.Value initialized with a type to which the expected type of the property can be transformed
         *
         * In general, a copy is made of the property contents and the caller is responsible for freeing the memory by calling GObject.Value.unset.
         *
         * Note that GObject.Object.get_property is really intended for language bindings, GObject.Object.get is much more convenient for C programming.
         *
         * @param property_name The name of the property to get
         * @param value Return location for the property value. Can be an empty GObject.Value initialized by G_VALUE_INIT (auto-initialized with expected type since GLib 2.60), a GObject.Value initialized with the expected property type, or a GObject.Value initialized with a transformable type
         */
        get_property(property_name: string, value: GObject.Value | any): any;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        get_qdata(quark: GLib.Quark): any | null;
        /**
         * Gets `n_properties` properties for an `object`.
         * Obtained properties will be set to `values`. All properties must be valid.
         * Warnings will be emitted and undefined behaviour may result if invalid
         * properties are passed in.
         *
         * @param names the names of each property to get
         * @param values the values of each property to get
         */
        getv(names: string[], values: (GObject.Value | any)[]): void;
        /**
         * Checks whether `object` has a [floating][floating-ref] reference.
         *
         * @returns %TRUE if `object` has a floating reference
         */
        is_floating(): boolean;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param property_name the name of a property installed on the class of `object`.
         */
        notify(property_name: string): void;
        /**
         * Emits a "notify" signal for the property specified by `pspec` on `object`.
         *
         * This function omits the property name lookup, hence it is faster than
         * g_object_notify().
         *
         * One way to avoid using g_object_notify() from within the
         * class that registered the properties, and using g_object_notify_by_pspec()
         * instead, is to store the GParamSpec used with
         * g_object_class_install_property() inside a static array, e.g.:
         *
         *
         * ```c
         *   typedef enum
         *   {
         *     PROP_FOO = 1,
         *     PROP_LAST
         *   } MyObjectProperty;
         *
         *   static GParamSpec *properties[PROP_LAST];
         *
         *   static void
         *   my_object_class_init (MyObjectClass *klass)
         *   {
         *     properties[PROP_FOO] = g_param_spec_int ("foo", NULL, NULL,
         *                                              0, 100,
         *                                              50,
         *                                              G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS);
         *     g_object_class_install_property (gobject_class,
         *                                      PROP_FOO,
         *                                      properties[PROP_FOO]);
         *   }
         * ```
         *
         *
         * and then notify a change on the "foo" property with:
         *
         *
         * ```c
         *   g_object_notify_by_pspec (self, properties[PROP_FOO]);
         * ```
         *
         *
         * @param pspec the #GParamSpec of a property installed on the class of `object`.
         */
        notify_by_pspec(pspec: GObject.ParamSpec): void;
        /**
         * Increases the reference count of `object`.
         *
         * Since GLib 2.56, if `GLIB_VERSION_MAX_ALLOWED` is 2.56 or greater, the type
         * of `object` will be propagated to the return type (using the GCC typeof()
         * extension), so any casting the caller needs to do on the return type must be
         * explicit.
         *
         * @returns the same `object`
         */
        ref(): GObject.Object;
        /**
         * Increase the reference count of `object,` and possibly remove the
         * [floating][floating-ref] reference, if `object` has a floating reference.
         *
         * In other words, if the object is floating, then this call "assumes
         * ownership" of the floating reference, converting it to a normal
         * reference by clearing the floating flag while leaving the reference
         * count unchanged.  If the object is not floating, then this call
         * adds a new normal reference increasing the reference count by one.
         *
         * Since GLib 2.56, the type of `object` will be propagated to the return type
         * under the same conditions as for g_object_ref().
         *
         * @returns `object`
         */
        ref_sink(): GObject.Object;
        /**
         * Releases all references to other objects. This can be used to break
         * reference cycles.
         *
         * This function should only be called from object system implementations.
         */
        run_dispose(): void;
        /**
         * Each object carries around a table of associations from
         * strings to pointers.  This function lets you set an association.
         *
         * If the object already had an association with that name,
         * the old association will be destroyed.
         *
         * Internally, the `key` is converted to a #GQuark using g_quark_from_string().
         * This means a copy of `key` is kept permanently (even after `object` has been
         * finalized) — so it is recommended to only use a small, bounded set of values
         * for `key` in your program, to avoid the #GQuark storage growing unbounded.
         *
         * @param key name of the key
         * @param data data to associate with that key
         */
        set_data(key: string, data?: any | null): void;
        /**
         * Sets a property on an object.
         *
         * @param property_name The name of the property to set
         * @param value The value to set the property to
         */
        set_property(property_name: string, value: GObject.Value | any): void;
        /**
         * Remove a specified datum from the object's data associations,
         * without invoking the association's destroy handler.
         *
         * @returns the data if found, or %NULL
         *          if no such data exists.
         * @param key name of the key
         */
        steal_data(key: string): any | null;
        /**
         * This function gets back user data pointers stored via
         * g_object_set_qdata() and removes the `data` from object
         * without invoking its destroy() function (if any was
         * set).
         * Usually, calling this function is only required to update
         * user data pointers with a destroy notifier, for example:
         *
         * ```c
         * void
         * object_add_to_user_list (GObject     *object,
         *                          const gchar *new_string)
         * {
         *   // the quark, naming the object data
         *   GQuark quark_string_list = g_quark_from_static_string ("my-string-list");
         *   // retrieve the old string list
         *   GList *list = g_object_steal_qdata (object, quark_string_list);
         *
         *   // prepend new string
         *   list = g_list_prepend (list, g_strdup (new_string));
         *   // this changed 'list', so we need to set it again
         *   g_object_set_qdata_full (object, quark_string_list, list, free_string_list);
         * }
         * static void
         * free_string_list (gpointer data)
         * {
         *   GList *node, *list = data;
         *
         *   for (node = list; node; node = node->next)
         *     g_free (node->data);
         *   g_list_free (list);
         * }
         * ```
         *
         * Using g_object_get_qdata() in the above example, instead of
         * g_object_steal_qdata() would have left the destroy function set,
         * and thus the partial string list would have been freed upon
         * g_object_set_qdata_full().
         *
         * @returns The user data pointer set, or %NULL
         * @param quark A #GQuark, naming the user data pointer
         */
        steal_qdata(quark: GLib.Quark): any | null;
        /**
         * Reverts the effect of a previous call to
         * g_object_freeze_notify(). The freeze count is decreased on `object`
         * and when it reaches zero, queued "notify" signals are emitted.
         *
         * Duplicate notifications for each property are squashed so that at most one
         * #GObject::notify signal is emitted for each property, in the reverse order
         * in which they have been queued.
         *
         * It is an error to call this function when the freeze count is zero.
         */
        thaw_notify(): void;
        /**
         * Decreases the reference count of `object`. When its reference count
         * drops to 0, the object is finalized (i.e. its memory is freed).
         *
         * If the pointer to the #GObject may be reused in future (for example, if it is
         * an instance variable of another object), it is recommended to clear the
         * pointer to %NULL rather than retain a dangling pointer to a potentially
         * invalid #GObject instance. Use g_clear_object() for this.
         */
        unref(): void;
        /**
         * This function essentially limits the life time of the `closure` to
         * the life time of the object. That is, when the object is finalized,
         * the `closure` is invalidated by calling g_closure_invalidate() on
         * it, in order to prevent invocations of the closure with a finalized
         * (nonexisting) object. Also, g_object_ref() and g_object_unref() are
         * added as marshal guards to the `closure,` to ensure that an extra
         * reference count is held on `object` during invocation of the
         * `closure`.  Usually, this function will be called on closures that
         * use this `object` as closure data.
         *
         * @param closure #GClosure to watch
         */
        watch_closure(closure: GObject.Closure): void;
        /**
         * the `constructed` function is called by g_object_new() as the
         *  final step of the object creation process.  At the point of the call, all
         *  construction properties have been set on the object.  The purpose of this
         *  call is to allow for object initialisation steps that can only be performed
         *  after construction properties have been set.  `constructed` implementors
         *  should chain up to the `constructed` call of their parent class to allow it
         *  to complete its initialisation.
         */
        vfunc_constructed(): void;
        /**
         * emits property change notification for a bunch
         *  of properties. Overriding `dispatch_properties_changed` should be rarely
         *  needed.
         *
         * @param n_pspecs
         * @param pspecs
         */
        vfunc_dispatch_properties_changed(n_pspecs: number, pspecs: GObject.ParamSpec): void;
        /**
         * the `dispose` function is supposed to drop all references to other
         *  objects, but keep the instance otherwise intact, so that client method
         *  invocations still work. It may be run multiple times (due to reference
         *  loops). Before returning, `dispose` should chain up to the `dispose` method
         *  of the parent class.
         */
        vfunc_dispose(): void;
        /**
         * instance finalization function, should finish the finalization of
         *  the instance begun in `dispose` and chain up to the `finalize` method of the
         *  parent class.
         */
        vfunc_finalize(): void;
        /**
         * the generic getter for all properties of this type. Should be
         *  overridden for every type with properties.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_get_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Emits a "notify" signal for the property `property_name` on `object`.
         *
         * When possible, eg. when signaling a property change from within the class
         * that registered the property, you should use g_object_notify_by_pspec()
         * instead.
         *
         * Note that emission of the notify signal may be blocked with
         * g_object_freeze_notify(). In this case, the signal emissions are queued
         * and will be emitted (in reverse order) when g_object_thaw_notify() is
         * called.
         *
         * @param pspec
         */
        vfunc_notify(pspec: GObject.ParamSpec): void;
        /**
         * the generic setter for all properties of this type. Should be
         *  overridden for every type with properties. If implementations of
         *  `set_property` don't emit property change notification explicitly, this will
         *  be done implicitly by the type system. However, if the notify signal is
         *  emitted explicitly, the type system will not emit it a second time.
         *
         * @param property_id
         * @param value
         * @param pspec
         */
        vfunc_set_property(property_id: number, value: GObject.Value | any, pspec: GObject.ParamSpec): void;
        /**
         * Sets multiple properties of an object at once. The properties argument should be a dictionary mapping property names to values.
         *
         * @param properties Object containing the properties to set
         */
        set(properties: { [key: string]: any }): void;
        /**
         * Blocks a handler of an instance so it will not be called during any signal emissions
         *
         * @param id Handler ID of the handler to be blocked
         */
        block_signal_handler(id: number): void;
        /**
         * Unblocks a handler so it will be called again during any signal emissions
         *
         * @param id Handler ID of the handler to be unblocked
         */
        unblock_signal_handler(id: number): void;
        /**
         * Stops a signal's emission by the given signal name. This will prevent the default handler and any subsequent signal handlers from being invoked.
         *
         * @param detailedName Name of the signal to stop emission of
         */
        stop_emission_by_name(detailedName: string): void;
    }
    class WifiP2PPeer extends Object {
        static '$gtype': GObject.GType<WifiP2PPeer>;
        // Properties
        /**
         * The flags of the P2P peer.
         */
        get flags(): NM.__80211ApFlags;
        /**
         * The hardware address of the P2P peer.
         */
        get hw_address(): string;
        /**
         * The hardware address of the P2P peer.
         */
        get hwAddress(): string;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * P2P peer was found.  A value of -1 means the peer has never been seen.
         */
        get last_seen(): number;
        /**
         * The timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * P2P peer was found.  A value of -1 means the peer has never been seen.
         */
        get lastSeen(): number;
        /**
         * The manufacturer of the P2P peer.
         */
        get manufacturer(): string;
        /**
         * The model of the P2P peer.
         */
        get model(): string;
        /**
         * The hardware address of the P2P peer.
         */
        get model_number(): string;
        /**
         * The hardware address of the P2P peer.
         */
        get modelNumber(): string;
        /**
         * The name of the P2P peer.
         */
        get name(): string;
        /**
         * The serial number of the P2P peer.
         */
        get serial(): string;
        /**
         * The current signal strength of the P2P peer.
         */
        get strength(): number;
        /**
         * The WFD information elements of the P2P peer.
         */
        get wfd_ies(): GLib.Bytes;
        /**
         * The WFD information elements of the P2P peer.
         */
        get wfdIes(): GLib.Bytes;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.WifiP2PPeer.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.WifiP2PPeer.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.WifiP2PPeer.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.WifiP2PPeer.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.WifiP2PPeer.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.WifiP2PPeer.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.WifiP2PPeer.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.WifiP2PPeer.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Validates a given connection against a given Wi-Fi P2P peer to ensure that
         * the connection may be activated with that peer. The connection must match the
         * `peer'`s address and in the future possibly other attributes.
         *
         * @returns %TRUE if the connection may be activated with this Wi-Fi P2P Peer,
         * %FALSE if it cannot be.
         * @param connection an #NMConnection to validate against `peer`
         */
        connection_valid(connection: NM.Connection): boolean;
        /**
         * Filters a given array of connections for a given #NMWifiP2PPeer object and
         * returns connections which may be activated with the P2P peer.  Any
         * returned connections will match the `peers'`s HW address and in the future
         * possibly other attributes.
         *
         * To obtain the list of connections that are compatible with this P2P peer,
         * use nm_client_get_connections() and then filter the returned list for a given
         * #NMDevice using nm_device_filter_connections() and finally filter that list
         * with this function.
         *
         * @returns an array of
         * #NMConnections that could be activated with the given `peer`. The array should
         * be freed with g_ptr_array_unref() when it is no longer required.
         * @param connections an array of #NMConnections to
         * filter
         */
        filter_connections(connections: NM.Connection[]): NM.Connection[];
        /**
         * Gets the flags of the P2P peer.
         *
         * @returns the flags
         */
        get_flags(): NM.__80211ApFlags;
        /**
         * Gets the hardware address of the P2P peer.
         *
         * @returns the hardware address
         */
        get_hw_address(): string;
        /**
         * Returns the timestamp (in CLOCK_BOOTTIME seconds) for the last time the
         * P2P peer was seen.  A value of -1 means the P2P peer has never been seen.
         *
         * @returns the last seen time in seconds
         */
        get_last_seen(): number;
        /**
         * Gets the manufacturer of the P2P peer.
         *
         * @returns the manufacturer
         */
        get_manufacturer(): string;
        /**
         * Gets the model of the P2P peer.
         *
         * @returns the model
         */
        get_model(): string;
        /**
         * Gets the model number of the P2P peer.
         *
         * @returns the model number
         */
        get_model_number(): string;
        /**
         * Gets the name of the P2P peer.
         *
         * @returns the name
         */
        get_name(): string;
        /**
         * Gets the serial number of the P2P peer.
         *
         * @returns the serial number
         */
        get_serial(): string;
        /**
         * Gets the current signal strength of the P2P peer as a percentage.
         *
         * @returns the signal strength (0 to 100)
         */
        get_strength(): number;
        /**
         * Gets the WFD information elements of the P2P peer.
         *
         * @returns the #GBytes containing the WFD IEs, or %NULL.
         */
        get_wfd_ies(): GLib.Bytes;
    }
    class WimaxNsp extends Object {
        static '$gtype': GObject.GType<WimaxNsp>;
        // Properties
        /**
         * The name of the WiMAX NSP.
         */
        get name(): string;
        /**
         * The network type of the WiMAX NSP.
         */
        get network_type(): NM.WimaxNspNetworkType;
        /**
         * The network type of the WiMAX NSP.
         */
        get networkType(): NM.WimaxNspNetworkType;
        /**
         * The signal quality of the WiMAX NSP.
         */
        get signal_quality(): number;
        /**
         * The signal quality of the WiMAX NSP.
         */
        get signalQuality(): number;
        /**
         * Compile-time signal type information.
         *
         * This instance property is generated only for TypeScript type checking.
         * It is not defined at runtime and should not be accessed in JS code.
         */
        '$signals': NM.WimaxNsp.SignalSignatures;
        // Constructors
        constructor(properties?: Partial<NM.WimaxNsp.ConstructorProps>, ...args: any[]);
        _init(...args: any[]): void;
        // Signals
        connect<K extends keyof NM.WimaxNsp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.WimaxNsp.SignalSignatures[K]>,
        ): number;
        connect(signal: string, callback: any): number;
        connect_after<K extends keyof NM.WimaxNsp.SignalSignatures>(
            signal: K,
            callback: GObject.SignalCallback<this, NM.WimaxNsp.SignalSignatures[K]>,
        ): number;
        connect_after(signal: string, callback: any): number;
        emit<K extends keyof NM.WimaxNsp.SignalSignatures>(
            signal: K,
            ...args: GObject.GjsParameters<NM.WimaxNsp.SignalSignatures[K]>
        ): void;
        emit(signal: string, ...args: any[]): void;
        // Methods
        /**
         * Validates a given connection against a given WiMAX NSP to ensure that the
         * connection may be activated with that NSP.  The connection must match the
         * `nsp'`s network name and other attributes.
         *
         * @returns %TRUE if the connection may be activated with this WiMAX NSP,
         * %FALSE if it cannot be.
         * @param connection an #NMConnection to validate against `nsp`
         */
        connection_valid(connection: NM.Connection): boolean;
        /**
         * Filters a given array of connections for a given #NMWimaxNsp object and
         * return connections which may be activated with the NSP.  Any returned
         * connections will match the `nsp'`s network name and other attributes.
         *
         * @returns an array of
         * #NMConnections that could be activated with the given `nsp`.  The array should
         * be freed with g_ptr_array_unref() when it is no longer required.
         * @param connections an array of #NMConnections to
         * filter
         */
        filter_connections(connections: NM.Connection[]): NM.Connection[];
        /**
         * Gets the name of the wimax NSP
         *
         * @returns the name
         */
        get_name(): string;
        /**
         * Gets the network type of the wimax NSP.
         *
         * @returns the network type
         */
        get_network_type(): NM.WimaxNspNetworkType;
        /**
         * Gets the WPA signal quality of the wimax NSP.
         *
         * @returns the signal quality
         */
        get_signal_quality(): number;
    }
}
export default classes;
